[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -m "not e2e"
markers =
    e2e: end-to-end tests that require real PostgreSQL, ChromaDB, and Docker services
