# =============================================================================
# NovelMind 后端 Python 依赖
# =============================================================================

# --- Web 框架 & 服务器 ---
fastapi>=0.115                # 异步 Web 框架，提供 REST API 路由、请求验证、自动文档
uvicorn[standard]>=0.32       # ASGI 服务器，运行 FastAPI 应用；standard 额外包含 uvloop/httptools 提升性能

# --- 数据库 ORM & 迁移 ---
sqlalchemy>=2.0               # Python ORM 框架，定义数据模型、构建查询（本项目使用异步模式）
asyncpg>=0.30                 # PostgreSQL 异步驱动，SQLAlchemy async engine 底层使用
alembic>=1.14                 # 数据库迁移工具，管理 schema 变更版本（与 SQLAlchemy 配合使用）

# --- 数据验证 & 配置 ---
pydantic>=2.13                # 数据验证库，FastAPI 的请求/响应模型基础；v2 性能大幅提升
pydantic-settings>=2.8        # 从环境变量/.env 文件加载配置到 Pydantic 模型，统一配置管理

# --- 文件处理 ---
python-multipart>=0.0.18      # 处理 multipart/form-data 请求，支持文件上传（小说文档导入）
chardet>=5.2                  # 字符编码检测，自动识别上传文件的编码（GBK/UTF-8 等）

# --- AI 模型集成 ---
litellm>=1.55                 # 统一 LLM 调用接口，支持 OpenAI/Anthropic/Ollama 等多厂商 API

# --- 向量检索 ---
pgvector>=0.3                 # PostgreSQL pgvector 的 Python 绑定，存储和查询向量嵌入

# --- HTTP 客户端 ---
httpx>=0.28                   # 异步 HTTP 客户端，调用外部 API（如 Embedding 服务、第三方接口）

# --- 数据库驱动（同步，Alembic 迁移使用）---
psycopg2-binary>=2.9          # PostgreSQL 同步驱动（二进制版），Alembic 迁移时使用

# --- 环境变量 ---
python-dotenv>=1.0            # 从 .env 文件加载环境变量到 os.environ
