# NovelMind 待办清单

## 当前 P1：导入可靠性

- [ ] 新增持久化 import_jobs 表和 migration
- [ ] 上传接口返回唯一 job ID
- [ ] worker 使用租约、幂等键、重试次数和错误状态
- [ ] 服务重启后恢复 queued/running 任务
- [ ] 增加并发导入、失败重试和重启恢复测试
- [ ] 完成后关闭 GSD `02-03`

## 已关闭的安全项

- [x] `.gitignore`、环境文件、依赖、构建和上传目录隔离
- [x] 上传路径 containment、随机名、大小限制和数据库失败补偿
- [x] JWT/HttpOnly Cookie 身份认证
- [x] 小说与模型配置资源所有权隔离
- [x] 自定义 AI URL 白名单与 DNS/IP SSRF 校验
- [x] 独立 JWT/数据加密密钥、旧数据兼容和密钥轮换
- [x] 用户/owner Alembic migration 和真实 PostgreSQL 检查
- [x] 动态 reader 路由、章节摘要响应和 `source_path` 移除
- [x] Next、Vitest、Vite、LiteLLM 漏洞版本升级
- [x] CI 默认分支和非交互 lint

## 后续产品能力

- [ ] RAG 分块、Embedding、索引和引用检索
- [ ] 混合语义搜索
- [ ] 剧情分析、人物关系和时间线
- [ ] 同人文续写、编辑、版本管理和导出
- [ ] 生产 TLS、秘密管理、备份和监控
