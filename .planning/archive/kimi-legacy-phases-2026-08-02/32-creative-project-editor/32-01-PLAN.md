# Phase 32 Plan 01: Project, Chapter, and Revision Backend

## Steps

1. Add the immutable revision model and migration.
2. Implement owner-scoped project/chapter services and CRUD endpoints.
3. Add revision listing, deterministic diff, and non-destructive rollback endpoints.
4. Keep AI continuation as a structured deferred response.

## Must-Haves

- A caller cannot list or mutate another owner's project, chapter, or revision.
- Project and chapter Markdown saves create traceable snapshots.
- Diff is deterministic and rollback preserves the prior snapshot while creating a new rollback snapshot.
- Alembic has one current head and the migration test matrix passes.

## Verification

- `tests/test_fanfiction.py`
- `tests/integration/test_postgres_migrations.py`
- `tests/contract/test_openapi_contract.py`
- Ruff and `compileall` for changed backend modules.

## Test, Fix, and Confirm

Run the focused suite, fix only Phase 32 regressions, and rerun until the evidence above is green.
