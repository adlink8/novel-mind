# Phase 32 Plan 02: Writing Editor and Autosave

## Steps

1. Extend the frontend API client for project, chapter, and revision operations.
2. Replace the writing placeholder section with an authenticated creative-project editor.
3. Add debounced autosave for project and chapter Markdown while retaining an explicit save action.
4. Display the Fanfiction Canon boundary and deferred AI continuation state.

## Must-Haves

- `/writing` can select an owned novel, create/select a project, edit Markdown, and manage chapters.
- Autosave calls the owner-scoped update API after edits without entering original-canon retrieval.
- The UI does not present generation as available.

## Verification

- Frontend API/component tests.
- TypeScript/lint/build checks using the repository scripts where available.
- Review rendered component structure and accessible labels.

## Test, Fix, and Confirm

Run the focused frontend tests and type/build checks, fix affected code, and rerun the same commands.
