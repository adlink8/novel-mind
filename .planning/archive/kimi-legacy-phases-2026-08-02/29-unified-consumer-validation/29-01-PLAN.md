# Phase 29-01 Plan: Real Workspace and Fallback

## Steps

1. Bind Structure Workspace to qualified real Arc/Global candidate data.
2. Exercise Reader Chat fallback when NM is unavailable or partial.
3. Verify citations always resolve to original leaf evidence and owner scope.

## Must-Haves

- Real data is distinguishable from chapter-tree fallback.
- Fallback is measured, not inferred from fixture behavior.
- No consumer cutover occurs.

## Verification

- Real Postgres API/browser smoke and fallback/citation adversarial tests.

## Test, Fix, and Confirm

Formal candidate data and read-only API/UI contracts are now verified. Final confirmation remains
pending a real browser session authenticated for the novel owner; no ownership transfer or pointer
mutation is used as a workaround.
