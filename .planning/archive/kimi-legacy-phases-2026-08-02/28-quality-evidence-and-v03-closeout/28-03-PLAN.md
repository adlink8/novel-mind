# Phase 28-03 Plan: Browser Residual and v0.3 Audit

## Steps

1. Run the real Postgres Reader Chat browser residual in the authorized environment.
2. Resolve or document browser/runtime failures without weakening gates.
3. Produce the v0.3 audit with implementation, sample-data, and qualification dimensions.

## Must-Haves

- Browser evidence is real and reproducible.
- A failed producer remains failed in the final audit.
- No consumer cutover.

## Verification

- `reader-chat-real.spec.ts`, full required checks, and v0.3 audit artifact.

## Test, Fix, and Confirm

The real formal-Postgres Reader Chat residual passed desktop, 390px mobile, and 768px tablet.
`28-03-AUDIT-2026-07-28.md` records implementation readiness, sample-data coverage,
quality qualification, browser residuals, and the no-cutover boundary. The audit remains
partial because an owner-scoped `/analysis?novel=91` browser session is unavailable; no
consumer cutover was performed.
