# Phase 28-02 Plan: Faithfulness, Cost, and Candidate Qualification

## Steps

1. Produce faithfulness and cost reports with price snapshots.
2. Run frozen candidate qualification across local/arc/global/no-answer/spoiler buckets.
3. Archive `qualified_candidate` or explicit `blocked` verdicts.

## Must-Haves

- Cost is calculated from authorized price data, never defaulted to zero.
- Candidate verdicts do not alter production pointers.
- Reports include full lineage and dependency status.

## Verification

- Qualification gate, cost, spoiler, citation, and no-pointer-write checks.

## Test, Fix, and Confirm

The signed repository fixture passed offline with comparable faithfulness and explicit cost.
Novel 91 now has a signed content-hash fixture, live calibration (`consistency=1.0`, critical
false accept `0`), and a real SUT report with `quality_comparable=true`, 100/100 reviewed,
Recall@5 `1.0`, accepted rate `0.96`, and estimated cost `$0.17057084`. The generic
`cost_usd=0` usage-log field remains explicitly excluded from cost truth. Full project-wide
v0.3 closure and owner-scoped consumer acceptance remain residuals.
