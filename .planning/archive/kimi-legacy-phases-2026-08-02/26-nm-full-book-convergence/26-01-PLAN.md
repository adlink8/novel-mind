# Phase 26-01 Plan: Failure Diagnosis and Recovery

## Steps

1. Read-only classify failed/pending chapter states by schema, package, budget, transport, and dependency reason.
2. Define retry/isolation/resume rules and a deterministic reason-code matrix.
3. Verify preflight against the authorized novel-91 source and candidate-only invariants.

## Must-Haves

- No retry runs without correct source data and budget authorization.
- Every isolated failure has a reason code and no active-pointer effect.
- Recovery decisions are reproducible from persisted evidence.

## Verification

- Read-only DB/data-source preflight and reason-code unit tests.
- Provider/live/paid execution requires explicit authorization and must remain candidate-only.

## Test, Fix, and Confirm

Diagnosis slice completed on the isolated backup-derived audit database: novel 91 has 515
chapters; before bounded recovery attempts, the version-1 candidate had 117 completed, 33
failed, and 365 pending stages plus one pending arc/volume planning stage. The latest persisted
state is 117 completed, 46 failed, and 353 pending. The active CI database has no matching
candidate. No pointer or consumer state changed. Recovery execution is not complete because
the authorized provider path remains unstable (Vertex 429; Gemini 503/timeout/invalid output);
record the result as `PARTIAL`, not complete.
