# Phase 26-02 Attempt — 2026-07-27

## Steps

1. Used the recovered backup-derived candidate database in an isolated PostgreSQL database.
2. Selected the authorized Gemini structured-output transport with model `gemini-2.5-flash`.
3. Ran bounded resumable single-stage attempts and inspected persisted attempt/budget evidence.

## Must-Haves

- The active CI database remained untouched.
- The build remained candidate-only and did not create or move an active pointer.
- Provider failures were persisted with sanitized status/reason evidence.

## Verification

- Existing candidate before execution: 117 completed, 33 failed, 365 pending chapter stages,
  plus one pending arc/volume planning stage.
- The Gemini transport was exercised with a real API key loaded from local settings without
  printing the key; the transport contract and enum normalization tests pass locally.
- Observed provider/output failures: Vertex HTTP 429; Gemini malformed/invalid structured output,
  HTTP 503, and timeout. `gemini-2.5-flash-lite` was rejected after a deterministic HTTP 404
  probe and was not used for the full run.
- Latest persisted stage state after the bounded attempts is 117 completed, 46 failed, and 353
  pending. The existing ledger remains capped at `$200.00`; settled cost is `$0.31962540` and
  reserved audit-attempt cost is `$0.52320000`.
- No qualification report, stable full-book coverage, active pointer, or Reader Chat cutover
  was produced.

## Test, Fix, and Confirm

The attempt is recorded as `WAIT_EXTERNAL`, not complete. The direct structured Gemini route is
implemented and locally validated, but provider stability and output-quality evidence are still
required before resuming the 515-chapter convergence.
