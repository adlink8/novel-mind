# Phase 26-02 Plan: Full-Book Chapter-State Run

## Steps

1. Run the authorized resumable chapter-state convergence over all 515 chapters.
2. Persist checkpoints, cache reuse, failure isolation, token/cost evidence, and final coverage.
3. Verify no active pointer mutation and reconcile the manifest from DB facts.

## Must-Haves

- All chapters end completed or explicitly isolated.
- Resume is idempotent and auditable.
- Candidate-only state remains intact.

## Verification

- Authorized integration/live run and PostgreSQL manifest checks.

## Test, Fix, and Confirm

An authorized isolated attempt was launched against the backup-derived candidate database.
The direct Gemini structured transport was exercised, but chapter-scale calls did not stabilize:
the observed results include malformed/invalid structured output, HTTP 503, and timeout. The
Vertex path also returned HTTP 429. Coverage therefore remains partial and no active pointer
was changed. Keep this plan open as `WAIT_EXTERNAL`; do not claim full-book completion.
