# Phase 26 Preflight — 2026-07-27

## Status

**PARTIAL — source recovered, live convergence not complete.** The user later authorized
provider/paid execution and promotion/cutover. This record still does not claim a completed
production run or qualify a candidate for promotion.

## Evidence

Against the local CI PostgreSQL service (`novelmind_ci`, read-only transaction):

- PostgreSQL reachable: `true`.
- `novel_91_count=0`.
- `narrative_index_builds_count=0`.
- `narrative_memory_versions` has no rows for the target scope.
- `narrative_active_pointers` has no rows for the target scope.

Additional read-only audit of `backups/novelmind-20260719-pre-cleanup.dump` in the isolated
temporary database `novelmind_audit_20260727` found:

- `novel_id=91` exists with exactly 515 chapters;
- one existing candidate version/run is present, with 117 completed chapter states, 33 failed,
  and 365 pending before the new attempts;
- no Narrative Memory active pointer, manifest, qualification report, or source snapshot was
  present in the restored subset;
- the existing builder ledger has a `$200.00` maximum and `$0.31962540` settled cost;
- Vertex transport returned HTTP 429; authorized Gemini API probes returned 200, but the
  chapter-scale structured run encountered HTTP 503/timeout and malformed/invalid model output.

## Decision

Do not claim 515-chapter convergence, Arc/Volume, Global, cost-report, qualification, or
promotion completion. The recovered backup is a candidate input, but the isolated run did not
reach a stable provider-backed completion; candidate-only and no-pointer boundaries remain
intact.
