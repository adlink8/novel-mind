# Phase 34-01 Plan: Export Pipeline

## Steps

1. Define version-consistent Markdown/EPUB export inputs and metadata.
2. Export a selected creative revision without mutating source content.
3. Verify round-trip content/version/hash consistency and error states.

## Must-Haves

- Export is tied to an explicit revision, not mutable latest state by accident.
- No export includes unauthorized Original Canon or hidden provider output.
- Export is deterministic for the same revision.

## Verification

- Backend export API tests verify explicit revision binding, deterministic EPUB bytes,
  provenance metadata, and owner-scoped access.
- Frontend API/component checks verify the two version-history download actions.
- OpenAPI baseline and contract fixtures are regenerated and pass the contract suite.

## Test, Fix, and Confirm

Local revision-bound export is complete and confirmed. Generated-output export, production
artifact publication, deployment, and final closeout remain outside the current authorization.
