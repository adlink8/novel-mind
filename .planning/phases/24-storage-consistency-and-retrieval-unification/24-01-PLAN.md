---
phase: 24-storage-consistency-and-retrieval-unification
plan: 01
type: execute
wave: 1
depends_on: [23-01, 23-02]
files_modified:
  - backend/app/services/indexing_service.py
  - backend/app/models/chunk_index_journal.py
  - backend/migrations/versions/24_chunk_index_journal.py
  - backend/tests/test_indexing_journal.py
requirements: [REQ-GOV-04]
autonomous: true
must_haves:
  truths:
    - "Chunk-to-Chroma writes have an idempotent journal and explicit completion state."
    - "Partial indexing cannot be reported as novel ready."
    - "Delete/rebuild failure leaves an auditable partial state rather than silently serving stale vectors."

---

# Plan 24-01: Chunk index journal and fail-closed writes

## Steps

### Task 1: Journal schema and idempotency
- Persist operation identity, source/build lineage, attempt, status and error detail.
- Add migration constraints and indexes for owner/novel/build/idempotency lookup.

### Task 2: Indexing service integration
- Mark journal pending/running/completed/failed around DB and Chroma operations.
- Make failed writes set explicit partial status and prevent ready promotion.

### Task 3: Regression tests
- Cover retry idempotency, failure state, delete window and successful completion.

### Test, Fix, and Confirm
- Run indexing, journal and migration tests on the current branch; preserve PR #23 as read-only evidence only.

## Must-Haves

- No stale vector is treated as authoritative after a failed write.
- Journal updates are scoped to owner/novel/build and safe to retry.
- No Narrative Memory promotion or pointer mutation.

## Verification

```powershell
pytest tests/test_indexing_journal.py tests/test_indexing.py -q
pytest tests/integration/test_postgres_migrations.py -q
```
