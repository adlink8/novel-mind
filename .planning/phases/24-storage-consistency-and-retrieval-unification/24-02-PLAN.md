---
phase: 24-storage-consistency-and-retrieval-unification
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - backend/app/services/indexing_reconcile.py
  - backend/scripts/run_index_reconcile.py
  - backend/tests/test_indexing_reconcile.py
requirements: [REQ-GOV-04, REQ-GOV-05]
autonomous: true
must_haves:
  truths:
    - "Database chunk rows and Chroma records can be reconciled by a deterministic manifest."
    - "Missing and orphan records are reported with repairable reason codes."
    - "Manifest identity is bound to the database/build snapshot."

---

# Plan 24-02: Reconcile and manifest integrity gate

## Steps

### Task 1: Deterministic inventory
- Enumerate expected DB leaves and actual Chroma records under owner/novel/build scope.
- Compute missing, orphan, hash mismatch and complete counts.

### Task 2: Gate and CLI
- Expose a read-only report and explicit repair mode with fail-closed status handling.
- Bind report checksum to build/database snapshot and preserve audit output.

### Task 3: Tests
- Cover clean, missing, orphan, mismatch, owner isolation and manifest drift cases.

### Test, Fix, and Confirm
- Run reconcile/indexing/search suites and verify no unrelated reader-chat regression.

## Must-Haves

- Reconcile never silently deletes or fabricates source facts.
- Repair is explicit and does not change Narrative Memory active state.
- Reports are reproducible from the same DB/build snapshot.

## Verification

```powershell
pytest tests/test_indexing_reconcile.py tests/test_knowledge_unit_search.py -q
pytest tests/test_knowledge_unit_search.py -q
```
