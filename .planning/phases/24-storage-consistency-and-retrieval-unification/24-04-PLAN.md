---
phase: 24-storage-consistency-and-retrieval-unification
plan: 04
type: execute
wave: 3
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - backend/app/services/retrieval_policy.py
  - backend/app/services/knowledge_units/search.py
  - backend/app/services/reader_chat/retrieval.py
  - backend/tests/test_retrieval_policy.py
  - backend/tests/integration/relationships/test_projection.py
  - .planning/phases/24-storage-consistency-and-retrieval-unification/24-VERIFICATION.md
requirements: [REQ-GOV-04, REQ-GOV-05]
autonomous: true
must_haves:
  truths:
    - "Production retrieval layer status is shared and keeps Narrative Memory disabled."
    - "Reader Chat source priority is defined by the shared retrieval contract."
    - "Projection tests prove only accepted PostgreSQL facts are replayed and projection failure does not mutate facts."
  artifacts:
    - path: "backend/app/services/retrieval_policy.py"
      provides: "Shared layer registry and Reader Chat source-priority contract"
    - path: "backend/tests/test_retrieval_policy.py"
      provides: "Contract tests for enabled/disabled layers and source ordering"
  key_links:
    - from: "backend/app/services/reader_chat/retrieval.py"
      to: "backend/app/services/retrieval_policy.py"
      via: "shared SOURCE_PRIORITY import"
    - from: "backend/app/services/knowledge_units/search.py"
      to: "backend/app/services/retrieval_policy.py"
      via: "shared RETRIEVAL_LAYERS import"

---

# Plan 24-04: Reader Chat priority and projection safety contract

## Steps

### Task 1: Extract shared retrieval policy
- Add a dependency-free policy module for production layer status and Reader Chat source priority.
- Preserve existing public names through imports/aliases so API behavior does not change.
- Keep `narrative_memory` explicitly disabled and fail closed for production routing.

### Task 2: Wire consumers and contract tests
- Replace duplicated registries/constants in knowledge-unit search and Reader Chat retrieval.
- Add deterministic tests for layer registry, source ordering, unknown source rejection, and NM disabled status.
- Re-run existing search-router and Reader Chat suites.

### Task 3: Prove projection boundary
- Extend or retain projection tests showing manifests contain accepted observations only.
- Verify disabled/missing adapter paths write only projection audit state and never mutate accepted facts.

### Test, Fix, and Confirm
- Run targeted backend unit tests and relationship projection integration tests where PostgreSQL is available.
- Fix regressions caused by import/API compatibility.
- Record verification status and residual dependency on PR #23 integration.

## Must-Haves

- Shared contract has no dependency on Narrative Memory active pointers or promotion.
- Reader Chat ordering remains selection, hierarchy, knowledge, timeline, relationship observation.
- Existing `RETRIEVAL_LAYERS` and `SOURCE_PRIORITY` imports remain compatible.
- Projection safety is covered by executable tests.

## Verification

```powershell
cd backend
pytest tests/test_retrieval_policy.py tests/test_search_router_fallback.py tests/unit/reader_chat -q
pytest tests/integration/relationships/test_projection.py -q
```

The phase remains partial until PR #23 (24-01/02) is merged and its CI evidence is available; this plan only closes 24-04.
