---
phase: 18-frontend-motion-and-transition-system
plan: 01
type: execute
wave: 1
depends_on: []
execution_gate: "Explicit implementation authorization required"
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/novels/[id]/page.tsx
  - frontend/src/components/app-theme-sync.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/app-theme-sync.test.tsx
  - frontend/src/components/ui/motion-contract.test.tsx
requirements: [UI-MOTION-01, UI-MOTION-04, UI-MOTION-05]
autonomous: false
must_haves:
  truths:
    - "All shared UI motion maps to 150/200/300ms semantic tokens with ease-out entry and ease-in exit."
    - "Persisted light/dark/custom theme is applied before first visible paint, without hydration flash or global layout animation."
    - "Reduced-motion users receive immediate, fully usable states with no non-essential translate, scale, pulse, or smooth scroll."
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Motion tokens, explicit-property transition utilities, theme-ready gate, and reduced-motion override"
    - path: "frontend/src/app/layout.tsx"
      provides: "Defensive pre-paint theme bootstrap"
    - path: "frontend/src/components/ui/motion-contract.test.tsx"
      provides: "Shared primitive duration/easing/reduced-motion contract coverage"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/app-theme-sync.tsx"
      via: "same validated persisted reader theme key and root class/color-scheme contract"
      pattern: "dark|custom|colorScheme"
    - from: "frontend/src/app/globals.css"
      to: "frontend/src/components/ui/dialog.tsx"
      via: "semantic duration/easing variables consumed by Base UI data states"
      pattern: "motion-duration|data-open|data-closed"
---

<objective>Create the CSS-first motion foundation and eliminate first-paint theme flash before applying motion to domain panels.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-frontend-motion-and-transition-system/18-CONTEXT.md
@.planning/phases/18-frontend-motion-and-transition-system/18-RESEARCH.md
@frontend/src/app/globals.css
@frontend/src/app/layout.tsx
@frontend/src/components/app-theme-sync.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/sheet.tsx
</context>

## Steps

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Authorize Phase 18 frontend motion implementation</action>
  <instructions>Planning-only lock is active. Before any file change or command, obtain the user's explicit instruction `批准执行 Phase 18`. Auto mode must not bypass this gate.</instructions>
  <verification>The current conversation contains explicit Phase 18 execution authorization.</verification>
  <resume-signal>批准执行 Phase 18</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 1: Define semantic motion and reduced-motion contracts</name>
  <files>frontend/src/app/globals.css, frontend/src/components/ui/motion-contract.test.tsx</files>
  <action>Add centralized fast/standard/spatial duration tokens (150/200/300ms), enter ease-out and exit ease-in curves, explicit-property semantic utilities/data-state selectors, and a theme-transition-ready gate. Keep the reduced-motion override last and make it remove non-essential transform, scale, pulse, smooth scroll and long transition delay while retaining visible state changes. Add a source/DOM contract test that rejects new linear easing, arbitrary duration values and decorative infinite animation in the shared motion layer. Do not add a dependency or global `transition: all`. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/components/ui/motion-contract.test.tsx</automated></verify>
  <done>One documented CSS contract owns duration/easing/reduced-motion behavior and can be mechanically checked.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Restore the persisted theme before first paint</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/novels/[id]/page.tsx, frontend/src/components/app-theme-sync.tsx, frontend/src/components/app-theme-sync.test.tsx, frontend/src/app/globals.css</files>
  <action>Implement a minimal dependency-free pre-hydration bootstrap that reads the existing reader preference storage key defensively, accepts only light/dark/custom, applies the root dark class, reader-theme data attribute and color-scheme before content paints, and never evaluates custom background as executable markup. For custom theme, accept only the existing six-digit hex contract, set a pre-paint `--reader-custom-background` variable plus derived foreground token, and make the stable reader surface consume those variables before hydration; invalid values use the current safe default. Keep AppThemeSync as the React reconciler and enable color-only theme transitions only after initial synchronization. Test saved dark, light, valid/invalid custom, malformed JSON, unavailable storage and React reconciliation without first-frame background change, class flicker or hydration mismatch. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/components/app-theme-sync.test.tsx src/components/reader/reader-preferences.test.tsx</automated></verify>
  <done>The first visible root theme matches persisted choice and later user switching is color-only, scoped and reduced-motion-safe.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Normalize shared UI primitive state transitions</name>
  <files>frontend/src/components/ui/dialog.tsx, frontend/src/components/ui/sheet.tsx, frontend/src/components/ui/dropdown-menu.tsx, frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/tabs.tsx, frontend/src/components/ui/button.tsx, frontend/src/components/ui/motion-contract.test.tsx</files>
  <action>Replace touched primitives' 100ms/arbitrary/transition-all behavior with semantic tokens and explicit properties. Dialog/dropdown/tooltip use short opacity/scale enter and shorter directional exit; sheet uses the spatial token and side-aware transform; tabs/buttons use standard/fast feedback with fixed geometry. Preserve all Base UI data attributes, focus behavior, ARIA and controlled APIs. Verify closed states cannot receive pointer input during exit and reduced-motion yields immediate state. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/components/ui/motion-contract.test.tsx; npm run lint; npm run build</automated></verify>
  <done>Shared primitives follow one directional motion vocabulary without API, focus, geometry or build regressions.</done>
</task>
</tasks>

## Must-Haves

- Motion uses the existing stack and adds no production dependency.
- Tokens are semantic and global; component code does not invent durations or curves.
- Theme is correct before first paint and theme transitions are never active during boot.
- Theme changes animate colors/borders only, not layout, images or custom background position/size.
- Reduced-motion remains the final authority and every state remains understandable without animation.
- Shared primitive geometry, controlled props, focus handling and ARIA behavior remain compatible.

## Verification

<verification>Run focused Vitest contracts for theme/motion, then frontend lint and Next production build. Inspect generated shared primitive classes for only semantic durations, directional easing and explicit transition properties.</verification>
<success_criteria>
- Persisted dark/light/custom values produce the expected root class and color-scheme before AppThemeSync effects.
- Invalid/unavailable storage does not throw or cause hydration mismatch.
- Dialog, sheet, dropdown, tooltip, tabs and buttons use 150/200/300ms tokens only.
- Reduced-motion removes non-essential transform/scale/pulse and leaves controls operable.
- Lint and production build pass without new warnings attributable to Phase 18.
</success_criteria>

<output>Create `18-01-SUMMARY.md` with token values, theme boot protocol, primitive mapping, reduced-motion behavior and exact verification evidence.</output>
