---
phase: 18-frontend-motion-and-transition-system
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
execution_gate: "Explicit implementation authorization required"
files_modified:
  - frontend/src/lib/use-dismissable-layer.ts
  - frontend/src/lib/use-dismissable-layer.test.tsx
  - frontend/src/components/app-shell.tsx
  - frontend/src/components/reader/chapter-sidebar.tsx
  - frontend/src/components/reader/reader-preferences.tsx
  - frontend/src/components/reader/reader-preferences.test.tsx
  - frontend/src/components/reader/reader-chat-panel.tsx
  - frontend/src/components/reader/reader-chat-panel.test.tsx
  - frontend/src/components/reader/search-panel.tsx
  - frontend/src/components/relationships/relationship-evidence-panel.tsx
  - frontend/src/components/clues/clue-evidence-panel.tsx
  - frontend/src/app/novels/[id]/page.tsx
requirements: [UI-MOTION-02, UI-MOTION-03, UI-MOTION-05, UI-MOTION-06]
autonomous: false
must_haves:
  truths:
    - "Every custom sidebar/settings/chat/search/evidence surface closes consistently by outside click, Escape, or explicit control without internal-click or trigger-race dismissal."
    - "Only the topmost nested layer dismisses, and focus returns to the opening trigger after close."
    - "Reader panels enter and exit without covering the chat composer, progress bar, mobile navigation, or changing business state."
  artifacts:
    - path: "frontend/src/lib/use-dismissable-layer.ts"
      provides: "Controlled topmost outside-pointer, Escape, presence, and focus-return contract"
    - path: "frontend/src/components/reader/reader-chat-panel.tsx"
      provides: "Motion-safe chat panel with preserved sessions/jobs/input behavior"
    - path: "frontend/src/components/clues/clue-evidence-panel.tsx"
      provides: "Spatial evidence panel and nested-confirmation dismissal ordering"
  key_links:
    - from: "frontend/src/lib/use-dismissable-layer.ts"
      to: "frontend/src/components/reader/reader-preferences.tsx"
      via: "controlled open/onOpenChange, root and trigger refs"
      pattern: "onOpenChange|triggerRef|layerRef"
    - from: "frontend/src/app/novels/[id]/page.tsx"
      to: "frontend/src/components/reader/reader-chat-panel.tsx"
      via: "stable bottom inset and progress/chat layout contract"
      pattern: "chatOpen|ProgressBar"
---

<objective>Give application navigation and reader/analysis panels one predictable spatial and dismissal behavior while preserving every existing controlled business flow.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-frontend-motion-and-transition-system/18-CONTEXT.md
@.planning/phases/18-frontend-motion-and-transition-system/18-RESEARCH.md
@.planning/phases/18-frontend-motion-and-transition-system/18-01-SUMMARY.md
@frontend/src/components/app-shell.tsx
@frontend/src/components/reader/reader-preferences.tsx
@frontend/src/components/reader/reader-chat-panel.tsx
@frontend/src/components/reader/search-panel.tsx
@frontend/src/app/novels/[id]/page.tsx
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish one controlled dismissable-layer contract</name>
  <files>frontend/src/lib/use-dismissable-layer.ts, frontend/src/lib/use-dismissable-layer.test.tsx</files>
  <action>Create a dependency-free hook/helper for bespoke non-Base-UI surfaces. It must accept controlled open/onDismiss, layer/trigger refs and modal/topmost options; handle composed-path pointerdown outside, Escape, opening-pointer protection, nested layer ordering, focus restoration and a bounded closing presence state using the spatial token. Closing content becomes non-interactive and is not a second source of business truth. Cover outside/internal/trigger pointer paths, Escape, nested layers, rapid reopen, unmount and focus return with fake timers where needed. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/lib/use-dismissable-layer.test.tsx</automated></verify>
  <done>All bespoke surfaces can share deterministic dismissal/presence semantics without global selector coupling.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply spatial motion to AppShell and reader controls</name>
  <files>frontend/src/components/app-shell.tsx, frontend/src/components/reader/chapter-sidebar.tsx, frontend/src/components/reader/reader-preferences.tsx, frontend/src/components/reader/reader-preferences.test.tsx, frontend/src/components/reader/search-panel.tsx, frontend/src/app/novels/[id]/page.tsx</files>
  <action>Use semantic tokens for desktop/mobile nav feedback, chapter drawer, settings popover and reader search sheet. Replace immediate conditional disappearance only where needed for exit presence, keep fixed dimensions during motion, use transform/opacity rather than width/height animation, and choose `scrollIntoView` auto under reduced-motion. Wire the shared dismissable contract so trigger toggle, outside click, Escape and focus return agree. Preserve existing reader mode/theme/autoscroll/search/chapter behavior and ensure immersive/full-page positioning remains unchanged. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/components/reader/reader-preferences.test.tsx src/components/reader/progress-bar.test.tsx; npm run lint</automated></verify>
  <done>Navigation, directory, settings and search surfaces transition consistently without layout, reading or input regressions.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Apply topmost dismissal and collision-safe motion to chat/evidence panels</name>
  <files>frontend/src/components/reader/reader-chat-panel.tsx, frontend/src/components/reader/reader-chat-panel.test.tsx, frontend/src/components/relationships/relationship-evidence-panel.tsx, frontend/src/components/clues/clue-evidence-panel.tsx, frontend/src/app/novels/[id]/page.tsx</files>
  <action>Replace duplicate chat outside-pointer logic and evidence-backdrop-only behavior with the shared layer contract while retaining each controlled API. Animate chat collapsed/open/close and right-side evidence entry/exit with spatial transform/opacity; nested clue confirmation dialogs must dismiss before the parent and internal form interaction must not close either layer. Reserve explicit bottom clearance between reader progress/mobile navigation and the chat composer at both desktop and narrow viewport, with no z-index change that places progress above input. Preserve conversation polling, jobs, selections, clue actions and relationship navigation byte-for-byte at the data/API seam. Test, Fix, and Confirm.</action>
  <verify><automated>cd frontend; npm test -- --run src/components/reader/reader-chat-panel.test.tsx src/components/clues/clue-workspace.test.tsx src/app/analysis/relationships.test.tsx; npm run lint; npm run build</automated></verify>
  <done>Chat and evidence surfaces close predictably, nested layers are safe, and the composer remains reachable above fixed progress/navigation UI.</done>
</task>
</tasks>

## Must-Haves

- Controlled `open` remains authoritative; presence state exists only to render an exit transition.
- Outside click closes only the topmost eligible surface; internal form clicks and the opening pointer never close it.
- Escape and explicit close remain available; focus returns to the trigger and modal semantics remain correct.
- Panel animations use transform/opacity and retain stable occupied geometry/insets.
- Reader chat/session/job, preferences, chapter, search, relationship and clue business behavior does not change.
- Desktop and mobile bottom fixed elements have non-overlapping, measurable regions.

## Verification

<verification>Run dismissable hook and reader/domain Vitest suites, frontend lint and Next build. Defer real desktop/mobile geometry and touch qualification to Plan 18-03's dedicated Playwright matrix.</verification>
<success_criteria>
- Hook tests pass for outside/internal/trigger/Escape/nested/rapid-reopen/focus-return paths.
- Reader settings and chat component tests retain existing behavior and add dismissal/presence coverage.
- Clue nested confirmation cannot dismiss or trigger the parent by accident.
- No touched surface uses arbitrary duration, linear easing, layout-dimension animation or permanent will-change.
- Lint and production build pass.
</success_criteria>

<output>Create `18-02-SUMMARY.md` with layer ordering, focus protocol, component adoption matrix, collision strategy and verification evidence.</output>
