---
phase: 17-frozen-single-book-qualification-and-candidate-verdict
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - backend/app/services/narrative_memory/qualification_baseline.py
  - backend/app/services/narrative_memory/qualification_runner.py
  - backend/app/services/narrative_memory/qualification_metrics.py
  - backend/app/services/narrative_memory/qualification_verdict.py
  - backend/tests/unit/narrative_memory/test_qualification_metrics.py
  - backend/tests/unit/narrative_memory/test_qualification_verdict.py
  - backend/tests/integration/narrative_memory/test_qualification_runner_pg.py
  - backend/tests/adversarial/test_narrative_memory_qualification.py
requirements: [V08-QUAL-02, V08-QUAL-03, V08-QUAL-04, V08-QUAL-05]
autonomous: false
execution_preconditions:
  - "17-01 fixture and policy are frozen and reviewed before result generation."
  - "Phases 12-16 are complete and independently verified."
  - "Any live provider run has a separately acknowledged hard budget; deterministic fake transports remain the default verification path."
must_haves:
  truths:
    - "Hierarchical candidate and leaf/raw baseline execute as paired strategies over the same source, cutoff, question, final leaf validator and generation/Judge budget."
    - "Every required per-case, per-bucket, per-strategy and aggregate retrieval, answer, safety, latency, cost, cache, reuse and fallback metric is present or the run is blocked."
    - "Deterministic scope/evidence/spoiler gates cannot be overridden by Judge scores, averages or baseline improvement."
  artifacts:
    - path: "backend/app/services/narrative_memory/qualification_baseline.py"
      provides: "Cutoff-first Phase 07 leaf/raw baseline using the Phase 15 final citation validator"
    - path: "backend/app/services/narrative_memory/qualification_metrics.py"
      provides: "Complete metric cells and per-case/bucket/strategy/aggregate recomputation"
    - path: "backend/app/services/narrative_memory/qualification_verdict.py"
      provides: "Pure predeclared threshold evaluator returning only two candidate verdicts"
  key_links:
    - from: "backend/app/services/narrative_memory/qualification_runner.py"
      to: "backend/app/services/narrative_memory/experiments.py"
      via: "explicit-version hierarchical strategy with default-off Phase 15 retrieval"
      pattern: "version_id|reading_cutoff"
    - from: "backend/app/services/narrative_memory/qualification_baseline.py"
      to: "backend/app/services/narrative_memory/citations.py"
      via: "same visible-leaf re-slice/hash authority as candidate"
      pattern: "leaf_raw_baseline|content_hash"
    - from: "backend/app/services/narrative_memory/qualification_metrics.py"
      to: "Phase 16 reuse report"
      via: "database-verifiable rebuilt/carried/stale and actual/avoided economics"
      pattern: "observed_actual|full_rebuild_upper_bound|avoided_upper_bound"
---

<objective>Run a fair same-source hierarchical-vs-leaf comparison and compute a complete, fail-closed single-book candidate report without persisting a final verdict yet.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-frozen-single-book-qualification-and-candidate-verdict/17-CONTEXT.md
@.planning/phases/17-frozen-single-book-qualification-and-candidate-verdict/17-RESEARCH.md
@.planning/phases/17-frozen-single-book-qualification-and-candidate-verdict/17-01-SUMMARY.md
@.planning/phases/15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety/15-03-SUMMARY.md
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-03-SUMMARY.md
@backend/app/services/narrative_memory/experiments.py
@backend/app/services/narrative_memory/citations.py
@backend/app/services/rag_quality.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Execute paired hierarchical and same-source leaf baseline cases</name>
  <files>backend/app/services/narrative_memory/qualification_baseline.py, backend/app/services/narrative_memory/qualification_runner.py, backend/tests/integration/narrative_memory/test_qualification_runner_pg.py</files>
  <action>Implement a cutoff-first leaf/raw baseline over the same frozen Phase 07 evidence universe and final Phase 15 citation validator, explicitly bypassing candidate node/claim/summary data. Implement a paired runner that validates common-envelope equality, alternates a pre-frozen strategy order, uses separate exact-cache namespaces, and records retrieval/answer/Judge/call artifacts with stable checksums. Candidate path invokes the explicit-version default-off Phase 15 experiment; both paths share generator prompt/schema/deployment, calibrated isolated Judge, timeout/retry, token/cost reservation, price snapshot and hard run ceiling. No-answer with no valid evidence follows the shared abstention policy rather than fabricating an answer. Deterministic fake generator/Judge transports are the default tests; provider failure, budget exhaustion, unknown usage or lineage drift block the pair. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_qualification_runner_pg.py -q -x</automated></verify>
  <done>Every case produces two comparable, independently cached, leaf-validated strategy artifacts or one explicit blocked pair with complete call evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compute complete quality, safety, performance and reuse metrics</name>
  <files>backend/app/services/narrative_memory/qualification_metrics.py, backend/tests/unit/narrative_memory/test_qualification_metrics.py, backend/tests/adversarial/test_narrative_memory_qualification.py</files>
  <action>Compute strict MetricCell objects with numerator/denominator/value/unit/status/case IDs for gold leaf recall@k, reciprocal rank, nDCG@k, route hit, fallback rate/reasons, citation accept/reject, no-answer abstention/false-answer, spoiler leakage, deterministic claim support/critical unsupported, calibrated Judge faithfulness/relevance, retrieval/generation/Judge/end-to-end latency p50/p95, calls/tokens/cost by role/strategy/total, cache hits, and Phase 16 rebuilt/carried/stale plus observed-actual, full-rebuild-upper-bound and avoided-upper-bound economics. Preserve Phase 16's formula inputs and estimate labels; never present an upper bound as observed savings. Aggregate per case, required bucket, strategy and whole run without hiding denominators. Reject empty required buckets, missing cells, invalid denominators, NaN/Infinity, unknown prices/usage, non-recomputable reuse values and public spoiler text. Test exact quantiles/ranking/ties and order independence. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_qualification_metrics.py tests/adversarial/test_narrative_memory_qualification.py -q -x</automated></verify>
  <done>One deterministic complete report accounts for every required quality, safety, latency, cost, cache, fallback and reuse dimension without silent zero-fill.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Apply the pure fail-closed two-verdict policy</name>
  <files>backend/app/services/narrative_memory/qualification_verdict.py, backend/tests/unit/narrative_memory/test_qualification_verdict.py, backend/tests/integration/narrative_memory/test_qualification_runner_pg.py, backend/tests/adversarial/test_narrative_memory_qualification.py</files>
  <action>Implement the fixed gate order from 17-RESEARCH: frozen integrity → scope/lineage → structural/build/retrieval/reuse prerequisites → metric completeness → zero-tolerance spoiler/citation/critical-unsupported gates → per-bucket/aggregate absolute thresholds → paired baseline non-regression → p95/budget/economics thresholds. Return only `qualified_candidate` when every gate passes; otherwise `blocked` with stable sorted reason codes and exact failing metric references. A favorable Judge, aggregate average or candidate improvement cannot override any deterministic failure. Include the mandatory single-book/no-promotion/no-consumer/v0.3 disclaimer in both outcomes. Add adversarial cases for future metadata, summary citation, wrong owner/version, no-answer hallucination, Judge false positive, cache crossover, partial build, missing reuse, cost undercount and threshold tuning attempt. Add a forbidden-capability scan excluding Phase 06 promotion functions, active baseline, pointer setters, APIs, Reader Chat and candidate mutation. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_qualification_metrics.py tests/unit/narrative_memory/test_qualification_verdict.py tests/integration/narrative_memory/test_qualification_runner_pg.py tests/adversarial/test_narrative_memory_qualification.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/qualification_baseline.py app/services/narrative_memory/qualification_runner.py app/services/narrative_memory/qualification_metrics.py app/services/narrative_memory/qualification_verdict.py tests/unit/narrative_memory/test_qualification_metrics.py tests/unit/narrative_memory/test_qualification_verdict.py tests/integration/narrative_memory/test_qualification_runner_pg.py tests/adversarial/test_narrative_memory_qualification.py</automated></verify>
  <done>The pure evaluator is deterministic, complete, non-promoting and incapable of qualifying any hard safety, lineage, completeness or pointer violation.</done>
</task>
</tasks>

## Must-Haves

- Baseline reads only cutoff-visible Phase 07 leaves and uses the same final citation validator as candidate.
- Both strategies share source/cutoff/query/retrieval/generation/Judge/pricing/budget fields exactly.
- Strategy caches are isolated and cache status is reported; cache cannot create asymmetric hidden work.
- All metrics retain denominators and case IDs and are complete at case/bucket/strategy/aggregate levels.
- No-answer, spoiler, invalid citation and critical unsupported evidence are deterministic hard gates.
- Judge is calibrated and isolated but never authoritative over deterministic gates.
- Missing/invalid metrics, prices, reuse authority or source lineage block; nothing is defaulted to success.
- Evaluation code has no persistence of final authority, promotion, active pointer, product API or consumer cutover.

## Verification

<verification>After explicit execution authorization and completion of 17-01 plus Phase 12–16, run paired PostgreSQL integration tests with deterministic transports, metric/verdict unit tests, adversarial safety tests, forbidden-capability scans and Ruff.</verification>
<success_criteria>
- Every frozen case produces comparable candidate and baseline artifacts or an explicit block.
- All required metrics are reproduced independent of row/case insertion order.
- Zero-tolerance violations always block even with perfect Judge/relevance or baseline improvement.
- Equal inputs produce byte-identical reports and reason-code ordering.
- No Phase 06 baseline promotion, production pointer, Reader Chat or candidate write capability is reachable.
</success_criteria>

<output>Create `17-02-SUMMARY.md` with paired-field proof, per-bucket metric inventory, threshold gate order, adversarial results and provider/cost accounting evidence.</output>
