---
phase: 17-frozen-single-book-qualification-and-candidate-verdict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/narrative_memory/qualification_contracts.py
  - backend/app/services/narrative_memory/qualification_fixtures.py
  - backend/tests/fixtures/narrative_memory/qualification/single_book_v1.json
  - backend/tests/fixtures/narrative_memory/qualification/policy_v1.json
  - backend/tests/unit/narrative_memory/test_qualification_contracts.py
  - backend/tests/unit/narrative_memory/test_qualification_fixtures.py
  - backend/tests/integration/narrative_memory/test_qualification_fixture_pg.py
requirements: [V08-QUAL-01, V08-QUAL-02, V08-QUAL-03, V08-QUAL-05]
autonomous: false
execution_preconditions:
  - "User explicitly re-authorizes implementation after planning-only mode."
  - "Phases 12-16 are complete and independently verified; Phase 13 WIP is resolved."
  - "A single owner/novel/candidate version and pre-result fixture authoring source are explicitly selected."
must_haves:
  truths:
    - "The single-book question fixture and threshold policy are frozen before any candidate result is observed and cannot contain result-derived fields."
    - "The fixture has non-empty local, cross-chapter/arc, whole-book/global, no-answer, and spoiler buckets with exact gold Phase 07 leaf identities."
    - "Candidate and leaf baseline case envelopes are field-identical for source, cutoff, query, budgets, answer lineage, pricing, and metrics except for retrieval strategy."
  artifacts:
    - path: "backend/app/services/narrative_memory/qualification_contracts.py"
      provides: "Strict frozen fixture, policy, case, paired-envelope, metric and two-verdict schemas"
    - path: "backend/app/services/narrative_memory/qualification_fixtures.py"
      provides: "Canonical freeze/hash and PostgreSQL gold-leaf prevalidation without candidate-result access"
    - path: "backend/tests/fixtures/narrative_memory/qualification/single_book_v1.json"
      provides: "Versioned five-bucket single-book qualification fixture"
  key_links:
    - from: "backend/app/services/narrative_memory/qualification_fixtures.py"
      to: "backend/app/services/narrative_memory/citations.py"
      via: "same authoritative Unicode leaf re-slice/hash validation planned by Phase 15"
      pattern: "content_hash|start_offset|end_offset"
    - from: "PairedCaseEnvelope"
      to: "QualificationPolicy"
      via: "one frozen cutoff/retrieval/generation/Judge/budget/price contract for both strategies"
      pattern: "hierarchical_candidate|leaf_raw_baseline"
---

<objective>Freeze a result-independent five-bucket single-book fixture and a strict paired-comparison policy before any qualification execution is possible.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/HANDOFF.json
@.planning/phases/17-frozen-single-book-qualification-and-candidate-verdict/17-CONTEXT.md
@.planning/phases/17-frozen-single-book-qualification-and-candidate-verdict/17-RESEARCH.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-VERIFICATION.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-VERIFICATION.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-VERIFICATION.md
@.planning/phases/15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety/15-VERIFICATION.md
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-VERIFICATION.md
@backend/app/services/narrative_memory/contracts.py
@backend/app/services/narrative_memory/citations.py
</context>

## Steps

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Authorize Phase 17 qualification after Phases 12–16 are complete and independently verified</action>
  <instructions>Planning-only lock is active. Before any file change or command, confirm passed verification reports for Phases 12 through 16 and obtain the user's explicit instruction `批准执行 Phase 17`. Auto mode must not bypass this gate.</instructions>
  <verification>Phase 12–16 verification reports are passed and the current conversation contains explicit Phase 17 execution authorization.</verification>
  <resume-signal>批准执行 Phase 17</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 1: Define strict frozen qualification and verdict contracts</name>
  <files>backend/app/services/narrative_memory/qualification_contracts.py, backend/tests/unit/narrative_memory/test_qualification_contracts.py</files>
  <action>Define frozen strict Pydantic contracts with `extra=forbid` and no coercion for fixture identity, policy, question case, five closed bucket values, expected answerability, graded gold Phase 07 leaf refs, spoiler-forbidden identities, PairedCaseEnvelope, generator/Judge/pricing/budget lineage, complete metric cells and reports. Public verdict is a closed two-value enum: `qualified_candidate|blocked`; include `qualification_kind=single_book_candidate` and a mandatory no-promotion/no-cutover/v0.3-scope disclaimer. Reject candidate answer/trace/score/report fields in fixture inputs, empty required buckets, duplicate case keys, cross-snapshot gold refs, invalid cutoff, unknown price, uncalibrated/same Generator-Judge lineage, NaN/Infinity and ambiguous thresholds. Add canonical Unicode-preserving JSON and SHA-256 helpers that never hash a digest into itself. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_qualification_contracts.py -q -x</automated></verify>
  <done>The qualification language is closed, deterministic and incapable of representing a result-derived fixture, incomplete metric or promotion-like verdict.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Freeze and prevalidate the five-bucket single-book fixture</name>
  <files>backend/app/services/narrative_memory/qualification_fixtures.py, backend/tests/fixtures/narrative_memory/qualification/single_book_v1.json, backend/tests/fixtures/narrative_memory/qualification/policy_v1.json, backend/tests/unit/narrative_memory/test_qualification_fixtures.py, backend/tests/integration/narrative_memory/test_qualification_fixture_pg.py</files>
  <action>Implement a freezer that accepts only an explicitly reviewed authoring bundle plus owner/novel/source/hierarchy/candidate lineage and emits immutable fixture/policy hashes before a qualification run exists. Validate every gold leaf in a fresh PostgreSQL session against the same owner, novel, Phase 07 build/snapshot, chapter, Unicode code-point offsets and content hash; verify each cutoff and spoiler forbidden set without serializing hidden titles/text into public fields. Create one deterministic controlled single-book fixture with nonzero cases in local, cross_chapter_arc, whole_book_global, no_answer and spoiler buckets and a policy with predeclared absolute/relative/safety/sample thresholds. Prove insertion-order independence, one-field hash sensitivity and byte-identical reload. Do not call a provider, candidate retrieval, Judge or Phase 17 result reader. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_qualification_fixtures.py tests/integration/narrative_memory/test_qualification_fixture_pg.py -q -x</automated></verify>
  <done>The fixture/policy are frozen before results, all five buckets and gold leaves are valid, and stale/tampered/cross-scope inputs fail with zero provider calls.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove paired-envelope comparability and execution gates</name>
  <files>backend/app/services/narrative_memory/qualification_contracts.py, backend/app/services/narrative_memory/qualification_fixtures.py, backend/tests/unit/narrative_memory/test_qualification_contracts.py, backend/tests/integration/narrative_memory/test_qualification_fixture_pg.py</files>
  <action>Build exactly two envelopes per case—hierarchical_candidate and leaf_raw_baseline—from one frozen common envelope. Compare every authoritative field and allow only strategy plus strategy-scoped cache namespace to differ; require identical query, source, build, cutoff, visible leaf universe, top-k/leaf/rerank limits, prompt/schema/deployment/decoding, timeout/retry, token/cost ceilings, price snapshot and metric version. Add preflight checks that Phase 12 is reusable_exact, Phase 13 candidate is sealed/structurally verified, Phase 14 terminal report is complete, and Phase 15/16 verified artifacts match the same lineage. Missing verification files, current Phase 13 WIP, partial build or unavailable price produces a deterministic blocked preflight before calls. Add a capability scan excluding candidate result readers during freeze, provider invocation, promotion, active/current pointer, Reader Chat and production API imports. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_qualification_contracts.py tests/unit/narrative_memory/test_qualification_fixtures.py tests/integration/narrative_memory/test_qualification_fixture_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/qualification_contracts.py app/services/narrative_memory/qualification_fixtures.py tests/unit/narrative_memory/test_qualification_contracts.py tests/unit/narrative_memory/test_qualification_fixtures.py tests/integration/narrative_memory/test_qualification_fixture_pg.py</automated></verify>
  <done>Every future comparison is demonstrably paired and Phase 17 cannot start while any Phase 12–16 authority or pricing prerequisite is incomplete.</done>
</task>
</tasks>

## Must-Haves

- Fixture and policy are frozen and hashed before any candidate result/metric access.
- All five required buckets are non-empty and each case has explicit answerability, cutoff and gold/exclusion authority.
- Gold evidence is a verified Phase 07 leaf slice, never candidate summary, claim, similarity or stored excerpt.
- Candidate and baseline differ only by retrieval strategy and strategy-isolated cache identity.
- Generator/Judge deployments are calibrated, frozen and isolated; unknown prices or budgets fail before calls.
- Current Phase 13 paused WIP and any missing Phase 14–16 verification are hard execution blockers.
- This plan contains no provider call, result tuning, API, pointer, promotion or consumer capability.

## Verification

<verification>After execution is separately authorized and Phase 12–16 verification artifacts exist, run strict contract/unit tests, fresh-PostgreSQL gold-leaf fixture tests, forbidden-capability scans and Ruff using `backend/.venv`.</verification>
<success_criteria>
- Five buckets have predeclared nonzero sample counts and byte-stable hashes.
- Every gold leaf re-slices to the frozen Chapter content and exact source/build/snapshot scope.
- Candidate-output fields and post-result fixture mutation are rejected.
- Paired envelopes are identical outside strategy/cache namespace; any other difference blocks.
- Provider call count remains zero throughout fixture freeze/preflight.
</success_criteria>

<output>Create `17-01-SUMMARY.md` with fixture/policy hashes, bucket counts, gold validation evidence, paired-field equality matrix and execution-gate proof.</output>
