---
phase: 14-durable-bottom-up-candidate-builder
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
execution_gate: "Explicit implementation authorization required"
files_modified:
  - backend/app/services/narrative_memory/global_builder.py
  - backend/app/services/narrative_memory/builder_packages.py
  - backend/app/services/narrative_memory/builder_worker.py
  - backend/app/services/narrative_memory/builder_report.py
  - backend/tests/unit/narrative_memory/test_global_packages.py
  - backend/tests/unit/narrative_memory/test_builder_report.py
  - backend/tests/integration/narrative_memory/test_global_worker_pg.py
  - backend/tests/integration/narrative_memory/test_builder_manifest_pg.py
requirements: [V08-BUILD-01, V08-BUILD-02, V08-BUILD-03, V08-BUILD-05]
autonomous: false
must_haves:
  truths:
    - "Exactly one Global Story stage runs only after every required Arc/Volume in the frozen full-cover plan is completed and revalidated; partial or blocked parents cause zero Global calls."
    - "Global claims, conflicts and open loops use only Phase 13 strict types and retain direct Phase 07 evidence links; the model cannot invent child, scope, visibility or evidence identity."
    - "Completion is accepted only when the Phase 13 database-recomputed manifest and structural report match the worker artifact, while every production pointer remains untouched."
  artifacts:
    - path: "backend/app/services/narrative_memory/global_builder.py"
      provides: "Validated-parent-only Global package, strict conflict/open-loop materialization and explicit-version persistence"
    - path: "backend/app/services/narrative_memory/builder_report.py"
      provides: "Deterministic calls/tokens/cost/cache/stage/source report bound to the database manifest"
    - path: "backend/tests/integration/narrative_memory/test_builder_manifest_pg.py"
      provides: "Independent manifest recomputation, mismatch blocking, seal/report and no-pointer proof"
  key_links:
    - from: "backend/app/services/narrative_memory/global_builder.py"
      to: "backend/app/services/narrative_memory/provenance.py"
      via: "revalidate full middle-level coverage and direct leaf closure before and after Global persistence"
      pattern: "validate"
    - from: "backend/app/services/narrative_memory/builder_worker.py"
      to: "backend/app/services/narrative_memory/manifests.py"
      via: "seal only the database-derived explicit-version manifest and compare worker artifact checksum"
      pattern: "manifest_checksum"
---

<objective>Build the single Global Story Model from complete validated parents, seal the database-derived candidate manifest, and emit a structurally accurate execution report without any production selection.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-durable-bottom-up-candidate-builder/14-CONTEXT.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-RESEARCH.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-02-SUMMARY.md
@backend/app/services/narrative_memory/provenance.py
@backend/app/services/narrative_memory/manifests.py
@backend/app/services/narrative_memory/builder_worker.py
@backend/app/services/narrative_memory/arc_planner.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the validated-parent-only Global package</name>
  <files>backend/app/services/narrative_memory/global_builder.py, backend/app/services/narrative_memory/builder_packages.py, backend/tests/unit/narrative_memory/test_global_packages.py</files>
  <action>Build exactly one canonical Global input from the frozen boundary plan and sorted Phase 13 Arc/Volume rows only after every required parent stage is completed and revalidated for owner/novel/version, range, child edges, checksums and direct leaf closure. Reject partial coverage, blocked/stale/foreign parents, a direct Chapter State bypass, mixed `story_arc`/`volume` hierarchies not allowed by the frozen plan, display-only facts and transient checkpoint content. Define strict Global output using the Phase 13 claim union; conflicts and unresolved/open loops must be typed claims with uncertainty/visibility and direct leaf references, not free prose. Script-rebind the singleton Global key, full-book range, child edges, scope, evidence and lineage; include all parent artifacts and the boundary checksum in exact-cache identity. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_global_packages.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/global_builder.py app/services/narrative_memory/builder_packages.py tests/unit/narrative_memory/test_global_packages.py</automated></verify>
  <done>Only one complete, evidence-closed and strictly typed parent set can produce a valid Global package.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Execute Global and seal only a database-derived candidate</name>
  <files>backend/app/services/narrative_memory/global_builder.py, backend/app/services/narrative_memory/builder_worker.py, backend/tests/integration/narrative_memory/test_global_worker_pg.py, backend/tests/integration/narrative_memory/test_builder_manifest_pg.py</files>
  <action>Create one `global_story:book` stage dependent on every frozen parent. If any required parent is missing/blocked/invalid, persist a stable blocked reason and perform zero Global calls. Otherwise use the shared durable reservation/gateway/cache/cancel/checkpoint path, persist Global content by explicit version, then invoke the verified Phase 13 provenance and manifest services over fresh database rows. Compare the worker artifact checksum with the independently recomputed manifest; reject caller/model/self-reported checksums, structural failures or any mismatch. Insert the immutable seal and append-only structural report only through Phase 13 APIs, mark the run completed only after matching success, and never resolve or mutate a pointer. Test valid completion, cache completion, cancellation after provider, corrupt parent/link, injected row, insertion-order invariance, manifest mismatch, seal race and post-seal write rejection. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_global_worker_pg.py tests/integration/narrative_memory/test_builder_manifest_pg.py -q -x</automated></verify>
  <done>A complete Global candidate is sealed only when fresh database provenance and manifest checks agree; incomplete or tampered candidates remain explicit and unsealed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Produce a deterministic structural build report and no-pointer observation</name>
  <files>backend/app/services/narrative_memory/builder_report.py, backend/tests/unit/narrative_memory/test_builder_report.py, backend/tests/integration/narrative_memory/test_builder_manifest_pg.py</files>
  <action>Generate the run report from persisted stages, attempts, reservations/settlements, source states and Phase 13 manifest/report rows—not from mutable worker counters. Report completed/failed/blocked/cancelled stages, dependency closure, actual calls, input/output tokens, settled cost, cache hits, source statuses, worker artifact and database manifest checksums with stable arithmetic and reason ordering. Name the result only `completed_candidate`, `partial`, `paused`, `cancelled` or `failed`; do not emit Phase 17 `qualified_candidate`. In a fresh PostgreSQL observer, snapshot complete chunk/timeline/clue/narrative-unit pointer/revision/journal authority before a controlled full build and after report creation, assert byte-equivalence, absence of a narrative-memory pointer table and no promotion/current-version capability. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_global_packages.py tests/unit/narrative_memory/test_builder_report.py tests/integration/narrative_memory/test_global_worker_pg.py tests/integration/narrative_memory/test_builder_manifest_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/global_builder.py app/services/narrative_memory/builder_report.py app/services/narrative_memory/builder_worker.py tests/unit/narrative_memory/test_global_packages.py tests/unit/narrative_memory/test_builder_report.py tests/integration/narrative_memory/test_global_worker_pg.py tests/integration/narrative_memory/test_builder_manifest_pg.py</automated></verify>
  <done>The report is reproducible from PostgreSQL authority, agrees with the sealed manifest, makes no quality claim, and the fresh observer proves all production selectors unchanged.</done>
</task>
</tasks>

## Must-Haves

- Exactly one Global node/stage exists per explicit candidate version.
- Global cannot call with missing, partial, blocked, stale or invalid middle-level coverage.
- Global uses only validated Arc/Volume authority and cannot bypass directly to Chapter State.
- Conflicts/open loops remain strict typed, uncertainty-aware, visibility-bounded and directly evidence-linked.
- Phase 13 recomputes provenance and manifest from fresh database rows; worker/model checksums never self-qualify.
- Report metrics derive from persisted attempts/settlements/stages and do not claim Phase 17 qualification.
- Sealing, validation and reporting leave every production pointer/revision/journal byte-equivalent.

## Verification

<verification>Run strict Global package tests, controlled PostgreSQL Global/cache/cancel tests, manifest corruption/race tests, report arithmetic tests, fresh-observer no-pointer tests, prior Phase 14 regressions and Ruff.</verification>
<success_criteria>
- Partial/invalid parents result in zero Global calls and an explicit blocked dependency.
- Valid Global claims all resolve directly to exact same-snapshot Phase 07 leaves.
- Database-recomputed manifest equals the worker artifact under insertion-order variation and rejects every tamper.
- Calls/tokens/cost/cache/stage/source report totals reconcile exactly with immutable rows.
- No active pointer, promotion, implicit current version or quality verdict is introduced.
</success_criteria>

<output>Create `14-03-SUMMARY.md` with Global gating cases, manifest recomputation evidence, report reconciliation totals and fresh-observer pointer snapshots.</output>
