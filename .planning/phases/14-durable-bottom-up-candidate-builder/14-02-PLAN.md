---
phase: 14-durable-bottom-up-candidate-builder
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
execution_gate: "Explicit implementation authorization required"
files_modified:
  - backend/app/services/narrative_memory/arc_planner.py
  - backend/app/services/narrative_memory/builder_packages.py
  - backend/app/services/narrative_memory/builder_worker.py
  - backend/tests/unit/narrative_memory/test_arc_planner.py
  - backend/tests/unit/narrative_memory/test_arc_packages.py
  - backend/tests/integration/narrative_memory/test_arc_worker_pg.py
requirements: [V08-BUILD-01, V08-BUILD-02, V08-BUILD-03, V08-BUILD-05]
autonomous: false
must_haves:
  truths:
    - "Arc/Volume boundaries are selected without a model call, frozen before aggregation, and form one deterministic continuous, non-overlapping full cover of the eligible chapter range."
    - "An Arc/Volume provider package contains only completed revalidated Chapter States plus direct Phase 07 leaf closure; display summaries and transient worker output cannot become parent authority."
    - "A failed chapter blocks only the parent range that contains it and leaves independent completed parents and children byte-identical across resume."
  artifacts:
    - path: "backend/app/services/narrative_memory/arc_planner.py"
      provides: "Explicit-volume preference, deterministic fallback ranges, frozen plan checksum, and dependency closure"
    - path: "backend/tests/integration/narrative_memory/test_arc_worker_pg.py"
      provides: "PostgreSQL proof of validated-child-only aggregation, local blocking, cache/checkpoint reuse and resume"
  key_links:
    - from: "backend/app/services/narrative_memory/arc_planner.py"
      to: "narrative_memory_build_stages"
      via: "persist the immutable boundary plan and chapter-stage dependency keys before any parent call"
      pattern: "boundary_plan_checksum"
    - from: "arc/volume package"
      to: "narrative_memory_source_links"
      via: "carry direct validated child leaf links into every parent claim"
      pattern: "evidence_node_id"
---

<objective>Freeze legal Story Arc/Volume coverage and aggregate completed Chapter States into independently resumable, evidence-closed middle-level candidates.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-durable-bottom-up-candidate-builder/14-CONTEXT.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-RESEARCH.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-01-SUMMARY.md
@backend/app/services/narrative_memory/contracts.py
@backend/app/services/narrative_memory/provenance.py
@backend/app/services/narrative_memory/builder_worker.py
@backend/app/services/narrative_memory/builder_packages.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Select and freeze deterministic Volume/Arc boundaries</name>
  <files>backend/app/services/narrative_memory/arc_planner.py, backend/tests/unit/narrative_memory/test_arc_planner.py</files>
  <action>Implement a pure boundary planner that prefers explicit volume metadata only when its frozen source, ordering, positive inclusive ranges, continuity, non-overlap and exact eligible-chapter coverage validate. Otherwise apply one policy-versioned deterministic consecutive-arc rule with stable node/stage keys; never ask a model to choose boundaries. Return a canonical plan checksum covering source kind/status, policy, ranges and chapter identities. Reject gaps, overlaps, duplicates, missing/foreign chapters, mixed snapshot metadata and a boundary change after stages exist. Compute chapter→parent and parent→global dependency closure so a chapter failure maps only to its containing parent plus Global. Test explicit, fallback, Unicode labels as non-authoritative metadata, insertion-order independence, invalid inputs and checksum sensitivity. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_arc_planner.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/arc_planner.py tests/unit/narrative_memory/test_arc_planner.py</automated></verify>
  <done>The same frozen inputs always produce one legal full-cover boundary plan, and invalid or changed boundaries fail before parent provider work.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build strict validated-child Arc/Volume packages</name>
  <files>backend/app/services/narrative_memory/builder_packages.py, backend/tests/unit/narrative_memory/test_arc_packages.py</files>
  <action>Load parent input from sorted Phase 13 database rows for every child Chapter State in the frozen range, requiring completed matching stages, strict typed claims, expected content checksums, direct leaf links and current source revalidation. Reject missing/failed/stale/foreign children, display-summary-only packages, package-external IDs, arbitrary worker checkpoints and inherited evidence without an explicit parent-claim link. Define strict Arc/Volume output DTOs using the Phase 13 closed union, legal ranges/visibility and explicit child edges. Rebind owner/novel/version/node/claim/evidence coordinates server-side and require direct Phase 07 leaf closure for every aggregated claim. Include boundary plan, child manifests, optional-source lineage/status and all generation lineage in package/cache hashes. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_builder_packages.py tests/unit/narrative_memory/test_arc_packages.py -q -x</automated></verify>
  <done>Only complete revalidated Chapter State authority can enter a parent package, and every accepted parent claim remains directly evidence-bound.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Execute independent Arc/Volume stages with local blocking and resume</name>
  <files>backend/app/services/narrative_memory/builder_worker.py, backend/tests/integration/narrative_memory/test_arc_worker_pg.py</files>
  <action>Persist the frozen boundary plan/checksum before creating one parent stage per range and deterministic dependency keys to its Chapter State stages. Schedule only parents whose complete child set revalidates; mark a containing parent `blocked_dependency` without a call when any child failed, while allowing disjoint parents to complete. Route eligible parents through the same reservation, fixed gateway, bounded repair, exact cache, cancellation and checkpoint contract as 14-01, then persist strict nodes/claims/edges/direct links by explicit version. Exercise multiple volumes/arcs, partial chapter failure, unrelated parent completion, cancellation, crash, cache recovery, competing workers and a resume where only the failed chapter, its parent and later Global become eligible; compare unaffected child/parent rows and artifacts byte-for-byte. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_arc_planner.py tests/unit/narrative_memory/test_arc_packages.py tests/integration/narrative_memory/test_arc_worker_pg.py tests/integration/narrative_memory/test_chapter_state_worker_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/arc_planner.py app/services/narrative_memory/builder_packages.py app/services/narrative_memory/builder_worker.py tests/unit/narrative_memory/test_arc_planner.py tests/unit/narrative_memory/test_arc_packages.py tests/integration/narrative_memory/test_arc_worker_pg.py</automated></verify>
  <done>Middle-level candidates execute only from complete children, isolate local failures, and resume without rewriting any unaffected chapter or parent artifact.</done>
</task>
</tasks>

## Must-Haves

- Boundary selection is deterministic, versioned, frozen and provider-free.
- Explicit volume metadata is preferred only when it exactly and legally covers the eligible source; fallback arcs also provide exact full coverage.
- Every parent input comes from completed Phase 13 candidate authority, not transient checkpoints or display summaries.
- Every parent claim retains its own direct Phase 07 leaf links and legal child edges.
- A chapter failure blocks only its containing parent and Global; disjoint parents remain runnable.
- Retry/cancel/cache semantics preserve completed child and parent artifacts byte-identically.
- Reader Chat remains excluded from all boundary, package and aggregation paths.

## Verification

<verification>Run pure boundary/package tests, PostgreSQL parent-stage dependency/failure/resume tests, 14-01 chapter regressions, forbidden-field scans and Ruff.</verification>
<success_criteria>
- Explicit and fallback plans are stable, continuous, non-overlapping and exact-cover.
- No parent call occurs with an incomplete, failed, stale or foreign child.
- One broken chapter does not rerun or mutate disjoint completed children/parents.
- Parent cache keys change for every authoritative child, boundary, optional-source or generation-lineage change.
- Accepted Arc/Volume claims all pass direct leaf closure.
</success_criteria>

<output>Create `14-02-SUMMARY.md` with boundary policy/checksum examples, parent dependency graph, calls avoided by local blocking/cache, and byte-identity resume evidence.</output>
