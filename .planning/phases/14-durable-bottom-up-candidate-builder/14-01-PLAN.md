---
phase: 14-durable-bottom-up-candidate-builder
plan: 01
type: execute
wave: 1
depends_on: []
execution_gate: "Phase 13 complete and independently verified; explicit implementation authorization required"
files_modified:
  - backend/app/models/narrative_memory_builder.py
  - backend/app/models/__init__.py
  - backend/migrations/versions/14_narrative_memory_builder_control.py
  - backend/app/services/narrative_memory/builder_contracts.py
  - backend/app/services/narrative_memory/builder_repository.py
  - backend/app/services/narrative_memory/builder_budget.py
  - backend/app/services/narrative_memory/builder_gateway.py
  - backend/app/services/narrative_memory/builder_packages.py
  - backend/app/services/narrative_memory/builder_worker.py
  - backend/tests/unit/narrative_memory/test_builder_contracts.py
  - backend/tests/unit/narrative_memory/test_builder_packages.py
  - backend/tests/integration/narrative_memory/test_builder_control_pg.py
  - backend/tests/integration/narrative_memory/test_chapter_state_worker_pg.py
requirements: [V08-BUILD-01, V08-BUILD-02, V08-BUILD-03, V08-BUILD-05]
autonomous: false
must_haves:
  truths:
    - "A durable explicit-version run cannot reach provider transport unless the current Phase 12 eligibility report exactly matches the immutable Phase 13 version and provider_calls_allowed is true."
    - "Every model call is preceded by a committed row-locked budget reservation and exact frozen cache identity; unknown pricing, dependency drift, cancellation, or a budget ceiling produces zero new provider calls."
    - "Chapter State stages are independently checkpointed and idempotent, so cancellation, process loss, retry, and one chapter failure preserve completed siblings byte-for-byte."
  artifacts:
    - path: "backend/app/models/narrative_memory_builder.py"
      provides: "Builder-owned run, stage/checkpoint, call-attempt, budget ledger/reservation, and report authority without a pointer"
    - path: "backend/app/services/narrative_memory/builder_worker.py"
      provides: "Lease-safe Chapter State execution, cancellation polling, exact-cache recovery, and explicit-version persistence"
    - path: "backend/tests/integration/narrative_memory/test_chapter_state_worker_pg.py"
      provides: "Controlled-transport PostgreSQL proof of eligibility, budget, cache, checkpoint, cancellation, failure isolation, and resume"
  key_links:
    - from: "backend/app/services/narrative_memory/builder_worker.py"
      to: "backend/app/services/narrative_memory/audit.py"
      via: "consume the current EligibilityReport and its derived provider_calls_allowed guard before reserving or calling"
      pattern: "provider_calls_allowed"
    - from: "backend/app/services/narrative_memory/builder_worker.py"
      to: "backend/app/services/narrative_memory/authority.py"
      via: "persist strict Chapter State packages by explicit owner/novel/version only"
      pattern: "version_id"
---

<objective>Establish an isolated durable builder control plane and produce evidence-bound Chapter State candidates with fail-closed eligibility, budget, cache, checkpoint, cancellation and resume semantics.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-VERIFICATION.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-VERIFICATION.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-CONTEXT.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-RESEARCH.md
@backend/app/models/narrative_memory.py
@backend/app/services/narrative_memory/contracts.py
@backend/app/services/narrative_memory/authority.py
@backend/app/services/narrative_memory/audit.py
@backend/app/services/timeline/worker.py
@backend/app/services/timeline/model_gateway.py
@backend/app/services/timeline/budget.py
</context>

## Steps

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Authorize Phase 14 implementation after Phase 13 is complete and independently verified</action>
  <instructions>Planning-only lock is active. Before any file change or command, confirm `.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-VERIFICATION.md` exists with `status: passed` and obtain the user's explicit instruction `批准执行 Phase 14`. Auto mode must not bypass this gate.</instructions>
  <verification>Phase 13 verification is passed and the current conversation contains explicit Phase 14 execution authorization.</verification>
  <resume-signal>批准执行 Phase 14</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 1: Add the builder-only PostgreSQL control plane</name>
  <files>backend/app/models/narrative_memory_builder.py, backend/app/models/__init__.py, backend/migrations/versions/14_narrative_memory_builder_control.py, backend/tests/integration/narrative_memory/test_builder_control_pg.py</files>
  <action>After confirming the real Phase 13 single Alembic head and passed verification, add an explicit-version narrative-memory builder schema with owner/novel/version-scoped run, stage/checkpoint, immutable call attempt, row-locked budget ledger/reservation, and append-only build report tables. Use closed statuses/reason codes, lease/heartbeat/cancel fields, unique stage and reservation identities, composite scope FKs to NarrativeMemoryVersion, call/cache checksums, numeric token/cost ceilings, and database guards that make completed stages/call attempts/reports immutable. Permit controlled mutable execution state only on run, incomplete stage, ledger and reservation rows. Create no active key by novel, current-version resolver, pointer, journal, promotion, rollback or consumer table. Add upgrade → downgrade to the verified Phase 13 head → upgrade tests, concurrency constraints, trigger cleanup inspection, single-head proof and a forbidden-schema scan. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_builder_control_pg.py -q -x; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m alembic check; .\.venv\Scripts\ruff.exe check app/models/narrative_memory_builder.py migrations/versions/14_narrative_memory_builder_control.py tests/integration/narrative_memory/test_builder_control_pg.py</automated></verify>
  <done>The database durably records builder execution and spending by explicit candidate version while physically exposing no production selector.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Freeze strict Chapter State packages, budget, gateway, and exact cache</name>
  <files>backend/app/services/narrative_memory/builder_contracts.py, backend/app/services/narrative_memory/builder_packages.py, backend/app/services/narrative_memory/builder_budget.py, backend/app/services/narrative_memory/builder_gateway.py, backend/tests/unit/narrative_memory/test_builder_contracts.py, backend/tests/unit/narrative_memory/test_builder_packages.py</files>
  <action>Define strict frozen DTOs for run policy, source status, Chapter State input/output, stage checkpoint and call audit. Construct one chapter package only from the immutable Phase 13 version, the exact Phase 12 report, revalidated Phase 07 evidence leaves, and typed optional-signal slots; reject extra/coerced fields, chat/message/citation/similarity-shaped keys, caller-supplied scope/evidence hashes and arbitrary prose authority. Add fixed deployment capability/price snapshots, row-locked pre-call reservation and idempotent settlement. Compute exact-cache identity from stage, source snapshot, hierarchy/evidence/package checksum, prompt, schema, provider/model/revision, decoding, config, policy and optional-source lineage/status. Allow at most one same-deployment schema repair with its own reservation; only strict schema plus business/evidence-valid successful outputs become cache sources. Test key sensitivity, insertion-order stability, unknown-price/budget rejection, cache revalidation and zero fallback. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_builder_contracts.py tests/unit/narrative_memory/test_builder_packages.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/builder_contracts.py app/services/narrative_memory/builder_packages.py app/services/narrative_memory/builder_budget.py app/services/narrative_memory/builder_gateway.py tests/unit/narrative_memory/test_builder_contracts.py tests/unit/narrative_memory/test_builder_packages.py</automated></verify>
  <done>Packages and cache keys are deterministic and strict, and no transport path can run without supported frozen pricing and a durable reservation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Execute resumable independent Chapter State stages</name>
  <files>backend/app/services/narrative_memory/builder_repository.py, backend/app/services/narrative_memory/builder_worker.py, backend/tests/integration/narrative_memory/test_chapter_state_worker_pg.py</files>
  <action>Implement run creation and lease-safe dispatch requiring an explicit owner/novel/version and freshly recomputed Phase 12 EligibilityReport whose checksum/lineage matches the immutable version. Materialize deterministic `chapter_state:{chapter_id}` stages, re-read dependencies before every call, poll cancellation before reservation, before transport, after transport and before candidate persistence, and persist output only through the verified Phase 13 explicit-version authority after script-rebinding scope/evidence coordinates. Mark completion atomically with package/cache/artifact checksum and checkpoint; an exact retry returns without rewriting, while conflicting output fails closed. Continue independent chapters after a local failure, settle but discard post-cancel output, reclaim expired leases, and resume from the first incomplete/failed stage without rerunning completed siblings. Use controlled transport to cover normal, exact-cache, unknown price, budget exhaustion, dependency drift, pre/post-call cancellation, process loss at three transaction boundaries, competing workers and a single-chapter failure. Add a full-package forbidden import/capability scan for Reader Chat, promotion, pointer and implicit current version. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_builder_contracts.py tests/unit/narrative_memory/test_builder_packages.py tests/integration/narrative_memory/test_builder_control_pg.py tests/integration/narrative_memory/test_chapter_state_worker_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/builder_*.py tests/unit/narrative_memory/test_builder_*.py tests/integration/narrative_memory/test_builder_control_pg.py tests/integration/narrative_memory/test_chapter_state_worker_pg.py</automated></verify>
  <done>A real PostgreSQL run builds independent Chapter States in deterministic order, survives cancellation/crash/retry, and never repeats or mutates a completed sibling.</done>
</task>
</tasks>

## Must-Haves

- Execution fails closed until Phase 13 is complete and independently verified.
- Phase 12 eligibility is recomputed and matched to the version; no caller Boolean can authorize a provider call.
- Builder execution tables are domain-owned and explicit-version scoped; no production pointer or implicit selector exists.
- Budget reservation commits before transport; unknown pricing, budget rejection, cancellation or dependency drift means zero new calls.
- Cache identity binds all authoritative source/package and generation lineage; cached output is revalidated and audited.
- Completed Chapter State stages and candidate rows remain byte-identical through retry, crash and sibling failure.
- Reader Chat and all promotion/pointer capabilities are absent from contracts, runtime imports, packages and persistence.

## Verification

<verification>Run migration round-trip and schema inspection, strict package/cache unit tests, controlled-transport PostgreSQL worker tests, concurrency/cancellation/crash adversarial tests, forbidden-capability scans and Ruff using `backend/.venv`.</verification>
<success_criteria>
- One additive Alembic head exists and downgrade removes only Phase 14 builder objects.
- Every actual transport call has a prior reservation and immutable attempt row; ceilings are never exceeded under concurrency.
- Exact cache skips transport but records cache-hit lineage; any frozen input change misses.
- One failed chapter leaves all completed siblings byte-identical and retry starts only at incomplete/failed stages.
- No pointer, promotion, Reader Chat, fallback deployment or current-version capability is reachable.
</success_criteria>

<output>Create `14-01-SUMMARY.md` with the migration revision/table inventory, frozen cache identity, call/budget/cancel semantics, controlled-transport counts and checkpoint/failure-isolation evidence.</output>
