---
phase: 11-clue-and-foreshadow-tracking
plan: 04
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - frontend/src/lib/clue-api.ts
  - frontend/src/lib/clue-api.contract.test.ts
  - frontend/src/components/clues/clue-controls.tsx
  - frontend/src/components/clues/clue-band.tsx
  - frontend/src/components/clues/clue-evidence-panel.tsx
  - frontend/src/components/clues/clue-workspace.tsx
  - frontend/src/components/clues/clue-workspace.test.tsx
  - frontend/src/app/analysis/page.tsx
  - frontend/src/app/analysis/page.test.tsx
autonomous: true
requirements:
  - REQ-CLUE-04
  - REQ-CLUE-05
  - REQ-CLUE-06
must_haves:
  truths:
    - "Users inspect clues inside /analysis with the existing novel selector and full-book confirmation."
    - "The clue band, keyboard list, filters and evidence panel use the exact same spoiler-safe visible set."
    - "Users can confirm, reject, annotate and adjust links without exposing summary middleware or adding a top-level route."
  artifacts:
    - path: "frontend/src/components/clues/clue-workspace.tsx"
      provides: "Clue state, version, filters and human action orchestration"
      contains: "ClueWorkspace"
    - path: "frontend/src/components/clues/clue-band.tsx"
      provides: "Responsive clue timeline band and payoff chain"
      contains: "ClueBand"
    - path: "frontend/src/components/clues/clue-evidence-panel.tsx"
      provides: "Evidence, links and protected action panel"
      contains: "ClueEvidencePanel"
    - path: "frontend/src/app/analysis/page.tsx"
      provides: "Timeline/clue workbench switch under one novel selector"
      contains: "线索与伏笔"
  key_links:
    - from: "frontend/src/components/clues/clue-workspace.tsx"
      to: "frontend/src/lib/clue-api.ts"
      via: "typed active/running version queries and actions"
      pattern: "clueApi"
    - from: "frontend/src/app/analysis/page.tsx"
      to: "Phase 08 full-book confirmation"
      via: "shared fullBook state and timeline preference action"
      pattern: "confirmFullBook"
---

<objective>
Add the clue tracker to the existing analysis workspace with a responsive band, filters, evidence/payoff panel and protected human actions.

Purpose: Deliver the complete user surface while preserving Phase 08 navigation, spoiler confirmation and timeline behavior.
Output: Typed clue client, accessible clue components, /analysis integration and frontend tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clue-and-foreshadow-tracking/11-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-CONTEXT.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-03-SUMMARY.md
@frontend/src/app/analysis/page.tsx
@frontend/src/components/timeline/timeline-controls.tsx
@frontend/src/components/timeline/timeline-chart.tsx
@frontend/src/lib/api.ts

<interfaces>
Existing page contract:
~~~typescript
const [novelId, setNovelId] = useState("");
const [fullBook, setFullBook] = useState(false);
const [confirmFullBook, setConfirmFullBook] = useState(false);
// timelineApi.setFullBookPreference persists the only allowed full-book switch.
~~~

Plan 03 clue API envelope must provide:
~~~typescript
type ClueState = "candidate" | "active" | "reinforced" | "paid_off" | "dismissed";
type ClueVersionSource = "active" | "running_candidate";
interface ClueVersionView {
  clues: VisibleClue[];
  status_options: ClueState[];
  people: CluePersonRef[];
  counts: Record<ClueState, number>;
}
~~~

Keep clue types in frontend/src/lib/clue-api.ts so the existing user-modified frontend/src/lib/api.ts is not overwritten.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed clue client and action contracts</name>
  <files>frontend/src/lib/clue-api.ts, frontend/src/lib/clue-api.contract.test.ts</files>
  <behavior>
    - Queries transmit status/person/full_book/version source without client-derived hidden counts.
    - confirm, reject, annotate and adjustLink send distinct typed action payloads.
    - No clue-specific full-book mutation endpoint exists.
    - API types contain evidence/link/version provenance required by the panel.
  </behavior>
  <action>Create an isolated Axios clue client with strict TypeScript types matching Plan 03 responses and actions. Read the existing Phase 08 full-book preference through page state; do not add or call a clue preference endpoint. Include active/running-candidate separation and version diff types. Preserve the dirty shared api.ts file by importing the existing Axios instance if exported or using the repository's non-duplicating client boundary. Apply D-05/D-07/D-12. Test, Fix, and Confirm: verify exact paths, params and payloads in contract tests.</action>
  <verify>
    <automated>cd frontend; npm test -- --run src/lib/clue-api.contract.test.ts</automated>
  </verify>
  <done>Frontend has a typed clue API contract with all human actions and no second spoiler preference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build accessible clue band, filters and evidence/payoff panel</name>
  <files>frontend/src/components/clues/clue-controls.tsx, frontend/src/components/clues/clue-band.tsx, frontend/src/components/clues/clue-evidence-panel.tsx, frontend/src/components/clues/clue-workspace.tsx, frontend/src/components/clues/clue-workspace.test.tsx</files>
  <behavior>
    - Band and keyboard list render the same visible clue IDs in stable chapter/source order.
    - State/person filters come only from the server-visible envelope.
    - Evidence panel renders cue/reinforcement/payoff chain, source links, version provenance and validated associations.
    - Confirm/reject/annotation/link adjustment update through API and refresh without optimistic fact fabrication.
  </behavior>
  <action>Implement the clue workspace with active/running version tabs, state/person controls, responsive horizontal narrative band, accessible companion list and a dialog/drawer evidence panel. Render paid_off as an evidence chain from early cue through reinforcements to later payoff; never infer chain segments client-side. Human actions require explicit confirmation for reject/link replacement and show needs_relink/version diff states. Use existing ECharts dependency only if it improves band consistency; no new package per D-08/D-12. Test, Fix, and Confirm: cover empty/loading/running/partial/completed/error, keyboard selection, filter parity and all four actions.</action>
  <verify>
    <automated>cd frontend; npm test -- --run src/components/clues/clue-workspace.test.tsx</automated>
  </verify>
  <done>The clue surface is accessible, responsive and evidence-driven, with no client-created status or hidden metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate clues into the existing analysis workbench</name>
  <files>frontend/src/app/analysis/page.tsx, frontend/src/app/analysis/page.test.tsx</files>
  <behavior>
    - /analysis retains one novel selector and switches between timeline and clue views.
    - Selecting a novel starts/resumes only the selected workbench analysis and preserves timeline behavior.
    - Full-book confirmation remains the existing Phase 08 dialog/preference and applies to both views.
    - No summary/theme/pace/plot middleware menu or top-level clue route appears.
  </behavior>
  <action>Add a small workbench tab switch for “时间线” and “线索与伏笔” under the existing /analysis header. Keep the existing timeline components and queries intact when timeline is selected; mount ClueWorkspace with novelId/fullBook and shared confirmation callbacks when clues are selected. Reuse the same timeline_full_book persistence path and warning copy per D-07/D-08, and do not expose intermediate analysis types per D-11. Test, Fix, and Confirm: regress all existing analysis page tests plus clue view selection and spoiler confirmation.</action>
  <verify>
    <automated>cd frontend; npm test -- --run src/app/analysis/page.test.tsx src/components/clues/clue-workspace.test.tsx src/lib/clue-api.contract.test.ts</automated>
  </verify>
  <done>/analysis hosts timeline and clues as two evidence products under one selector and one explicit spoiler preference, with existing timeline tests still green.</done>
</task>

</tasks>

## Must-Haves

- No new top-level route or summary middleware menu.
- Server-provided visible data is the only source for filters, counts and chains.
- All four protected human actions are usable.
- Timeline and clue views share the Phase 08 full-book confirmation.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API response→browser | Hidden fields or unsafe HTML could leak/render untrusted prose |
| user actions→API | Human decisions and link IDs require explicit, scoped payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-13 | Information Disclosure | clue filters/counts | mitigate | render server-visible envelope only; browser spoiler assertions |
| T-11-14 | Tampering | human actions | mitigate | typed payloads, explicit confirmation and refresh from authority |
| T-11-15 | Spoofing | version source tabs | mitigate | explicit active/running IDs from API; never merge envelopes |
| T-11-16 | Cross-site Scripting | evidence text | mitigate | React text rendering; no raw HTML/tooltip interpolation from prose |
| T-11-SC | Tampering | npm packages | mitigate | no package installation; use existing ECharts/components |
</threat_model>

## Verification

<verification>
Run targeted frontend tests, then the full Vitest suite and production build. Browser verification is intentionally owned by Plan 05 after backend qualification data exists.
</verification>

<success_criteria>
- Typed contract/component/page tests pass.
- Existing timeline page behavior remains green.
- Production build succeeds.
- No /clues route or intermediate analysis menu is generated.
</success_criteria>

<output>
Create .planning/phases/11-clue-and-foreshadow-tracking/11-04-SUMMARY.md when done
</output>

