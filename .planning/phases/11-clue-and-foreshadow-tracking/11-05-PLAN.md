---
phase: 11-clue-and-foreshadow-tracking
plan: 05
type: execute
wave: 5
depends_on:
  - 11-02
  - 11-03
  - 11-04
files_modified:
  - backend/evals/clue_fiction.v1.json
  - backend/app/services/clues/eval.py
  - backend/scripts/run_clue_qualification.py
  - backend/tests/unit/clues/test_eval.py
  - backend/tests/adversarial/test_clue_spoilers_and_versions.py
  - backend/tests/integration/clues/test_real_qualification.py
  - backend/tests/ci/test_clue_release_gate.py
  - frontend/e2e/clue-real.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REQ-CLUE-01
  - REQ-CLUE-02
  - REQ-CLUE-03
  - REQ-CLUE-04
  - REQ-CLUE-05
  - REQ-CLUE-06
  - REQ-CLUE-07
must_haves:
  truths:
    - "Frozen fiction qualification separates recall quality from lifecycle publication quality and has zero critical false active/paid_off."
    - "Real API and desktop/mobile browser journeys prove spoiler-safe clue viewing and all protected human actions."
    - "Release passes only from fresh PostgreSQL authority, measured metrics and internally executed fixed commands; blocked dependencies fail closed."
  artifacts:
    - path: "backend/evals/clue_fiction.v1.json"
      provides: "Frozen fiction clue and hard-negative fixture"
      contains: "fixture_version"
    - path: "backend/scripts/run_clue_qualification.py"
      provides: "Offline/live/E2E/release qualification CLI"
      contains: "verify_release"
    - path: "frontend/e2e/clue-real.spec.ts"
      provides: "Desktop and mobile real-stack spoiler/action journey"
      contains: "clue"
    - path: "backend/tests/ci/test_clue_release_gate.py"
      provides: "Forgery, missing-metric and command-boundary release tests"
      contains: "blocked_release"
  key_links:
    - from: "backend/app/services/clues/eval.py"
      to: "backend/evals/clue_fiction.v1.json"
      via: "frozen fixture and policy hashes"
      pattern: "clue_fiction.v1"
    - from: "backend/scripts/run_clue_qualification.py"
      to: "PostgreSQL clue authority"
      via: "fresh async_session_factory observer"
      pattern: "verify_release_evidence_from_db"
    - from: ".github/workflows/ci.yml"
      to: "backend/tests/integration/clues/test_real_qualification.py"
      via: "PostgreSQL integration gate"
      pattern: "test_real_qualification"
---

<objective>
Close Phase 11 with frozen semantic/adversarial metrics, real API/browser proof and an executable fail-closed release authority.

Purpose: Prevent plausible-looking clue output, spoiler-safe unit mocks or self-reported reports from qualifying without primary evidence and real system observations.
Output: Frozen fixture/evaluator, qualification CLI, PostgreSQL/API/browser tests and CI release gate.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clue-and-foreshadow-tracking/11-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-CONTEXT.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-AI-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-VALIDATION.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-02-SUMMARY.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-03-SUMMARY.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-04-SUMMARY.md
@backend/scripts/run_timeline_qualification.py
@backend/tests/integration/timeline/test_real_qualification.py
@frontend/e2e/timeline-real.spec.ts
@.github/workflows/ci.yml

<interfaces>
Phase 08 release precedent:

- executable CLI owns fixed cwd/argv commands;
- combined stdout/stderr bytes are hashed internally;
- a fresh SQLAlchemy session independently reconstructs run/version/pointer/evidence/attempt authority;
- missing/tampered report, nonzero command, DB mismatch or blocked live metrics returns blocked_release and nonzero exit;
- real browser uses Next.js + FastAPI + PostgreSQL, with only provider transport controlled.

Apply the same authority pattern to clue tables and do not trust fixture evaluator self-hashes alone.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze fiction fixtures and implement lifecycle quality evaluation</name>
  <files>backend/evals/clue_fiction.v1.json, backend/app/services/clues/eval.py, backend/tests/unit/clues/test_eval.py, backend/tests/adversarial/test_clue_spoilers_and_versions.py</files>
  <behavior>
    - Fixture contains at least 24 labeled examples and required hard-negative categories.
    - Reports separate candidate recall from active/reinforced/paid_off precision/F1.
    - Critical unsupported acceptance, false paid_off, spoiler leak, cross-scope link and override overwrite gates require zero.
    - Same frozen inputs/transcripts/lineage produce identical artifact and manifest checksums.
  </behavior>
  <action>Create an immutable fiction-only dev/frozen fixture with exact evidence coordinates and expected lifecycle/link outcomes. Implement deterministic scoring for schema/evidence validity, recall, state macro F1, paid_off precision, false-positive classes, spoiler-derived fields, override/version preservation, reproducibility, calls/tokens/cost and p50/p95. Enforce 11-SPEC numeric thresholds and D-01/D-04/D-10; do not add history examples or use frozen cases to tune recall thresholds. Add cross-version and spoiler adversarial tests, including chat-only and unresolved relationship-observation negatives. Test, Fix, and Confirm: all critical counts are exactly zero and threshold boundaries fail closed.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_eval.py tests/adversarial/test_clue_spoilers_and_versions.py -q -x</automated>
  </verify>
  <done>The frozen evaluator emits a lineage-bound, reproducible, fiction-only report and rejects every critical false-positive/spoiler/version violation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real PostgreSQL, API and dual-viewport browser behavior</name>
  <files>backend/scripts/run_clue_qualification.py, backend/tests/integration/clues/test_real_qualification.py, frontend/e2e/clue-real.spec.ts</files>
  <behavior>
    - Qualification executes the production clue worker/query/API against PostgreSQL with only model transport controlled.
    - Browser sees active/reinforced status before payoff cutoff and paid_off only after the existing full-book confirmation.
    - Desktop and 390px projects render identical visible clue IDs, usable filters/evidence and no page overflow.
    - Confirm, reject, annotate and link adjustment persist, survive reanalysis and show provenance.
  </behavior>
  <action>Build qualification CLI modes for offline/live runs plus E2E seed/resume/reanalysis using real clue tables, hierarchy/timeline evidence, FastAPI and Next.js. Add a fresh PostgreSQL integration test that observes persisted run/version/lifecycle/evidence/link/override/attempt/budget/pointer rows after production execution. Add Playwright coverage in both configured desktop and mobile projects for progressive candidate, spoiler cutoff, full-book reveal, status/person filtering, evidence/payoff chain and all four human actions. Only provider transport may be controlled; no API/browser route stubbing. Apply D-05/D-07/D-08/D-10. Test, Fix, and Confirm: run PostgreSQL qualification and clue-real.spec.ts successfully in both viewports.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/clues/test_real_qualification.py -q -x; cd ..\frontend; npm run test:e2e -- clue-real.spec.ts</automated>
  </verify>
  <done>Real-stack evidence proves owner isolation, progressive versions, spoiler protection, UI parity and protected actions on desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce executable release authority in CI</name>
  <files>backend/scripts/run_clue_qualification.py, backend/tests/ci/test_clue_release_gate.py, .github/workflows/ci.yml</files>
  <action>Add a --verify-release entry that accepts only report/policy/output locations, owns a fixed no-shell command list, captures combined output bytes, recomputes SHA-256, and independently imports the production session factory to read PostgreSQL authority. Reject caller-provided commands, digests, DB observations or success flags. Require complete frozen/live/API/browser evidence, quality_comparable=true and all D-10 thresholds; blocked/missing live metrics, command failure, DB mismatch or tamper returns blocked_release and nonzero exit. Wire secretless contract tests to PR CI and PostgreSQL real qualification/release verification to integration/nightly according to existing Phase 06/08 workflow tiers. Keep relationship/chat implementations absent per D-11. Test, Fix, and Confirm: run forgery/missing/dependency/command/DB mismatch negatives plus one genuine positive path.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/ci/test_clue_release_gate.py tests/integration/clues/test_real_qualification.py -q -x</automated>
  </verify>
  <done>Only internally observed commands, PostgreSQL truth and complete measured metrics can produce a release-qualified verdict, and CI executes the authority path.</done>
</task>

</tasks>

## Must-Haves

- Fiction fixture has 24+ examples and required hard negatives.
- Critical false active/paid_off, spoiler leak, owner leak and override overwrite are zero.
- Cost/latency are measured and bound to lineage; blocked is not zero.
- Real desktop and mobile browser journeys use real API/PostgreSQL.
- Release CLI owns commands/digests/DB observer and fails closed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| report→release verifier | Report may be forged, stale, incomplete or self-attested |
| subprocess→command evidence | Exit/output/digest may be fabricated |
| database→release verdict | Caller may supply copied rather than fresh authority |
| browser→API | Spoiler and owner boundaries must hold in real transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-17 | Spoofing | qualification report | mitigate | canonical lineage hashes + fresh PostgreSQL observer |
| T-11-18 | Tampering | command evidence | mitigate | fixed argv/cwd, no shell, internal byte capture and digest recomputation |
| T-11-19 | Repudiation | release verdict | mitigate | machine-readable command/DB observations and nonzero blocked exit |
| T-11-20 | Information Disclosure | browser/API | mitigate | real default/full-book owner-scoped E2E assertions |
| T-11-21 | Denial of Service | live qualification | accept | bounded timeout/budget; blocked_dependency is explicit and non-qualifying |
| T-11-SC | Tampering | package installs | mitigate | no new packages; slopcheck remains required if scope changes |
</threat_model>

## Verification

<verification>
Run the complete backend clue unit/integration/adversarial/CI suite, Alembic upgrade/current/check on PostgreSQL, full frontend Vitest/build, both clue Playwright projects, offline qualification and release verification. Then run existing Phase 08 timeline suites as regression evidence.
</verification>

<success_criteria>
- All REQ-CLUE-01..07 gates in 11-VALIDATION.md are satisfied.
- paid_off precision ≥0.90; active/reinforced macro F1 ≥0.85; every critical count is zero.
- API p95 ≤500 ms and model p95 ≤60 s under frozen policy; total cost stays within policy.
- Real PostgreSQL and both browser viewports pass.
- Tamper, missing metrics, blocked dependency, nonzero command and DB mismatch all block release.
- Phase 09/10 and history/writing capabilities remain absent.
</success_criteria>

<output>
Create .planning/phases/11-clue-and-foreshadow-tracking/11-05-SUMMARY.md when done
</output>

