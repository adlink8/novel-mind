---
phase: 11-clue-and-foreshadow-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - backend/app/services/clues/__init__.py
  - backend/app/services/clues/sources.py
  - backend/app/services/clues/candidates.py
  - backend/app/services/clues/evidence.py
  - backend/app/services/clues/llm_judge.py
  - backend/app/services/clues/gates.py
  - backend/prompts/clue_semantic_judge.v1.txt
  - backend/tests/unit/clues/test_candidates.py
  - backend/tests/unit/clues/test_evidence_gates.py
  - backend/tests/unit/clues/test_llm_judgment.py
  - backend/tests/integration/clues/test_source_protocols.py
  - backend/tests/adversarial/test_clue_false_positives.py
autonomous: true
requirements:
  - REQ-CLUE-01
  - REQ-CLUE-02
  - REQ-CLUE-03
  - REQ-CLUE-07
must_haves:
  truths:
    - "Deterministic scripts build every cross-chapter candidate and allowed-evidence package before an LLM is called."
    - "LLM output is semantic judgment only and cannot directly create lifecycle state, links, versions or writes."
    - "Similarity, chat and unresolved relationship observations remain recall signals and produce zero accepted states by themselves."
  artifacts:
    - path: "backend/app/services/clues/candidates.py"
      provides: "Deterministic cross-chapter candidate recall"
      contains: "ClueCandidateRecallService"
    - path: "backend/app/services/clues/evidence.py"
      provides: "Bounded cue/later evidence package and exact scope validation"
      contains: "ClueEvidencePackage"
    - path: "backend/app/services/clues/gates.py"
      provides: "Schema/evidence/threshold/conflict/temporal lifecycle gates"
      contains: "ClueGateService"
  key_links:
    - from: "backend/app/services/clues/candidates.py"
      to: "backend/app/services/chunking/pg_store.py"
      via: "active hierarchy evidence nodes and source offsets"
      pattern: "load_hierarchy_trees|get_active_build_id"
    - from: "backend/app/services/clues/llm_judge.py"
      to: "backend/app/services/clues/gates.py"
      via: "strict ClueSemanticJudgment; no persistence side effect"
      pattern: "ClueSemanticJudgment"
---

<objective>
Build the deterministic candidate/evidence boundary, strict LLM semantic judge and local acceptance gates.

Purpose: Establish the hardest paid-off correctness constraint before durable orchestration or UI can publish anything.
Output: Clue service package, Phase 09/10 null source protocols, versioned prompt and adversarially tested gates.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clue-and-foreshadow-tracking/11-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-CONTEXT.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-AI-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-01-SUMMARY.md
@backend/app/services/knowledge/candidates.py
@backend/app/services/knowledge/evidence.py
@backend/app/services/knowledge/llm_judge.py
@backend/app/services/knowledge/gates.py
@backend/app/services/timeline/evidence.py
@backend/app/services/chunking/pg_store.py

<interfaces>
From backend/app/services/timeline/evidence.py:
~~~python
@dataclass(frozen=True)
class EvidenceUnit:
    evidence_id: str
    source_start: int
    source_end: int
    text: str
    content_hash: str

@dataclass(frozen=True)
class EvidencePackage:
    owner_id: int
    novel_id: int
    chapter_id: int
    source_snapshot_hash: str
    hierarchy_build_id: str
    hierarchy_checksum: str
~~~

From backend/app/services/knowledge/candidates.py:
~~~python
class CandidateRecallService:
    # deterministic RelationCandidateDraft construction
~~~

Use these patterns, but clue packages must support one early cue window plus multiple later windows and must remain fiction-only.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic cross-chapter recall and read-only source protocols</name>
  <files>backend/app/services/clues/sources.py, backend/app/services/clues/candidates.py, backend/app/services/clues/evidence.py, backend/tests/unit/clues/test_candidates.py, backend/tests/integration/clues/test_source_protocols.py</files>
  <behavior>
    - Same hierarchy/timeline/source inputs produce stable candidate IDs, package hashes and ordering.
    - Recall combines lexical/vector/adjacency/entity/timeline and optional source reason codes without converting scores to state.
    - A missing or unavailable Phase 09 public reader is recorded as `source_unavailable`; no relationship-derived signal is emitted, and no relation/chat tables are required.
    - Conversation sources can return only primary selection/citation refs; free-form messages are rejected.
  </behavior>
  <action>Implement ClueCandidateRecallService, bounded ClueEvidencePackage and a read-only VersionedRelationshipObservationSource protocol. Bind it only to the completed Phase 09 public reader; when unavailable, record `source_unavailable` and omit relationship-derived signals rather than manufacturing a zero-result substitute. Pull primary evidence from the active Phase 07 hierarchy and optional Phase 08 event refs; validate owner, novel, build, chapter, offsets and hashes. Store recall signals separately from evidence and state per D-02, D-04 and D-06. Do not import relationship/chat business modules, add a conversation source, or add writes to adjacent phases per D-11. Test, Fix, and Confirm: prove deterministic replay, bounded packages, explicit unavailable-source behavior and chat/similarity non-authority.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_candidates.py tests/integration/clues/test_source_protocols.py -q -x</automated>
  </verify>
  <done>Cross-chapter candidates are reproducible, evidence-scoped and independent of absent Phase 09/10 implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict semantic judgment with no write authority</name>
  <files>backend/app/services/clues/llm_judge.py, backend/prompts/clue_semantic_judge.v1.txt, backend/tests/unit/clues/test_llm_judgment.py</files>
  <behavior>
    - The model may return cue_only, reinforcement, payoff, unrelated or ambiguous with allowed evidence IDs.
    - Extra fields, package-external IDs, malformed JSON, tool requests and unsupported enums fail.
    - Provider retry is zero; at most one same-deployment repair is represented as an explicit caller-controlled attempt.
    - Judge methods return DTOs/audits and perform no DB state or lifecycle writes.
  </behavior>
  <action>Create the versioned fiction-only prompt and a strict async judge adapter that receives only ClueEvidencePackage, disables tools/stream/provider retries, caps context/output and returns a parsed ClueSemanticJudgment plus usage/latency/raw hash audit. Keep state, budget, cache and persistence outside this class per D-02, D-09 and D-12. Test prompt injection, tool smuggling, malformed schema, refusal and allowed-ID enforcement. Test, Fix, and Confirm: run the strict judgment tests with a fake transport and assert zero hidden retries/writes.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_llm_judgment.py -q -x</automated>
  </verify>
  <done>The only AI output is a locally validated semantic judgment tied to the exact evidence package and model lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce evidence, temporal, threshold and conflict gates</name>
  <files>backend/app/services/clues/gates.py, backend/app/services/clues/__init__.py, backend/tests/unit/clues/test_evidence_gates.py, backend/tests/adversarial/test_clue_false_positives.py</files>
  <behavior>
    - active requires cue evidence; reinforced requires distinct new later evidence; paid_off requires cue+later payoff and from-state reinforced.
    - Similar wording, repeated motifs, shared people/location and vector score alone never pass.
    - Order conflicts, entity conflicts, unsupported relation refs and human-protected dismissal block publication.
    - Gate decisions contain stable machine-readable failure codes and no side effects.
  </behavior>
  <action>Implement a deterministic gate pipeline in exact scope→schema→evidence→offset/hash→temporal order→transition→threshold/conflict→human-protection order. Return a GateDecision consumed later by lifecycle persistence; do not write lifecycle rows here. Encode D-03/D-04 paid-off constraints and D-06 non-fact signals. Build hard-negative adversarial cases before tuning thresholds. Export the service package. Test, Fix, and Confirm: require zero critical false active/paid_off on the adversarial set.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_evidence_gates.py tests/adversarial/test_clue_false_positives.py -q -x</automated>
  </verify>
  <done>Every publishable transition has role-correct primary evidence and no recall-only signal can bypass the deterministic gates.</done>
</task>

</tasks>

## Must-Haves

- Candidate generation and evidence packaging are deterministic and cross-chapter.
- LLM has no DB, tool, state, budget or publish authority.
- paid_off requires early cue, later payoff and legal prior state.
- Phase 09 reader absence is explicit and auditable; Phase 10 chat is not a source protocol.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| novel text→LLM prompt | Untrusted prose may contain prompt/tool instructions |
| recall signals→candidate | Similarity and optional future sources are untrusted hints |
| LLM output→gate | Model IDs, evidence and semantic claims are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-04 | Elevation of Privilege | clue LLM | mitigate | no tools/writes/state fields; local strict validation |
| T-11-05 | Tampering | evidence refs | mitigate | allowed-ID, offset/hash and owner/build checks |
| T-11-06 | Spoofing | optional Phase 09/10 refs | mitigate | typed protocols, version validation and null defaults |
| T-11-07 | Information Disclosure | prompt package | mitigate | bounded evidence only; no chat history or credentials |
| T-11-SC | Tampering | package installs | mitigate | no installs; existing stack only per D-12 |
</threat_model>

## Verification

<verification>
Run all clue candidate/judge/gate tests and the adversarial false-positive set; inspect that service files contain no AsyncSession write or lifecycle persistence call.
</verification>

<success_criteria>
- Deterministic package checksums repeat exactly.
- Strict judgment failures create no accepted transition.
- Critical false active/paid_off count is zero.
- Tests pass with Phase 09/10 sources absent.
</success_criteria>

<output>
Create .planning/phases/11-clue-and-foreshadow-tracking/11-02-SUMMARY.md when done
</output>
