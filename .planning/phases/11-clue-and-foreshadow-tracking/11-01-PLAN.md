---
phase: 11-clue-and-foreshadow-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/clue.py
  - backend/app/models/clue.py
  - backend/app/models/__init__.py
  - backend/app/schemas/__init__.py
  - backend/migrations/versions/11_clue_tracking.py
  - backend/tests/unit/clues/test_schemas.py
  - backend/tests/unit/clues/test_lifecycle.py
  - backend/tests/integration/clues/test_persistence.py
autonomous: true
requirements:
  - REQ-CLUE-02
  - REQ-CLUE-03
  - REQ-CLUE-05
must_haves:
  truths:
    - "Clue lifecycle history is append-only and only legal candidate/active/reinforced/paid_off/dismissed transitions persist."
    - "active, reinforced and paid_off rows cannot exist without role-correct evidence; paid_off requires an earlier cue and later payoff."
    - "Machine versions and human overrides are separate, so reanalysis cannot overwrite a human decision."
  artifacts:
    - path: "backend/app/schemas/clue.py"
      provides: "Strict clue, evidence, lifecycle, link, override and API contracts"
      contains: "ClueLifecycleState"
    - path: "backend/app/models/clue.py"
      provides: "PostgreSQL clue authority tables"
      contains: "class ClueLifecycleEvent"
    - path: "backend/migrations/versions/11_clue_tracking.py"
      provides: "Alembic schema with append-only constraints and indexes"
      contains: "11cluetrack01"
  key_links:
    - from: "backend/app/models/clue.py"
      to: "analysis/clue run and version lineage"
      via: "owner_id, novel_id, version_id and immutable manifest fields"
      pattern: "ClueAnalysisVersion"
    - from: "backend/app/models/clue.py"
      to: "backend/app/schemas/clue.py"
      via: "matching lifecycle/evidence/link enums"
      pattern: "candidate.*active.*reinforced.*paid_off.*dismissed"
---

<objective>
Freeze the fiction-only clue contracts and PostgreSQL authority required by all later waves.

Purpose: Make illegal lifecycle history, unsupported paid-off states, cross-scope links and machine/manual conflation structurally impossible before semantic processing is added.
Output: Strict schemas, clue-owned ORM tables, Alembic migration and focused contract/persistence tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clue-and-foreshadow-tracking/11-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-CONTEXT.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-AI-SPEC.md
@backend/app/models/base.py
@backend/app/models/analysis.py
@backend/app/models/timeline.py
@backend/app/models/chunk_build.py

<interfaces>
Existing authority patterns to preserve:

From backend/app/models/analysis.py:
~~~python
class AnalysisVersion(TimestampMixin, Base):
    owner_id
    novel_id
    parent_version_id
    source_snapshot_hash
    hierarchy_build_id
    hierarchy_checksum
    prompt_hash
    schema_hash
    model_lineage
    config_hash
    manifest
    manifest_checksum
~~~

From backend/app/models/timeline.py:
~~~python
class TimelineOverride(TimestampMixin, Base):
    supersedes_id
    status
    needs_relink

class TimelineActivePointer(TimestampMixin, Base):
    version_id
    revision
    manifest_checksum
~~~

Do not reuse AnalysisRun/AnalysisVersion rows directly: their active owner/novel uniqueness is timeline-specific in current production behavior. Create clue-owned authority without modifying timeline runtime files.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict clue and lifecycle contracts</name>
  <files>backend/app/schemas/clue.py, backend/tests/unit/clues/test_schemas.py, backend/tests/unit/clues/test_lifecycle.py</files>
  <behavior>
    - Legal transitions are candidate→active|dismissed, active→reinforced|dismissed, reinforced→reinforced|paid_off|dismissed; paid_off/dismissed are terminal.
    - active requires cue evidence, reinforced requires a new reinforcement evidence identity, and paid_off requires distinct cue/payoff evidence with later chapter/source order.
    - Link schemas require exactly one target kind and never accept chat text or similarity score as evidence.
    - All LLM-facing and API schemas reject extra fields and history domain values.
  </behavior>
  <action>Create strict Pydantic enums and models for clue version lineage, machine clue, evidence roles, lifecycle append events, typed links, human actions, version diff and visible API envelopes. Implement a pure transition validator/replay function used by later persistence and query layers. Encode D-01, D-03, D-04, D-05, D-06 and D-11 exactly; do not add a mutable authoritative current-state contract. Test, Fix, and Confirm: run the two focused unit files until legal/illegal transition, evidence order and strict-schema cases pass.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_schemas.py tests/unit/clues/test_lifecycle.py -q -x</automated>
  </verify>
  <done>Strict fiction-only contracts reject every illegal transition, unsupported state, malformed link and evidence-invalid paid_off case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create clue-owned PostgreSQL authority and migration</name>
  <files>backend/app/models/clue.py, backend/migrations/versions/11_clue_tracking.py, backend/tests/integration/clues/test_persistence.py</files>
  <behavior>
    - Lifecycle events and overrides are inserted and superseded by new rows, never updated in place.
    - Concurrent duplicate lifecycle transitions are rejected/idempotently observed by version/logical-clue/event identity constraints.
    - paid_off persistence requires cue and payoff evidence rows whose narrative coordinates are strictly ordered.
    - Active pointer moves require expected revision and preserve a pointer journal.
  </behavior>
  <action>Add clue-owned run, version, machine clue, evidence ref, lifecycle event, typed link, override, model-call attempt, budget reservation, active pointer and pointer journal ORM records. Use modern Mapped columns, owner/novel/version indexes, unique stage keys, exact-one-target checks and enum/check constraints. Create Alembic revision 11cluetrack01 with down_revision 10analysistime01. The database is PostgreSQL authority per D-03, D-05 and D-09; do not alter timeline or future relationship/chat tables. Test, Fix, and Confirm: exercise real insert/replay/constraint/CAS behavior and run Alembic offline SQL/head checks.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/clues/test_persistence.py -q -x; .\.venv\Scripts\python.exe -m alembic heads</automated>
  </verify>
  <done>One Alembic head exists at 11cluetrack01 and PostgreSQL/SQLite-compatible tests prove append-only history, evidence ordering, scope uniqueness and pointer journals.</done>
</task>

<task type="auto">
  <name>Task 3: Register clue models and schemas without touching adjacent business logic</name>
  <files>backend/app/models/__init__.py, backend/app/schemas/__init__.py</files>
  <action>Export all new clue ORM and schema contracts through existing package registries so Base.metadata, tests and future API imports discover them. Preserve current timeline exports and do not import or create Phase 09/10 implementations per D-06 and D-11. Test, Fix, and Confirm: rerun clue contracts plus model metadata creation and verify no second Alembic head appears.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues tests/integration/clues/test_persistence.py -q -x; .\.venv\Scripts\python.exe -m alembic heads</automated>
  </verify>
  <done>Clue contracts are importable from app.models/app.schemas, metadata builds cleanly and existing timeline/knowledge registrations remain intact.</done>
</task>

</tasks>

## Must-Haves

- No lifecycle history overwrite path exists.
- No paid_off record can pass without early cue and later payoff evidence.
- Machine versions, human overrides and pointer journals are separate.
- No Phase 09/10 table, API or business implementation is introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| service→PostgreSQL | Concurrent or malformed lifecycle writes cross into authority state |
| future source ref→clue link | Unimplemented/external identifiers must not bypass owner/version scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | Tampering | clue lifecycle tables | mitigate | append-only rows, legal-transition validator, unique identities and DB constraints |
| T-11-02 | Information Disclosure | clue links | mitigate | owner/novel/version columns and no raw chat payload field |
| T-11-03 | Elevation of Privilege | active pointer | mitigate | expected-revision CAS and journal |
| T-11-SC | Tampering | package installs | mitigate | no package installs in this plan; fail planning if a new dependency is proposed without legitimacy audit |
</threat_model>

## Verification

<verification>
Run the targeted schema/lifecycle/persistence suite, Alembic heads, offline migration SQL, then the existing timeline model tests to detect registration regressions.
</verification>

<success_criteria>
- All task commands pass.
- Alembic reports exactly one head at 11cluetrack01.
- Illegal and evidence-invalid transitions fail before any visible state can be derived.
- Existing timeline tables and runtime files are unchanged.
</success_criteria>

<output>
Create .planning/phases/11-clue-and-foreshadow-tracking/11-01-SUMMARY.md when done
</output>

