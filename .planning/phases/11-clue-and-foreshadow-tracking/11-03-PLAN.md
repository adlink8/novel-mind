---
phase: 11-clue-and-foreshadow-tracking
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - backend/app/services/clues/budget.py
  - backend/app/services/clues/versions.py
  - backend/app/services/clues/worker.py
  - backend/app/services/clues/lifecycle.py
  - backend/app/services/clues/overrides.py
  - backend/app/services/clues/query.py
  - backend/app/api/clues.py
  - backend/app/main.py
  - backend/tests/unit/clues/test_overrides.py
  - backend/tests/unit/clues/test_query_projection.py
  - backend/tests/integration/clues/test_worker_versions.py
  - backend/tests/integration/clues/test_spoiler_api.py
  - backend/tests/integration/clues/test_override_reanalysis.py
autonomous: true
requirements:
  - REQ-CLUE-01
  - REQ-CLUE-02
  - REQ-CLUE-03
  - REQ-CLUE-04
  - REQ-CLUE-05
must_haves:
  truths:
    - "A clue run resumes from durable checkpoints, reserves budget before each model call and never moves active on failure."
    - "Lifecycle writes and human actions append history; reanalysis creates a distinct comparable version and preserves overrides."
    - "Every owner-scoped API response filters by reading progress before deriving visible state, counts, filters, links or payoff chains."
  artifacts:
    - path: "backend/app/services/clues/worker.py"
      provides: "Durable candidate analysis orchestration"
      contains: "run_clue_worker"
    - path: "backend/app/services/clues/lifecycle.py"
      provides: "Append-only lifecycle and protected human actions"
      contains: "append_lifecycle_event"
    - path: "backend/app/services/clues/query.py"
      provides: "Visible-set-first clue projection"
      contains: "build_clue_version_view"
    - path: "backend/app/api/clues.py"
      provides: "Owner-scoped run/query/reanalysis/human action endpoints"
      exports: ["router"]
  key_links:
    - from: "backend/app/services/clues/worker.py"
      to: "backend/app/services/clues/gates.py"
      via: "persist lifecycle only after deterministic GateDecision"
      pattern: "ClueGateService"
    - from: "backend/app/services/clues/query.py"
      to: "Novel.reading_progress.timeline_full_book"
      via: "Phase 08 preference reuse before derivation"
      pattern: "timeline_full_book"
    - from: "backend/app/api/clues.py"
      to: "backend/app/services/clues/query.py"
      via: "require_owned_novel and owner-scoped projection"
      pattern: "require_owned_novel"
---

<objective>
Wire clue semantics into durable versioned execution, append-only lifecycle/override behavior and a spoiler-safe API.

Purpose: Turn validated decisions into recoverable, comparable and user-controllable product data without touching timeline, relationship or chat business implementations.
Output: Durable worker/version/budget services, lifecycle/override/query services, clue API and integration coverage.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clue-and-foreshadow-tracking/11-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-CONTEXT.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-AI-SPEC.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-VALIDATION.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-01-SUMMARY.md
@.planning/phases/11-clue-and-foreshadow-tracking/11-02-SUMMARY.md
@backend/app/services/timeline/worker.py
@backend/app/services/timeline/query.py
@backend/app/services/timeline/overrides.py
@backend/app/api/timeline.py
@backend/app/models/novel.py

<interfaces>
From backend/app/services/timeline/query.py:
~~~python
async def _chapter_cutoff(session, novel) -> int | None:
    # invalid/missing progress resolves to first chapter

async def build_version_view(...):
    # visible IDs are selected before evidence, participants, overrides,
    # edges, aggregates and previews
~~~

From backend/app/services/timeline/overrides.py:
~~~python
def relink_overrides(overrides, *, old_events, new_events) -> RelinkResult:
    # exactly one evidence-identity match relinks; otherwise needs_relink
~~~

Do not edit backend/app/services/timeline/worker.py or backend/app/api/timeline.py. Reuse their established behavior through imported shared data only where safe.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement durable clue worker, exact cache, budget and version promotion</name>
  <files>backend/app/services/clues/budget.py, backend/app/services/clues/versions.py, backend/app/services/clues/worker.py, backend/tests/integration/clues/test_worker_versions.py</files>
  <behavior>
    - Concurrent start/resume creates one active clue run per owner/novel/config lineage.
    - Completed stage keys and validated exact-cache entries generate zero duplicate model calls after restart.
    - Unknown pricing, exhausted budget, unavailable capability or unresolved outcome pauses before any additional call.
    - Failed/rejected candidates do not move active; qualified promotion and rollback use expected-revision CAS and manifests.
  </behavior>
  <action>Build clue-owned lease/checkpoint orchestration around the Plan 02 recall/judge/gate contracts. Freeze source snapshot, Phase 07 active hierarchy, selected Phase 08 timeline version, prompt/schema/model/config/price lineage before calls. Persist reservation and call attempt before network I/O, allow one explicit same-deployment schema repair, cache only locally validated output, append chapter/candidate artifacts atomically with checkpoints, qualify the complete version and move the clue pointer only via CAS. Implement D-02 and D-09 without modifying timeline worker or using provider fallback. Test, Fix, and Confirm: force restart, cancellation, unknown pricing, outcome_unknown, failed candidate, stale CAS, rollback and cache mutation cases.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/clues/test_worker_versions.py -q -x</automated>
  </verify>
  <done>Clue runs are idempotent, budget-safe, immutable and rollbackable; failure leaves the previous active version byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist lifecycle and protected human overrides across reanalysis</name>
  <files>backend/app/services/clues/lifecycle.py, backend/app/services/clues/overrides.py, backend/tests/unit/clues/test_overrides.py, backend/tests/integration/clues/test_override_reanalysis.py</files>
  <behavior>
    - Gate-approved machine transitions append one lifecycle event and never update a prior event.
    - confirm/reject append protected state events, annotation leaves state unchanged, and link adjustment appends a superseding override.
    - Reanalysis creates a machine diff while preserving human rows.
    - Exactly one stable evidence match relinks; zero/multiple matches set needs_relink.
  </behavior>
  <action>Implement append_lifecycle_event with transition/evidence replay from Plan 01 and separate append-only human action services for confirm, reject, annotation and link adjustment. Store actor/provenance/supersedes and protect active human decisions from worker transitions. Compute version comparison over machine clues/lifecycle/links separately from override overlays. Apply D-03/D-04/D-05 and D-11; no update/delete of lifecycle or prior override rows. Test, Fix, and Confirm: verify legal transitions, repeated reinforcement evidence rejection, protected dismissal, supersession chains and ambiguous relink.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_overrides.py tests/integration/clues/test_override_reanalysis.py -q -x</automated>
  </verify>
  <done>Human choices survive every machine reanalysis and all current states can be reproduced from append-only records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose owner-scoped visible-set-first clue API</name>
  <files>backend/app/services/clues/query.py, backend/app/api/clues.py, backend/app/main.py, backend/tests/unit/clues/test_query_projection.py, backend/tests/integration/clues/test_spoiler_api.py</files>
  <behavior>
    - Default/no-progress/full-book-disabled queries hide all future evidence and derived metadata.
    - Before payoff cutoff, a machine paid_off clue is projected as the state derivable from visible lifecycle events.
    - Person/status options, counts, links, evidence panels and payoff chain contain no hidden values.
    - Cross-owner/novel/version resources return 404 and human actions require owned novel scope.
  </behavior>
  <action>Implement build_clue_version_view with owner/version resolution, Phase 08 chapter cutoff and timeline_full_book preference check before any derived field. Return separate active/running-candidate envelopes, visible clue bands, filters, evidence/link panels and machine version diff. Add /api/clues routes for start/status/cancel/resume/reanalyze, active/version reads and confirm/reject/annotate/adjust-link actions; register the router in main.py. Do not add a clue-specific full-book preference endpoint and do not edit timeline API per D-07/D-11. Test, Fix, and Confirm: compare default/full-book responses field-by-field and test authorization/action contracts.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/clues/test_query_projection.py tests/integration/clues/test_spoiler_api.py -q -x</automated>
  </verify>
  <done>The API exposes only owner-authorized, cutoff-derived clue state and supports all protected human actions without adjacent business writes.</done>
</task>

</tasks>

## Must-Haves

- Budget reservation and stage identity persist before every model call.
- Active pointer moves only after complete qualification.
- Human actions are append-only and survive reanalysis.
- Spoiler filtering precedes state, counts, filters, links, evidence and chain derivation.
- No clue-specific full-book preference is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client→clue API | Owner IDs, version IDs, human actions and full-book flags are untrusted |
| worker→provider | Calls can duplicate or exceed budget across failures |
| machine version→human overlay | Reanalysis may attempt to overwrite protected decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-08 | Spoofing | clue API ownership | mitigate | require_user + require_owned_novel + scoped resource queries |
| T-11-09 | Information Disclosure | spoiler projection | mitigate | visible-set-first cutoff and derived-field regression tests |
| T-11-10 | Tampering | override/reanalysis | mitigate | append-only rows, supersedes chain, needs_relink |
| T-11-11 | Denial of Service | model calls/budget | mitigate | atomic reservation, lease, checkpoints, cancellation and call ceilings |
| T-11-12 | Repudiation | promotion/rollback | mitigate | version manifests, expected-revision pointer journal and call audits |
| T-11-SC | Tampering | package installs | mitigate | no installs; existing stack only |
</threat_model>

## Verification

<verification>
Run worker/version, override/reanalysis, query projection and spoiler API suites; then run existing timeline tests to prove Phase 08 behavior remains unchanged.
</verification>

<success_criteria>
- Restart creates zero duplicate completed-stage calls.
- Budget/dependency pause creates zero later calls.
- Reanalysis preserves all human decisions.
- Default API leaks zero future values in every response field.
- Existing timeline API/worker files remain untouched.
</success_criteria>

<output>
Create .planning/phases/11-clue-and-foreshadow-tracking/11-03-SUMMARY.md when done
</output>

