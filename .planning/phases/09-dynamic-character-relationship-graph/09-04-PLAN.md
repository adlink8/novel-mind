---
phase: 09-dynamic-character-relationship-graph
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/lib/api.ts
  - frontend/src/lib/relationships.contract.test.ts
  - frontend/src/app/analysis/page.tsx
  - frontend/src/app/analysis/relationships.test.tsx
  - frontend/src/components/relationships/relationship-workspace.tsx
  - frontend/src/components/relationships/relationship-graph.tsx
  - frontend/src/components/relationships/relationship-controls.tsx
  - frontend/src/components/relationships/relationship-evidence-panel.tsx
autonomous: true
requirements: [REQ-REL-03, REQ-REL-04, REQ-REL-05, REQ-REL-06]
must_haves:
  truths:
    - "D-08/D-21: /analysis contains a relationship workspace with filters, zoom, evidence and keyboard-equivalent controls, not intermediate summaries."
    - "D-19: Cytoscape.js is the relationship renderer with its built-in TypeScript declarations."
    - "D-20: Existing ECharts timeline remains the timeline renderer and shares only selected source/version/narrative position state."
    - "D-22: Normal, large and filters-required modes degrade deterministically without browser lockup or partial-truth display."
  artifacts:
    - path: frontend/src/components/relationships/relationship-graph.tsx
      provides: Cytoscape graph lifecycle, styles, layout, selection and accessible companion list
      contains: "cytoscape"
    - path: frontend/src/components/relationships/relationship-evidence-panel.tsx
      provides: Edge evidence/provenance and chapter navigation
    - path: frontend/src/app/analysis/page.tsx
      provides: Shared novel/version/full-book/narrative-position workspace orchestration
  key_links:
    - from: frontend/src/components/relationships/relationship-workspace.tsx
      to: /api/relationships/{novel_id}/graph
      via: typed relationshipsApi query
      pattern: "relationshipsApi"
    - from: frontend/src/app/analysis/page.tsx
      to: frontend/src/components/timeline/timeline-chart.tsx
      via: preserved ECharts timeline branch and narrative position callback
      pattern: "TimelineChart"
---

<objective>
在现有 `/analysis` 中增加 Cytoscape.js 人物关系工作区，与 ECharts 主时间线共享版本/剧透/叙事位置，同时保持可访问性和大图降级。

Purpose: 将安全 graph API 转成可用、可验证的分析体验。
Output: exact-pinned Cytoscape dependency、typed API、relationship components、Vitest contracts。
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/09-dynamic-character-relationship-graph/09-RESEARCH.md
@.planning/phases/09-dynamic-character-relationship-graph/09-CONTEXT.md
@frontend/src/lib/api.ts
@frontend/src/app/analysis/page.tsx
@frontend/src/app/analysis/page.test.tsx
@frontend/src/components/timeline/timeline-chart.tsx
@frontend/src/components/timeline/timeline-controls.tsx
@backend/app/schemas/relationship.py

<interfaces>
Relationships API response is Plan 01 RelationshipGraphEnvelope. AnalysisPage already owns novelId, source active|running_candidate, fullBook confirmation and timeline envelope. Preserve these contracts and add shared throughChapter/selected narrative position.
</interfaces>
</context>

## Must-Haves

- Cytoscape receives only server-filtered nodes/edges.
- Canvas and keyboard companion list use the same arrays.
- ECharts timeline code and full-book confirmation remain authoritative.

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Cytoscape and add typed graph API contracts</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/lib/api.ts, frontend/src/lib/relationships.contract.test.ts</files>
  <read_first>09-RESEARCH.md Package Legitimacy Audit, frontend/package.json, frontend/package-lock.json, frontend/src/lib/api.ts, backend/app/schemas/relationship.py, frontend/src/lib/api.contract.test.ts</read_first>
  <behavior>
    - API client serializes source/version/through_chapter/character_ids/relation_types/full_book exactly.
    - Response types include cutoff, provenance, visible filters/counts, evidence locator and degradation.
    - No client owner_id and no @types/cytoscape package.
  </behavior>
  <action>Per D-19, install exact `cytoscape@3.34.0` from the verified package audit and update lockfile. Do not add `@types/cytoscape`. Add relationship graph/evidence/override TypeScript contracts and API methods matching OpenAPI; preserve all timeline types and methods. Write contract tests before adapter changes.</action>
  <verify><automated>cd frontend; npm test -- --run src/lib/relationships.contract.test.ts; npm ls cytoscape @types/cytoscape</automated></verify>
  <acceptance_criteria>
    - npm ls reports cytoscape@3.34.0 and no installed @types/cytoscape.
    - Contract test asserts query names and omits owner_id.
    - Existing timeline API contract remains unchanged.
  </acceptance_criteria>
  <done>Frontend has a legitimate, pinned graph library and exact backend contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build relationship workspace, Cytoscape lifecycle and timeline linkage</name>
  <files>frontend/src/app/analysis/page.tsx, frontend/src/app/analysis/relationships.test.tsx, frontend/src/components/relationships/relationship-workspace.tsx, frontend/src/components/relationships/relationship-graph.tsx, frontend/src/components/relationships/relationship-controls.tsx, frontend/src/components/relationships/relationship-evidence-panel.tsx</files>
  <read_first>frontend/src/app/analysis/page.tsx, frontend/src/components/timeline/timeline-chart.tsx, frontend/src/components/timeline/timeline-controls.tsx, frontend/src/components/timeline/timeline-status.tsx, frontend/src/lib/api.ts, Cytoscape official API guidance summarized in 09-RESEARCH.md</read_first>
  <behavior>
    - Workspace switches only timeline/relationships and never exposes backend intermediate summaries.
    - Person/type/chapter controls refetch server-filtered graph; edge/node selection opens evidence and chapter navigation.
    - Canvas and keyboard list select the same graph elements; source/version switches clear stale selection.
    - Large mode uses low-cost styling/layout; filters-required does not instantiate Cytoscape with partial elements.
  </behavior>
  <action>Add shared workspace state to AnalysisPage per D-08/D-20 without replacing or rewriting TimelineChart/ECharts. Per D-21, add zoom/pan controls, person/type/through-chapter filters, evidence side panel with machine/manual provenance and `/novels/{id}?chapter=` link, and a keyboard companion list derived from the same nodes/edges. Implement client-only Cytoscape instance creation, event listener cleanup and destroy/recycle behavior. Use built-in `cose` normal layout and non-animated low-cost `concentric`/simple styles in large mode; pixelRatio 1, selected-only labels and simplified edges in large mode per D-22. Timeline selection/narrative position updates throughChapter; API remains the spoiler authority.</action>
  <verify><automated>cd frontend; npm test -- --run src/app/analysis/relationships.test.tsx src/lib/relationships.contract.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests cover normal, large, filters_required, active/candidate switch, evidence selection and cleanup.
    - timeline-chart.tsx still imports echarts and relationship-graph.tsx imports cytoscape.
    - No UI string offers plot summary, theme, style, pace or chapter summary modes.
    - 390px component render has no document horizontal overflow outside intended graph viewport.
  </acceptance_criteria>
  <done>Relationship workspace is accessible, linked and bounded without changing the timeline renderer.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm frontend slice</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/lib/api.ts, frontend/src/lib/relationships.contract.test.ts, frontend/src/app/analysis/page.tsx, frontend/src/app/analysis/relationships.test.tsx, frontend/src/components/relationships/relationship-workspace.tsx, frontend/src/components/relationships/relationship-graph.tsx, frontend/src/components/relationships/relationship-controls.tsx, frontend/src/components/relationships/relationship-evidence-panel.tsx</files>
  <read_first>All Plan 04 files and Vitest/build output.</read_first>
  <action>Run relationship tests, full frontend tests, lint and production build. Fix only Phase 09 defects. Confirm Cytoscape listeners/instances clean up on novel/version/workspace switches, ECharts tests remain green, package lock is deterministic and no deprecated type package appears.</action>
  <verify><automated>cd frontend; npm test -- --run; npm run lint; npm run build</automated></verify>
  <acceptance_criteria>
    - Full Vitest, lint and Next production build pass.
    - Existing Phase 08 analysis/timeline tests pass unchanged or with only intentional shared-state assertions.
    - npm lock contains exact Cytoscape version and no unrelated dependency churn.
  </acceptance_criteria>
  <done>Frontend slice is production-buildable and ready for real browser qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API graph labels/evidence → DOM/Cytoscape | Novel text and labels are untrusted display data |
| Large payload → browser renderer | Unbounded layouts can deny service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-10 | Information Disclosure | frontend filters/counts | mitigate | display only server envelope; no local hidden-set derivation |
| T-09-11 | Denial of Service | Cytoscape layout | mitigate | server caps, large mode, no instance above cap, cleanup tests |
| T-09-12 | Tampering/XSS | labels/tooltips | mitigate | React text rendering; no HTML formatter from novel content |
| T-09-SC | Tampering | cytoscape install | mitigate | exact verified package, lockfile, no deprecated @types package |
</threat_model>

## Verification

<verification>Vitest, lint and build prove typed contracts, workspace behavior, Cytoscape lifecycle, ECharts preservation and degradation.</verification>

<success_criteria>REQ-REL-03/04/05/06 frontend experience is complete, accessible and safe before browser release tests.</success_criteria>

<output>Create `.planning/phases/09-dynamic-character-relationship-graph/09-04-SUMMARY.md` when executed.</output>
