---
phase: 09-dynamic-character-relationship-graph
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - backend/app/services/relationships/query.py
  - backend/app/services/relationships/overrides.py
  - backend/app/services/relationships/projection.py
  - backend/app/api/relationships.py
  - backend/app/main.py
  - backend/tests/integration/relationships/test_api.py
  - backend/tests/integration/relationships/test_projection.py
autonomous: true
requirements: [REQ-REL-02, REQ-REL-03, REQ-REL-05, REQ-REL-06]
must_haves:
  truths:
    - "D-06: Every node, type, filter, count and evidence preview derives from the same visible accepted observation set."
    - "D-09: The API resolves cutoff from persisted reading progress; missing or invalid progress exposes only chapter one, and no chapters yields an empty graph."
    - "D-10: Full-book disclosure reuses only the persisted Phase 08 timeline_full_book preference."
    - "D-11: Every graph response is owner/novel/version/spoiler scoped; active and running candidate never merge or silently fall back."
    - "D-12: A client-selected version is re-proven server-side inside the current owner and novel before query or override access."
    - "D-17/D-18: Overrides overlay accepted observations without mutation and ambiguous relinks remain needs_relink."
    - "D-02: Neo4j/projection is replayable from accepted PostgreSQL manifests and never authoritative."
    - "D-23/D-24: Downstream phases receive a read-only filtered contract and accepted IDs/evidence only."
  artifacts:
    - path: backend/app/services/relationships/query.py
      provides: Version proof, narrative fold and visible-set-first spoiler graph projection
      contains: "class RelationshipGraphQueryService"
    - path: backend/app/api/relationships.py
      provides: Owner/version/spoiler graph, evidence and append-only override endpoints
      exports: [router]
    - path: backend/app/services/relationships/projection.py
      provides: Accepted-observation replay manifest and optional Neo4j adapter boundary
      contains: "replay_accepted_observations"
  key_links:
    - from: backend/app/api/relationships.py
      to: backend/app/services/relationships/query.py
      via: authenticated owned-novel dependency and server-proven version
      pattern: "require_owned_novel"
    - from: backend/app/services/relationships/query.py
      to: Novel.reading_progress
      via: Phase 08 cutoff/timeline_full_book policy
      pattern: "timeline_full_book"
---

<objective>
交付 graph read model、protective override actions 和 replayable projection boundary，使 API 在任何位置都只返回同一 visible accepted set。

Purpose: 把事实 authority 转换为安全可消费的图契约，同时封死 metadata spoiler 和 projection write-back。
Output: query/override/projection services、FastAPI routes、PostgreSQL API/projection tests。
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/09-dynamic-character-relationship-graph/09-CONTEXT.md
@backend/app/services/timeline/query.py
@backend/app/services/timeline/overrides.py
@backend/app/services/timeline/promotion.py
@backend/app/services/knowledge/graph_sync.py
@backend/app/api/timeline.py
@backend/app/api/dependencies.py
@backend/app/schemas/relationship.py

<interfaces>
Use Plan 01 RelationshipGraphEnvelope and override schemas without changing them. Reuse the Phase 08 reading cutoff: missing/invalid progress -> first chapter; request full-book only when persisted reading_progress.timeline_full_book is true.
</interfaces>
</context>

## Must-Haves

- One visible relation set drives every response field.
- Active, running candidate and explicit history versions never merge.
- Overrides and projection are derived layers over accepted observations.

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement version proof, narrative fold and spoiler-safe query</name>
  <files>backend/app/services/relationships/query.py, backend/tests/integration/relationships/test_api.py</files>
  <read_first>backend/app/services/timeline/query.py, backend/app/models/novel.py, backend/app/models/analysis.py, backend/app/models/relationship.py, backend/app/schemas/relationship.py</read_first>
  <behavior>
    - Default active, running candidate and explicit historical version resolve only inside owner/novel scope.
    - Position fold returns different relationship states before/after changes without mutating rows.
    - Future nodes/types/counts/evidence are absent before all metadata is derived.
  </behavior>
  <action>Build RelationshipGraphQueryService per D-06, D-09, D-10, D-11 and D-12. Prove source/version server-side; per D-10 reuse only the Phase 08 `timeline_full_book` preference, and per D-11 never merge or silently fall back across active/running/history versions. Select accepted observations within selected narrative position, fold transition chains, apply eligible overrides, then per D-06 derive Character endpoints, relation filters, counts and evidence previews from that same collection. Enforce normal/large/filters_required thresholds from D-22; over-cap responses contain no graph elements. Do not read CharacterRelation.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_api.py -k "query or spoiler or version or fold" -q</automated></verify>
  <acceptance_criteria>
    - Serialized response scan contains no hidden future names, labels, counts or evidence.
    - active/running/history fixtures remain source-isolated.
    - seeded legacy CharacterRelation rows do not change response checksum.
    - Over-cap result has empty nodes/edges and mode filters_required.
  </acceptance_criteria>
  <done>Read model is deterministic, spoiler-safe and version-isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire protective overrides, graph API and replayable projection</name>
  <files>backend/app/services/relationships/overrides.py, backend/app/services/relationships/projection.py, backend/app/api/relationships.py, backend/app/main.py, backend/tests/integration/relationships/test_api.py, backend/tests/integration/relationships/test_projection.py</files>
  <read_first>backend/app/services/timeline/overrides.py, backend/app/services/timeline/promotion.py, backend/app/services/knowledge/graph_sync.py, backend/app/api/timeline.py, backend/app/main.py, backend/app/schemas/relationship.py</read_first>
  <behavior>
    - Character merge/relation type/interval corrections insert superseding rows and never update prior rows.
    - Cross-version relink applies only for one exact stable evidence signature match.
    - Replay from PostgreSQL accepted manifest yields stable checksum; adapter failure leaves DB truth unchanged.
  </behavior>
  <action>Create `/api/relationships/{novel_id}` routes for graph, visible evidence, append-only character-merge/relationship overrides and projection manifest. Use require_owned_novel and never accept owner_id. Validate version, endpoints and evidence again on every write. Implement override append/supersede/relink services per D-17/D-18. Per D-02, implement an optional one-way projection adapter that reads accepted observations and writes only projection audit/checkpoint; disabled/missing Neo4j is a transparent non-authoritative state, while replay checksum is deterministic. Expose a read-only service function documented for Phase 10 and accepted IDs/evidence contract for Phase 11 without creating their tables/routes per D-23/D-24.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_api.py tests/integration/relationships/test_projection.py -q</automated></verify>
  <acceptance_criteria>
    - Cross-owner and cross-version API requests return 404 without existence metadata.
    - Prior override rows remain byte-identical after supersession; ambiguous relink is needs_relink.
    - Empty/replayed projection has identical manifest checksum and failure cannot mutate observation status.
    - main.py mounts exactly one authenticated relationship router.
  </acceptance_criteria>
  <done>API, corrections and projection all preserve PostgreSQL truth and scope.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm backend graph contract</name>
  <files>backend/app/services/relationships/query.py, backend/app/services/relationships/overrides.py, backend/app/services/relationships/projection.py, backend/app/api/relationships.py, backend/app/main.py, backend/tests/integration/relationships/test_api.py, backend/tests/integration/relationships/test_projection.py</files>
  <read_first>All Plan 03 files, generated OpenAPI and test failures.</read_first>
  <action>Run API/projection integration tests and an OpenAPI generation smoke test. Fix only Phase 09 defects. Confirm serialized response leakage checks, version-source isolation, full-book denial/allow, evidence access and projection replay. Capture endpoint schema and query performance baseline for Plan 05.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_api.py tests/integration/relationships/test_projection.py -q; .venv\Scripts\python.exe -c "from app.main import app; assert '/api/relationships/{novel_id}/graph' in app.openapi()['paths']"</automated></verify>
  <acceptance_criteria>
    - All targeted tests and OpenAPI assertion pass.
    - No graph endpoint uses legacy CharacterRelation or client owner_id.
    - Response schema contains explicit version, cutoff, provenance and degradation.
  </acceptance_criteria>
  <done>Backend contract is safe for the frontend and downstream read-only consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → graph API | filters/version/full-book are untrusted selections |
| PostgreSQL → Neo4j adapter | projection may fail or be stale but cannot be authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-07 | Information Disclosure | graph metadata | mitigate | visible-set-first derivation and serialized leakage tests |
| T-09-08 | Elevation of Privilege | version/override endpoints | mitigate | require_owned_novel, server-proven version and endpoint/evidence scope |
| T-09-09 | Tampering | Neo4j projection | mitigate | one-way replay, stable manifest, no acceptance/write-back methods |
| T-09-SC | Tampering | package installs | mitigate | no package install in this plan |
</threat_model>

## Verification

<verification>PostgreSQL integration and OpenAPI checks prove owner/version/spoiler/override/projection behavior end to end.</verification>

<success_criteria>REQ-REL-02/03/05/06 backend graph surface is complete and no metadata or projection path bypasses accepted observations.</success_criteria>

<output>Create `.planning/phases/09-dynamic-character-relationship-graph/09-03-SUMMARY.md` when executed.</output>
