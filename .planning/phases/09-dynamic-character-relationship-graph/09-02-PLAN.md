---
phase: 09-dynamic-character-relationship-graph
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - backend/app/services/relationships/candidates.py
  - backend/app/services/relationships/evidence.py
  - backend/app/services/relationships/judgment.py
  - backend/app/services/relationships/gates.py
  - backend/app/services/relationships/worker.py
  - backend/prompts/relationship_semantic_judge.v1.txt
  - backend/tests/unit/relationships/test_pipeline.py
  - backend/tests/integration/relationships/test_pipeline.py
autonomous: true
requirements: [REQ-REL-01, REQ-REL-02, REQ-REL-06]
must_haves:
  truths:
    - "D-13/D-14: LLM performs bounded semantic judgment only; scripts own source selection, evidence, thresholds, state and writes."
    - "D-15/D-16: Threshold and state-machine outcomes are deterministic, versioned and auditable."
    - "D-05/D-07: Only fiction accepted judgments for five character relation types can become observations."
  artifacts:
    - path: backend/app/services/relationships/gates.py
      provides: Deterministic fiction/scope/schema/evidence/interval/conflict/threshold gate
      contains: "AUTO_ACCEPT_THRESHOLD = 0.85"
    - path: backend/app/services/relationships/worker.py
      provides: Idempotent version-bound observation build and persistence orchestration
      contains: "class RelationshipObservationWorker"
    - path: backend/prompts/relationship_semantic_judge.v1.txt
      provides: Frozen evidence-only fiction semantic prompt
  key_links:
    - from: backend/app/services/relationships/candidates.py
      to: KnowledgeRelationJudgment
      via: accepted/accepted source query
      pattern: "status == \"accepted\""
    - from: backend/app/services/relationships/gates.py
      to: RelationshipObservation
      via: only GateDecision.accepted persistence path
      pattern: "gate_status"
---

<objective>
实现 accepted judgment → versioned candidate/evidence package → strict LLM judgment → deterministic gates → append-only observation 的唯一事实写入路径。

Purpose: 证明相似度、LLM、history、chat 或 legacy rows 均不能直接成图。
Output: 关系分析服务、冻结 prompt、unit/PostgreSQL integration tests。
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/09-dynamic-character-relationship-graph/09-AI-SPEC.md
@.planning/phases/09-dynamic-character-relationship-graph/09-CONTEXT.md
@.planning/phases/04-llm/04-AI-SPEC.md
@backend/app/services/knowledge/candidates.py
@backend/app/services/knowledge/evidence.py
@backend/app/services/knowledge/llm_judge.py
@backend/app/services/knowledge/gates.py
@backend/app/services/timeline/worker.py
@backend/app/models/relationship.py

<interfaces>
Consume Plan 01 strict schemas and ORM. Source query is KnowledgeRelationJudgment.status='accepted' AND gate_status='accepted', joined to candidate.domain_profile='fiction', endpoints of kind character, selected AnalysisVersion, and valid evidence refs.
</interfaces>
</context>

## Must-Haves

- One deterministic build path is the sole accepted-observation writer.
- Threshold, interval and conflict outcomes have stable reason codes.
- LLM cannot select out-of-package IDs or state transitions.

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic candidate and evidence packages</name>
  <files>backend/app/services/relationships/candidates.py, backend/app/services/relationships/evidence.py, backend/tests/unit/relationships/test_pipeline.py</files>
  <read_first>09-AI-SPEC.md, backend/app/services/knowledge/candidates.py, backend/app/services/knowledge/evidence.py, backend/app/models/knowledge.py, backend/app/models/relationship.py</read_first>
  <behavior>
    - Only accepted/accepted fiction judgments with character endpoints and five allowed labels yield candidates.
    - Recall scores and same_entity can enqueue identity review but never create an edge observation.
    - Evidence package binds analysis version, hierarchy/source checksum, offsets/hashes and allowed IDs.
  </behavior>
  <action>Implement deterministic source selection per D-05/D-07/D-13/D-14. Revalidate accepted source state at build time, resolve Character endpoints within the same novel, join normalized evidence to the selected AnalysisVersion source/hierarchy, and compute stable candidate/package hashes. Explicitly reject history, causes/precedes, unresolved endpoint, cross-version evidence and chat/vector-only inputs before any provider call.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/unit/relationships/test_pipeline.py -k "candidate or evidence" -q</automated></verify>
  <acceptance_criteria>
    - Fixture matrix proves only five fiction relationship labels produce packages.
    - Package contains no full novel and every selectable ID is enumerated.
    - Recall metadata is persisted separately and no candidate function writes RelationshipObservation.
  </acceptance_criteria>
  <done>Every model call receives a bounded, versioned, evidence-valid candidate package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict judgment, gates and idempotent worker</name>
  <files>backend/app/services/relationships/judgment.py, backend/app/services/relationships/gates.py, backend/app/services/relationships/worker.py, backend/prompts/relationship_semantic_judge.v1.txt, backend/tests/unit/relationships/test_pipeline.py, backend/tests/integration/relationships/test_pipeline.py</files>
  <read_first>09-AI-SPEC.md, backend/app/services/knowledge/llm_judge.py, backend/app/services/knowledge/gates.py, backend/app/services/timeline/worker.py, backend/app/services/ai_service.py, backend/app/models/relationship.py</read_first>
  <behavior>
    - Strict output can only echo package IDs/enums and cannot choose owner/version/status.
    - confidence >=.85 accepts only after all gates; .65-.849 reviews; below .65 rejects.
    - Same version/package/policy retry is idempotent; new analysis version creates distinct observations without mutation.
  </behavior>
  <action>Use the existing AIService structured call path, provider retries 0 and one persisted same-deployment repair. Freeze prompt/schema/model/decoding/policy hashes. Implement ordered source/fiction/scope/schema/evidence/interval/conflict/threshold gates and stable reason codes per D-15/D-16. Persist candidate, judgment audit and accepted observation in short transactions; no network call under DB lock. Implement exact-cache call_skipped audit and build checkpoints. Never transparently fallback model or write Neo4j here.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/unit/relationships/test_pipeline.py tests/integration/relationships/test_pipeline.py -q</automated></verify>
  <acceptance_criteria>
    - Forged/out-of-package evidence, history, prompt injection instructions and invalid intervals produce zero accepted rows.
    - Threshold boundary tests at .6499/.65/.8499/.85 have exact expected states.
    - Repeat worker run creates zero duplicate calls/observations; different version stays isolated.
    - LLM outage/malformed second response leaves auditable non-accepted state.
  </acceptance_criteria>
  <done>The only write path enforces the locked AI/script boundary and immutable observation semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm analysis pipeline</name>
  <files>backend/app/services/relationships/candidates.py, backend/app/services/relationships/evidence.py, backend/app/services/relationships/judgment.py, backend/app/services/relationships/gates.py, backend/app/services/relationships/worker.py, backend/tests/unit/relationships/test_pipeline.py, backend/tests/integration/relationships/test_pipeline.py</files>
  <read_first>All Plan 02 files and failing outputs.</read_first>
  <action>Run unit and real PostgreSQL integration suites, inspect accepted rows and provider-call audits, then fix only relationship pipeline defects. Confirm source judgment revocation, version mismatch, budget/dependency failure and retry all fail closed. Record calls/tokens/cost/latency and accepted/review/rejected counts.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/unit/relationships/test_pipeline.py tests/integration/relationships/test_pipeline.py -q</automated></verify>
  <acceptance_criteria>
    - All targeted tests pass without network or skip.
    - Critical false accepts are zero.
    - Accepted rows retain complete source/evidence/version lineage and immutable checksums.
  </acceptance_criteria>
  <done>Pipeline is deterministic around the LLM and safe for API consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Novel evidence → LLM | Prompt injection and forged narrative claims are untrusted data |
| LLM response → gate | IDs, confidence and rationale are untrusted until local validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Spoofing | Character/evidence IDs | mitigate | package allowlists plus owner/novel/version/hash revalidation |
| T-09-05 | Tampering | model confidence/state | mitigate | script thresholds and state machine; model schema has no status fields |
| T-09-06 | Denial of Service | retry/provider calls | mitigate | provider retries 0, one persisted repair, checkpoints and exact cache |
| T-09-SC | Tampering | package installs | mitigate | no install in this plan |
</threat_model>

## Verification

<verification>Unit and PostgreSQL integration prove all non-authoritative signals create zero facts and threshold/state/version semantics are exact.</verification>

<success_criteria>REQ-REL-01/02/06 accepted-observation pipeline is evidence-bound, fiction-only, idempotent and fully audited.</success_criteria>

<output>Create `.planning/phases/09-dynamic-character-relationship-graph/09-02-SUMMARY.md` when executed.</output>

