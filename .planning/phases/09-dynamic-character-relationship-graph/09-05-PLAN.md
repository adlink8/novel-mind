---
phase: 09-dynamic-character-relationship-graph
plan: 05
type: execute
wave: 5
depends_on: [09-02, 09-03, 09-04]
files_modified:
  - backend/evals/relationship_fiction.v1.json
  - backend/scripts/run_relationship_qualification.py
  - backend/tests/adversarial/test_relationship_boundaries.py
  - backend/tests/integration/relationships/test_performance.py
  - backend/tests/integration/relationships/test_release_gate.py
  - frontend/e2e/relationships-real.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [REQ-REL-01, REQ-REL-02, REQ-REL-03, REQ-REL-04, REQ-REL-05, REQ-REL-06]
must_haves:
  truths:
    - "All REQ-REL-01..06 critical gates pass on PostgreSQL and real desktop/mobile browser paths before release."
    - "D-22 performance/degradation and D-02 projection replay have measured, fail-closed evidence."
    - "D-23/D-24 downstream contracts are present while session/chat/clue artifacts remain absent."
  artifacts:
    - path: backend/scripts/run_relationship_qualification.py
      provides: Frozen semantic, authority, performance and command-evidence release verdict
      contains: "blocked_release"
    - path: frontend/e2e/relationships-real.spec.ts
      provides: Real API/browser spoiler, interaction, override and degradation proof
    - path: backend/evals/relationship_fiction.v1.json
      provides: Frozen fiction-only relation/transition/interval/adversarial labels
  key_links:
    - from: backend/scripts/run_relationship_qualification.py
      to: PostgreSQL relationship observations
      via: fresh independent session authority reads
      pattern: "session_factory"
    - from: .github/workflows/ci.yml
      to: relationship release gate
      via: blocking CI command and artifact upload
      pattern: "run_relationship_qualification.py"
---

<objective>
建立 Phase 09 的最终质量与发布门禁，覆盖 semantic precision、authority、owner/version/spoiler、override、projection、Cytoscape browser 和超大图降级。

Purpose: 让“关系图可信且不会剧透”成为可重复的发布证据，而非人工观感。
Output: frozen fixture、adversarial/performance/release tests、real Playwright、CI gate。
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/09-dynamic-character-relationship-graph/09-VALIDATION.md
@.planning/phases/09-dynamic-character-relationship-graph/09-AI-SPEC.md
@backend/scripts/run_timeline_qualification.py
@backend/evals/timeline_fiction.v1.json
@backend/tests/integration/timeline/test_real_qualification.py
@frontend/e2e/timeline-real.spec.ts
@.github/workflows/ci.yml
</context>

## Must-Haves

- No critical leak or false fact can be averaged away by a score.
- Release evidence is independently observed, version-bound and command-backed.
- Real browser verifies both normal behavior and degradation on two viewports.

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build frozen fiction, adversarial and performance qualification</name>
  <files>backend/evals/relationship_fiction.v1.json, backend/tests/adversarial/test_relationship_boundaries.py, backend/tests/integration/relationships/test_performance.py</files>
  <read_first>09-AI-SPEC.md evaluation gates, backend/evals/timeline_fiction.v1.json, backend/evals/knowledge_graph_fiction_sample.json, backend/tests/adversarial/test_timeline_evidence.py, backend/tests/integration/timeline/test_spoilers.py</read_first>
  <behavior>
    - Frozen fiction cases cover establish/change/end, five types, interval anchors, ambiguity and hard negatives.
    - History/vector/chat/legacy/forged/cross-owner/cross-version/future-metadata attacks create zero facts/leaks.
    - 10k observations and three graph tiers meet query/payload/degradation budgets.
  </behavior>
  <action>Create a versioned fiction-only corpus with at least 30 labeled relationship spans and 15 adversarial cases. Include same pair changing state, overlapping compatible relations, aliases, ambiguous merge, prompt injection, pure co-occurrence, temporal-only relation and future evidence. Add independent PostgreSQL adversarial tests for every trust boundary and a measured performance test that seeds 10,000 accepted observations, warms queries, records p50/p95, asserts <=500/1500 payload caps and exact filters_required semantics. Critical failures are boolean blockers, not averaged metrics.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/adversarial/test_relationship_boundaries.py tests/integration/relationships/test_performance.py -q</automated></verify>
  <acceptance_criteria>
    - Fixture contains fiction only and all five canonical edge types.
    - Critical false accepts and future metadata leaks are zero.
    - Measured p95 <=300ms on qualification environment or release blocks with metrics, never self-waives.
  </acceptance_criteria>
  <done>Frozen semantic/security/performance evidence exercises the full Phase 09 risk surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real browser journeys and executable release authority</name>
  <files>backend/scripts/run_relationship_qualification.py, backend/tests/integration/relationships/test_release_gate.py, frontend/e2e/relationships-real.spec.ts, .github/workflows/ci.yml</files>
  <read_first>backend/scripts/run_timeline_qualification.py, backend/tests/integration/timeline/test_real_qualification.py, frontend/e2e/timeline-real.spec.ts, playwright.config.ts, .github/workflows/ci.yml, frontend/package-lock.json</read_first>
  <behavior>
    - Real desktop/mobile browser selects novel/version, links timeline position, filters, zooms, opens evidence, applies override and proves future data absent.
    - Large mode remains interactive; over-cap shows filters-required with no Cytoscape elements.
    - Release passes only with fresh PostgreSQL observations, replay checksum, package lock and successful internally executed commands.
  </behavior>
  <action>Follow Phase 08's real-stack qualification authority: Next.js, FastAPI and PostgreSQL are real; only model transport is controlled. Implement deterministic E2E seed/resume helpers through the qualification CLI, not browser route mocks. The release CLI owns fixed argv/cwd commands, captures stdout/stderr bytes internally, recomputes digests, opens a fresh DB session to verify accepted observations/spoiler/projection/override authority, validates fixture/schema/policy/version/package-lock hashes, and emits passed or blocked_release. Add the relationship targeted jobs/artifacts to existing CI gate without weakening prior required checks.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_release_gate.py -q; cd ..\frontend; npm run test:e2e -- relationships-real.spec.ts</automated></verify>
  <acceptance_criteria>
    - Playwright passes chromium-desktop and chromium-mobile-390 with real APIs and PostgreSQL.
    - Browser response capture proves future names/types/counts absent before full-book confirmation.
    - Missing/tampered report, failed command, DB mismatch, replay mismatch or over-budget performance produces non-zero blocked_release.
    - CI retains all Phase 08 timeline gates and adds Phase 09 evidence artifact.
  </acceptance_criteria>
  <done>Real user journey and release authority are executable and fail closed.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm Phase 09 release</name>
  <files>backend/evals/relationship_fiction.v1.json, backend/scripts/run_relationship_qualification.py, backend/tests/adversarial/test_relationship_boundaries.py, backend/tests/integration/relationships/test_performance.py, backend/tests/integration/relationships/test_release_gate.py, frontend/e2e/relationships-real.spec.ts, .github/workflows/ci.yml</files>
  <read_first>All Phase 09 summaries, 09-VALIDATION.md, all Plan 05 files and command output.</read_first>
  <action>Run the complete backend relationship suite, frontend unit/lint/build, real two-viewport Playwright and executable release verifier. Fix only Phase 09 defects, rerun every failed command and retain final exact digests/metrics. Add a deterministic scope scan proving history, conversation/session/message and clue candidate/state artifacts were not added, while Phase 10/11 dependency contract names remain documented.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/unit/relationships tests/integration/relationships tests/adversarial/test_relationship_boundaries.py -q; .venv\Scripts\python.exe scripts/run_relationship_qualification.py --verify-release --report artifacts/relationship-report.json; cd ..\frontend; npm test -- --run; npm run lint; npm run build; npm run test:e2e -- relationships-real.spec.ts</automated></verify>
  <acceptance_criteria>
    - All listed commands pass with zero skip for Phase 09 tests.
    - Release verdict is passed and binds independent PostgreSQL and command evidence.
    - Source scan finds no history product contract, chat/session/message implementation or clue lifecycle implementation in Phase 09 changes.
    - Every REQ-REL and D-01..D-24 row in 09-VALIDATION source audit remains covered.
  </acceptance_criteria>
  <done>Phase 09 has complete, independently verifiable release evidence and no Phase 10/11 scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture/report → release verdict | Static or self-asserted reports may be forged |
| Browser → real services | Route mocks can hide owner/spoiler failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-13 | Spoofing | qualification authority | mitigate | fresh PostgreSQL session and fixed internally executed commands |
| T-09-14 | Repudiation | release artifacts | mitigate | canonical hashes and exact output byte digests |
| T-09-15 | Denial of Service | large graph | mitigate | measured tiers, hard cap, real browser interaction budgets |
| T-09-SC | Tampering | dependency lock | mitigate | verify exact Cytoscape lock and package audit during release |
</threat_model>

## Verification

<verification>The full matrix in 09-VALIDATION runs and the executable verifier independently confirms PostgreSQL, commands, package lock, browser and scope evidence.</verification>

<success_criteria>All REQ-REL-01..06 pass with no critical override, and Phase 10/11 remain data dependencies only.</success_criteria>

<output>Create `.planning/phases/09-dynamic-character-relationship-graph/09-05-SUMMARY.md` when executed.</output>

