---
phase: 09-dynamic-character-relationship-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/relationship.py
  - backend/app/models/__init__.py
  - backend/app/schemas/relationship.py
  - backend/migrations/versions/11_relationship_observations.py
  - backend/tests/integration/relationships/test_persistence.py
autonomous: true
requirements: [REQ-REL-01, REQ-REL-02, REQ-REL-05]
must_haves:
  truths:
    - "D-01/D-03: PostgreSQL stores immutable, append-only relationship observations bound to one owner, novel, analysis version and accepted source judgment."
    - "D-04: Legacy character_relations rows cannot satisfy a Phase 09 observation contract."
    - "D-17/D-18: Character merge, relation type and interval corrections are append-only overrides with explicit supersession and relink state."
  artifacts:
    - path: backend/app/models/relationship.py
      provides: Relationship run, candidate, judgment, accepted observation, evidence, override and projection-audit ORM authority
      contains: "class RelationshipObservation"
    - path: backend/migrations/versions/11_relationship_observations.py
      provides: PostgreSQL tables, indexes, constraints and append-only triggers
      contains: "revision = \"11relobserve01\""
    - path: backend/app/schemas/relationship.py
      provides: Strict enums and request/response contracts consumed by later plans
      contains: "class RelationshipGraphEnvelope"
  key_links:
    - from: backend/app/models/relationship.py
      to: backend/app/models/analysis.py
      via: analysis_version_id foreign keys
      pattern: "ForeignKey\\(\"analysis_versions.id\""
    - from: backend/app/models/relationship.py
      to: backend/app/models/knowledge.py
      via: source accepted judgment/candidate foreign keys
      pattern: "knowledge_relation_judgments.id"
---

<objective>
建立 Phase 09 的 PostgreSQL authority 与 interface-first contracts，使后续 pipeline、API、projection 和 UI 都只能围绕不可变 observation/version/evidence/override 工作。

Purpose: 先消除 legacy snapshot 语义，锁定最不可逆的数据契约。
Output: ORM、strict schemas、Alembic migration、真实 PostgreSQL 持久化测试。
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dynamic-character-relationship-graph/09-SPEC.md
@.planning/phases/09-dynamic-character-relationship-graph/09-CONTEXT.md
@.planning/phases/09-dynamic-character-relationship-graph/09-RESEARCH.md
@backend/app/models/knowledge.py
@backend/app/models/analysis.py
@backend/app/models/timeline.py
@backend/app/models/character.py
@backend/migrations/versions/10_analysis_timeline_versions.py

<interfaces>
Existing authorities:
- KnowledgeRelationJudgment: id, owner_id, novel_id, relation_candidate_id, relation_type, confidence, evidence_refs, status, gate_status, prompt/model lineage.
- AnalysisVersion: id, owner_id, novel_id, status and frozen source/hierarchy/prompt/schema/model/config manifest.
- Character: id, novel_id, name and aliases; CharacterRelation is legacy snapshot only.

New contract names required by downstream plans:
- RelationshipBuildRun, RelationshipObservationCandidate, RelationshipObservationJudgment, RelationshipObservation, RelationshipEvidenceLink.
- CharacterIdentityOverride, RelationshipOverride, RelationshipProjectionAudit.
- RelationshipGraphEnvelope, RelationshipGraphNode, RelationshipGraphEdge, RelationshipEvidenceResponse, RelationshipDegradation.
</interfaces>
</context>

## Must-Haves

- Accepted facts and overrides are physically append-only in PostgreSQL.
- Every fact has owner/novel/version/source-judgment/evidence/policy lineage.
- The schema is fiction-only and exposes only the five D-07 edge types.

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable relationship authority and strict contracts</name>
  <files>backend/app/models/relationship.py, backend/app/models/__init__.py, backend/app/schemas/relationship.py, backend/tests/integration/relationships/test_persistence.py</files>
  <read_first>09-SPEC.md, 09-CONTEXT.md, backend/app/models/knowledge.py, backend/app/models/analysis.py, backend/app/models/timeline.py, backend/app/models/character.py, backend/app/models/__init__.py</read_first>
  <behavior>
    - Accepted observation requires matching owner/novel/version, accepted source judgment, endpoints, canonical type, valid interval, evidence checksum, policy/schema/prompt lineage and idempotency key.
    - Relation type accepts only ally/enemy/family/mentor/romantic; history and temporal labels fail schema validation.
    - Overrides require author, reason, stable evidence signature, supersedes_id and active|superseded|needs_relink state.
  </behavior>
  <action>Create modern SQLAlchemy Mapped models per D-01/D-03/D-05/D-07. Keep legacy CharacterRelation unchanged and absent from new relationships. Define explicit check/unique/index contracts for observation state, transition, interval ordering, source/target inequality, version scope and idempotency. Add strict Pydantic enums and all downstream graph/override/evidence/degradation envelopes now, so Plans 02-04 implement against stable interfaces. Export ORM classes from models/__init__.py. Tests must construct valid and invalid rows/contracts before implementation.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_persistence.py -k "contract" -q</automated></verify>
  <acceptance_criteria>
    - relationship.py contains all eight required ORM classes and no history relation enum.
    - schema rejects causes, precedes, same_entity and unknown edge types.
    - accepted observation has FKs to analysis_versions and knowledge_relation_judgments plus owner/novel indexes.
    - CharacterRelation source is unchanged and no new graph response references it.
  </acceptance_criteria>
  <done>Interface contracts compile and contract tests prove fiction, scope, interval, lineage and override shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PostgreSQL migration and database append-only enforcement</name>
  <files>backend/migrations/versions/11_relationship_observations.py, backend/tests/integration/relationships/test_persistence.py</files>
  <read_first>backend/migrations/versions/10_analysis_timeline_versions.py, backend/app/models/relationship.py, backend/tests/integration/timeline/test_persistence.py</read_first>
  <behavior>
    - Migration from 10analysistime01 creates every Phase 09 table in dependency order.
    - UPDATE or DELETE of accepted observations and correction rows fails at the PostgreSQL boundary.
    - Duplicate idempotency keys and invalid cross-table scopes are rejected.
  </behavior>
  <action>Create revision `11relobserve01` using repository metadata pattern plus explicit PostgreSQL append-only trigger function for `relationship_observations`, `character_identity_overrides`, and `relationship_overrides`; supersession is always a new INSERT. Add composite indexes supporting version/cutoff/pair/type queries and evidence lookup. Do not migrate legacy CharacterRelation rows into accepted observations per D-04. Test against PostgreSQL 16, including upgrade from Phase 08 head and attempted UPDATE/DELETE.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m alembic upgrade head; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_persistence.py -k "postgres or append_only or migration" -q</automated></verify>
  <acceptance_criteria>
    - alembic current reports 11relobserve01 head.
    - PostgreSQL rejects UPDATE/DELETE for accepted facts and overrides with a deterministic error.
    - Legacy character_relations row count is unchanged and relationship_observations remains empty after migration.
    - explain/inspection confirms composite version+interval and evidence indexes exist.
  </acceptance_criteria>
  <done>Real PostgreSQL is the enforceable append-only authority and migration introduces no synthetic facts.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm persistence slice</name>
  <files>backend/app/models/relationship.py, backend/app/models/__init__.py, backend/app/schemas/relationship.py, backend/migrations/versions/11_relationship_observations.py, backend/tests/integration/relationships/test_persistence.py</files>
  <read_first>All files modified by Tasks 1-2 and their test output.</read_first>
  <action>Run targeted contracts, Alembic current/check and model import checks. Fix only Phase 09 schema/migration defects. Confirm no accepted observation can be produced without accepted source judgment lineage and no prior row can be mutated. Record exact command output for the summary.</action>
  <verify><automated>cd backend; .venv\Scripts\python.exe -m pytest tests/integration/relationships/test_persistence.py -q; .venv\Scripts\python.exe -m alembic current; .venv\Scripts\python.exe -m alembic check</automated></verify>
  <acceptance_criteria>
    - Targeted test file passes with no skip.
    - ORM import and Alembic head succeed.
    - Any pre-existing unrelated Alembic drift is reported separately and not hidden.
  </acceptance_criteria>
  <done>Persistence slice is reproducible and ready for pipeline consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LLM/source judgment → PostgreSQL | Untrusted semantic output must not bypass accepted source/evidence contracts |
| API/service → append-only tables | Application bugs must not mutate historical truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01 | Tampering | relationship observations/overrides | mitigate | DB checks, FKs, idempotency and UPDATE/DELETE rejection triggers |
| T-09-02 | Information Disclosure | owner/novel/version fields | mitigate | non-null scoped FKs/indexes and later query proof; no client owner field |
| T-09-03 | Repudiation | manual correction | mitigate | author/reason/created/supersedes and immutable rows |
| T-09-SC | Tampering | package installs | mitigate | no package install in this plan; Cytoscape legitimacy audited in 09-RESEARCH |
</threat_model>

## Verification

<verification>
- PostgreSQL migration and append-only tests pass.
- Strict schema rejects all non-fiction/non-person relation types.
- Source and version lineage are mandatory and indexed.
</verification>

<success_criteria>
REQ-REL-01/02/05 data contracts are executable, immutable and ready for downstream plans without interpreting legacy snapshot semantics.
</success_criteria>

<output>Create `.planning/phases/09-dynamic-character-relationship-graph/09-01-SUMMARY.md` when executed.</output>

