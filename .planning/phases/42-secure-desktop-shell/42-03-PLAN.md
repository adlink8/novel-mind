---
phase: 42-secure-desktop-shell
plan: "03"
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - desktop/tests/fixtures/routes.ts
  - desktop/tests/e2e/route-parity.spec.ts
  - desktop/tests/e2e/critical-workflows.spec.ts
  - desktop/tests/e2e/renderer-privileges.spec.ts
  - frontend/src/types/desktop-bridge.d.ts
  - frontend/src/lib/desktop/capabilities.ts
autonomous: true
requirements: [REQ-DESK-01, REQ-DESK-02]
must_haves:
  truths:
    - "D-42-06/D-42-07: shell parity and privilege negatives are automated gates."
  artifacts:
    - path: desktop/tests/e2e/critical-workflows.spec.ts
      provides: existing workflow parity through Electron
  key_links:
    - from: frontend/src/lib/desktop/capabilities.ts
      to: desktop/src/shared/bridge-contract.ts
      via: typed optional desktop capability boundary
---

<objective>完成 Electron 内 13 路由与关键工作流回归，同时证明 renderer 权限没有扩大。</objective>

<execution_context>@desktop/tests/security/policy.spec.ts
@desktop/tests/security/ipc.spec.ts
@frontend/src/app
@frontend/src/components
@.planning/phases/42-secure-desktop-shell/42-CONTEXT.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Add typed optional desktop capability consumption</name>
<read_first>desktop/src/shared/bridge-contract.ts; desktop/src/preload/index.ts; frontend/src/app/layout.tsx</read_first>
<files>frontend/src/types/desktop-bridge.d.ts; frontend/src/lib/desktop/capabilities.ts</files>
<action>Declare the exact `novelMindDesktop` interface and one resolver that returns supported/unsupported capability state. Browser mode must return unsupported without errors; no route/component may import Electron or infer desktop mode from Node globals.</action>
<acceptance_criteria>Frontend typecheck passes in browser and desktop builds; `rg` finds no Electron import outside `desktop/`; missing bridge produces explicit unsupported state.</acceptance_criteria>
<verify><automated>cd frontend; npm run typecheck; if (rg "from ['\"]electron|require\(['\"]electron" src) { exit 1 }</automated></verify>
<done>Desktop capabilities are optional and isolated from business UI.</done>
</task>
<task type="auto">
<name>Task 2: Prove route, workflow and renderer-privilege parity</name>
<read_first>desktop/proof/tests/route-inventory.json; frontend/tests; desktop/tests/security/policy.spec.ts</read_first>
<files>desktop/tests/fixtures/routes.ts; desktop/tests/e2e/route-parity.spec.ts; desktop/tests/e2e/critical-workflows.spec.ts; desktop/tests/e2e/renderer-privileges.spec.ts</files>
<action>Reuse the 13-route inventory and existing browser critical flows for library/import, reader, analysis/chat, Visual Bible/illustration, derivative editor and export. Run them in Electron, then attempt require/process/fs/shell/raw IPC, popup and unapproved navigation from renderer fixtures.</action>
<acceptance_criteria>All 13 routes and selected verified workflows complete through Electron; every privilege/navigation attack is blocked with stable evidence.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/e2e</automated></verify>
<done>The secure shell preserves the product surface without privileged renderer access.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/e2e/route-parity.spec.ts; desktop/tests/e2e/critical-workflows.spec.ts; desktop/tests/e2e/renderer-privileges.spec.ts</read_first>
<files>desktop/tests/e2e/route-parity.spec.ts; desktop/tests/e2e/critical-workflows.spec.ts; desktop/tests/e2e/renderer-privileges.spec.ts; frontend/src/lib/desktop/capabilities.ts</files>
<action>Run desktop E2E twice and browser frontend tests once, fix desktop-only assumptions or parity regressions, and capture route/workflow/negative-test evidence.</action>
<acceptance_criteria>Desktop suite passes twice, browser suite remains green, and evidence maps every route plus each negative to REQ-DESK-01/02.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/e2e; cd ../frontend; npm test -- --run</automated></verify>
<done>Phase 42 parity and security evidence are complete.</done>
</task>
</tasks>

## Must-Haves

- All 13 routes remain present.
- Browser mode remains a working development/test harness.

## Verification

Run Electron E2E twice and existing frontend tests once.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| existing UI workflows -> desktop bridge | Route code could accidentally gain privileged behavior |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-42-03-01 | Elevation of Privilege | renderer regressions | mitigate | explicit negative E2E suite |
| T-42-03-02 | Tampering | route parity | mitigate | frozen route inventory and browser comparison |
</threat_model>

