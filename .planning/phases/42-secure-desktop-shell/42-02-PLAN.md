---
phase: 42-secure-desktop-shell
plan: "02"
type: execute
wave: 1
depends_on: ["42-01"]
files_modified:
  - desktop/src/main/security/csp.ts
  - desktop/src/main/security/navigation.ts
  - desktop/src/main/security/permissions.ts
  - desktop/src/main/ipc/register.ts
  - desktop/src/main/ipc/validate-sender.ts
  - desktop/src/shared/bridge-schema.ts
  - desktop/tests/security/policy.spec.ts
  - desktop/tests/security/ipc.spec.ts
autonomous: true
requirements: [REQ-DESK-02]
must_haves:
  truths:
    - "D-42-03/D-42-04/D-42-05/D-42-07: navigation, windows, permissions and IPC are deny-by-default."
  artifacts:
    - path: desktop/src/main/ipc/validate-sender.ts
      provides: shared sender/frame/origin authorization
  key_links:
    - from: desktop/src/main/ipc/register.ts
      to: desktop/src/shared/bridge-schema.ts
      via: payload parse before handler logic
---

<objective>实现 CSP、导航/窗口/权限策略和 sender-validated IPC 的完整负面安全边界。</objective>

<execution_context>@desktop/src/main/create-window.ts
@desktop/src/preload/index.ts
@desktop/src/shared/bridge-contract.ts
@.planning/phases/42-secure-desktop-shell/42-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Enforce CSP, navigation, window and permission policy</name>
<read_first>desktop/src/main/create-window.ts; desktop/src/main/index.ts; .planning/phases/42-secure-desktop-shell/42-RESEARCH.md</read_first>
<files>desktop/src/main/security/csp.ts; desktop/src/main/security/navigation.ts; desktop/src/main/security/permissions.ts; desktop/tests/security/policy.spec.ts</files>
<action>Allow only the approved local app origin and required self/data asset classes; deny unexpected navigation, redirects, `window.open`, webview creation, downloads and permission requests. External-link handling accepts only validated HTTPS URLs through an explicit capability and never renderer-controlled shell arguments.</action>
<acceptance_criteria>Policy tests block attacker origin, javascript/file/custom unexpected schemes, popup, webview, permission and untrusted external URL while preserving local app navigation/assets.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/security/policy.spec.ts</automated></verify>
<done>BrowserWindow trust scope is explicit and deny-by-default.</done>
</task>
<task type="auto">
<name>Task 2: Register schema- and sender-validated IPC</name>
<read_first>desktop/src/preload/index.ts; desktop/src/shared/bridge-contract.ts; desktop/src/main/create-window.ts</read_first>
<files>desktop/src/main/ipc/register.ts; desktop/src/main/ipc/validate-sender.ts; desktop/src/shared/bridge-schema.ts; desktop/tests/security/ipc.spec.ts</files>
<action>Define bounded serializable schemas for each capability, validate sender `webContents`, frame and approved origin before parsing/dispatch, reject unknown channels and duplicate registrations, return stable redacted error codes, and remove handlers on shutdown/reload.</action>
<acceptance_criteria>Wrong sender/frame/origin, malformed/oversized payload and unknown channel all reject without invoking capability logic; approved calls return only schema-declared data.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/security/ipc.spec.ts</automated></verify>
<done>IPC cannot bypass origin, sender or payload boundaries.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/security/policy.spec.ts; desktop/tests/security/ipc.spec.ts; desktop/src/main/security/csp.ts; desktop/src/main/ipc/register.ts</read_first>
<files>desktop/src/main/security/csp.ts; desktop/src/main/security/navigation.ts; desktop/src/main/security/permissions.ts; desktop/src/main/ipc/register.ts; desktop/src/main/ipc/validate-sender.ts; desktop/tests/security/policy.spec.ts; desktop/tests/security/ipc.spec.ts</files>
<action>Run all policy/IPC negatives twice, inspect Electron security warnings, and fix any wildcard CSP, stale handler, permissive scheme or unredacted error. Confirm no `ipcRenderer` object crosses the bridge.</action>
<acceptance_criteria>All negative tests pass twice, no Electron security warning is emitted, and static search finds no generic bridge IPC exposure.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/security/ipc.spec.ts; npx playwright test tests/security/policy.spec.ts; if (rg "exposeInMainWorld.*ipcRenderer|send:\s*ipcRenderer|invoke:\s*ipcRenderer" src) { exit 1 }</automated></verify>
<done>Desktop security policies are tested as release-blocking behavior.</done>
</task>
</tasks>

## Must-Haves

- CSP/navigation/window/permission and IPC sender tests fail closed.
- Error payloads and logs contain no secrets or environment details.

## Verification

Run policy and IPC positive/negative suites plus static generic-IPC scan.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| renderer input -> main handlers | Compromised UI can send malicious messages |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-42-02-01 | Spoofing | IPC sender | mitigate | webContents/frame/origin validation |
| T-42-02-02 | Elevation of Privilege | navigation/window | mitigate | deny-by-default policy and no generic shell |
| T-42-02-03 | Denial of Service | IPC payload | mitigate | bounded schemas and handler lifecycle |
</threat_model>

