---
phase: 42-secure-desktop-shell
plan: "01"
type: execute
wave: 0
depends_on: ["41-03"]
files_modified:
  - desktop/package.json
  - desktop/tsconfig.json
  - desktop/src/main/index.ts
  - desktop/src/main/create-window.ts
  - desktop/src/preload/index.ts
  - desktop/src/shared/bridge-contract.ts
  - desktop/tests/shell-smoke.spec.ts
autonomous: false
requirements: [REQ-DESK-01, REQ-DESK-02]
must_haves:
  truths:
    - "D-42-01/D-42-02: main owns lifecycle; renderer is sandboxed UI-only."
    - "Production dependency installation requires explicit user approval and a Phase 41 GO."
  artifacts:
    - path: desktop/src/shared/bridge-contract.ts
      provides: minimal typed DesktopBridge contract
  key_links:
    - from: desktop/src/preload/index.ts
      to: desktop/src/shared/bridge-contract.ts
      via: contextBridge with one method per capability
---

<objective>在 Phase 41 GO 后建立正式 Electron main/preload 骨架，默认安全并承载现有 Next renderer。</objective>

<execution_context>@.planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md
@.planning/phases/42-secure-desktop-shell/42-CONTEXT.md
@.planning/phases/42-secure-desktop-shell/42-RESEARCH.md
@frontend/package.json</execution_context>

## Steps

<tasks>
<task type="checkpoint:decision" gate="blocking">
<name>Task 1: Confirm Phase 41 GO and production dependency approval</name>
<read_first>.planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md; desktop/proof/package.json; frontend/package.json</read_first>
<files>desktop/package.json; desktop/tsconfig.json</files>
<action>Verify `41-DECISION.md` is GO with matching evidence hashes, then present exact Electron/build/test dependency versions for user approval. Do not install, lock or copy proof dependencies into the production desktop package before approval.</action>
<acceptance_criteria>GO evidence validates and explicit approval is recorded for the exact dependency set.</acceptance_criteria>
<verify><automated>if (-not (Select-String -Path .planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md -Pattern '^Verdict: GO$' -Quiet)) { exit 1 }</automated></verify>
<done>Production shell starts only from an approved green proof.</done>
</task>
<task type="auto">
<name>Task 2: Create the secure main/preload/renderer boundary</name>
<read_first>desktop/proof/src/main.ts; .planning/phases/42-secure-desktop-shell/42-RESEARCH.md; frontend/src/app/layout.tsx</read_first>
<files>desktop/src/main/index.ts; desktop/src/main/create-window.ts; desktop/src/preload/index.ts; desktop/src/shared/bridge-contract.ts; desktop/tests/shell-smoke.spec.ts</files>
<action>Create one production BrowserWindow with `contextIsolation: true`, `sandbox: true`, `nodeIntegration: false`, `webSecurity: true` and the approved app origin. Expose only typed `getRuntimeStatus`, `requestRuntimeRestart`, `getBootstrap` and `onRuntimeStatus` capabilities; no generic send/invoke, filesystem, shell, env or process object. Keep the Next renderer code web-compatible.</action>
<acceptance_criteria>Shell smoke loads the existing app; renderer cannot access `require`, Node filesystem, shell, env or raw ipcRenderer; bridge declarations compile on both preload and renderer sides.</acceptance_criteria>
<verify><automated>cd desktop; npm run typecheck; npx playwright test tests/shell-smoke.spec.ts</automated></verify>
<done>The production-shaped shell is secure by construction and reuses the existing renderer.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/src/main/create-window.ts; desktop/src/preload/index.ts; desktop/src/shared/bridge-contract.ts; desktop/tests/shell-smoke.spec.ts</read_first>
<files>desktop/src/main/create-window.ts; desktop/src/preload/index.ts; desktop/src/shared/bridge-contract.ts; desktop/tests/shell-smoke.spec.ts</files>
<action>Run typecheck/smoke, add assertions for every security flag and exposed key, then remove any accidental Electron import from frontend sources. Confirm browser development still renders without `window.novelMindDesktop`.</action>
<acceptance_criteria>Desktop and browser modes pass; bridge exposes only four named capabilities and no renderer Node API.</acceptance_criteria>
<verify><automated>cd desktop; npm run typecheck; npx playwright test tests/shell-smoke.spec.ts; cd ../frontend; npm run typecheck</automated></verify>
<done>Shell boundary works in desktop and degrades safely in browser development.</done>
</task>
</tasks>

## Must-Haves

- Phase 41 GO and dependency approval are blocking.
- No parallel UI and no generic privileged bridge.

## Verification

Typecheck both packages and run shell/browser smoke with privilege-negative assertions.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| preload -> renderer | Privileged methods cross into web-controlled code |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-42-01-01 | Elevation of Privilege | preload bridge | mitigate | capability-specific methods, isolation and sandbox |
| T-42-01-02 | Information Disclosure | bootstrap capability | mitigate | no secrets/env/process details returned |
</threat_model>

