---
phase: 39-derivative-export-closeout
plan: "05"
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - agent-service/skills/prepare-export/SKILL.md
  - agent-service/skills/prepare-export/skill.yaml
  - agent-service/skills/prepare-export/input.schema.json
  - agent-service/skills/prepare-export/output.schema.json
  - agent-service/tests/skills/prepare-export.test.ts
  - backend/tests/integration/agent_runtime/test_phase_39_skill.py
  - backend/app/services/derivative_export/preparation.py
  - backend/app/services/derivative_export/materializer.py
  - backend/app/api/derivative_export.py
  - backend/tests/unit/derivative_export/test_preparation.py
  - backend/tests/integration/test_derivative_export_preparation.py
domain_tool_ids:
  read:
    - get_novel
    - get_chapter
    - search_novel_text
    - get_narrative_memory
  actions:
    - approve_export
    - materialize_export
autonomous: false
requirements: [REQ-FORK-05, REQ-AGENT-03, REQ-AGENT-04, REQ-AGENT-07]
must_haves:
  truths:
    - "Phase 39 is consumed through versioned prepare-export; Agent output is candidate-only until deterministic validation and any required Web Approval complete."
    - "Every run binds owner, novel, branch/fork, SkillVersion, SkillRun, ToolRuns, source/input hashes, model/runtime lineage and Artifact revision."
    - "Agent Service cannot write Original Canon, domain tables or export bundles directly; deterministic preparation and materialization services own the API-backed export boundary."
    - "Forged approval, wrong branch/fork, stale revision, cancellation, schema drift and forbidden Tool/action fail closed without an authoritative write."
  artifacts:
    - path: "agent-service/skills/prepare-export/skill.yaml"
      provides: "Versioned branch-aware Skill manifest and policy declarations."
    - path: "backend/tests/integration/agent_runtime/test_phase_39_skill.py"
      provides: "Candidate, validator, approval and deterministic-publisher boundary proof."
  key_links:
    - from: "SkillRun"
      to: "ExportPreparationArtifact"
      via: "immutable branch-aware Artifact lineage"
    - from: "ApprovalRequest"
      to: "backend/app/services/derivative_export/materializer.py"
      via: "server-authoritative approve_export action and preparation_hash"
    - from: "ExportPreparationArtifact(status=approved)"
      to: "backend/app/services/derivative_export/materializer.py"
      via: "only approved frozen manifest may materialize a bundle"
---

<objective>
Integrate Phase 39 with the Issue #29 Novel Agent Runtime through versioned prepare-export,
producing ExportPreparationArtifact while keeping validation, approval and publication deterministic.

Purpose: provide an executable Agent consumption slice without allowing Pi or the browser to
become a domain, Canon, branch or publication authority.
Output: Skill package, deterministic preparation/materialization services, API routes and
unit/integration/adversarial tests with explicit implementation ownership.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AGENT-RUNTIME-CONTRACT.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-PLAN.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-05-PLAN.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-02-PLAN.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-04-PLAN.md
@.planning/phases/39-derivative-export-closeout/39-CONTEXT.md
@.planning/phases/39-derivative-export-closeout/39-RESEARCH.md
@.planning/phases/39-derivative-export-closeout/39-PATTERNS.md
@.planning/phases/39-derivative-export-closeout/39-VALIDATION.md
</execution_context>

## Steps

<tasks>
<task type="auto">
  <name>Task 1: Define the branch-aware Skill and Artifact schemas</name>
  <read_first>.planning/AGENT-RUNTIME-CONTRACT.md; .planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-PLAN.md; .planning/phases/25.3-pi-package-compatibility-governance/25.3-02-PLAN.md</read_first>
  <files>agent-service/skills/prepare-export/SKILL.md; agent-service/skills/prepare-export/skill.yaml; agent-service/skills/prepare-export/input.schema.json; agent-service/skills/prepare-export/output.schema.json; agent-service/tests/skills/prepare-export.test.ts</files>
  <action>Define prepare-export with exact version, schemas, budget, cancellation, read/write permissions, forbidden spaces, approval actions and allowed tools: read approved frozen branch revisions, assets, citations and export policy; prepare manifest and preview only. Load only through the Phase 25.2 ResourceLoader allowlist and Phase 25.3 ToolRegistryManifest. Materialize ExportPreparationArtifact with artifact/revision IDs, SkillRun/ToolRun IDs, owner/novel/branch/fork, source snapshot, base revision, input/content hashes, evidence refs, runtime/model/generator lineage, validator report and approval/publish lineage where applicable. Unknown fields, tools, actions and missing branch scope fail closed.</action>
  <acceptance_criteria>The Skill registry accepts the pinned manifest and rejects ambient discovery, undeclared tools/actions, missing branch scope and incompatible schemas.</acceptance_criteria>
  <verify><automated>cd agent-service && npx vitest run tests/skills/prepare-export.test.ts</automated></verify>
  <done>The versioned Skill and branch-aware Artifact contracts are registry-valid and replayable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and prove deterministic preparation/materialization ownership</name>
  <read_first>.planning/AGENT-RUNTIME-CONTRACT.md; agent-service/skills/prepare-export/skill.yaml; backend/app/services/derivative_export/snapshot.py; backend/app/services/derivative_export/package.py; backend/app/api/derivative_export.py; .planning/phases/25.3-pi-package-compatibility-governance/25.3-04-PLAN.md</read_first>
  <files>backend/app/services/derivative_export/preparation.py; backend/app/services/derivative_export/materializer.py; backend/app/api/derivative_export.py; backend/tests/unit/derivative_export/test_preparation.py; backend/tests/integration/test_derivative_export_preparation.py; backend/tests/integration/agent_runtime/test_phase_39_skill.py; agent-service/tests/skills/prepare-export.test.ts</files>
  <action>Implementation ownership is explicit: 39-05 owns `preparation.py` and its deterministic `prepare_export` operation, which reads only approved/published derivative revisions, assets, citations and policy to freeze an ExportPreparationArtifact, manifest and preparation_hash; 39-05 owns `materializer.py` and its deterministic `materialize_export` operation, which accepts only an approved ExportPreparationArtifact and produces the bundle through the existing package/serializer services; 39-05 owns the authenticated preparation/approval/materialization routes in `backend/app/api/derivative_export.py`; 39-05 owns unit/integration coverage in `test_preparation.py`, `test_derivative_export_preparation.py` and the Agent boundary suite. Gate owner/project/fork/version parity, membership, stale hash, security and reproducibility. `approve_export` is server-authoritative and bound to the artifact revision plus preparation_hash; download is authenticated retrieval and never changes Artifact status. Assert wrong scope, stale hash, forged/expired approval, validator failure, timeout and cancellation produce blocked/cancelled/rejected outcomes with no bundle or Original-authority write.</action>
  <acceptance_criteria>Preparation, materialization, API and tests have one explicit 39-05 owner; only an approved, validated ExportPreparationArtifact reaches `materialize_export`, and the resulting bundle is reproducible from its frozen manifest.</acceptance_criteria>
  <verify><automated>cd backend && python -m pytest tests/unit/derivative_export/test_preparation.py tests/integration/test_derivative_export_preparation.py tests/integration/agent_runtime/test_phase_39_skill.py -q</automated></verify>
  <done>Deterministic preparation/materialization, approval-aware API routes and their tests enforce the ExportPreparationArtifact boundary server-side.</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>agent-service/tests/skills/prepare-export.test.ts; backend/tests/integration/agent_runtime/test_phase_39_skill.py; .planning/phases/39-derivative-export-closeout/39-VALIDATION.md</read_first>
  <files>agent-service/skills/prepare-export/SKILL.md; agent-service/skills/prepare-export/skill.yaml; agent-service/skills/prepare-export/input.schema.json; agent-service/skills/prepare-export/output.schema.json; agent-service/tests/skills/prepare-export.test.ts; backend/tests/integration/agent_runtime/test_phase_39_skill.py</files>
  <action>Run the Skill, preparation, materialization and API suites twice and fix failures. Replay success, cancellation, retry, wrong owner/branch/fork, stale preparation hash, unknown Tool/action, validator failure, approval forgery, pending/rejected artifact materialization and Original-authority mutation attempts. Confirm ExportPreparationArtifact is the official Agent output and download never alters Canon, Artifact status or approval lineage.</action>
  <acceptance_criteria>All targeted suites pass twice with stable lineage/status, approved-artifact-only materialization, reproducible manifest/bundle hashes and zero forbidden writes.</acceptance_criteria>
  <verify><automated>cd agent-service && npx vitest run tests/skills/prepare-export.test.ts && cd ../backend && python -m pytest tests/unit/derivative_export/test_preparation.py tests/integration/test_derivative_export_preparation.py tests/integration/agent_runtime/test_phase_39_skill.py -q</automated></verify>
  <done>Phase 39 Agent consumption, deterministic preparation/materialization ownership and approved-artifact-only export are verified.</done>
</task>
</tasks>

## Must-Haves

- Branch/fork scope and SkillRun/ToolRun/Artifact lineage are mandatory.
- Agent output is candidate-only; server-side Validator and ApprovalRequest fail closed.
- `backend/app/services/derivative_export/preparation.py`, `materializer.py`, `backend/app/api/derivative_export.py` and the three named test files are owned by 39-05 for this preparation/materialization slice.
- `materialize_export` accepts only `ExportPreparationArtifact(status=approved)` with matching `approve_export` ApprovalRequest and preparation_hash; download is read-only.

## Verification

- Run targeted Node and FastAPI integration tests.
- Cover cancellation, stale lineage, branch escape, forged approval and Original mutation.
- Confirm ExportPreparationArtifact is reproducible from its frozen inputs.

## Success Criteria

- prepare-export is the explicit Phase 39 Agent consumer.
- ExportPreparationArtifact carries complete branch-aware lineage and legal state transitions.
- No direct database, Original Canon, active-pointer, shell, filesystem or ambient-package path exists.
