---
phase: 43-managed-local-runtime-and-data-lifecycle
plan: "03"
type: execute
wave: 1
depends_on: ["43-01"]
files_modified:
  - desktop/src/data/app-data-layout.ts
  - desktop/src/data/version-state.ts
  - desktop/src/data/backup.ts
  - desktop/src/data/migration-runner.ts
  - desktop/tests/data/app-data-layout.test.ts
  - desktop/tests/data/migration-recovery.test.ts
  - docs/desktop-data-lifecycle.md
autonomous: true
requirements: [REQ-DESK-05, REQ-DESK-07]
must_haves:
  truths:
    - "D-43-05/D-43-06: all mutable state is versioned under appData and migration begins from recoverable evidence."
  artifacts:
    - path: desktop/src/data/migration-runner.ts
      provides: idempotent backup-first migration transaction
  key_links:
    - from: desktop/src/data/version-state.ts
      to: desktop/src/runtime/desktop-runtime.ts
      via: runtime cannot report ready until migration state is committed
---

<objective>建立 `%APPDATA%/NovelMind` 版本化布局、备份优先迁移和失败恢复契约。</objective>

<execution_context>@desktop/src/runtime/desktop-runtime.ts
@backend/alembic.ini
@backend/app/config.py
@.planning/phases/43-managed-local-runtime-and-data-lifecycle/43-CONTEXT.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Define immutable-resource and mutable app-data layout</name>
<read_first>desktop/src/runtime/types.ts; backend/app/config.py; docker-compose.yml; .planning/phases/43-managed-local-runtime-and-data-lifecycle/43-RESEARCH.md</read_first>
<files>desktop/src/data/app-data-layout.ts; desktop/src/data/version-state.ts; desktop/tests/data/app-data-layout.test.ts; docs/desktop-data-lifecycle.md</files>
<action>Resolve and create versioned `data`, `logs`, `backups`, `runtime`, `secrets` and migration metadata beneath Electron `app.getPath('userData')` corresponding to `%APPDATA%/NovelMind`. Normalize/contain every path, reject traversal/install-root writes, record layout/schema/runtime versions and document preserve/delete ownership.</action>
<acceptance_criteria>Tests prove all mutable paths stay within userData, resource paths are read-only inputs, repeated initialization is idempotent and traversal is rejected.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/data/app-data-layout.test.ts</automated></verify>
<done>Desktop data has one explicit, versioned and contained root.</done>
</task>
<task type="auto">
<name>Task 2: Implement backup-first migration and recovery</name>
<read_first>desktop/src/data/app-data-layout.ts; desktop/src/data/version-state.ts; backend/alembic.ini; backend/app/config.py</read_first>
<files>desktop/src/data/backup.ts; desktop/src/data/migration-runner.ts; desktop/tests/data/migration-recovery.test.ts; docs/desktop-data-lifecycle.md</files>
<action>Create a manifest/hash-backed backup before any compatible migration, run database/vector/app metadata steps in declared order, write committed version state atomically, and on failure retain old data/backup plus typed recovery instructions. Never mark runtime ready from partial migration.</action>
<acceptance_criteria>Success preserves data hashes and advances version once; injected failure preserves recoverable backup/old version and returns failed migration state; retry is idempotent.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/data/migration-recovery.test.ts</automated></verify>
<done>Upgrade cannot silently strand or discard user data.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/data/app-data-layout.test.ts; desktop/tests/data/migration-recovery.test.ts; desktop/src/data/migration-runner.ts</read_first>
<files>desktop/src/data/app-data-layout.ts; desktop/src/data/version-state.ts; desktop/src/data/backup.ts; desktop/src/data/migration-runner.ts; desktop/tests/data/app-data-layout.test.ts; desktop/tests/data/migration-recovery.test.ts; docs/desktop-data-lifecycle.md</files>
<action>Run layout/migration tests twice with read-only install root, denied appData write, corrupt backup and interrupted migration. Fix atomicity/recovery gaps and compare documented policy to behavior.</action>
<acceptance_criteria>Every fault yields typed failure with no ready state or data loss; docs match preserve/recovery behavior.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/data/app-data-layout.test.ts tests/data/migration-recovery.test.ts</automated></verify>
<done>Data lifecycle is deterministic, recoverable and documented.</done>
</task>
</tasks>

## Must-Haves

- No mutable writes to installation resources.
- Backup/recovery evidence precedes version advancement.

## Verification

Run path-containment and migration fault/recovery suites.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| installer resources -> user data migration | Paths and old data may be malformed or attacker-controlled |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-43-03-01 | Tampering | migration state | mitigate | hashes, atomic commit and backup manifest |
| T-43-03-02 | Elevation of Privilege | paths | mitigate | canonical containment and no install-root writes |
| T-43-03-03 | Denial of Service | backup growth | mitigate | bounded retention and explicit insufficient-space failure |
</threat_model>

