---
phase: 43-managed-local-runtime-and-data-lifecycle
plan: "02"
type: execute
wave: 1
depends_on: ["43-01"]
files_modified:
  - desktop/src/runtime/process-graph.ts
  - desktop/src/runtime/port-allocator.ts
  - desktop/src/runtime/readiness.ts
  - desktop/src/runtime/process-owner.ts
  - desktop/src/runtime/logging.ts
  - desktop/tests/runtime/process-graph.test.ts
  - desktop/tests/runtime/process-tree.windows.test.ts
autonomous: true
requirements: [REQ-DESK-03, REQ-DESK-07]
must_haves:
  truths:
    - "D-43-03/D-43-07/D-43-08: readiness is dependency-aware and shutdown owns the complete Windows process tree."
  artifacts:
    - path: desktop/src/runtime/process-graph.ts
      provides: five-component dependency graph supervisor
  key_links:
    - from: desktop/src/runtime/process-graph.ts
      to: desktop/src/runtime/readiness.ts
      via: component-specific readiness before dependent start
---

<objective>实现五组件进程图、动态端口、真实 readiness、日志与 Windows 进程树所有权。</objective>

<execution_context>@desktop/src/runtime/desktop-runtime.ts
@desktop/src/runtime/packaged-process-adapter.ts
@desktop/proof/runtime-manifest.json
@backend/app/main.py
@agent-service/package.json</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Build dependency-aware startup and readiness</name>
<read_first>desktop/src/runtime/types.ts; desktop/src/runtime/packaged-process-adapter.ts; desktop/proof/runtime-manifest.json; backend/app/main.py</read_first>
<files>desktop/src/runtime/process-graph.ts; desktop/src/runtime/port-allocator.ts; desktop/src/runtime/readiness.ts; desktop/tests/runtime/process-graph.test.ts</files>
<action>Start persistence/vector before backend migration/readiness, Agent Service after backend, and Next after bootstrap inputs exist. Reserve loopback ports without fixed assumptions, use component-specific health/protocol checks rather than sleeps, enforce bounded deadlines/retries, and never emit global ready while a mandatory component is degraded/failed.</action>
<acceptance_criteria>Tests prove exact dependency order, dynamic non-conflicting endpoints, readiness timeouts and no ready state after a failed mandatory dependency.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/process-graph.test.ts</automated></verify>
<done>The managed graph reaches ready only from verified component readiness.</done>
</task>
<task type="auto">
<name>Task 2: Own logs and clean Windows process-tree shutdown</name>
<read_first>desktop/src/runtime/process-graph.ts; desktop/src/runtime/packaged-process-adapter.ts; scripts/keep-alive.ps1</read_first>
<files>desktop/src/runtime/process-owner.ts; desktop/src/runtime/logging.ts; desktop/tests/runtime/process-tree.windows.test.ts</files>
<action>Track only processes spawned for the current runtime instance, pipe/redact/rotate stdout-stderr, support targeted restart, drain then terminate descendants within bounded timeout, and never kill unrelated matching-name processes. Hide child console windows in packaged mode.</action>
<acceptance_criteria>Windows test fixture leaves zero owned descendants, preserves unrelated sentinel processes, records bounded redacted logs and opens no child console window.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/process-tree.windows.test.ts</automated></verify>
<done>Process-tree lifecycle and diagnostics are owned without collateral termination.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/runtime/process-graph.test.ts; desktop/tests/runtime/process-tree.windows.test.ts; desktop/src/runtime/logging.ts</read_first>
<files>desktop/src/runtime/process-graph.ts; desktop/src/runtime/port-allocator.ts; desktop/src/runtime/readiness.ts; desktop/src/runtime/process-owner.ts; desktop/src/runtime/logging.ts; desktop/tests/runtime/process-graph.test.ts; desktop/tests/runtime/process-tree.windows.test.ts</files>
<action>Run graph/tree suites twice with occupied port, hung readiness, crash during startup and shutdown timeout. Fix race/leak/redaction issues and confirm unrelated user processes survive.</action>
<acceptance_criteria>All fault cases terminate in typed state within deadline, no owned orphan remains and logs contain no token/provider key.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/process-graph.test.ts tests/runtime/process-tree.windows.test.ts</automated></verify>
<done>Runtime orchestration is bounded, observable and leak-free.</done>
</task>
</tasks>

## Must-Haves

- Health semantics are stronger than open-port checks.
- Shutdown never kills unrelated processes.

## Verification

Run process graph and Windows tree fault-injection suites.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| runtime supervisor -> Windows process tree | Incorrect ownership can kill unrelated processes or leak secrets |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-43-02-01 | Denial of Service | process owner | mitigate | instance-bound ownership and bounded shutdown |
| T-43-02-02 | Information Disclosure | logs | mitigate | structured redaction and rotation |
| T-43-02-03 | Spoofing | readiness | mitigate | component-specific authenticated/protocol probes |
</threat_model>

