---
phase: 43-managed-local-runtime-and-data-lifecycle
plan: "01"
type: execute
wave: 0
depends_on: ["42-02"]
files_modified:
  - desktop/src/runtime/types.ts
  - desktop/src/runtime/desktop-runtime.ts
  - desktop/src/runtime/packaged-process-adapter.ts
  - desktop/src/runtime/development-process-adapter.ts
  - desktop/tests/runtime/adapter-contract.test.ts
autonomous: true
requirements: [REQ-DESK-03, REQ-DESK-07]
must_haves:
  truths:
    - "D-43-01/D-43-02/D-43-04: DesktopRuntime exposes only ensureReady/status/restart/shutdown and never becomes domain authority."
  artifacts:
    - path: desktop/src/runtime/desktop-runtime.ts
      provides: deep lifecycle module contract
    - path: desktop/tests/runtime/adapter-contract.test.ts
      provides: shared adapter contract suite
  key_links:
    - from: desktop/src/main/index.ts
      to: desktop/src/runtime/desktop-runtime.ts
      via: one runtime interface
---

<objective>定义稳定的 DesktopRuntime 深模块和开发/打包双适配器契约。</objective>

<execution_context>@desktop/src/main/index.ts
@desktop/proof/src/topology.ts
@.planning/phases/43-managed-local-runtime-and-data-lifecycle/43-CONTEXT.md
@.planning/phases/43-managed-local-runtime-and-data-lifecycle/43-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Define lifecycle states and the deep runtime interface</name>
<read_first>desktop/src/main/index.ts; desktop/proof/src/topology.ts; .planning/phases/43-managed-local-runtime-and-data-lifecycle/43-RESEARCH.md</read_first>
<files>desktop/src/runtime/types.ts; desktop/src/runtime/desktop-runtime.ts</files>
<action>Define component IDs and runtime states `stopped|starting|migrating|ready|degraded|failed|stopping`, redacted error codes, readiness snapshots and `ensureReady/status/restart/shutdown` methods. Make illegal transitions and ready-with-failed-dependency unrepresentable or explicitly rejected. Exclude novel/domain/database operations.</action>
<acceptance_criteria>Public exports contain exactly the four lifecycle methods; snapshots carry component state/readiness/endpoints without secrets; transition tests enumerate all terminals.</acceptance_criteria>
<verify><automated>cd desktop; npm run typecheck; npm test -- --run tests/runtime/adapter-contract.test.ts</automated></verify>
<done>The runtime has a small, typed and authority-safe public surface.</done>
</task>
<task type="auto">
<name>Task 2: Implement contract-equivalent packaged and development adapters</name>
<read_first>desktop/src/runtime/types.ts; Makefile; docker-compose.yml; desktop/proof/src/topology.ts</read_first>
<files>desktop/src/runtime/packaged-process-adapter.ts; desktop/src/runtime/development-process-adapter.ts; desktop/tests/runtime/adapter-contract.test.ts</files>
<action>Implement the same spawn/observe/restart/shutdown adapter contract. Development mode may use existing developer entrypoints; packaged mode uses only Phase 41 approved bundled paths. Inject process operations for tests, keep PID/executable internals private, and return stable errors for missing executable/spawn/exit/timeout.</action>
<acceptance_criteria>Both adapters pass one contract suite and differ only in process source/configuration; packaged adapter never falls back to PATH or Docker.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/adapter-contract.test.ts</automated></verify>
<done>Shell/runtime callers are independent of packaged versus development process details.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/src/runtime/desktop-runtime.ts; desktop/src/runtime/packaged-process-adapter.ts; desktop/src/runtime/development-process-adapter.ts; desktop/tests/runtime/adapter-contract.test.ts</read_first>
<files>desktop/src/runtime/types.ts; desktop/src/runtime/desktop-runtime.ts; desktop/src/runtime/packaged-process-adapter.ts; desktop/src/runtime/development-process-adapter.ts; desktop/tests/runtime/adapter-contract.test.ts</files>
<action>Run typecheck/contract tests twice, inject spawn error, early exit and repeated shutdown, then fix state or cleanup divergence. Static-scan the runtime public API for domain imports.</action>
<acceptance_criteria>Both adapters behave identically for success/failure/idempotent shutdown and no backend domain module is imported by desktop runtime.</acceptance_criteria>
<verify><automated>cd desktop; npm run typecheck; npm test -- --run tests/runtime/adapter-contract.test.ts; if (rg "backend/app/(models|services|api)|NovelService|ChapterService" src/runtime) { exit 1 }</automated></verify>
<done>DesktopRuntime is deep, deterministic and domain-neutral.</done>
</task>
</tasks>

## Must-Haves

- Four-method interface and typed state machine.
- Packaged adapter has no PATH/Docker fallback.

## Verification

Run shared adapter contract and static authority-boundary scan.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| main process -> runtime adapters | Lifecycle requests can spawn or kill OS processes |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-43-01-01 | Elevation of Privilege | packaged adapter | mitigate | approved absolute executables and bounded arguments |
| T-43-01-02 | Denial of Service | restart API | mitigate | typed targets, budgets and idempotency |
</threat_model>

