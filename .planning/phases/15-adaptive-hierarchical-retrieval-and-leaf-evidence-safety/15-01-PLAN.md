---
phase: 15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety
plan: 01
type: execute
wave: 1
depends_on: []
execution_gate: "Phase 14 complete and independently verified; explicit implementation authorization required"
files_modified:
  - backend/app/services/narrative_memory/retrieval_contracts.py
  - backend/app/services/narrative_memory/routing.py
  - backend/app/services/narrative_memory/candidate_reader.py
  - backend/tests/unit/narrative_memory/test_retrieval_routing.py
  - backend/tests/integration/narrative_memory/test_retrieval_candidates_pg.py
requirements: [V08-RETR-01, V08-RETR-04, V08-RETR-05]
autonomous: false
must_haves:
  truths:
    - "The same normalized question, selection, persisted cutoff, explicit candidate version and policy always produce the same local/arc/global/mixed route and closed reason codes without a provider call."
    - "Owner, novel, version, source snapshot, hierarchy build and spoiler cutoff are applied before candidate materialization, ranking, counting, status derivation or cache identity."
    - "Adding cross-scope or future nodes, claims, titles, scores and statuses cannot change any visible route, candidate, count, score, trace field or cache result."
  artifacts:
    - path: "backend/app/services/narrative_memory/retrieval_contracts.py"
      provides: "Frozen strict scope, question, route, visible-candidate and safe-trace contracts"
    - path: "backend/app/services/narrative_memory/routing.py"
      provides: "Versioned deterministic local/arc/global/mixed routing policy"
    - path: "backend/app/services/narrative_memory/candidate_reader.py"
      provides: "Explicit-version cutoff-first PostgreSQL visible-set loaders and scope-complete cache envelopes"
  key_links:
    - from: "backend/app/services/narrative_memory/candidate_reader.py"
      to: "backend/app/models/narrative_memory.py"
      via: "lossless owner/novel/version/seal/report scoped reads"
      pattern: "owner_id.*novel_id.*version_id"
    - from: "RetrievalScope"
      to: "visible candidate queries and cache identity"
      via: "source/hierarchy/cutoff/policy hashes carried into every operation"
      pattern: "cutoff.*manifest.*policy"
---

<objective>Create a deterministic route decision and visible candidate boundary whose behavior is completely independent of future or foreign data.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety/15-CONTEXT.md
@.planning/phases/15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety/15-RESEARCH.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-04-SUMMARY.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-03-SUMMARY.md
@backend/app/models/narrative_memory.py
@backend/app/services/narrative_memory/contracts.py
@backend/app/services/reader_chat/context.py
</context>

## Steps

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Authorize Phase 15 implementation after Phase 14 is complete and independently verified</action>
  <instructions>Planning-only lock is active. Before any file change or command, confirm Phase 14 has all summaries plus `14-VERIFICATION.md` with `status: passed`, then obtain the user's explicit instruction `批准执行 Phase 15`. Auto mode must not bypass this gate.</instructions>
  <verification>Phase 14 verification is passed and the current conversation contains explicit Phase 15 execution authorization.</verification>
  <resume-signal>批准执行 Phase 15</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 1: Freeze retrieval scope, route and safe trace contracts</name>
  <files>backend/app/services/narrative_memory/retrieval_contracts.py, backend/tests/unit/narrative_memory/test_retrieval_routing.py</files>
  <action>Define strict frozen DTOs for RetrievalScope, RetrievalQuestion, RouteDecision, VisibleCandidate, TraversalStep shell, safe source/fallback status and cache envelope. Require explicit owner_id, novel_id and candidate version_id; frozen candidate manifest/source snapshot/hierarchy build+checksum; persisted reading cutoff snapshot+hash; policy version+hash; and bounded budgets. Use closed enums/reason codes and forbid free-text rationale, hidden counts, raw cache keys, candidate summaries/titles and untyped metadata in public traces. Add canonical query normalization/hashing that preserves Chinese Unicode semantics and never logs raw question text. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_retrieval_routing.py -q -x</automated></verify>
  <done>Only complete explicit-version scopes and non-leaking route/trace shapes validate, and their hashes are deterministic.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the deterministic local/arc/global/mixed router</name>
  <files>backend/app/services/narrative_memory/routing.py, backend/tests/unit/narrative_memory/test_retrieval_routing.py</files>
  <action>Implement a versioned policy using only normalized question text, explicit chapter/selection anchors, persisted cutoff authorization and fixed pattern precedence. Cover local entity/fact/selection intent, arc/cross-chapter/causal intent, global/whole-book intent and mixed/ambiguous/multi-signal fallback. Full-book/global wording without full-book authorization must not widen scope; return a safe visible route. Emit ordered start levels plus closed reason codes with stable tie-breaks. Prove the router never receives candidate rows, summaries, node counts, embeddings or provider access and that route decisions actually select different start-level loader calls. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_retrieval_routing.py -q -x</automated></verify>
  <done>A frozen route matrix covers all four modes, ambiguous/no-answer inputs and unauthorized global requests with byte-stable decisions.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Load visible candidates with cutoff-first SQL and isolated cache identity</name>
  <files>backend/app/services/narrative_memory/candidate_reader.py, backend/tests/integration/narrative_memory/test_retrieval_candidates_pg.py</files>
  <action>Implement read-only PostgreSQL loaders for chapter_state, story_arc/volume, global_story, edges, claims and source links. Require an explicit Phase 13 version whose sealed manifest and structural report match and whose Phase 14 run/stage result is complete; never resolve current/active state. Apply owner/novel/version/build/snapshot and claim/node cutoff predicates in SQL before materialization, ranking, counts and source status: claims require visible_from_chapter at/before cutoff, upper nodes require their complete chapter_end at/before cutoff, and Global requires persisted authorization covering its complete range. Never partially expose an arc/global node crossing the cutoff. Build a cache key/envelope including every scope, manifest, cutoff, route/query policy and budget hash; revalidate cache hits and expose no raw key. Rank visible sets deterministically with stable IDs only after admission. Seed foreign/future rows and prove they cannot affect visible results, counts, scores, statuses or cache hits. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_retrieval_routing.py tests/integration/narrative_memory/test_retrieval_candidates_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/retrieval_contracts.py app/services/narrative_memory/routing.py app/services/narrative_memory/candidate_reader.py tests/unit/narrative_memory/test_retrieval_routing.py tests/integration/narrative_memory/test_retrieval_candidates_pg.py</automated></verify>
  <done>Only a structurally complete explicit candidate contributes visible candidates, and all observable metadata is derived after scope/cutoff filtering.</done>
</task>
</tasks>

## Must-Haves

- `local|arc|global|mixed` is a closed, deterministic, policy-versioned decision with no model/candidate-data dependency.
- Query scope always contains owner, novel, explicit version, manifest, source/hierarchy lineage, persisted cutoff and policy hashes.
- SQL visibility gates run before ranking, counts, scores, statuses, trace or cache construction.
- Cache identity and hit validation include tenant, novel, version, snapshot, manifest, cutoff, query/route policy and budgets.
- Future/foreign data produces zero observable difference; no “hidden future exists” reason or count is emitted.
- Candidate loading never resolves an active pointer or mutates Phase 13/14 authority.

## Verification

<verification>Run the pure route/contract matrix, PostgreSQL visible-set/cache isolation suite and Ruff with `backend/.venv`; inspect SQL tests to confirm filters precede materialization rather than post-redaction.</verification>
<success_criteria>
- Four route modes and stable reason codes are covered by fixed fixtures.
- Distinct routes call distinct candidate loaders/start levels.
- Future and cross-scope adversarial rows leave serialized visible output byte-identical.
- Incomplete/unsealed/failed Phase 14 candidates and mismatched manifests fail closed before retrieval.
</success_criteria>

<output>Create `15-01-SUMMARY.md` with the route policy matrix, visible-set SQL predicates, cache identity fields and adversarial non-interference evidence.</output>
