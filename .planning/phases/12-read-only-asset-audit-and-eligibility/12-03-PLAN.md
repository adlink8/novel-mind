---
phase: 12-read-only-asset-audit-and-eligibility
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - backend/app/api/asset_audit.py
  - backend/app/main.py
  - backend/scripts/run_asset_audit.py
  - backend/tests/integration/narrative_memory/test_audit_entrypoints.py
  - backend/tests/integration/narrative_memory/test_audit_no_side_effects.py
requirements: [V08-AUDIT-01, V08-AUDIT-03, V08-AUDIT-04]
autonomous: true
must_haves:
  truths:
    - "An authorized operator can reproduce the same scoped report through fixed API/CLI entry points."
    - "Audit leaves provider calls, domain rows, active pointers, revisions, and journals unchanged."
    - "A blocked required hierarchy cannot cross the explicit pre-provider guard."
  artifacts:
    - path: "backend/scripts/run_asset_audit.py"
      provides: "Fixed operator command and stable JSON report"
    - path: "backend/tests/integration/narrative_memory/test_audit_no_side_effects.py"
      provides: "Fresh observer before/after proof"
  key_links:
    - from: "backend/app/api/asset_audit.py"
      to: "backend/app/services/narrative_memory/audit_pg.py"
      via: "authenticated owner-scoped dependency"
      pattern: "current_user"
---

<objective>Expose the audit to operators and prove with an independent observer that running it has zero model, repair, or pointer side effects.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-CONTEXT.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-RESEARCH.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-02-SUMMARY.md
@backend/app/main.py
@backend/scripts/run_clue_qualification.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add authenticated read-only audit API</name>
  <files>backend/app/api/asset_audit.py, backend/app/main.py, backend/tests/integration/narrative_memory/test_audit_entrypoints.py</files>
  <action>Add an admin/operator read endpoint following existing authentication and 404 owner-isolation patterns. Return the strict report envelope only; expose no repair/start/promote action. Test, Fix, and Confirm success, unauthenticated, non-admin, cross-owner, and blocked reports.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_entrypoints.py -q -x</automated></verify>
  <done>The API is read-only, owner-scoped, stable, and cannot trigger a build.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add fixed CLI report command</name>
  <files>backend/scripts/run_asset_audit.py, backend/tests/integration/narrative_memory/test_audit_entrypoints.py</files>
  <action>Create a non-interactive CLI accepting owner/novel identity and emitting canonical JSON plus a meaningful success/blocked exit code. Reuse the same service as the API and do not add automatic repair or provider options. Test, Fix, and Confirm deterministic output and error handling.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_entrypoints.py -q -x</automated></verify>
  <done>Repeated runs over unchanged data emit identical report payloads.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove zero provider/data/pointer side effects</name>
  <files>backend/tests/integration/narrative_memory/test_audit_no_side_effects.py</files>
  <action>Use a fresh PostgreSQL observer to snapshot relevant row counts/checksums, Chunk/Timeline/Clue pointers and revisions/journals before and after API and CLI audits. Inject a provider spy that must remain at zero and add a forbidden-capability scan over the audit package. Test, Fix, and Confirm both reusable and malformed/blocked cases.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_entrypoints.py tests/integration/narrative_memory/test_audit_no_side_effects.py -q -x</automated></verify>
  <done>Independent before/after evidence proves audit does not call, repair, dispatch, promote, or mutate pointers.</done>
</task>
</tasks>

## Must-Haves
- Operator entry points are authenticated, owner-scoped, and read-only.
- Blocked hierarchy reports stop before provider calls.
- Before/after authority and pointer snapshots are identical.

## Verification
<verification>Run unit + PostgreSQL audit suites, API/CLI tests, capability scan, and targeted Phase 07/08/09/11 regressions.</verification>
<success_criteria>
- Both entry points emit the same canonical report.
- Provider spy count is zero in all cases.
- Database/pointer observer reports no mutation.
</success_criteria>

<output>Create `12-03-SUMMARY.md` and Phase 12 verification evidence when done.</output>
