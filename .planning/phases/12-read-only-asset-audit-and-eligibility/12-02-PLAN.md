---
phase: 12-read-only-asset-audit-and-eligibility
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - backend/app/services/narrative_memory/audit_pg.py
  - backend/app/services/narrative_memory/audit.py
  - backend/tests/integration/narrative_memory/test_audit_pg.py
requirements: [V08-AUDIT-01, V08-AUDIT-02, V08-AUDIT-03]
autonomous: true
must_haves:
  truths:
    - "The active Phase 07 hierarchy is inventoried losslessly and exact reuse is never inferred from a silently repaired tree."
    - "Owner, novel, build, snapshot, manifest, tree, offsets, hashes, and coverage are proved before reusable_exact."
    - "Timeline, relationship, and clue availability follows each domain's real version authority and remains optional."
  artifacts:
    - path: "backend/app/services/narrative_memory/audit_pg.py"
      provides: "SELECT-only PostgreSQL adapters and exact hierarchy checks"
      contains: "PostgresAuditSource"
    - path: "backend/tests/integration/narrative_memory/test_audit_pg.py"
      provides: "Malformed hierarchy and optional-source integration proof"
  key_links:
    - from: "backend/app/services/narrative_memory/audit_pg.py"
      to: "backend/app/models/chunk_build.py"
      via: "owner-scoped Novel join then active build/node SELECTs"
      pattern: "ChunkActivePointer"
---

<objective>Connect the pure eligibility engine to current PostgreSQL authorities without introducing writes or hiding malformed persisted state.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-CONTEXT.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-RESEARCH.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-01-SUMMARY.md
@backend/app/models/chunk_build.py
@backend/app/services/chunking/pg_store.py
@backend/app/models/timeline.py
@backend/app/models/relationship.py
@backend/app/models/clue.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement lossless owner-scoped hierarchy inventory</name>
  <files>backend/app/services/narrative_memory/audit_pg.py, backend/tests/integration/narrative_memory/test_audit_pg.py</files>
  <action>Load the target Novel under owner scope, then SELECT active ChunkBuild and every persisted node without using the tree loader's skip behavior. Record missing, duplicate, foreign-scope, level, parent/child, ordering, and chapter coverage anomalies. Do not call any persistence helper. Test, Fix, and Confirm with valid, missing-pointer, cross-owner, orphan, duplicate, and missing-chapter fixtures.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_pg.py -q -x</automated></verify>
  <done>Malformed rows remain visible and no cross-owner build identity is disclosed or accepted.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove snapshot, manifest, offsets, content hashes, and rebuild scope</name>
  <files>backend/app/services/narrative_memory/audit_pg.py, backend/app/services/narrative_memory/audit.py, backend/tests/integration/narrative_memory/test_audit_pg.py</files>
  <action>Re-slice authoritative Chapter content using Unicode code-point offsets, recompute content/tree/manifest inputs using existing stable-hash conventions, and compare the active build identity. Emit deterministic chapter/range rebuild hints only where proof is possible; otherwise block. Test, Fix, and Confirm each mismatch independently and verify valid fixtures reach reusable_exact.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_pg.py -q -x</automated></verify>
  <done>Exact reuse means every required invariant was recomputed rather than trusted from stored labels.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Adapt optional timeline, relationship, and clue authorities</name>
  <files>backend/app/services/narrative_memory/audit_pg.py, backend/tests/integration/narrative_memory/test_audit_pg.py</files>
  <action>Add SELECT-only availability/lineage adapters using each domain's actual pointer/run/version model. Preserve unavailable versus healthy empty; never fabricate a shared active pointer. Test, Fix, and Confirm missing, stale, inaccessible, empty, and compatible cases.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_audit_pg.py tests/integration/clues/test_source_protocols.py -q -x</automated></verify>
  <done>Optional sources enrich the report without becoming required or invented facts.</done>
</task>
</tasks>

## Must-Haves
- All PostgreSQL operations are SELECT-only.
- Exact hierarchy reuse is independently recomputed.
- Optional authorities are modeled honestly and remain non-blocking.

## Verification
<verification>Run the new PostgreSQL suite plus targeted Phase 07 hierarchy and Phase 11 source-protocol regressions.</verification>
<success_criteria>
- Valid hierarchy returns reusable_exact; every corrupted invariant fails closed with stable reasons.
- Owner isolation and unavailable-vs-empty cases pass.
- No migration or model change exists.
</success_criteria>

<output>Create `12-02-SUMMARY.md` when done.</output>
