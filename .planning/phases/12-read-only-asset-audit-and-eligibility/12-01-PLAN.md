---
phase: 12-read-only-asset-audit-and-eligibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/narrative_memory/__init__.py
  - backend/app/services/narrative_memory/audit_contracts.py
  - backend/app/services/narrative_memory/audit_sources.py
  - backend/app/services/narrative_memory/audit.py
  - backend/tests/unit/narrative_memory/__init__.py
  - backend/tests/unit/narrative_memory/test_audit_contracts.py
  - backend/tests/unit/narrative_memory/test_audit.py
autonomous: true
requirements:
  - V08-AUDIT-01
  - V08-AUDIT-02
  - V08-AUDIT-03
  - V08-AUDIT-04
must_haves:
  truths:
    - "Every audited asset receives exactly one closed eligibility status and stable reason codes."
    - "A required hierarchy permits later provider calls only when it is reusable_exact; missing or unknown required state fails closed."
    - "Optional unavailable sources remain distinguishable from healthy empty sources and never become invented facts."
    - "The first slice has no provider, ORM writer, repair, promotion, worker dispatch, or active-pointer capability."
  artifacts:
    - path: "backend/app/services/narrative_memory/audit_contracts.py"
      provides: "Strict status, reason, inventory, result and report contracts"
      contains: "class EligibilityStatus"
    - path: "backend/app/services/narrative_memory/audit.py"
      provides: "Pure deterministic evaluator and provider-call guard"
      contains: "provider_calls_allowed"
    - path: "backend/app/services/narrative_memory/audit_sources.py"
      provides: "Read-only source protocol and in-memory adapter"
      contains: "Protocol"
  key_links:
    - from: "backend/app/services/narrative_memory/audit.py"
      to: "backend/app/services/narrative_memory/audit_contracts.py"
      via: "closed status precedence and canonical report construction"
      pattern: "EligibilityStatus"
    - from: "backend/app/services/narrative_memory/audit.py"
      to: "future Phase 14 provider boundary"
      via: "fail-closed report predicate with no provider import"
      pattern: "provider_calls_allowed"
---

<objective>
Freeze the read-only eligibility language and pure decision engine before connecting any database or operator entry point.

Purpose: Make “audit before provider call” an executable invariant rather than documentation, while preserving unavailable optional sources as an explicit state.
Output: A dependency-free narrative-memory audit package with strict contracts, read-only protocols, an in-memory adapter, deterministic evaluator, and focused unit tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-CONTEXT.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-RESEARCH.md
@.planning/REQUIREMENTS.md
@backend/app/services/chunking/schemas.py
@backend/app/services/clues/sources.py

<interfaces>
The public vocabulary is locked to four statuses: reusable_exact, rebuild_required, blocked, optional_unavailable. Hierarchy is required; timeline, relationship, and clue are optional. This plan must not import SQLAlchemy models or any provider/promotion module.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict canonical audit contracts</name>
  <files>backend/app/services/narrative_memory/audit_contracts.py, backend/tests/unit/narrative_memory/test_audit_contracts.py</files>
  <behavior>
    - Only the four approved statuses, required/optional requirements, and known asset kinds validate.
    - Reports reject extra fields, duplicate asset kinds, invalid owner/novel identities, unordered/duplicate reason codes, and malformed rebuild ranges.
    - Serialization is stable for the same logical inventory regardless of input ordering.
  </behavior>
  <action>Create strict Pydantic enums/models for asset kind, requirement, eligibility status, closed reason codes, inventory identity, rebuild scope, per-asset eligibility, and complete report. Make reason/result ordering canonical and expose a schema/policy version. Do not add summary text as an authoritative field. Test, Fix, and Confirm by running the focused contract tests until strictness and stable serialization pass.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_audit_contracts.py -q -x</automated></verify>
  <done>The contract represents every approved state without free-form authority and produces deterministic serialized reports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only source protocol and in-memory inventory adapter</name>
  <files>backend/app/services/narrative_memory/audit_sources.py, backend/tests/unit/narrative_memory/test_audit.py</files>
  <behavior>
    - A source can only return observations/inventory; it exposes no save, repair, promote, dispatch, or pointer mutation method.
    - Missing optional assets remain unavailable, while a present asset with zero domain rows remains a distinct healthy observation.
    - Cross-owner/novel inventory is rejected before evaluation.
  </behavior>
  <action>Define a minimal async read protocol and immutable in-memory implementation for tests. Preserve lossless observations and anomaly reason codes rather than normalizing them. Keep the protocol independent of ORM classes so Plan 12-02 can adapt PostgreSQL without widening the evaluator. Test, Fix, and Confirm with protocol/in-memory cases in the focused evaluator suite.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_audit.py -q -x</automated></verify>
  <done>Tests can supply scoped required and optional inventory without any mutation-capable interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic eligibility policy and fail-closed guard</name>
  <files>backend/app/services/narrative_memory/audit.py, backend/app/services/narrative_memory/__init__.py, backend/tests/unit/narrative_memory/test_audit.py</files>
  <behavior>
    - Required missing/scope-invalid hierarchy is blocked; deterministic stale anomalies become rebuild_required; fully proved hierarchy becomes reusable_exact.
    - Optional missing/unavailable/incompatible assets become optional_unavailable and do not masquerade as empty facts.
    - provider_calls_allowed is true only when all required results are reusable_exact; unknown, missing, duplicate, or malformed results are false.
    - Output ordering and rebuild hints are deterministic.
  </behavior>
  <action>Implement the pure audit orchestration and explicit status-precedence table. Derive the provider guard from the validated complete report, never from caller flags. Export only safe contracts/evaluator functions. Add a capability test/static scan that the package contains no gateway/provider, SQLAlchemy write, promotion, pointer setter, repair, or worker dispatch imports. Test, Fix, and Confirm by running the complete new unit directory.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory -q -x</automated></verify>
  <done>All status/guard cases pass and the package is structurally incapable of calls or writes.</done>
</task>

</tasks>

## Must-Haves

- Status vocabulary and precedence are closed and deterministic.
- Required hierarchy fails closed before any future provider boundary.
- Optional unavailable is never treated as healthy empty data.
- No database/provider/pointer mutation capability enters the first slice.

## Verification

<verification>
Run the new unit suite, import the package from a clean interpreter, scan its imports for forbidden capabilities, then run the existing clue source-protocol unit tests as the unavailable-vs-empty regression check.
</verification>

<success_criteria>
- New narrative-memory unit tests pass with no skips.
- Identical logical input produces byte-identical model dumps.
- Every malformed or incomplete required report keeps provider_calls_allowed false.
- No existing service/model file is modified.
</success_criteria>

<output>
Create `.planning/phases/12-read-only-asset-audit-and-eligibility/12-01-SUMMARY.md` when done.
</output>
