---
phase: 16-dependency-aware-local-rebuild-and-carry-forward
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
execution_gate: "Explicit implementation authorization required"
files_modified:
  - backend/app/services/narrative_memory/carry_forward.py
  - backend/app/services/narrative_memory/rebuild_executor.py
  - backend/app/services/narrative_memory/builder_repository.py
  - backend/app/services/narrative_memory/builder_worker.py
  - backend/tests/unit/narrative_memory/test_carry_forward.py
  - backend/tests/integration/narrative_memory/test_carry_forward_pg.py
  - backend/tests/integration/narrative_memory/test_local_rebuild_pg.py
requirements: [V08-REUSE-01, V08-REUSE-02, V08-REUSE-03]
autonomous: false
must_haves:
  truths:
    - "Only assets marked carried by a fresh immutable plan and proven against the target hierarchy are copied; semantic node/claim checksums remain identical while target scope/link/manifest checksums are recomputed."
    - "Carry-forward rebuild items create no Phase 14 stage, provider attempt, reservation, embedding or duplicate index write; only frozen dirty items may enter the existing Phase 14 worker contract."
    - "A stale/ambiguous source link, changed boundary or incompatible dependency converts reuse to blocked/dirty before copy, and no parent candidate row is updated or deleted."
  artifacts:
    - path: "backend/app/services/narrative_memory/carry_forward.py"
      provides: "Exact semantic copy with target Phase 07 leaf rebind, conflict rejection and idempotent audit"
    - path: "backend/app/services/narrative_memory/rebuild_executor.py"
      provides: "Frozen dirty-only Phase 14 stage mask plus independent carry-forward dependency resolution without a new stage status or gateway"
    - path: "backend/tests/integration/narrative_memory/test_local_rebuild_pg.py"
      provides: "PostgreSQL proof of zero-call carry, dirty-only execution, conservative suffix behavior and target manifest closure"
  key_links:
    - from: "backend/app/services/narrative_memory/carry_forward.py"
      to: "backend/app/services/narrative_memory/authority.py"
      via: "revalidate strict DTOs and insert into the explicit unsealed target version only"
      pattern: "target_version_id"
    - from: "backend/app/services/narrative_memory/rebuild_executor.py"
      to: "backend/app/services/narrative_memory/builder_worker.py"
      via: "frozen plan checksum and dirty stage keys; provider authorization remains Phase 14-owned"
      pattern: "rebuild_plan_checksum.*stage_key"
---

<objective>Carry forward only proven-clean semantic authority into a new explicit candidate and execute only the frozen dirty stages through the existing durable Phase 14 worker.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-CONTEXT.md
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-RESEARCH.md
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-01-SUMMARY.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-03-SUMMARY.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-04-SUMMARY.md
@backend/app/services/narrative_memory/authority.py
@backend/app/services/narrative_memory/provenance.py
@backend/app/services/narrative_memory/builder_worker.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Revalidate and copy checksum-identical semantic authority</name>
  <files>backend/app/services/narrative_memory/carry_forward.py, backend/tests/unit/narrative_memory/test_carry_forward.py, backend/tests/integration/narrative_memory/test_carry_forward_pg.py</files>
  <action>Load a fresh immutable plan plus complete parent rows for each `carried` asset and lock the explicit unsealed target version. Recompute plan/version/source/hierarchy/boundary/policy compatibility, strict Phase 13 DTO validation and semantic node/claim/dependency checksums. Map every parent direct evidence fingerprint to exactly one target Phase 07 leaf under the same scoped stable chapter identity, offsets, content hash and fresh Chapter re-slice; similarity or text-only matching is forbidden. Insert new target nodes/claims/edges/source links through Phase 13 authority with stable semantic keys, preserving authoritative node/claim typed-content checksums while recomputing target version/build/source-link/edge/manifest components. Copy no DB IDs, parent manifest/seal/report, builder checkpoint/call/budget row or retrieval cache. Record an idempotent carry result in rebuild items/report detail; exact retry is a no-op and any target conflict or changed mapping fails before partial parent carry. Test no-change, partial carry, new build IDs with exact leaf equivalence, Unicode offsets, ambiguous/missing target leaf, stale plan, cross-scope data and concurrent retries. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_carry_forward.py tests/integration/narrative_memory/test_carry_forward_pg.py -q -x</automated></verify>
  <done>Every clean semantic asset is recreated once under valid target lineage with identical authoritative content checksums and freshly proven source links.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Translate the frozen closure into carry items and dirty-only Phase 14 stages</name>
  <files>backend/app/services/narrative_memory/rebuild_executor.py, backend/app/services/narrative_memory/builder_repository.py, backend/app/services/narrative_memory/builder_worker.py, backend/tests/integration/narrative_memory/test_local_rebuild_pg.py</files>
  <action>Implement a provider-free executor that verifies the plan checksum and materializes target authority in two disjoint forms: clean assets remain immutable Phase 16 rebuild items with the already-defined canonical `decision='carried'`, source/target semantic checksums and copied target rows, while only `decision='dirty'` assets receive normal Phase 14 pending stages; `stale_blocked` items prevent dependent scheduling and sealing. Do not add `carried_forward` to the closed Phase 14 stage status or create stage rows for clean items, and do not introduce a second action enum beside `decision`. Enforce that only stage keys listed dirty in the frozen plan can enter Phase 14 package, budget reservation, cache lookup or gateway code. Do not recreate Phase 12 provider authorization: Phase 14 must freshly recheck target eligibility, pricing, budget, cancellation and exact-cache lineage for every dirty call. Parent stages become runnable only after child dependencies resolve through either a validated `decision='carried'` rebuild item or a completed/revalidated dirty stage. Reject a changed plan after run creation, any stage/call/reservation/cache-write row for a carried item, and a dirty stage skipped by an incompatible parent cache. Test stable edit, no-change, mixed cache/carry/dirty, cancellation/resume, stage concurrency and stale-blocked target. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_carry_forward_pg.py tests/integration/narrative_memory/test_local_rebuild_pg.py tests/integration/narrative_memory/test_builder_failure_isolation_pg.py -q -x</automated></verify>
  <done>The durable builder executes exactly the frozen dirty closure, while carry-forward rebuild items are complete, evidence-valid, stage-free and provably call-free.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove conservative propagation, target sealing and parent immutability</name>
  <files>backend/app/services/narrative_memory/carry_forward.py, backend/app/services/narrative_memory/rebuild_executor.py, backend/tests/integration/narrative_memory/test_carry_forward_pg.py, backend/tests/integration/narrative_memory/test_local_rebuild_pg.py</files>
  <action>Run PostgreSQL edit/insert/delete/reorder/evidence-remap/old-new-boundary fixtures through plan, carry and controlled Phase 14 dirty execution. Require stable-boundary edits to preserve all unaffected rows byte-identically at the semantic checksum level; uncertain boundaries or missing cross-chapter lineage must rebuild the declared suffix/parents/Global and carry nothing stale. Recompute the target Phase 13 provenance graph/manifest from database rows and seal only when every plan item resolves to carried or rebuilt with direct target leaf closure; stale/partial/conflicting targets remain unsealed. Snapshot parent candidate rows/seal/report and all production pointers/revisions/journals before/after, requiring byte-equivalence. Statically scan oracle/carry/executor modules for provider, embedding/index, promotion, pointer, current-version and Reader Chat capability; only `builder_worker` may reach provider for dirty stages. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_carry_forward.py tests/integration/narrative_memory/test_carry_forward_pg.py tests/integration/narrative_memory/test_local_rebuild_pg.py tests/integration/narrative_memory/test_builder_manifest_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/carry_forward.py app/services/narrative_memory/rebuild_executor.py app/services/narrative_memory/builder_repository.py app/services/narrative_memory/builder_worker.py tests/unit/narrative_memory/test_carry_forward.py tests/integration/narrative_memory/test_carry_forward_pg.py tests/integration/narrative_memory/test_local_rebuild_pg.py</automated></verify>
  <done>Every complete target seals from mixed carried/rebuilt database authority, every uncertain case expands safely, and parent/production authority remains unchanged.</done>
</task>
</tasks>

## Must-Haves

- Carry-forward copies strict semantic authority into a new explicit version; it never aliases parent rows or copies parent seal/report/checkpoints.
- Semantic node/claim checksums remain identical; target scope, evidence links, edges and manifest are recomputed and revalidated.
- Missing, ambiguous, stale or cross-scope evidence mapping blocks carry before partial writes.
- Oracle/carry/executor perform zero provider, reservation, embedding/index or pointer operations.
- Only the existing Phase 14 worker may call provider, and only for frozen dirty stage keys after its normal eligibility/budget/cache checks.
- Carry-forward rebuild items have zero Phase 14 stage/call/reservation/cache-write rows and remain idempotent under retry/concurrency.
- Uncertain boundary/dependency cases expand conservatively; target seal requires every item resolved and direct target leaf closure.
- Parent candidate and production pointers/revisions/journals remain byte-equivalent.

## Verification

<verification>Run strict carry unit tests, PostgreSQL target-leaf mapping/idempotency/concurrency tests, controlled dirty-only Phase 14 execution, conservative boundary fixtures, target manifest/seal proof, static capability scans, parent/pointer fresh observers and Ruff.</verification>
<success_criteria>
- No-change produces full semantic carry and zero provider calls.
- A stable one-chapter edit calls exactly the changed Chapter State, containing parent and Global; unaffected semantic checksums remain identical.
- Insert/delete/reorder/boundary uncertainty rebuilds at least the declared conservative suffix and never carries a stale parent.
- Every carried claim resolves to a freshly re-sliced target leaf and every target manifest is independently recomputable.
- No carry-forward rebuild item has a Phase 14 stage, provider attempt, reservation, embedding or duplicate index write.
</success_criteria>

<output>Create `16-02-SUMMARY.md` with semantic-vs-lineage checksum rules, target leaf mapping evidence, exact stage/call matrices for each change fixture, conservative expansion and parent/no-pointer snapshots.</output>
