---
phase: 16-dependency-aware-local-rebuild-and-carry-forward
plan: 01
type: execute
wave: 1
depends_on: []
execution_gate: "Phases 13, 14 and 15 complete and independently verified; explicit implementation authorization required"
files_modified:
  - backend/app/models/narrative_memory_rebuild.py
  - backend/app/models/__init__.py
  - backend/migrations/versions/16_narrative_memory_rebuild_authority.py
  - backend/app/services/narrative_memory/rebuild_contracts.py
  - backend/app/services/narrative_memory/dependency_graph.py
  - backend/app/services/narrative_memory/change_oracle.py
  - backend/tests/unit/narrative_memory/test_dependency_graph.py
  - backend/tests/unit/narrative_memory/test_change_oracle.py
  - backend/tests/integration/narrative_memory/test_rebuild_authority_pg.py
  - backend/tests/integration/narrative_memory/test_change_oracle_pg.py
requirements: [V08-REUSE-01, V08-REUSE-03]
autonomous: false
must_haves:
  truths:
    - "The dependency graph is reconstructed losslessly from explicit parent candidate, Phase 14 boundary/stage and target Phase 07 authority; caller JSON, database IDs, insertion order, retrieval telemetry and model output cannot determine reuse."
    - "Edit, insert, delete, reorder, evidence remap, boundary change and optional-source drift yield deterministic dirty/carried/stale decisions with stable propagation paths."
    - "Any ambiguous chapter/evidence mapping, boundary or cross-chapter lineage expands the dirty set conservatively and the oracle performs zero provider, embedding, pointer or production writes."
  artifacts:
    - path: "backend/app/models/narrative_memory_rebuild.py"
      provides: "Append-only explicit-version rebuild plan, normalized decision item and reuse-report authority without a selector"
    - path: "backend/app/services/narrative_memory/dependency_graph.py"
      provides: "Canonical source/evidence → Chapter State → Arc/Volume → Global dependency graph"
    - path: "backend/app/services/narrative_memory/change_oracle.py"
      provides: "Closed change classification, conservative propagation and immutable plan materialization"
  key_links:
    - from: "backend/app/services/narrative_memory/dependency_graph.py"
      to: "Phase 13 nodes/claims/edges/source links and Phase 14 boundary/stage authority"
      via: "lossless explicit owner/novel/parent-version loading and canonical semantic keys"
      pattern: "parent_version_id.*target_version_id"
    - from: "backend/app/services/narrative_memory/change_oracle.py"
      to: "narrative_memory_rebuild_items"
      via: "persist sorted direct and propagated reason codes plus old/new/dependency checksums"
      pattern: "dirty.*carried.*stale_blocked"
---

<objective>Create immutable rebuild-plan authority and a provider-free change oracle that computes the minimum provably safe dirty closure while expanding every uncertainty conservatively.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-CONTEXT.md
@.planning/phases/16-dependency-aware-local-rebuild-and-carry-forward/16-RESEARCH.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-VERIFICATION.md
@.planning/phases/14-durable-bottom-up-candidate-builder/14-VERIFICATION.md
@.planning/phases/15-adaptive-hierarchical-retrieval-and-leaf-evidence-safety/15-VERIFICATION.md
@backend/app/models/narrative_memory.py
@backend/app/models/narrative_memory_builder.py
@backend/app/services/narrative_memory/arc_planner.py
@backend/app/services/narrative_memory/provenance.py
</context>

## Steps

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Authorize Phase 16 implementation after Phases 13–15 are complete and independently verified</action>
  <instructions>Planning-only lock is active. Before any file change or command, confirm passed verification reports for Phases 13, 14 and 15, then obtain the user's explicit instruction `批准执行 Phase 16`. Auto mode must not bypass this gate.</instructions>
  <verification>Phase 13–15 verification reports are passed and the current conversation contains explicit Phase 16 execution authorization.</verification>
  <resume-signal>批准执行 Phase 16</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 1: Add append-only rebuild plan and report authority</name>
  <files>backend/app/models/narrative_memory_rebuild.py, backend/app/models/__init__.py, backend/migrations/versions/16_narrative_memory_rebuild_authority.py, backend/tests/integration/narrative_memory/test_rebuild_authority_pg.py</files>
  <action>After confirming Phases 13-15 are passed, query the real single Alembic head at execution time (Phase 15 adds no migration, so this is expected to be the latest verified Phase 14/earlier authority head) and freeze that exact revision as `down_revision`. Add candidate-only tables for immutable rebuild plans, normalized rebuild items and append-only reuse reports. Scope every plan to owner, novel, explicit parent version and explicit unsealed target version; freeze old/new source snapshot, hierarchy build/checksum, boundary plan, compatibility/oracle policy and plan checksum. Give each item a stable semantic asset key/kind/range, closed decision (`dirty`, `carried`, `stale_blocked`, `not_applicable`), sorted direct/propagated reasons, predecessor keys and old/new/dependency checksums. Use composite scope FKs, uniqueness, checksum/range constraints and UPDATE/DELETE rejection. Create no active key, current resolver, pointer, promotion, rollback, provider call, budget or retrieval-consumer table. Test migration upgrade → downgrade to that captured prior head → upgrade, trigger cleanup, cross-scope rejection, append-only behavior, duplicate/conflicting items and forbidden-schema inventory. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_rebuild_authority_pg.py -q -x; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m alembic check; .\.venv\Scripts\ruff.exe check app/models/narrative_memory_rebuild.py migrations/versions/16_narrative_memory_rebuild_authority.py tests/integration/narrative_memory/test_rebuild_authority_pg.py</automated></verify>
  <done>PostgreSQL can preserve a complete explicit-version rebuild decision and report without any mutable production selection or execution authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reconstruct the canonical dependency graph and classify exact changes</name>
  <files>backend/app/services/narrative_memory/rebuild_contracts.py, backend/app/services/narrative_memory/dependency_graph.py, backend/tests/unit/narrative_memory/test_dependency_graph.py, backend/tests/integration/narrative_memory/test_change_oracle_pg.py</files>
  <action>Define strict frozen graph/change/decision contracts with closed kinds and canonical hashes. Load the complete sealed parent candidate, its Phase 14 frozen boundary/stages and optional-source dependencies, plus the exact target Phase 12 eligible hierarchy, before scope validation so foreign rows cannot hide. Build stable semantic vertices and edges for source/chapter/evidence, Chapter State, Arc/Volume, Global, boundary plan and actually consumed optional facts; exclude database IDs, display text, retrieval query/trace/score/cache and caller-provided dependency JSON from identity. Match chapters only by stable scoped identity plus separately compared narrative order, and target evidence only by authoritative chapter identity, code-point offsets/content hash/re-slice and complete leaf-set fingerprint—never title/text similarity or embedding. Classify no-change, edit, insert, delete, reorder, evidence split/merge/remap, boundary and optional-source lineage changes with stable reasons. Test graph insertion-order/ID independence, checksum sensitivity, foreign/duplicate/unknown rows, stale seal/report, target eligibility mismatch and zero provider/embedding/pointer capabilities. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_dependency_graph.py tests/integration/narrative_memory/test_change_oracle_pg.py -q -x</automated></verify>
  <done>Repository authority deterministically yields the same dependency graph and exact change set, while every unprovable mapping remains explicit rather than guessed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Compute minimal safe closure with monotonic conservative expansion</name>
  <files>backend/app/services/narrative_memory/change_oracle.py, backend/tests/unit/narrative_memory/test_change_oracle.py, backend/tests/integration/narrative_memory/test_change_oracle_pg.py</files>
  <action>Implement stable topological propagation from changed source/evidence to Chapter State, containing Arc/Volume and Global. Under proven stable chapter mapping, boundary and cross-chapter dependency metadata, keep a simple edit local to its chapter plus containing parent and Global. For insert/delete/reorder, boundary changes, ambiguous evidence mapping, missing continuation metadata, incompatible schema/policy or optional-source uncertainty, expand from the earliest affected point through the affected old/new range, required suffix/next proven stable boundary and Global. Require monotonic safety: adding an unknown can only retain or enlarge the dirty/stale set. Persist one canonical immutable plan only after recomputing its graph/closure checksum from sorted database-derived data; stale authority between calculation and insert must fail. Cover no-change, first/middle/last edit, insertion/deletion/reorder, overlapping old/new arc boundaries, explicit-volume stabilization, book-suffix fallback and mixed uncertainty. Wrap real oracle execution with a fresh observer proving zero model attempts, reservations, embeddings/index writes and production pointer/revision/journal changes. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_dependency_graph.py tests/unit/narrative_memory/test_change_oracle.py tests/integration/narrative_memory/test_rebuild_authority_pg.py tests/integration/narrative_memory/test_change_oracle_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/rebuild_contracts.py app/services/narrative_memory/dependency_graph.py app/services/narrative_memory/change_oracle.py tests/unit/narrative_memory/test_dependency_graph.py tests/unit/narrative_memory/test_change_oracle.py tests/integration/narrative_memory/test_change_oracle_pg.py</automated></verify>
  <done>Every fixture produces an explainable minimal safe closure, and every uncertainty expands rather than weakens it with zero provider or production side effects.</done>
</task>
</tasks>

## Must-Haves

- Execution is blocked until Phases 13-15 are complete/verified and the user re-authorizes implementation.
- Graph and oracle inputs are explicit-version, owner/novel/source/hierarchy scoped and reconstructed from PostgreSQL authority.
- Database IDs, insertion order, summaries, similarity, embeddings and Phase 15 retrieval telemetry do not affect decisions.
- Edit/insert/delete/reorder/evidence-remap/boundary/optional-source changes have closed stable reason codes and propagation paths.
- Uncertain mapping, boundary or cross-chapter lineage expands dirty scope monotonically to a safe range/suffix and Global.
- Oracle, graph and plan persistence have no provider, budget, embedding/index, pointer, promotion or production-write capability.
- Rebuild authority is append-only and contains no implicit current selector.

## Verification

<verification>Run migration round-trip/schema tests, graph/change/closure unit matrices, PostgreSQL lossless-scope and stale-authority tests, monotonic uncertainty properties, fresh-observer zero-call/no-pointer proof and Ruff.</verification>
<success_criteria>
- Fixed fixtures produce byte-stable graph/plan checksums and sorted reason paths.
- A proven stable one-chapter edit dirties exactly leaf/chapter/parent/Global; uncertainty never produces a smaller set.
- Insert/delete/reorder and boundary changes expand to the documented stable boundary or conservative suffix.
- Cross-scope/stale/unsealed/incomplete inputs cannot persist a plan.
- Observer counts for provider attempts, reservations, embeddings/index writes and production pointer changes remain zero.
</success_criteria>

<output>Create `16-01-SUMMARY.md` with migration revision/table inventory, graph identity fields, change/reason matrix, propagation examples and zero-provider/no-pointer observer evidence.</output>
