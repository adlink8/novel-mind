---
phase: 37-constrained-derivative-generation
plan: 03
type: execute
wave: 2
depends_on: [37-02]
files_modified:
  - backend/app/services/derivative_generation/gates.py
  - backend/app/services/derivative_generation/fixtures.py
  - backend/tests/adversarial/test_derivative_consistency.py
  - backend/tests/unit/derivative_generation/test_gates.py
autonomous: true
requirements: [REQ-FORK-03, REQ-CRE-06, REQ-FORK-06]
must_haves:
  truths: [人物/既定事实/时间线/clue 违规 fail closed, 冻结样例集是资格门, BranchSuggestion 是 disabled-by-default 的候选输出且不自动 fork/复用 approval]
  artifacts: ["backend/app/services/derivative_generation/gates.py", "backend/tests/adversarial/test_derivative_consistency.py"]
  key_links: ["candidate + package -> deterministic checks -> blocked/reviewable verdict", "gate verdict -> BranchSuggestion -> separate approval boundary"]
---
<objective>建立确定性一致性检查与冻结 continuation qualification fixtures。</objective>
<execution_context>@.planning/phases/37-constrained-derivative-generation/37-CONTEXT.md
@.planning/phases/37-constrained-derivative-generation/37-RESEARCH.md
@.planning/phases/37-constrained-derivative-generation/37-VALIDATION.md</execution_context>
## Steps
<tasks>
<task type="auto">
<name>Task 1: Implement contradiction and qualification gates</name>
<read_first>backend/app/services/derivative_generation/context_package.py; backend/app/services/derivative_generation/candidate.py; backend/app/services/narrative_memory; .planning/phases/37-constrained-derivative-generation/37-CONTEXT.md</read_first>
<files>backend/app/services/derivative_generation/gates.py, backend/app/services/derivative_generation/fixtures.py, backend/tests/unit/derivative_generation/test_gates.py, backend/tests/adversarial/test_derivative_consistency.py</files>
<action>按 D-37-03 对 candidate 与 package 做人物行为、canon fact、时间顺序/因果、未回收 clue、world rule、evidence 引用和 scope checks；建立高动作/安静情感/视觉歧义之外的冻结 continuation fixtures；任一冲突返回 blocked，不自动修正为 canon。按 D-37-05 让 gate verdict 输出 `branch_suggestions: BranchSuggestion[]`，每项严格包含 `choice_text`、`branch_summary`、`triggering_conflict`、`canon_delta_hash`、`evidence_refs`、`enabled_by_default: false`；建议只描述候选分支，不创建 fork、不写 Canon、不发放或复用任何 approval。</action>
<acceptance_criteria>各类故意违规均可定位到 evidence/package 字段；通过不等于 promotion，失败保留 candidate 和 reason。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/derivative_generation/test_gates.py tests/adversarial/test_derivative_consistency.py -q</automated></verify>
<done>人物、事实、时序因果、clue、world rule、evidence 和 scope gates 对冻结 continuation fixtures 给出确定性 verdict。</done>
</task>
<task type="auto">
<name>Task 2: Test, Fix, and Confirm</name>
<read_first>backend/app/services/derivative_generation/gates.py; backend/tests/adversarial/test_derivative_consistency.py</read_first>
<files>backend/tests/unit/derivative_generation/test_gates.py, backend/tests/adversarial/test_derivative_consistency.py</files>
<action>运行 intentional fact/time/clue/character violations、stale evidence、cross-fork、budget、missing world rule 以及 BranchSuggestion schema/default/no-auto-fork/no-approval-reuse tests；修复所有误放行并确认 deterministic verdict。</action>
<acceptance_criteria>冻结样例集全通过；任何 gate failure 都是 blocked/review_required 且不可 silent publish。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/derivative_generation/test_gates.py tests/adversarial/test_derivative_consistency.py -q</automated></verify>
<done>intentional violation、stale evidence、cross-fork、budget 和 missing rule 均阻断，不自动升级为 canon。</done>
</task>
</tasks>
## Must-Haves

- 每个 `BranchSuggestion` 都有 `choice_text`、`branch_summary`、`triggering_conflict`、`canon_delta_hash`、`evidence_refs`，且 `enabled_by_default` 固定为 `false`。
- BranchSuggestion 只能作为 candidate/review 输出；不得自动 fork、写入 Original/Interpretation/NM 或复用 divergence/publication approval。

## Verification

<verification>运行全部 deterministic gate、BranchSuggestion schema/default/approval-boundary 与 adversarial fixtures。</verification>
<success_criteria>生成候选具备可解释的事实/时间/人物/clue 一致性资格结论，并能输出 disabled-by-default、证据绑定且不自动 fork 的 BranchSuggestion。</success_criteria>
<must_haves>evidence、spoiler、owner、budget、candidate-only、BranchSuggestion disabled-by-default 和 approval 门不可绕过。</must_haves>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| candidate -> gate | 候选可能伪造事实或引用 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-37-03-01 | Tampering | consistency gates | mitigate | deterministic cross-checks and frozen fixtures |
| T-37-03-02 | Information Disclosure | spoiler checker | mitigate | cutoff-scoped fixture assertions |
| T-37-03-SC | Tampering | test supply chain | mitigate | no package install |
</threat_model>
<output>Execution may create 37-03-SUMMARY.md; planner does not.</output>
