---
phase: 37-constrained-derivative-generation
plan: 02
type: execute
wave: 1
depends_on: [37-01]
files_modified:
  - backend/app/models/derivative_generation_job.py
  - backend/app/services/derivative_generation/candidate.py
  - backend/app/services/derivative_generation/runner.py
  - backend/app/api/derivative_generation.py
autonomous: true
requirements: [REQ-FORK-03, REQ-CRE-05, REQ-CRE-06]
must_haves:
  truths: [LLM/agent 只能产严格 schema candidate, provider/budget/usage/failure lineage 持久化]
  artifacts: ["backend/app/services/derivative_generation/runner.py", "backend/app/models/derivative_generation_job.py"]
  key_links: ["sealed package -> ai_router -> schema candidate -> deterministic gate"]
---
<objective>实现 provider-neutral 的候选生成任务，保持 agent-candidate/script-publish 边界。</objective>
<execution_context>@.planning/phases/37-constrained-derivative-generation/37-CONTEXT.md
@.planning/phases/37-constrained-derivative-generation/37-RESEARCH.md
@backend/app/services/reader_chat/budget.py</execution_context>
## Steps
<tasks>
<task type="auto">
<name>Task 1: Add budgeted candidate runner</name>
<read_first>backend/app/services/derivative_generation/context_package.py; backend/app/services/reader_chat/budget.py; backend/app/services/ai_router.py; .planning/phases/37-constrained-derivative-generation/37-CONTEXT.md</read_first>
<files>backend/app/models/derivative_generation_job.py, backend/app/services/derivative_generation/candidate.py, backend/app/services/derivative_generation/runner.py, backend/app/api/derivative_generation.py</files>
<action>按 D-37-02 使用现有 ai_router/ai_service，输入只接受 sealed package，输出严格 continuation/rewrite candidate DTO；记录 prompt/model/package hash、usage、cost、budget、retry/cancel/failure reason，超预算或 schema 非法不调用/不发布，任务状态可恢复。</action>
<acceptance_criteria>fake gateway 可重放；同一 idempotency key 不重复扣费/生成；provider 输出永不直接写 Original 或 active pointer。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/derivative_generation/test_candidate_runner.py -q</automated></verify>
<done>runner 只消费 sealed package，记录 prompt/model/package hash、usage/cost/budget，并保持可恢复 candidate 状态。</done>
</task>
<task type="auto">
<name>Task 2: Test, Fix, and Confirm</name>
<read_first>backend/app/services/derivative_generation/runner.py; backend/tests/unit/derivative_generation/test_candidate_runner.py</read_first>
<files>backend/tests/unit/derivative_generation/test_candidate_runner.py, backend/tests/integration/test_derivative_generation_job.py</files>
<action>注入 provider timeout、invalid schema、budget exhausted、cancel/retry、duplicate idempotency、cross-fork input；修复状态机后确认 candidate-only 和 cost/evidence lineage。</action>
<acceptance_criteria>失败有稳定 reason code，恢复不重复调用，所有候选待 deterministic gate/approval。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/derivative_generation/test_candidate_runner.py tests/integration/test_derivative_generation_job.py -q</automated></verify>
<done>timeout、非法 schema、超预算、取消重试、重复幂等和跨 fork 输入均有确定性状态与 evidence lineage。</done>
</task>
</tasks>
## Must-Haves

## Verification

<verification>运行 fake provider、budget、idempotency、failure/recovery 测试。</verification>
<success_criteria>生成请求可执行、可取消、可恢复、可计费但不具备事实发布权。</success_criteria>
<must_haves>budget、evidence、owner、spoiler、candidate-only 与 approval 状态均持久化。</must_haves>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| external model -> candidate API | provider 输出不可信且可能越界 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-37-02-01 | Tampering | candidate parser | mitigate | strict schema and package hash match |
| T-37-02-02 | Denial of Service | provider job | mitigate | durable budget and cancellation |
| T-37-02-SC | Tampering | LiteLLM | mitigate | existing dependency only |
</threat_model>
<output>Execution may create 37-02-SUMMARY.md; planner does not.</output>
