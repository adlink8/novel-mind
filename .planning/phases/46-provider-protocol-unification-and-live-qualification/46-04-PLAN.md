---
phase: 46-provider-protocol-unification-and-live-qualification
plan: "04"
type: execute
wave: 3
depends_on: ["46-03"]
files_modified:
  - backend/app/models/ai_usage_log.py
  - backend/alembic/versions/20260812_4601_provider_usage_lineage.py
  - backend/app/services/ai_service.py
  - backend/app/api/usage.py
  - backend/tests/test_usage.py
  - backend/tests/unit/services/test_provider_usage.py
  - frontend/src/lib/api/usage.ts
  - frontend/src/components/settings/models-section.tsx
  - frontend/src/components/settings/usage-section.tsx
  - frontend/src/components/settings/usage-section.test.tsx
  - desktop/tests/integration/provider-settings.spec.ts
  - .planning/phases/46-provider-protocol-unification-and-live-qualification/46-VERIFICATION.md
autonomous: true
requirements: [REQ-PROVIDER-04]
must_haves:
  truths:
    - "D-46-12: provider/model/usage/error/cost lineage is owner-scoped and unknown remains explicit."
    - "UI qualification and usage claims derive from backend/evidence authority."
  artifacts:
    - path: backend/app/models/ai_usage_log.py
      provides: owner/model/request/protocol/price-status usage lineage
    - path: desktop/tests/integration/provider-settings.spec.ts
      provides: packaged settings discovery/status/error/browser proof
    - path: .planning/phases/46-provider-protocol-unification-and-live-qualification/46-VERIFICATION.md
      provides: independently reproducible phase verdict
  key_links:
    - from: backend/app/services/ai_service.py
      to: backend/app/models/ai_usage_log.py
      via: one terminal usage recorder for success and failure
    - from: frontend/src/components/settings/usage-section.tsx
      to: backend/app/api/usage.py
      via: owner-scoped known/unknown cost summary
---

<objective>补齐 owner 隔离的调用血缘、诚实成本/能力展示和桌面浏览器证据，并生成 Phase 46 收口结论。</objective>

<execution_context>@46-CONTEXT.md
@46-UI-SPEC.md
@46-QUALIFICATION.json
@backend/app/models/ai_usage_log.py
@backend/app/api/usage.py</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: RED - expose owner and cost-truth gaps in usage accounting</name>
<read_first>backend/app/models/ai_usage_log.py; backend/app/services/ai_service.py; backend/app/api/usage.py; backend/app/services/clues/_worker_primitives.py; backend/tests/test_usage.py</read_first>
<files>backend/tests/test_usage.py; backend/tests/unit/services/test_provider_usage.py</files>
<action>Add failing tests proving two owners cannot see each other's usage; every success/failure/timeout/stream terminal carries saved model ID, canonical provider/model, request/run lineage, protocol profile version and latency; token absence is explicit; cost is `known` only with an immutable price snapshot and usage, otherwise `unknown` rather than numeric zero. Include duplicate-terminal/idempotency tests.</action>
<acceptance_criteria>Tests fail against current global aggregation and `cost_usd=0.0` placeholder behavior, while documenting migration/backfill semantics for legacy rows.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/test_usage.py tests/unit/services/test_provider_usage.py -q</automated></verify>
<done>A red feedback loop covers owner isolation and honest cost status.</done>
</task>

<task type="auto">
<name>Task 2: GREEN - persist one terminal provider usage record</name>
<read_first>backend/app/models/ai_usage_log.py; backend/app/models/ai_model.py; backend/app/services/model_deployment.py; backend/app/services/ai_service.py; existing Alembic head</read_first>
<files>backend/app/models/ai_usage_log.py; backend/alembic/versions/20260812_4601_provider_usage_lineage.py; backend/app/services/ai_service.py; backend/tests/unit/services/test_provider_usage.py</files>
<action>Add owner/model-config/request/run/protocol-profile fields, terminal error class, nullable token/cost fields, cost-status and price-snapshot lineage with indexes/constraints. Migrate legacy rows as `owner_unknown`/`cost_unknown` without inventing ownership or price. Centralize idempotent terminal recording for chat and stream. Compute cost only from a persisted immutable price snapshot multiplied by actual provider usage.</action>
<acceptance_criteria>One call produces one terminal row; failures are recorded without secret/error-body leakage; known price computes reproducibly; absent usage/price remains null+unknown; Alembic upgrade/downgrade and head check pass.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/unit/services/test_provider_usage.py -q; alembic upgrade head; alembic check</automated></verify>
<done>Usage lineage is owner-safe, terminal and price-honest.</done>
</task>

<task type="auto">
<name>Task 3: Serve owner-scoped usage and provider qualification truth</name>
<read_first>backend/app/api/usage.py; backend/app/core/security.py; 46-QUALIFICATION.json; frontend/src/lib/api/usage.ts; frontend/src/components/settings/models-section.tsx; frontend/src/components/settings/usage-section.tsx</read_first>
<files>backend/app/api/usage.py; backend/tests/test_usage.py; frontend/src/lib/api/usage.ts; frontend/src/components/settings/models-section.tsx; frontend/src/components/settings/usage-section.tsx; frontend/src/components/settings/usage-section.test.tsx</files>
<action>Require current user in usage queries and filter every aggregate by owner. Return known cost totals separately from unknown-cost call counts and include token/terminal/provider summaries without exposing secrets. Expose model qualification status from backend-owned evidence metadata. Render “未知费用/未实测/PARTIAL/BLOCKED” per 46-UI-SPEC; never format unknown as `$0.00` or infer green from a saved model.</action>
<acceptance_criteria>Two-owner API tests isolate all aggregates; the UI distinguishes zero known cost from unknown cost, saved from qualified, and provider unavailable from invalid credential.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/test_usage.py -q; cd ../frontend; npm test -- --run src/components/settings/usage-section.test.tsx src/components/settings/models-section.test.tsx; npx tsc --noEmit</automated></verify>
<done>Settings and usage panels present backend truth without hardcoded success.</done>
</task>

<task type="auto">
<name>Task 4: Verify packaged settings workflow and derive closeout verdict</name>
<read_first>46-UI-SPEC.md; 46-QUALIFICATION.json; desktop/tests/integration existing launcher; frontend/src/app/settings/page.tsx; backend/scripts/qualify_text_providers.py</read_first>
<files>desktop/tests/integration/provider-settings.spec.ts; .planning/phases/46-provider-protocol-unification-and-live-qualification/46-VERIFICATION.md</files>
<action>Add Electron Playwright coverage for backend provider profile load, provider switch/default URL/credential semantics, discovery success fixture, live unavailable path, manual fallback, first default, qualification badges, usage unknown/known states, 763px and 390px, keyboard focus and zero console errors. Generate verification from requirements/tests/qualification hashes. A missing live row yields PARTIAL/BLOCKED and cannot be overridden by browser fixtures.</action>
<acceptance_criteria>Packaged/browser tests pass without real secrets; verification maps all four requirements to evidence, preserves Phase 22 and Phase 41 facts, and reports live-provider gaps exactly as in qualification JSON.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/integration/provider-settings.spec.ts; cd ../backend; python scripts/qualify_text_providers.py verify-evidence --input ../.planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json --require-five --scan-secrets</automated></verify>
<done>A reproducible Phase 46 verdict exists with honest live qualification boundaries.</done>
</task>

<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>backend/tests/test_usage.py; backend/tests/unit/services/test_provider_usage.py; frontend settings tests; desktop/tests/integration/provider-settings.spec.ts; 46-VERIFICATION.md</read_first>
<files>all Phase 46-04 files</files>
<action>Run migration, owner-isolation, usage, frontend and Electron suites twice. Tamper one evidence hash and remove one provider row to prove the verifier fails. Fix only implementation defects; do not edit evidence to force green. Run all affected lint/type checks and source/secret scans.</action>
<acceptance_criteria>Suites pass twice, tamper/missing-row checks fail as expected, zero cross-owner rows or secret findings occur, and the final verdict matches actual live evidence.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/test_usage.py tests/unit/services/test_provider_usage.py -q; alembic check; python -m ruff check app tests; cd ../frontend; npm test -- --run src/components/settings; npx tsc --noEmit; npm run lint -- --quiet; cd ../desktop; npx playwright test tests/integration/provider-settings.spec.ts</automated></verify>
<done>REQ-PROVIDER-04 and Phase 46 closeout are ready for independent review.</done>
</task>
</tasks>

## Must-Haves

- Every aggregate is owner-scoped; legacy unknown ownership is excluded, not guessed.
- `$0.00` means verified zero known cost, never missing price/usage.
- Browser fixtures cannot promote a missing real-provider qualification row.

## Verification

Run migration/accounting, two-owner API, settings component and Electron E2E plus tamper gates.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| provider response -> usage ledger | usage may be missing, malformed or duplicated |
| usage database -> owner summary | global aggregation can disclose another user's activity |
| qualification evidence -> UI/verdict | stale/tampered fixtures can fabricate readiness |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-04-01 | Information Disclosure | usage API | mitigate | mandatory owner_id filter and two-owner tests |
| T-46-04-02 | Tampering | cost/price snapshot | mitigate | immutable snapshot lineage and nullable unknown state |
| T-46-04-03 | Repudiation | terminal usage | mitigate | idempotency key, request/run linkage and stable terminal status |
| T-46-04-04 | Spoofing | qualification badge | mitigate | backend evidence hash/status only; fixture cannot mark live |
</threat_model>
