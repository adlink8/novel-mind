---
phase: 46-provider-protocol-unification-and-live-qualification
plan: "03"
type: execute
wave: 2
depends_on: ["46-01", "46-02"]
files_modified:
  - backend/scripts/qualify_text_providers.py
  - backend/tests/contract/test_provider_qualification.py
  - agent-service/tests/provider-qualification.test.ts
  - .planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json
  - .planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.md
autonomous: false
requirements: [REQ-PROVIDER-03]
user_setup:
  - service: openai
    why: "Real catalog, connection and Pi qualification requires an operator-owned API key and may incur cost."
    env_vars:
      - name: NOVELMIND_QUALIFY_OPENAI_API_KEY
        source: "Operator's OpenAI project key; inject for one qualification process only."
  - service: anthropic
    why: "Real Anthropic qualification requires an operator-owned API key and may incur cost."
    env_vars:
      - name: NOVELMIND_QUALIFY_ANTHROPIC_API_KEY
        source: "Operator's Anthropic Console key; inject for one qualification process only."
  - service: gemini
    why: "Real Google AI Studio qualification requires an operator-owned Gemini API key."
    env_vars:
      - name: NOVELMIND_QUALIFY_GEMINI_API_KEY
        source: "Operator's Google AI Studio key; inject for one qualification process only."
  - service: ollama
    why: "Live local qualification requires an operator-started reachable Ollama daemon and installed text model."
    local_dev:
      - "Start Ollama and make its loopback Base URL/model available before the bounded run."
  - service: custom_openai_compatible
    why: "Custom compatibility qualification requires an operator-controlled URL and optional key."
    env_vars:
      - name: NOVELMIND_QUALIFY_CUSTOM_BASE_URL
        source: "Operator's OpenAI-compatible service URL."
      - name: NOVELMIND_QUALIFY_CUSTOM_API_KEY
        source: "Optional operator service key; inject for one qualification process only."
must_haves:
  truths:
    - "D-46-09..11: real-provider evidence is credential-gated, redacted and never replaced by mocks."
    - "Each provider has separate catalog, direct test and Pi run outcomes."
  artifacts:
    - path: backend/scripts/qualify_text_providers.py
      provides: bounded credential-injected qualification runner
    - path: .planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json
      provides: redacted machine-readable five-provider evidence matrix
  key_links:
    - from: backend/scripts/qualify_text_providers.py
      to: backend/app/services/model_deployment.py
      via: temporary owner-scoped saved configuration and normal production API seams
    - from: backend/scripts/qualify_text_providers.py
      to: agent-service/src/agent/provider.ts
      via: logical Pi gateway chat/stream request
---

<objective>建立不泄密、不伪绿、成本有界的五供应商真实目录、连接和 Pi 执行资格矩阵。</objective>

<execution_context>@46-CONTEXT.md
@46-RESEARCH.md
@backend/app/api/models.py
@backend/app/api/gateway.py
@agent-service/src/agent/provider.ts</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: RED/GREEN - build the redacted qualification harness</name>
<read_first>backend/app/api/models.py; backend/app/api/gateway.py; backend/app/services/model_deployment.py; agent-service/src/agent/provider.ts; existing local auth/test database fixtures</read_first>
<files>backend/scripts/qualify_text_providers.py; backend/tests/contract/test_provider_qualification.py; agent-service/tests/provider-qualification.test.ts</files>
<action>Test-drive a CLI/harness that reads credentials only from named process environment variables or an interactive non-echo source, creates isolated test-owner model rows through normal APIs, runs one bounded catalog request, one direct connection test and one minimal Pi chat/stream, then removes/invalidates temporary DB rows without printing secrets. Emit stable status/error class, provider/model, timestamps, latency, token/cost availability and response hashes only. Add timeout, max-token, zero-unbounded-retry and secret-canary scans.</action>
<acceptance_criteria>Fixtures prove success, invalid credential, unavailable host, malformed response, Pi failure and missing credential; output contains no raw prompt/content/key/token/header and each provider step is independently terminal.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/contract/test_provider_qualification.py -q; cd ../agent-service; npm test -- --run tests/provider-qualification.test.ts</automated></verify>
<done>A safe harness exists before any real credential is accepted.</done>
</task>

<task type="checkpoint:human-action">
<name>Task 2: Operator credential and cost authorization gate</name>
<read_first>46-CONTEXT.md decisions D-46-09..11; backend/scripts/qualify_text_providers.py --help; 46-VALIDATION.md manual gate</read_first>
<files>none</files>
<action>Ask the operator to supply, outside tracked files and chat/log output, the credentials and endpoint context they choose to qualify: OpenAI key; Anthropic key; Gemini key; a reachable Ollama daemon; and a custom OpenAI-compatible URL with optional key. State the exact maximum calls/tokens and expected possible cost before execution. Missing entries remain `BLOCKED: credential_not_supplied` or `BLOCKED: service_unreachable`.</action>
<acceptance_criteria>There is explicit per-provider authorization/context for every real call that will run; no credential is written to repository, shell history, planning artifacts or assistant output; absent authorization is preserved as a blocked row.</acceptance_criteria>
<verify><manual>Operator confirms the provider subset and bounded spend; runner preflight reports only credential presence booleans and endpoint hostnames.</manual></verify>
<done>The harness may call only explicitly authorized providers; others are blocked without simulation.</done>
</task>

<task type="auto">
<name>Task 3: Execute catalog, direct test and Pi run matrix</name>
<read_first>backend/scripts/qualify_text_providers.py; backend/app/api/models.py; backend/app/api/gateway.py; agent-service/src/agent/provider.ts; provider-specific limits in 46-RESEARCH.md</read_first>
<files>.planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json</files>
<action>Against the CI/test database and isolated qualification owner, execute authorized providers sequentially with one catalog traversal, one minimal test and one minimal Pi completion/stream. For Ollama use only the explicitly allowed local host. Capture redacted status, protocol/profile version and evidence hashes. Do not retry authentication, payment, policy or schema failures; bound transient retry to the declared policy.</action>
<acceptance_criteria>The JSON matrix has exactly five provider rows and three step outcomes each; successful rows prove returned model identity and Pi terminal completion; unavailable/unauthorized rows are BLOCKED/PARTIAL with stable reasons; temporary credentials/data are absent after run.</acceptance_criteria>
<verify><automated>cd backend; python scripts/qualify_text_providers.py verify-evidence --input ../.planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json --require-five --scan-secrets</automated></verify>
<done>Real-provider truth is captured without claiming unsupported rows.</done>
</task>

<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>46-QUALIFICATION.json; backend/tests/contract/test_provider_qualification.py; agent-service/tests/provider-qualification.test.ts</read_first>
<files>backend/scripts/qualify_text_providers.py; .planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json; .planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.md</files>
<action>Recompute the human-readable matrix from JSON, run hash/schema/secret checks twice and rerun only authorized failed rows when the failure is a harness defect. Never rerun credential, quota, billing, policy or unavailable-service failures without new operator action. Mark each requirement dimension VERIFIED/PARTIAL/BLOCKED from evidence.</action>
<acceptance_criteria>Machine and Markdown matrices agree, evidence checks pass twice, no secret/content is present, and every green statement resolves to a real provider result rather than a fixture.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/contract/test_provider_qualification.py -q; python scripts/qualify_text_providers.py verify-evidence --input ../.planning/phases/46-provider-protocol-unification-and-live-qualification/46-QUALIFICATION.json --require-five --scan-secrets</automated></verify>
<done>REQ-PROVIDER-03 has an honest evidence-backed verdict.</done>
</task>
</tasks>

## Must-Haves

- No real call before explicit provider-specific credential/cost authorization.
- Missing credentials and services are blocked evidence, never mocked success.
- Exactly three independent outcomes per provider: catalog, direct test and Pi run.

## Verification

Validate the harness with fixtures, then validate real evidence schema/hash/redaction twice.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| operator credential -> qualification process | secret must never enter tracked files or output |
| provider output -> evidence artifact | content may be sensitive or attacker-controlled |
| qualification harness -> paid API | retries/tokens can create unbounded cost |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-46-03-01 | Information Disclosure | evidence/logs | mitigate | allowlisted metadata, canary secret scan and no bodies/headers |
| T-46-03-02 | Repudiation | live result | mitigate | timestamps, profile version and response/evidence hashes |
| T-46-03-03 | Denial of Service | real calls | mitigate | sequential bounded requests, max tokens/time and no auth retry |
| T-46-03-04 | Spoofing | mock as live | eliminate | evidence mode records live transport and rejects fixture markers |
</threat_model>
