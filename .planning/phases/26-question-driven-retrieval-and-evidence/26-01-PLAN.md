---
phase: 26-question-driven-retrieval-and-evidence
plan: 01
type: execute
wave: 1
depends_on: [26-00]
files_modified: [backend/app/services/queryplan/schemas.py, backend/app/services/queryplan/parser.py, backend/app/services/queryplan/repository.py, backend/app/models/queryplan.py, backend/alembic/versions/20260801_2601_query_plan_trace.py, backend/tests/unit/queryplan/test_contracts.py, backend/tests/unit/queryplan/test_parser.py, backend/tests/integration/queryplan/test_trace_replay.py, backend/tests/fixtures/queryplan/questions_v1.json]
autonomous: true
requirements: [REQ-QP-01]
must_haves:
  truths: ["QueryPlan parsing and validation are deterministic and fail closed", "A durable QueryPlanTrace can be replayed after process restart without changing the plan"]
  artifacts:
    - path: backend/app/services/queryplan/schemas.py
      provides: "Typed QueryPlan, QueryPlanTrace, EvidenceRef and availability contracts"
    - path: backend/app/models/queryplan.py
      provides: "Immutable durable QueryPlanTrace projection"
    - path: backend/app/services/queryplan/repository.py
      provides: "Owner/version-scoped append and idempotent replay access"
    - path: backend/alembic/versions/20260801_2601_query_plan_trace.py
      provides: "Single-head reversible QueryPlanTrace migration with revision 20260801_2601"
    - path: backend/tests/integration/queryplan/test_trace_replay.py
      provides: "Restart replay and immutable idempotency tests"
  key_links:
    - from: backend/app/services/queryplan/parser.py
      to: backend/app/services/queryplan/repository.py
      via: "persists validated QueryPlanTrace with owner/novel/version/cutoff lineage"
    - from: backend/app/services/queryplan/repository.py
      to: backend/app/models/queryplan.py
      via: "replays the same immutable trace by idempotency key"
---

<objective>建立严格、可持久化追踪的 QueryPlan 契约与确定性问题解析。</objective>

<execution_context>@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-CONTEXT.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-RESEARCH.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-PATTERNS.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-VALIDATION.md</execution_context>

## Steps

<tasks>
<task type="auto"><name>Task 1: QueryPlan schema and deterministic parser</name><read_first>backend/app/schemas/reader_chat.py; backend/app/services/narrative_memory/routing.py; 26-CONTEXT.md decisions D-01,D-02,D-03,D-11,D-12,D-14</read_first><files>backend/app/services/queryplan/schemas.py; backend/app/services/queryplan/parser.py; backend/tests/fixtures/queryplan/questions_v1.json</files><action>新增 queryplan 包，定义 intent、owner_id、novel_id、version_id、spoiler_cutoff、dimensions、fallback、answer_constraints、anchor、trace 和 availability 所需的严格 Pydantic 契约。解析必须拒绝未知/歧义 intent、越界 scope、future probing、矛盾约束；owner/novel/version/cutoff 必填，默认使用 reading-progress cutoff，whole-book 仅接受显式开关，按 D-01/D-02/D-03/D-11/D-12/D-14 执行。契约事务边界止于验证成功的不可变 payload；任一校验失败只返回稳定 blocked/clarification reason，不创建 trace 或写数据库。不要把 Phase 25.2 当作已实现服务；将其作为待执行上游边界记录在契约注释/测试上下文。数据模型若需要 Alembic 迁移，先建立 migration gate：迁移必须可回滚且不得改变既有 chat 表；若仅内存/JSON 契约则明确无需 schema migration。</action><acceptance_criteria>代表性 reader/analysis 问题生成稳定 QueryPlan；未知、歧义、未来问题和矛盾约束 fail-closed；每个计划含 owner/novel/version/cutoff 与可序列化 trace；契约失败不产生持久化副作用。</acceptance_criteria><verify><automated>cd backend; pytest tests/unit/queryplan/test_contracts.py tests/unit/queryplan/test_parser.py -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Task 2: Durable QueryPlanTrace repository and restart replay</name><read_first>backend/app/models/eval.py; backend/app/services/narrative_memory/builder_repository.py; backend/alembic/versions/; 26-CONTEXT.md</read_first><files>backend/app/models/queryplan.py; backend/app/services/queryplan/repository.py; backend/alembic/versions/20260801_2601_query_plan_trace.py; backend/tests/integration/queryplan/test_trace_replay.py</files><action>为已验证 QueryPlan 增加 immutable durable QueryPlanTrace 模型与 repository，保存 canonical plan payload、trace id/idempotency key、owner/novel/version/cutoff、schema/parser version、source/dataset lineage、availability/fallback、created-at 与 blocked reason；事务边界为单次 gate 后的 trace append，唯一 idempotency key 冲突只重放既有行，任何校验、owner、lineage 或数据库错误都 rollback 且不留下半条 trace；禁止更新既有 trace、跨 owner 读取、active-pointer 或 promotion 写入。创建精确 migration `20260801_2601_query_plan_trace.py`，声明 `revision = "20260801_2601"`、唯一 `down_revision` 指向当前单 head，upgrade/downgrade 成对可逆；先检查 head 冲突、表/列兼容和旧数据库回读，不改变既有 chat 表。integration test 必须提交 trace、关闭/重启 session/process、按 idempotency key replay，断言 payload/lineage/checksum 相同且重复执行不产生第二条 trace。</action><acceptance_criteria>Trace 可 durable 保存、唯一重放、跨 restart 读取；migration single-head、upgrade/downgrade 可逆且兼容既有数据库；任何 lineage/owner 不匹配或事务失败均 fail closed 且无部分写入。</acceptance_criteria><verify><automated>cd backend; pytest tests/integration/queryplan/test_trace_replay.py -q; alembic heads</automated></verify><done>Durable QueryPlanTrace model/repository and revision 20260801_2601 migration have atomic failure boundaries, restart replay, single-head, reversible and compatibility proof.</done></task>
</tasks>
<tasks>
<task type="auto"><name>Test, Fix, and Confirm</name><read_first>backend/tests/unit/queryplan/test_contracts.py; backend/tests/integration/queryplan/test_trace_replay.py</read_first><files>backend/tests/unit/queryplan/test_contracts.py; backend/tests/integration/queryplan/test_trace_replay.py</files><action>运行 QueryPlan contract 与 durable trace replay，修复 migration、idempotency、owner/cutoff 或 lineage 漂移；执行门由前置的 26-00 计划负责。</action><acceptance_criteria>满足 26-00 前置条件时，contract、migration 和 restart replay 全部通过。</acceptance_criteria><verify><automated>Push-Location backend; pytest tests/unit/queryplan/test_contracts.py tests/integration/queryplan/test_trace_replay.py -q; $code=$LASTEXITCODE; Pop-Location; exit $code</automated></verify><done>QueryPlan contract 与 durable replay 均有可重复的通过证据。</done></task>
</tasks>

## Must-Haves

<must_haves>typed QueryPlan、确定性校验、可持久化 trace。</must_haves>

- QueryPlan 严格覆盖 intent、scope、cutoff、dimensions、fallback、answer constraints 和 trace。
- unknown/ambiguous intent、越界、future probing、contradictory constraints 均 fail-closed。
- 不虚构 Phase 25.2 已落地；不包含 promotion、active pointer、第二检索栈。

## Verification

<verification>执行 parser/contract 单测，并审查所有计划字段的 owner、spoiler、budget、evidence、approval、candidate-only 门。</verification>
<success_criteria>REQ-QP-01 与 D-01..D-03、D-11..D-14 有可执行契约和冻结 fixture。</success_criteria>
