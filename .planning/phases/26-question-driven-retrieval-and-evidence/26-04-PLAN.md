---
phase: 26-question-driven-retrieval-and-evidence
plan: 04
type: execute
wave: 4
depends_on: [26-03]
files_modified: [backend/app/services/queryplan/service.py, backend/app/services/reader_chat/context.py, backend/app/services/reader_chat/retrieval.py, backend/app/services/analysis_chat/query_adapter.py, backend/tests/integration/queryplan/test_chat_consumers.py, frontend/src/components/analysis/analysis-chat-panel.tsx, frontend/e2e/reader-chat-queryplan.spec.ts]
autonomous: true
requirements: [REQ-QP-04]
must_haves:
  truths: ["Reader and Analysis Chat share one QueryPlan/retrieval/evidence core while retaining selection versus structure-range anchors", "Frozen Manifest is passed to the existing cited-answer gateway and factual output is leaf-only", "Quick contract/smoke checks run before Playwright is used as the phase gate"]
  artifacts:
    - path: backend/app/services/queryplan/service.py
      provides: "Shared Reader/Analysis QueryPlan seam"
    - path: backend/app/services/reader_chat/gateway.py
      provides: "Citation authority gateway"
    - path: backend/app/schemas/reader_chat.py
      provides: "Manifest validation authority"
    - path: backend/tests/integration/queryplan/test_chat_consumers.py
      provides: "Consumer contract and smoke tests"
    - path: frontend/e2e/reader-chat-queryplan.spec.ts
      provides: "Reader/Analysis browser proof"
  key_links:
    - from: Reader/Analysis Chat
      to: backend/app/services/queryplan/service.py
      via: "both consumers share QueryPlan and retrieval"
    - from: backend/app/services/queryplan/service.py
      to: backend/app/services/reader_chat/gateway.py
      via: "Frozen Manifest reaches cited-answer validation"
    - from: backend/tests/integration/queryplan/test_chat_consumers.py
      to: frontend/e2e/reader-chat-queryplan.spec.ts
      via: "contract smoke passes before Playwright stage gate"
---

<objective>让 Reader Chat 与 Analysis Chat 共享 QueryPlan、检索、引用和预算权威，仅保留不同 anchor，并冻结 QA 样本。</objective>

<execution_context>@.planning/phases/26-question-driven-retrieval-and-evidence/26-CONTEXT.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-VALIDATION.md
@.planning/phases/25.1-analysis-chat-workspace-range-anchor/*-SUMMARY.md</execution_context>

## Steps

<tasks>
<task type="auto"><name>Task 1: Shared consumer seam and trace/citation exposure</name><read_first>backend/app/services/reader_chat/context.py; backend/app/services/reader_chat/retrieval.py; frontend/src/components/analysis/analysis-chat-panel.tsx; frontend/src/components/analysis/ChapterRange.tsx; 26-CONTEXT.md D-10,D-11,D-12,D-13</read_first><files>backend/app/services/queryplan/service.py; backend/app/services/reader_chat/context.py; backend/app/services/reader_chat/retrieval.py; backend/app/services/analysis_chat/query_adapter.py; frontend/src/components/analysis/analysis-chat-panel.tsx</files><action>把两个消费者接到同一个 QueryPlan service/context builder；Reader 使用 selection anchor，Analysis 使用 ChapterRange/structure anchor。服务端重新验证 owner、cutoff、spoiler、budget、evidence 和 approval，返回 trace、availability、fallback、citation jump；默认 reading-progress cutoff，whole-book 由 per-novel switch 明确开启。保留现有会话/选中结构，不建立第二检索栈，不把 Phase 25.2 当成当前已验证实现；D-10/D-11/D-12/D-13/D-14 全部落地。新增持久化字段时先完成 schema migration gate、回滚和旧会话兼容测试。</action><acceptance_criteria>两种聊天入口结果共享检索/引用/预算逻辑但 anchor 不同；无 uncited factual assertion；取消、retry、loading/error 和 partial 可见。</acceptance_criteria><verify><automated>cd backend; pytest tests/integration/queryplan/test_chat_consumers.py -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><read_first>frontend/e2e/reader-chat*.spec.ts; frontend/src/components/analysis/analysis-chat-panel.test.tsx; 26-VALIDATION.md</read_first><files>backend/tests/integration/queryplan/test_chat_consumers.py; frontend/e2e/reader-chat-queryplan.spec.ts; frontend/src/components/analysis/analysis-chat-panel.tsx</files><action>运行 desktop/mobile、selection/range citation jump、cutoff switch、no-answer、partial/failure、keyboard/focus 流程；修复 citation anchor 错位、spoiler metadata 泄漏、结构选择丢失和未授权全书读取。确认 approval 仅适用于可展示证据，NM 仍 candidate-only、无 active pointer/cutover。</action><acceptance_criteria>Reader/Analysis Chat 共享核心且浏览器路径可用，Citation 跳转到叶子原文，未来章节 metadata 不泄漏。</acceptance_criteria><verify><automated>cd frontend; npx playwright test e2e/reader-chat-queryplan.spec.ts</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Task 3: Consumer gateway contract and browser smoke gate</name><read_first>backend/app/services/reader_chat/gateway.py; backend/app/schemas/reader_chat.py; frontend/e2e/reader-chat-queryplan.spec.ts; 26-VALIDATION.md</read_first><files>backend/tests/integration/queryplan/test_chat_consumers.py; frontend/e2e/reader-chat-queryplan.spec.ts; frontend/src/components/analysis/analysis-chat-panel.tsx</files><action>26-00 gate 脚本必须是执行入口首个命令；随后运行快速 backend contract/smoke，断言 Frozen Manifest -> `backend/app/services/reader_chat/gateway.py::business_validate_answer` -> `backend/app/schemas/reader_chat.py::validate_answer_against_manifest` 的 leaf-only allowlist、selection/range anchor、owner/cutoff/spoiler/budget；只有 smoke 通过后才运行 Playwright 阶段门。Playwright 只证明真实浏览器行为，不替代服务契约；确认 summary/score/routing/chat ref 不能成为 citation。</action><acceptance_criteria>contract/smoke 在无浏览器时能证明调用链和 leaf-only 约束；Playwright 仅作为阶段 gate，两个消费者的 citation 跳转仍到叶子原文。</acceptance_criteria><verify><automated>
python scripts/check_phase_execution_gate.py
$gateCode = $LASTEXITCODE
if ($gateCode -ne 0) { exit $gateCode }
Push-Location backend
pytest tests/integration/queryplan/test_chat_consumers.py -q
$smokeCode = $LASTEXITCODE
Pop-Location
if ($smokeCode -ne 0) { exit $smokeCode }
Push-Location frontend
npx playwright test e2e/reader-chat-queryplan.spec.ts
$playwrightCode = $LASTEXITCODE
Pop-Location
exit $playwrightCode
</automated></verify><done>Execution gate is checked first; quick contract/smoke passes before the Playwright gate and browser verification confirms leaf-only citation behavior for both consumer anchors.</done></task>
</tasks>
<tasks>
<task type="auto"><name>Test, Fix, and Confirm: fail-fast consumer browser gate</name><read_first>scripts/check_phase_execution_gate.py; backend/tests/integration/queryplan/test_chat_consumers.py; frontend/e2e/reader-chat-queryplan.spec.ts</read_first><files>backend/tests/integration/queryplan/test_chat_consumers.py; frontend/e2e/reader-chat-queryplan.spec.ts</files><action>按 gate → backend smoke → Playwright 顺序执行；任一步非零立即退出，不得继续慢验证。</action><acceptance_criteria>Playwright 只在资格 gate 与 consumer smoke 均通过后启动。</acceptance_criteria><verify><automated>python scripts/check_phase_execution_gate.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location backend; pytest tests/integration/queryplan/test_chat_consumers.py -q; $code=$LASTEXITCODE; Pop-Location; if ($code -ne 0) { exit $code }; Push-Location frontend; npx playwright test e2e/reader-chat-queryplan.spec.ts; $code=$LASTEXITCODE; Pop-Location; exit $code</automated></verify><done>三个阶段按顺序通过，或在首个失败点非零退出。</done></task>

</tasks>

## Must-Haves
<must_haves>共享 retrieval core、不同 anchor、citation-safe consumers。</must_haves>
- REQ-QP-04、D-10..D-14 全覆盖。
- Reader/Analysis distinct anchors、shared authority、owner/spoiler/budget/evidence/approval/candidate-only 门均可验证。

## Verification

<verification>先运行 backend consumer/gateway contract/smoke；仅 smoke 通过后运行 frontend Playwright 作为阶段 gate；记录 Phase 22 仍 0/3，不将其改写为通过。</verification>
<success_criteria>Phase 26 goal 的 typed plan→fused retrieval→verified citation→两消费者链路闭合。</success_criteria>
