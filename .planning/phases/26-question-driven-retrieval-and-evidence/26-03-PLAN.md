---
phase: 26-question-driven-retrieval-and-evidence
plan: 03
type: execute
wave: 3
depends_on: [26-02]
files_modified: [backend/app/services/queryplan/evidence.py, backend/app/services/queryplan/service.py, backend/tests/adversarial/test_queryplan_evidence.py, backend/tests/integration/queryplan/test_manifest.py]
autonomous: true
requirements: [REQ-QP-03]
must_haves:
  truths: ["Every candidate resolves to frozen leaf/raw evidence with Unicode offsets and matching hash", "Frozen Manifest is immutable before generation and stale or unauthorized lineage fails closed", "Only leaf evidence reaches the cited-answer gateway"]
  artifacts:
    - path: backend/app/services/queryplan/evidence.py
      provides: "Leaf EvidenceRef materialization and Frozen Manifest freeze"
    - path: backend/app/services/queryplan/service.py
      provides: "QueryPlan service call into cited-answer gateway"
    - path: backend/tests/integration/queryplan/test_manifest.py
      provides: "Manifest mutation, hash and lineage tests"
    - path: backend/tests/adversarial/test_queryplan_evidence.py
      provides: "Leaf-only citation and fail-closed tests"
  key_links:
    - from: FrozenManifest
      to: backend/app/services/reader_chat/gateway.py::business_validate_answer
      via: "frozen manifest is the gateway input"
    - from: backend/app/services/reader_chat/gateway.py
      to: backend/app/schemas/reader_chat.py::validate_answer_against_manifest
      via: "only allowlisted leaf/raw EvidenceRef keys are accepted"
    - from: backend/tests/adversarial/test_queryplan_evidence.py
      to: answer materialization
      via: "non-leaf citations are rejected before answer creation"
---

<objective>将候选解析为冻结快照中的叶子/raw EvidenceRef，校验 offset/hash 并冻结 retrieval manifest。</objective>

<execution_context>@.planning/phases/26-question-driven-retrieval-and-evidence/26-CONTEXT.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="auto"><name>Task 1: Evidence materialization and manifest freeze</name><read_first>backend/app/services/narrative_memory/citations.py; backend/app/services/narrative_memory/source_snapshot.py; backend/app/services/reader_chat/context.py; backend/app/services/reader_chat/retrieval.py; 26-CONTEXT.md D-07,D-08,D-09,D-14</read_first><files>backend/app/services/queryplan/evidence.py; backend/app/services/queryplan/service.py; backend/tests/integration/queryplan/test_manifest.py; backend/tests/adversarial/test_queryplan_evidence.py</files><action>复用现有 source snapshot/citation validator，解析候选到 leaf/raw chapter、Unicode offsets、content hash 和 owner/novel/version/cutoff，生成不可变 manifest；manifest 在 generation/answer 前冻结，hash/offset/owner/spoiler/version 任一不匹配即拒绝。只允许 leaf/raw EvidenceRef，禁止 summary、score、routing metadata、chat text；无证据事实必须 abstain，并记录 omitted/fallback。若需 durable trace/manifest 表，先创建 Alembic migration、唯一键、回滚和旧数据兼容测试；不得绕过 owner、budget、approval、candidate-only 门。</action><acceptance_criteria>冻结 manifest 可重放；变更文本、hash、offset、owner、spoiler 或 version 均 fail-closed；未引用事实不能进入 answer gateway。</acceptance_criteria><verify><automated>cd backend; pytest tests/integration/queryplan/test_manifest.py tests/adversarial/test_queryplan_evidence.py -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><read_first>backend/tests/adversarial/test_reader_chat_boundaries.py; backend/tests/integration/queryplan/test_manifest.py</read_first><files>backend/tests/adversarial/test_queryplan_evidence.py; backend/tests/integration/queryplan/test_manifest.py</files><action>注入 stale hash、Unicode offset shift、错误 owner、越界 cutoff、摘要伪 citation 和 chat-text citation，修复所有未 fail-closed 的路径；确认 manifest freeze 后 retry 仅按 checksum 重用，不重新选择来源，也不触发 promotion/active-pointer。</action><acceptance_criteria>全部证据反例被拒绝，manifest lineage 完整且 answer 只能消费允许的 EvidenceRef。</acceptance_criteria><verify><automated>cd backend; pytest tests/adversarial/test_queryplan_evidence.py tests/integration/queryplan -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Task 3: Frozen Manifest to cited-answer gateway leaf-only contract</name><read_first>backend/app/services/reader_chat/gateway.py; backend/app/schemas/reader_chat.py; backend/tests/unit/reader_chat/test_gateway.py; 26-CONTEXT.md D-07,D-08,D-09</read_first><files>backend/app/services/queryplan/service.py; backend/tests/integration/queryplan/test_manifest.py; backend/tests/adversarial/test_queryplan_evidence.py</files><action>把调用链写死并测试：QueryPlan service 生成并冻结 Frozen Manifest 后，调用现有 `backend/app/services/reader_chat/gateway.py::business_validate_answer`，由该函数调用 `backend/app/schemas/reader_chat.py::validate_answer_against_manifest`。增加 leaf-only E2E contract assertion，传入 summary、score、routing metadata 或 chat-text ref 必须在 gateway 前拒绝；允许的仅是 manifest allowlisted leaf/raw EvidenceRef，且无证据事实必须 abstain。</action><acceptance_criteria>命名函数/文件调用链可由测试证明；非 leaf ref fail closed；合法 leaf citation 可通过。</acceptance_criteria><verify><automated>cd backend; pytest tests/integration/queryplan/test_manifest.py tests/adversarial/test_queryplan_evidence.py tests/unit/reader_chat/test_gateway.py -q</automated></verify><done>Frozen Manifest is proven to feed the existing cited-answer gateway, and the gateway contract rejects every non-leaf citation.</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><read_first>scripts/check_phase_execution_gate.py; backend/tests/adversarial/test_queryplan_evidence.py; backend/tests/integration/queryplan/test_manifest.py; backend/tests/unit/reader_chat/test_gateway.py</read_first><files>backend/tests/adversarial/test_queryplan_evidence.py; backend/tests/integration/queryplan/test_manifest.py; backend/app/services/queryplan/service.py</files><action>按 Phase 26 执行门顺序运行 evidence adversarial、manifest integration 和 cited-answer gateway contract；修复 stale hash、Unicode offset、owner/cutoff、summary/score/routing/chat citation、manifest mutation 或调用链漂移。重复确认冻结 manifest 只允许 hash/offset 校验过的 leaf/raw EvidenceRef，任何失败均 fail closed、无 answer/Artifact、无 promotion/active-pointer 写入。</action><acceptance_criteria>证据、manifest 和 gateway targeted tests 全绿且可重复；所有非 leaf 或来源漂移反例被拒绝；当前资格不足时前置 gate 仍保持非零。</acceptance_criteria><verify><automated>python scripts/check_phase_execution_gate.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Push-Location backend; pytest tests/adversarial/test_queryplan_evidence.py tests/integration/queryplan/test_manifest.py tests/unit/reader_chat/test_gateway.py -q; $code=$LASTEXITCODE; Pop-Location; exit $code</automated></verify><done>Frozen Manifest 到 cited-answer gateway 的 leaf-only 证据链和 fail-closed gate 有可重复验证。</done></task>
</tasks>

## Must-Haves
<must_haves>叶子 EvidenceRef、hash 校验、冻结 manifest。</must_haves>
- REQ-QP-03 的 leaf/raw、Unicode offset、hash、frozen snapshot lineage 全部存在。
- D-07/D-08/D-09/D-14 的 stale rejection、citation allowlist、abstention、candidate-only 门通过。

## Verification

<verification>执行 manifest integration 与 evidence adversarial tests；如新增表则执行 migration upgrade/downgrade 验证。</verification>
<success_criteria>每个 factual answer 的来源可追到冻结叶子原文，任何来源漂移均被拒绝。</success_criteria>
