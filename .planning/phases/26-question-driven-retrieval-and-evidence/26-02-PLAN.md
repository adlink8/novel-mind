---
phase: 26-question-driven-retrieval-and-evidence
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: [backend/app/services/queryplan/adapters.py, backend/app/services/queryplan/fusion.py, backend/tests/unit/queryplan/test_adapters.py, backend/tests/unit/queryplan/test_fusion.py]
autonomous: true
requirements: [REQ-QP-02, REQ-QP-05]
must_haves:
  truths: ["Every requested dimension reports available/partial/unavailable with stable reason and provenance", "A missing exact/domain reader follows exact/domain reader -> deterministic heuristic candidate recall -> stable partial/unavailable reason", "Heuristic extraction is candidate-only and never becomes a fact, EvidenceRef or citation", "Fusion is deterministic, single-source comparable, and changes explainably with dimension set", "Missing dimensions never become empty-success or uncited facts"]
  artifacts:
    - path: backend/app/services/queryplan/adapters.py
      provides: "Dimension adapter registry with explicit availability and the exact-reader -> heuristic-candidate -> stable-reason fallback chain"
    - path: backend/app/services/queryplan/fusion.py
      provides: "Deterministic provenance-preserving fusion"
    - path: backend/tests/unit/queryplan/test_adapters.py
      provides: "Adapter availability, three-stage fallback, stable reasons and candidate-only tests"
    - path: backend/tests/unit/queryplan/test_fusion.py
      provides: "Deterministic single-source comparison tests"
  key_links:
    - from: QueryPlan.scope/cutoff
      to: backend/app/services/queryplan/adapters.py
      via: "existing dimension readers return explicit availability"
    - from: backend/app/services/queryplan/adapters.py
      to: backend/app/services/queryplan/fusion.py
      via: "available/partial/unavailable results are fused without empty-success"
    - from: exact/domain reader
      to: deterministic heuristic extraction
      via: "reader-unavailable dimensions may receive deterministic recall candidates only"
    - from: deterministic heuristic extraction
      to: stable partial/unavailable reason
      via: "candidate-only output never becomes a fact or citation and unresolved coverage is explicit"
    - from: backend/app/services/queryplan/fusion.py
      to: EvidenceRef
      via: "owner/version/cutoff/budget/provenance are preserved"
---

<objective>以统一 QueryPlan 适配 raw、事件/因果、人物、关系、时间线、线索、世界实体和 candidate-only NM，并以明确的三段 fallback chain 进行诚实融合。</objective>

<execution_context>@.planning/phases/26-question-driven-retrieval-and-evidence/26-CONTEXT.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-RESEARCH.md
@.planning/phases/26-question-driven-retrieval-and-evidence/26-PATTERNS.md</execution_context>

## Steps

<tasks>
<task type="auto"><name>Task 1: Dimension adapters, explicit fallback chain and deterministic fusion</name><read_first>backend/app/services/knowledge_units/search.py; backend/app/services/timeline/query.py; backend/app/services/relationships/query.py; backend/app/services/clues/query.py; backend/app/services/narrative_memory/candidate_reader.py; 26-CONTEXT.md D-04,D-05,D-06,D-12,D-14,D-15</read_first><files>backend/app/services/queryplan/adapters.py; backend/app/services/queryplan/fusion.py; backend/tests/unit/queryplan/test_adapters.py; backend/tests/unit/queryplan/test_fusion.py</files><action>实现 adapter registry，按 QueryPlan 对每个维度固定执行三段链：第一段优先调用 exact/domain reader；仅当 reader 缺失、不支持或明确返回 unavailable 时，第二段在同一 owner/version/cutoff/source snapshot 范围内运行确定性 heuristic extraction；第三段把无候选或覆盖不足表示为稳定的 partial/unavailable reason。exact/domain reader 的已验证结果才可标记 available；heuristic 结果必须标记 candidate_only、保留 provenance、budget 和 evidence eligibility=false，不得生成事实、EvidenceRef 或 citation，也不得被 fusion 当作已验证事实。character-state 与 world-entity 当前无生产 reader 时必须走该链，不得 empty-success；NM 只读 candidate 且 ADR-0002 标记。明确 reason code（例如 `reader_unavailable`, `heuristic_candidate_only`, `dimension_unavailable`），并保持 reason 可重放。融合公式可自行选择但必须确定性、可与单源比较、维度集合改变时结果可解释改变，按 D-04/D-05/D-06/D-12/D-14 过滤 owner/version/cutoff，禁止 chat text、summary、score 或路由元数据充当证据。无新依赖；若 adapter 状态需要持久化，先走 schema migration gate 并写回滚测试。</action><acceptance_criteria>每个维度都有状态、provenance 和稳定 reason；exact/domain reader 优先；heuristic 只能提供候选召回且不具备事实或 citation eligibility；缺失/partial 可见；融合输出稳定且不会把不可用误报为零结果或事实。</acceptance_criteria><verify><automated>cd backend; pytest tests/unit/queryplan/test_adapters.py tests/unit/queryplan/test_fusion.py -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><read_first>backend/tests/fixtures/narrative_memory/qualification/single_book_v1.json; backend/tests/unit/queryplan/test_adapters.py; backend/tests/unit/queryplan/test_fusion.py</read_first><files>backend/tests/unit/queryplan/test_adapters.py; backend/tests/unit/queryplan/test_fusion.py</files><action>用冻结单书覆盖每一维度、exact/domain reader 命中、reader 缺失、heuristic candidate-only、无候选 partial/unavailable、cutoff、NM candidate-only 和 deterministic fallback；修复维度泄漏、排序 tie 不稳定、预算未传递及 owner 越界。增加断言：heuristic 输出无 EvidenceRef、不可进入 citation 或 factual-answer 输入，且 reason/status 在重复运行中稳定。确认 evidence/approval 门只允许后续 evidence 阶段使用叶子原文，且没有 active-pointer 写入。</action><acceptance_criteria>adapter/fusion 测试全绿，单源 baseline 与多维结果可比较；exact/domain reader 优先级可证明；heuristic 永远保持 candidate-only；所有 partial/unavailable 都带稳定 reason code。</acceptance_criteria><verify><automated>cd backend; pytest tests/unit/queryplan -q</automated></verify><done>Acceptance criteria are satisfied and the task artifact is ready for its declared downstream dependency.</done></task>
</tasks>

## Must-Haves
<must_haves>维度 availability、provenance、确定性 fusion。</must_haves>
- REQ-QP-02 全部维度都有显式 adapter 状态。
- REQ-QP-05 的 exact/domain reader -> deterministic heuristic candidate recall -> stable partial/unavailable reason 链可重放。
- D-04/D-05/D-06/D-12/D-14 的 candidate-only、spoiler、owner、fallback、citation 边界可测试。
- Heuristic 只能改善候选召回，不能成为事实、EvidenceRef 或 citation。

## Verification

<verification>运行 adapter/fusion 单测并核对没有新依赖、没有空成功、没有第二检索栈；针对 reader 缺失、heuristic candidate-only、无候选 partial/unavailable 和稳定 reason 执行对抗性断言。</verification>
<success_criteria>多维检索可重放、可解释降级、排序变化与维度输入相关；exact/domain reader 优先，heuristic 仅作候选召回，未解决维度以稳定 partial/unavailable reason 结束。</success_criteria>
