---
phase: 26-question-driven-retrieval-and-evidence
plan: "00"
type: execute
wave: 0
depends_on: ["22-G3", "25.1", "25.2", "25.3"]
files_modified:
  - scripts/check_phase_execution_gate.py
  - backend/tests/integration/queryplan/test_execution_preflight.py
autonomous: true
requirements: [REQ-NIGHTLY-03, REQ-QP-01, REQ-AGENT-01, REQ-AGENT-03]
must_haves:
  truths:
    - "Execution is blocked unless Phase 22 has three real consecutive scheduled green observations."
    - "Execution is blocked unless Phase 25.1, Phase 25.2 and Phase 25.3 each have an authoritative VERIFICATION.md with status: passed."
    - "Missing, malformed, stale or contradictory evidence, including a planning-only override, returns non-zero and creates no product state."
  artifacts:
    - path: scripts/check_phase_execution_gate.py
      provides: "Single fail-closed Phase 26+ execution preflight for Phase 22 3/3 and passed Phase 25.1/25.2/25.3 verification artifacts"
    - path: backend/tests/integration/queryplan/test_execution_preflight.py
      provides: "Current-blocked, synthetic-pass and per-gate negative coverage for the preflight"
---

<objective>
建立 Phase 26+ 唯一、只读、可机器验证的执行前置门。本计划只建立门禁及其测试，
不创建 QueryPlan、World Model、Narrative Memory 或任何 Agent 产品数据。
</objective>

<execution_context>
@.planning/STATE.md
@.planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md
@.planning/phases/25.1-analysis-chat-workspace-range-anchor/25.1-VERIFICATION.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-VERIFICATION.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-VERIFICATION.md
@.planning/config.json
</execution_context>

## Steps

<tasks>
<task type="auto">
  <name>Task 1: Build the read-only execution preflight</name>
  <read_first>.planning/STATE.md; .planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md; .planning/phases/25.1-analysis-chat-workspace-range-anchor/25.1-VERIFICATION.md; .planning/phases/25.2-embedded-novel-agent-runtime/25.2-VERIFICATION.md; .planning/phases/25.3-pi-package-compatibility-governance/25.3-VERIFICATION.md; .planning/config.json</read_first>
  <files>scripts/check_phase_execution_gate.py</files>
  <action>创建独立 CLI，仅读取权威证据：Phase 22 validation ledger 必须有三条真实、连续、scheduled、green 记录，且每条包含 run、artifact、result；Phase 25.1、25.2、25.3 各自的 VERIFICATION.md 必须存在、可解析且明确为 `status: passed`。缺失、pending、非 scheduled、非 green、字段不全、解析异常或 lineage 不一致都打印稳定 blocked reason 并以非零退出。忽略 gate_overrides/planning authorization；CLI 不写产品数据、不改变状态、不创建后续阶段文件。</action>
  <acceptance_criteria>当前仓库因 Phase 22 仍为 0/3 且上游 verification 缺失而稳定 blocked；只有四组权威证据同时满足时返回 0。</acceptance_criteria>
  <verify><automated>python scripts/check_phase_execution_gate.py</automated></verify>
  <done>Phase 26+ 的执行门已可独立调用，失败路径 fail closed 且不会产生产品写入。</done>
</task>

<task type="auto">
  <name>Task 2: Cover each qualification gate and override behavior</name>
  <read_first>scripts/check_phase_execution_gate.py; .planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md; .planning/phases/25.1-analysis-chat-workspace-range-anchor/25.1-VERIFICATION.md; .planning/phases/25.2-embedded-novel-agent-runtime/25.2-VERIFICATION.md; .planning/phases/25.3-pi-package-compatibility-governance/25.3-VERIFICATION.md</read_first>
  <files>backend/tests/integration/queryplan/test_execution_preflight.py</files>
  <action>用临时 fixture 或注入的只读证据覆盖当前 blocked、Phase 22 少于 3/3、非 scheduled/非 green、Phase 25.1 缺失或非 passed、Phase 25.2 缺失或非 passed、Phase 25.3 缺失或非 passed、malformed YAML/字段和 planning override；另覆盖四组证据完整且 lineage 一致时返回 0。测试不得写入 QueryPlan、Agent Artifact、数据库或 active pointer。</action>
  <acceptance_criteria>每个单独门禁失败均返回稳定非零；仅 synthetic complete evidence 返回 0；planning override 永远不改变 verdict。</acceptance_criteria>
  <verify><automated>Push-Location backend
python -m pytest tests/integration/queryplan/test_execution_preflight.py -q
$testCode = $LASTEXITCODE
Pop-Location
exit $testCode</automated></verify>
  <done>Phase 22、25.1、25.2、25.3 的正反例和 override-inert 行为均有可执行测试。</done>
</task>
</tasks>

<tasks>
<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>scripts/check_phase_execution_gate.py; backend/tests/integration/queryplan/test_execution_preflight.py; .planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md; .planning/STATE.md</read_first>
  <files>scripts/check_phase_execution_gate.py; backend/tests/integration/queryplan/test_execution_preflight.py</files>
  <action>先运行 preflight 测试并修复解析、连续性、status、lineage 或 override 绕过；再直接运行当前仓库 gate，确认预期 blocked 路径以非零退出。若未来四组证据齐全，重复运行并确认返回 0；任何失败都保留非零结果，不得把 blocked 报告为通过。</action>
  <acceptance_criteria>测试套件通过；当前真实证据不足时 CLI 非零；完整 synthetic/qualified evidence 路径可重复返回 0，且两种结果都不写产品状态。</acceptance_criteria>
  <verify><automated>Push-Location backend
python -m pytest tests/integration/queryplan/test_execution_preflight.py -q
$testCode = $LASTEXITCODE
Pop-Location
if ($testCode -ne 0) { exit $testCode }
python scripts/check_phase_execution_gate.py
$gateCode = $LASTEXITCODE
if ($gateCode -eq 0) { Write-Error "Expected the current repository preflight to remain blocked"; exit 1 }
exit 0</automated></verify>
  <done>Preflight tests are green and the current repository proves fail-closed non-zero behavior without unlocking Phase 26.</done>
</task>
</tasks>

## Must-Haves

- The gate owns `scripts/check_phase_execution_gate.py` and its preflight tests; downstream plans only consume it.
- Phase 22 requires three real consecutive scheduled green observations with run/artifact/result lineage.
- Phase 25.1, 25.2 and 25.3 each require their own `VERIFICATION.md` with `status: passed`.
- Missing, malformed, non-consecutive, non-green, stale or override-only evidence fails closed with non-zero exit.
- No QueryPlan, world-model, narrative-memory, Agent Artifact, approval, publication or active-pointer write occurs in this plan.

## Verification

- Run the preflight integration tests.
- Run the CLI against the current repository and preserve its non-zero blocked result.
- Re-run with complete synthetic evidence and confirm zero only when all four qualification groups pass.

## Success Criteria

Phase 26-01 may start only after this plan's gate returns zero; planning authorization alone never unlocks execution.
