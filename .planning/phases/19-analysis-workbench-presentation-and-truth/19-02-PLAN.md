---
phase: 19-analysis-workbench-presentation-and-truth
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/src/components/timeline/timeline-chart.tsx
  - frontend/src/components/timeline/timeline-chart.test.tsx
  - frontend/src/app/analysis/page.tsx
requirements: [V09-TL-01, V09-TL-02]
autonomous: true
must_haves:
  truths:
    - "Drill-down timeline uses chapter-scaled X and event_type (or equivalent) Y swimlanes, not index%4 jitter as the primary layout."
    - "Axis labels communicate chapter progression, not only node index."
---

# 19-02 Timeline multi-lane chapter plot view

## Objective

Replace the “single horizontal jitter line” drill-down with a **novel plot timeline**: chapter ruler + type swimlanes.

## Steps

### Task 1: ECharts layout rewrite in `timeline-chart.tsx`
- X: `narrative_chapter_number` (+ micro offset by source_start/index within chapter)
- Y: fixed lanes for `plot | conflict | character | world`
- Hide y labels as type names; show chapter labels on x
- Keep density overview stages; drill-in uses swimlanes
- Labels: short title when ≤ threshold events in view; else hover
- Causal edges only when few events or causal toggle

### Task 2: Tests
- Update `timeline-chart.test.tsx` for stages + drill-in behavior
- Test, Fix, and Confirm

## Must-Haves

- Visually ≥2 distinct Y lanes when multiple event types present
- Not primary-dependent on `index % 4` scatter jitter

## Verification

```
cd frontend
npx vitest run src/components/timeline/timeline-chart.test.tsx src/app/analysis/page.test.tsx --reporter=dot
```
