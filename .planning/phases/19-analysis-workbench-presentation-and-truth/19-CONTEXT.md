# Phase 19 Context — Analysis Workbench Presentation & Truth

## Why this phase

Phases 08/09/11 shipped pipelines and basic UI, but product UX collapsed into similar “summary” surfaces:

- Timeline drill-down: ECharts index scatter with fake y-jitter (`index % 4`)
- Relationships: co-occurrence heuristics painted as ally/enemy fiction types
- Clues: title = raw cue text[:80]; no plant→payoff span; lifecycle underused

v0.8 is archived. This phase is a **new product surface + honesty contract** phase, not a reopen of 08/09/11.

## Non-goals

- Narrative-memory promotion / rebuild
- Full KG retrain
- Neo4j as authority
- New top-level nav / plot-theme-pace menus

## Depends on

Phase 07 hierarchy, 08 timeline, 09 relationships, 11 clues (consume contracts).

## Research anchors

- Audit: timeline-chart y-jitter; provisional `_infer_provisional_type`; clue worker title from cue text
- UX: Plottr plotlines, Milanote ego maps, setup/payoff dual-track
- Implementation plan: Slice A truth → B timeline → C relationships → D clues → E polish

## Success (product)

1. Relationships: 3s distinguish accepted vs provisional co-occurrence; default not flooded by guessed types
2. Timeline: multi-lane chapter plot view, not a single line of dots
3. Clues: plant→(reinforce)→payoff span visible; not timeline-like event strip
4. Spoiler cutoff + active/running_candidate unchanged in spirit
