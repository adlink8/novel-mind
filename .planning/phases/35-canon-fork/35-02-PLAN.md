---
phase: 35-canon-fork
plan: 02
type: execute
wave: 1
depends_on: [35-01]
files_modified:
  - backend/app/models/canon_fork.py
  - backend/app/services/canon_fork/snapshot.py
  - backend/app/services/canon_fork/lineage.py
  - backend/app/api/canon_fork.py
autonomous: true
requirements: [REQ-FORK-01, REQ-CRE-01]
must_haves:
  truths: [fork 创建时冻结 owner/novel/version/cutoff/source snapshot/hash, 未授权全书 cutoff 不能被客户端提升]
  artifacts: ["backend/app/services/canon_fork/snapshot.py", "backend/app/api/canon_fork.py"]
  key_links: ["require_user/novel scope -> server-derived cutoff -> immutable fork manifest"]
---
<objective>实现 Canon Fork 的 owner-scoped、snapshot-scoped、cutoff-scoped 创建与读取。</objective>
<execution_context>@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/35-canon-fork/35-CONTEXT.md
@.planning/phases/35-canon-fork/35-RESEARCH.md
@.planning/phases/35-canon-fork/35-PATTERNS.md</execution_context>
## Steps
<tasks>
<task type="auto">
<name>Task 1: Freeze fork snapshot and API</name>
<read_first>backend/app/api/dependencies.py; backend/app/services/narrative_memory/retrieval_manifests.py; backend/app/models/canon_space.py; .planning/phases/35-canon-fork/35-CONTEXT.md</read_first>
<files>backend/app/models/canon_fork.py, backend/app/services/canon_fork/snapshot.py, backend/app/services/canon_fork/lineage.py, backend/app/api/canon_fork.py</files>
<action>按 D-35-03 实现 POST/GET fork contracts；服务端从 owner/novel 与明确授权计算 cutoff，seal source snapshot/hash、原作 version 和 citation lineage，禁止客户端传入可扩大范围的 cutoff；所有 mutation 只创建 candidate fork，不创建或切换 production active pointer。</action>
<acceptance_criteria>同一输入得到相同 manifest hash；owner/novel 不匹配返回 404；full-book 需要显式授权且 scope 可审计。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/test_canon_fork.py -q</automated></verify>
<done>fork API 生成冻结 owner/novel/version/cutoff/source snapshot/hash/citation lineage 的 candidate，且不创建 active pointer。</done>
</task>
<task type="auto">
<name>Task 2: Test, Fix, and Confirm</name>
<read_first>backend/app/api/canon_fork.py; backend/tests/integration/test_canon_fork.py</read_first>
<files>backend/tests/integration/test_canon_fork.py, backend/tests/adversarial/test_canon_fork_scope.py</files>
<action>覆盖跨 owner、跨 novel、未来 cutoff、source hash 变化、重复 fork、原作写入和 active-pointer 请求；修复所有非 fail-closed 响应。</action>
<acceptance_criteria>scope/cutoff/hash 任一不一致都阻断，创建成功只返回 immutable candidate fork。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/test_canon_fork.py tests/adversarial/test_canon_fork_scope.py -q</automated></verify>
<done>跨 owner/novel、未来 cutoff、stale hash、Original 写入和 active-pointer 请求均 fail closed 并保留 reason。</done>
</task>
</tasks>
## Must-Haves

## Verification

<verification>运行 API、scope、snapshot hash 和 no-pointer 测试。</verification>
<success_criteria>后续编辑、生成、视觉和导出只能消费可验证 fork manifest。</success_criteria>
<must_haves>owner/version/cutoff/source snapshot/hash/citation 全部冻结；approval 与 candidate-only 保持有效。</must_haves>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser -> fork API | 客户端可能伪造 owner/cutoff/version |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-35-02-01 | Elevation of Privilege | canon_fork API | mitigate | server-derived scope and owner dependency |
| T-35-02-02 | Information Disclosure | cutoff resolver | mitigate | reject future/unread rows before materialization |
| T-35-02-SC | Tampering | packages | mitigate | no install; lockfile audit |
</threat_model>
<output>Execution may create 35-02-SUMMARY.md; planner does not.</output>
