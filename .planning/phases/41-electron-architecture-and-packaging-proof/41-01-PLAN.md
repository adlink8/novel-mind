---
phase: 41-electron-architecture-and-packaging-proof
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - desktop/proof/package.json
  - desktop/proof/src/topology.ts
  - desktop/proof/src/main.ts
  - desktop/proof/tests/topology.test.ts
  - desktop/proof/README.md
autonomous: false
requirements: [REQ-DESK-04]
must_haves:
  truths:
    - "D-41-04/D-41-05: proof topology names every runtime executable, endpoint, readiness probe, writable path and shutdown owner."
    - "Adding Electron or packaging dependencies is blocked until the user approves the selected production dependency set."
  artifacts:
    - path: desktop/proof/src/topology.ts
      provides: typed proof-only process topology
    - path: desktop/proof/tests/topology.test.ts
      provides: fail-closed topology contract tests
  key_links:
    - from: desktop/proof/src/main.ts
      to: desktop/proof/src/topology.ts
      via: proof harness consumes one validated topology
---

<objective>建立可丢弃的 Electron/本地运行时证明骨架与 fail-closed 拓扑契约，不实现正式桌面产品。</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/41-electron-architecture-and-packaging-proof/41-CONTEXT.md
@.planning/phases/41-electron-architecture-and-packaging-proof/41-RESEARCH.md
@frontend/package.json
@agent-service/package.json
@Makefile
@docker-compose.yml
</execution_context>

## Steps

<tasks>
<task type="checkpoint:decision" gate="blocking">
<name>Task 1: Approve the proof dependency set</name>
<read_first>frontend/package.json; agent-service/package.json; .planning/phases/41-electron-architecture-and-packaging-proof/41-RESEARCH.md</read_first>
<files>desktop/proof/package.json; desktop/proof/README.md</files>
<action>Compare current supported Electron and one Windows packaging candidate against Node >=22.19, Next canary and Windows-only proof needs. Record exact versions, license/source, why each dependency is necessary and what existing capability is insufficient. Stop for user approval before installing or locking any new production dependency.</action>
<acceptance_criteria>`desktop/proof/README.md` names exact candidates and the execution transcript contains explicit dependency approval before package installation.</acceptance_criteria>
<verify><automated>if (-not (Select-String -Path desktop/proof/README.md -Pattern 'Electron|Windows|Node >=22.19' -Quiet)) { exit 1 }</automated></verify>
<done>The minimal proof dependency set is evidence-backed and explicitly authorized.</done>
</task>
<task type="auto">
<name>Task 2: Define the proof topology and fail-closed harness</name>
<read_first>Makefile; docker-compose.yml; backend/app/main.py; agent-service/package.json; frontend/package.json; desktop/proof/README.md</read_first>
<files>desktop/proof/src/topology.ts; desktop/proof/src/main.ts; desktop/proof/tests/topology.test.ts</files>
<action>Create typed descriptors for `next`, `fastapi`, `agent_service`, `postgres_pgvector` and `vector_store` with executable source, arguments, loopback endpoint allocation, dependency list, readiness probe, immutable resource root, writable app-data root, log sink and shutdown owner. Reject missing fields, fixed packaged ports, non-loopback binds and writable install paths before any process starts.</action>
<acceptance_criteria>Topology tests reject every omitted component/owner/probe and accept one complete five-component graph; the proof main process has no domain or database API.</acceptance_criteria>
<verify><automated>cd desktop/proof; npm test -- --run tests/topology.test.ts</automated></verify>
<done>The proof cannot start from an incomplete or unsafe runtime topology.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/proof/src/topology.ts; desktop/proof/src/main.ts; desktop/proof/tests/topology.test.ts</read_first>
<files>desktop/proof/src/topology.ts; desktop/proof/src/main.ts; desktop/proof/tests/topology.test.ts; desktop/proof/README.md</files>
<action>Run typecheck and topology tests, add missing negative cases for fixed ports, external bind, install-directory writes and unknown components, then confirm the harness remains proof-only and disposable.</action>
<acceptance_criteria>Typecheck and tests pass twice; every unsafe topology case fails before spawn.</acceptance_criteria>
<verify><automated>cd desktop/proof; npm run typecheck; npm test -- --run tests/topology.test.ts</automated></verify>
<done>Proof topology and dependency gate are deterministic and fail closed.</done>
</task>
</tasks>

## Must-Haves

- D-41-04, D-41-05 and dependency approval are explicit.
- No product UI or backend domain logic moves into the proof harness.

## Verification

Run proof package typecheck and topology contract/negative tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| packaged topology -> spawned process | Executable/path/env data can launch unintended code or expose services |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-41-01-01 | Elevation of Privilege | topology launcher | mitigate | allowlisted components/executables and loopback-only binds |
| T-41-01-02 | Tampering | writable paths | mitigate | reject install-root writes; use proof app-data root |
</threat_model>

