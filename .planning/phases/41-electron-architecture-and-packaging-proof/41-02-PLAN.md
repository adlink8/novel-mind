---
phase: 41-electron-architecture-and-packaging-proof
plan: "02"
type: execute
wave: 1
depends_on: ["41-01"]
files_modified:
  - frontend/next.config.mjs
  - desktop/proof/scripts/build-next-standalone.ps1
  - desktop/proof/src/next-server.ts
  - desktop/proof/tests/route-parity.spec.ts
  - desktop/proof/tests/route-inventory.json
autonomous: true
requirements: [REQ-DESK-04]
must_haves:
  truths:
    - "D-41-02/D-41-06: the existing Next renderer and all 13 routes are the proof target."
    - "Standalone proof explicitly copies public and .next/static and fails on missing assets."
  artifacts:
    - path: desktop/proof/tests/route-inventory.json
      provides: frozen 13-route proof inventory
    - path: desktop/proof/tests/route-parity.spec.ts
      provides: route/render/navigation evidence
  key_links:
    - from: frontend/.next/standalone/server.js
      to: desktop/proof/src/next-server.ts
      via: loopback managed child process
---

<objective>证明现有 Next standalone 构建可由 Electron 本地回环启动，并保持 13 路由、静态资产和客户端导航完整。</objective>

<execution_context>@.planning/phases/41-electron-architecture-and-packaging-proof/41-CONTEXT.md
@.planning/phases/41-electron-architecture-and-packaging-proof/41-RESEARCH.md
@frontend/next.config.mjs
@frontend/src/app
@desktop/proof/src/topology.ts</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Build a complete Next standalone proof artifact</name>
<read_first>frontend/next.config.mjs; frontend/package.json; desktop/proof/src/topology.ts; .planning/phases/41-electron-architecture-and-packaging-proof/41-RESEARCH.md</read_first>
<files>frontend/next.config.mjs; desktop/proof/scripts/build-next-standalone.ps1; desktop/proof/src/next-server.ts</files>
<action>First reconcile and preserve the pre-existing dirty change in `frontend/next.config.mjs`. Enable the narrow standalone proof, build from `frontend`, copy `public` and `.next/static` into `.next/standalone`, start `server.js` on an allocated loopback port, capture stdout/stderr, wait for HTTP readiness and terminate the owned process on timeout/exit.</action>
<acceptance_criteria>The standalone tree starts without `npm install` in the artifact, serves `/_next/static` and public assets, uses no fixed packaged port and leaves no child process after shutdown.</acceptance_criteria>
<verify><automated>powershell -File desktop/proof/scripts/build-next-standalone.ps1 -VerifyOnly</automated></verify>
<done>Next standalone is complete, observable and lifecycle-owned.</done>
</task>
<task type="auto">
<name>Task 2: Freeze and verify the 13-route inventory</name>
<read_first>frontend/src/app; desktop/proof/src/next-server.ts; frontend/tests</read_first>
<files>desktop/proof/tests/route-inventory.json; desktop/proof/tests/route-parity.spec.ts</files>
<action>Derive the 13 current page routes from `frontend/src/app`, freeze expected path/parameter fixtures, then test HTTP response, visible route root, critical static assets and at least one client navigation transition per route group through the Electron proof window. Fail when the discovered inventory count differs from 13 until the roadmap decision is explicitly revised.</action>
<acceptance_criteria>Exactly 13 inventory entries render through the standalone server; missing route, JS/CSS asset, hydration or client navigation fails the suite.</acceptance_criteria>
<verify><automated>cd desktop/proof; npx playwright test tests/route-parity.spec.ts</automated></verify>
<done>All existing renderer routes are proven without a parallel UI.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/proof/tests/route-parity.spec.ts; desktop/proof/tests/route-inventory.json; desktop/proof/scripts/build-next-standalone.ps1</read_first>
<files>frontend/next.config.mjs; desktop/proof/scripts/build-next-standalone.ps1; desktop/proof/src/next-server.ts; desktop/proof/tests/route-parity.spec.ts</files>
<action>Build twice from a clean frontend output, run route parity twice, kill the server during a run, and fix nondeterministic asset/path/shutdown behavior. Confirm browser development still starts with existing rewrites.</action>
<acceptance_criteria>Two clean standalone builds and parity runs pass; forced child failure is reported and dev-mode smoke remains green.</acceptance_criteria>
<verify><automated>powershell -File desktop/proof/scripts/build-next-standalone.ps1 -VerifyOnly; cd desktop/proof; npx playwright test tests/route-parity.spec.ts</automated></verify>
<done>Standalone route parity is reproducible and development mode is preserved.</done>
</task>
</tasks>

## Must-Haves

- Existing Next/React routes remain the only business UI.
- Dirty `frontend/next.config.mjs` changes are reconciled, never overwritten.

## Verification

Build the standalone tree and run 13-route Electron proof parity twice.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| standalone server -> renderer | Missing or substituted assets can change executed UI code |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-41-02-01 | Tampering | standalone assets | mitigate | deterministic copy and route/asset assertions |
| T-41-02-02 | Denial of Service | server lifecycle | mitigate | bounded readiness and owned shutdown |
</threat_model>

