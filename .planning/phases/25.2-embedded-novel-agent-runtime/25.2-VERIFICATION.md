---
phase: 25.2
slug: embedded-novel-agent-runtime
status: passed
verified_at: 2026-08-02
source_commit: 6988ceb
---

# Phase 25.2 — Verification

> Independent evidence that Phase 25.2 (Embedded Novel Agent Runtime) implementation
> satisfies its plans. Every claim below was executed in this repository on 2026-08-02.

## Execution Context

- Execution override: user-authorized 2026-08-02 (Phase 22 3/3 gate skipped; recorded in
  `.planning/STATE.md` + `config.json` `gate_overrides.phase_25.2_execution`).
- Baseline: master `418a120` (Pi runtime planning merged 2026-08-02).
- All 8 plans of Phase 25.2 (00, 01, 02, 03, 04, 05, 06, 07) completed with SUMMARYs.

## Verification Results

| Plan | What was verified | Command | Result |
|---|---|---|---|
| 25.2-01 spike | Pi SDK 0.83.0 no-tools / custom tool / loader closure / lifecycle / events / storage seam / skill injection | `node spikes/0[1-7]*.mjs` | 7/7 ALL PASS; A1=NO SEAM, A2=PROVEN |
| 25.2-01 scaffold | agent-service package + vitest | `cd agent-service && npx vitest run` | 66 passed (incl. scaffold) |
| 25.2-02 facade | 7-tool contract + error codes | `cd backend && python -m pytest tests/contract -q` | 89 passed |
| 25.2-02 adversarial | every tool x every fail-closed axis | `cd backend && python -m pytest tests/adversarial/test_agent_tools_adversarial.py -q` | 47 passed |
| 25.2-02 unit sweep | no regressions | `cd backend && python -m pytest tests/unit -q` | 452 passed |
| 25.2-03 migration | Alembic 26agentrun01 up/down cycle | `python -m alembic heads` + `pytest tests/integration/agent_runtime/test_migration.py` | `26agentrun01 (head)`; 5 passed |
| 25.2-03 runtime | registry fail-closed / finalize / cancel-no-write / status transitions / e2e replay | `cd backend && python -m pytest tests/integration/agent_runtime -q` | 15 passed |
| 25.2-03 skill assets | D-09 fields / NM excluded / schemas valid | yaml+json structural check | PASS |
| 25.2-07 skill pkg | skill-local tests | `cd agent-service && npx vitest run tests/skills/answer-reading-question.test.ts` | 29 passed |
| 25.2-05 agent service | registry/session-factory/loader/server | `cd agent-service && npx vitest run` | 66 passed; tsc clean |
| 25.2-04 frontend | sse.ts + panel + tab | `cd frontend && npm run test -q` | 266 passed |
| CI policy | policy suite | `cd backend && python -m pytest tests/ci -q` | 22 passed |

## Known Non-Blocking Items

- 3 backend tests fail in the full sweep (canon_space `search_units` missing `space` param,
  `retrieval_policy` `is` identity assertion, `security` upload isolation) — verified
  pre-existing, unrelated to Phase 25.2 (bidirectional exclusion test).
- 29 frontend typecheck errors in `creative-project-editor.tsx` +
  `reader-chat-budget-section.tsx` — local untracked work-in-progress from other phases,
  not in git HEAD, zero errors in Phase 25.2 files.
- Live three-process UAT (real provider key question → agent answer → citation jump) not
  executed — requires a provider key; all layers verified via mocks/stubs/fixtures.

## Conclusion

Phase 25.2 implementation is verified against its plans. Verdict: **passed**.
Phase 25.3 may proceed per its entry gate.
