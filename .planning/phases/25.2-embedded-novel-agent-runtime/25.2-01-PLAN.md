---
phase: 25.2-embedded-novel-agent-runtime
plan: 01
type: execute
wave: 1
depends_on: ["25.2-00"]
files_modified:
  - agent-service/package.json
  - agent-service/tsconfig.json
  - agent-service/vitest.config.ts
  - agent-service/src/config.ts
  - agent-service/spikes/01-no-tools-enumeration.mjs
  - agent-service/spikes/02-custom-tool-roundtrip.mjs
  - agent-service/spikes/03-resource-loader-allowlist.mjs
  - agent-service/spikes/04-session-lifecycle.mjs
  - agent-service/spikes/05-streaming-events.mjs
  - agent-service/spikes/06-storage-injection-seam.mjs
  - agent-service/spikes/07-skill-instruction-injection.mjs
  - .planning/spikes/embedded-novel-agent/CONTEXT.md
  - .planning/spikes/embedded-novel-agent/EXPERIMENTS.md
  - .planning/spikes/embedded-novel-agent/FINDINGS.md
autonomous: false
requirements: [REQ-AGENT-01]

must_haves:
  truths:
    - "Pi default coding tools (bash/read/edit/write/grep/find/ls) are provably absent from every created session (D-05)"
    - "A custom NovelMind domain tool can be registered and round-trips a call with typed parameters (D-06 seam)"
    - "ResourceLoader loads only allowlisted resources — zero ambient skills/extensions/prompts discovered (D-18)"
    - "Session create/resume/cancel/retry all work via abort()/continue() with stopReason surfaced (D-19 runtime seam)"
    - "Pi events (message_update, tool_execution_*, turn_end, agent_end) are capturable for SSE serialization"
    - "The custom SessionStorage injection question (A1) is answered with recorded evidence, or the documented fallback (in-memory Pi session + NovelMind-owned run state) is confirmed viable"
    - "Skill instructions can be injected without the disabled read tool (Pitfall 3 settled)"
  artifacts:
    - path: "agent-service/package.json"
      provides: "pinned agent-service scaffold"
      contains: "@earendil-works/pi-coding-agent"
    - path: "agent-service/vitest.config.ts"
      provides: "self-contained node test config"
      contains: "environment"
    - path: ".planning/spikes/embedded-novel-agent/EXPERIMENTS.md"
      provides: "one recorded experiment per capability"
      min_lines: 60
  key_links:
    - from: "agent-service/spikes/01-no-tools-enumeration.mjs"
      to: "@earendil-works/pi-coding-agent@0.83.0 createAgentSession"
      via: "noTools: all session creation"
      pattern: "noTools"
    - from: "agent-service/spikes/06-storage-injection-seam.mjs"
      to: "pi-agent-core SessionStorage interface"
      via: "custom storage wiring attempt + fallback proof"
      pattern: "SessionStorage"
---

<objective>
Prove that the Pi SDK (0.83.0, pinned) can be embedded in a standalone Node `agent-service/` with
NovelMind's constraints: no default coding tools (D-05), custom domain tools only (D-06), fully
controlled ResourceLoader (D-18), session create/resume/cancel/retry, streaming events, and a
settled answer on the custom `SessionStorage` injection seam (open question A1) with the documented
fallback (in-memory Pi session + NovelMind-owned run state, D-11).

Purpose: every later plan (25.2-03/04) builds on these seams; an unverified assumption here
invalidates the whole phase. This plan is a spike — outputs are recorded experiments and findings,
not production code. The authoritative go/no-go and storage decision is owned by 25.2-06. No write
path is exercised (D-22).

Output: `agent-service/` scaffold + spike scripts; `.planning/spikes/embedded-novel-agent/` with
CONTEXT.md, EXPERIMENTS.md and FINDINGS.md. The runtime decision is owned by 25.2-06.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-CONTEXT.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md

Execution gate (D-21): this plan MUST NOT start before Phase 22 records three consecutive
scheduled green observations per `.planning/STATE.md`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold agent-service with pinned Pi packages</name>
  <files>agent-service/package.json, agent-service/tsconfig.json, agent-service/vitest.config.ts, agent-service/src/config.ts</files>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Standard Stack, Package Legitimacy Audit, Pattern 1)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md (sections: agent-service/package.json, agent-service/vitest.config.ts)
    - frontend/package.json (house conventions: private, pinned dev tooling)
    - frontend/vitest.config.ts (vitest config analog, lines 14-23)
  </read_first>
  <action>
    Create `agent-service/` as a new self-contained package (do NOT wire it into the frontend or
    backend tooling). package.json: `"private": true`, `"type": "module"`,
    `"engines": { "node": ">=22.19.0" }`, scripts `dev` (node --watch src/server entry placeholder
    is NOT created — spike scripts only), `test` = `vitest run`. Install EXACT pins:
    `@earendil-works/pi-coding-agent@0.83.0`, `@earendil-works/pi-agent-core@0.83.0`,
    `@earendil-works/pi-ai@0.83.0`, plus `typebox@1.3.7`; dev deps `typescript`, `vitest` (pin
    exact, no `latest`, no `^` on Pi packages). Before install re-run
    `npm view <pkg> version time.modified engines scripts.postinstall` for all three Pi packages
    and record the output in EXPERIMENTS.md (re-verify per RESEARCH "Version verification"; abort
    if any version is not 0.83.0 or a postinstall script appeared). Do NOT install
    `@earendil-works/pi-storage-sqlite-node` yet — it is [SUS] and gated by Task 3.
    tsconfig.json: `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `target: "ES2023"`,
    `strict: true`, `outDir: "dist"`. vitest.config.ts: `environment: "node"`,
    include `tests/**/*.test.ts` and `spikes/**/*.test.mjs`, `testTimeout: 30000`,
    no jsdom, no react plugin (per PATTERNS analog diff). src/config.ts: read env
    `FASTAPI_BASE_URL` (default `http://127.0.0.1:8000`), `NOVELMIND_GATEWAY_TOKEN` (no default —
    fail fast if required and absent), `PORT` (default `3100`), exported as a frozen config object;
    never log token values (Security Domain V6).
  </action>
  <acceptance_criteria>
    - `cd agent-service && node -e "import('@earendil-works/pi-coding-agent').then(m=>console.log(typeof m.createAgentSession))"` prints `function`
    - `agent-service/package.json` contains the exact strings `"@earendil-works/pi-coding-agent": "0.83.0"`, `"@earendil-works/pi-agent-core": "0.83.0"`, `"@earendil-works/pi-ai": "0.83.0"` and does NOT contain `pi-storage-sqlite-node`
    - `agent-service/package.json` contains `"node": ">=22.19.0"` and `"type": "module"`
    - `cd agent-service && npx vitest run` exits 0 (empty suite passes or scaffold smoke test passes)
    - EXPERIMENTS.md contains the `npm view` re-verification output for all three core packages
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npm install && npx vitest run && node -e "import('@earendil-works/pi-coding-agent').then(m=>{if(typeof m.createAgentSession!=='function')process.exit(1)})"</automated>
  </verify>
  <done>Scaffold installs cleanly on Node v24, Pi 0.83.0 imports resolve, vitest runs, no SUS package present</done>
</task>

<task type="auto">
  <name>Task 2: Spike experiments — tools, loader, lifecycle, streaming, skill injection</name>
  <files>agent-service/spikes/01-no-tools-enumeration.mjs, agent-service/spikes/02-custom-tool-roundtrip.mjs, agent-service/spikes/03-resource-loader-allowlist.mjs, agent-service/spikes/04-session-lifecycle.mjs, agent-service/spikes/05-streaming-events.mjs, agent-service/spikes/07-skill-instruction-injection.mjs, .planning/spikes/embedded-novel-agent/CONTEXT.md, .planning/spikes/embedded-novel-agent/EXPERIMENTS.md</files>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Patterns 1, 3, 5; Pitfall 3; Anti-Patterns)
    - agent-service/package.json (created in Task 1)
    - agent-service/src/config.ts (created in Task 1)
  </read_first>
  <action>
    Write `.planning/spikes/embedded-novel-agent/CONTEXT.md` first: spike goal, D-05/D-06/D-18/D-19
    constraints, capability matrix, timebox note. Then write one runnable spike script per
    capability (`.mjs`, runnable via `node spikes/NN-name.mjs`, each prints a PASS/FAIL line per
    assertion and exits non-zero on FAIL). Model access: direct Pi built-in provider with whatever
    dev key is available is acceptable INSIDE the spike only (D-15); where no live model is
    available, scripts must still prove the SDK wiring (session creation, tool registration,
    resource enumeration, event subscription) and mark live-model assertions as SKIP with reason.
    Experiments:
    - 01-no-tools-enumeration: create session with `noTools: "all"` + `customTools` + explicit
      `tools` allowlist (RESEARCH Pattern 1); enumerate the tools the session actually exposes and
      assert none of `bash`, `read`, `edit`, `write`, `grep`, `find`, `ls` is present; attempt a
      `read` tool call path and assert it is rejected.
    - 02-custom-tool-roundtrip: `defineTool` with TypeBox `Type.Object` params for a stub
      `get_chapter` (returns canned JSON, no FastAPI call needed); assert registration, typed
      param validation (reject wrong-typed params), and execute round trip.
    - 03-resource-loader-allowlist: `DefaultResourceLoader` with `systemPromptOverride`,
      `skillsOverride`, `agentsFilesOverride` and inline `extensionFactories` (RESEARCH Pattern 3);
      enumerate every discovery surface the loader still touches (skills, prompts, themes,
      extensions, context files) and assert the loaded set equals the allowlist exactly; if any
      surface cannot be overridden, point `agentDir` at an empty controlled temp directory and
      record that as the mitigation (assumption A2 must end PROVEN or MITIGATED, not open).
    - 04-session-lifecycle: create session, `session.prompt()` (or dry equivalent), `session.abort()`,
      assert final assistant message stopReason is `aborted`; `agent.continue()` resume; assert
      cancel stops in-flight tool execution via the AbortSignal passed to `execute`.
    - 05-streaming-events: `session.subscribe(...)` capture of `message_update`,
      `tool_execution_start`, `tool_execution_end`, `turn_end`, `agent_end`; assert ordering and
      that the curated subset maps to the SSE frame shapes in RESEARCH Pattern 5.
    - 07-skill-instruction-injection: with `noTools: "all"` (so Pi's `read`-based skill loading is
      dead, Pitfall 3), inject skill instructions via system prompt / `before_agent_start`-style
      message and assert the session behavior reflects them (trace shows skill-specific
      instruction present in context).
    Record every experiment in EXPERIMENTS.md: capability, script, assertions, PASS/FAIL/SKIP,
    evidence excerpt. One recorded experiment per capability is a roadmap Must-Have.
  </action>
  <acceptance_criteria>
    - `ls agent-service/spikes/` contains 01, 02, 03, 04, 05, 07 scripts (06 is Task 4)
    - `cd agent-service && node spikes/01-no-tools-enumeration.mjs` exits 0 and its output contains PASS lines for each of bash/read/edit/write/grep/find/ls absence
    - `cd agent-service && node spikes/03-resource-loader-allowlist.mjs` exits 0; EXPERIMENTS.md records A2 as PROVEN or MITIGATED with the empty-agentDir evidence
    - `.planning/spikes/embedded-novel-agent/EXPERIMENTS.md` has one section per capability (7 sections minimum at this point: npm re-verify + 6 experiments) each with a PASS/FAIL/SKIP verdict
    - EXPERIMENTS.md contains no phrase "will be tested later" or "assumed working"
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && for f in spikes/0[123457]*.mjs; do node "$f" || exit 1; done</automated>
  </verify>
  <done>Six capabilities recorded with verdicts; A2 (ambient-surface closure) is PROVEN or MITIGATED; no capability left unrecorded</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify [SUS] package @earendil-works/pi-storage-sqlite-node before install</name>
  <files>agent-service/package.json</files>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Package Legitimacy Audit — slopcheck [SUS] FRESH_PACKAGE verdict)
  </read_first>
  <what-built>
    All spike work so far used only the three [VERIFIED] core packages. The next experiment
    (session-restore validation, D-17) requires `@earendil-works/pi-storage-sqlite-node@0.83.0`,
    which slopcheck flagged [SUS] (published ~10 days before research date). Per the package
    legitimacy gate, a human must verify it before install. D-17 restricts it to spike use only.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@earendil-works/pi-storage-sqlite-node — confirm version
       0.83.0 exists, publisher/repository links to github.com/earendil-works/pi (the same repo
       the phase's canonical refs cite).
    2. Run `npm view @earendil-works/pi-storage-sqlite-node@0.83.0 time.modified engines scripts` —
       confirm no `postinstall`/lifecycle scripts and `engines.node >=22.19.0`.
    3. Confirm it will be used ONLY for spike session-restore experiments (D-17) and will not ship
       in the formal service (25.2-03+ uses the PG adapter or in-memory fallback).
  </how-to-verify>
  <action>
    Present the three verification points above and wait for the human verdict. On "approved":
    install the exact pin `@earendil-works/pi-storage-sqlite-node@0.83.0` into agent-service and
    proceed to Task 4 experiment (a). On "rejected": do not install; Task 4 marks experiment (a)
    SKIP with reason and proves the in-memory fallback path only. This checkpoint is never
    auto-approvable (package legitimacy gate; workflow.auto_advance ignored).
  </action>
  <acceptance_criteria>
    - Human verdict recorded ("approved" or "rejected") before any install of the SUS package
    - If approved: `npm view @earendil-works/pi-storage-sqlite-node@0.83.0 scripts` output contains no postinstall; agent-service/package.json gains the exact pin `"@earendil-works/pi-storage-sqlite-node": "0.83.0"`
    - If rejected: agent-service/package.json does NOT contain `pi-storage-sqlite-node` and Task 4 records experiment (a) as SKIP with reason
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npm view @earendil-works/pi-storage-sqlite-node@0.83.0 version engines scripts</automated>
  </verify>
  <resume-signal>Type "approved" to install and continue, or "rejected" to skip sqlite experiments (Task 4 then runs the in-memory fallback path only)</resume-signal>
  <done>Human verdict recorded; SUS package either verified-and-pinned or excluded, with Task 4 path determined</done>
</task>

<task type="auto">
  <name>Task 4: Storage-seam experiment — sqlite session-restore, custom SessionStorage injection, fallback proof</name>
  <files>agent-service/spikes/06-storage-injection-seam.mjs, .planning/spikes/embedded-novel-agent/FINDINGS.md</files>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Pattern 4, Pitfall 2, assumption A1, Open Question 1)
    - agent-service/spikes/04-session-lifecycle.mjs (session creation pattern from Task 2)
    - .planning/spikes/embedded-novel-agent/EXPERIMENTS.md (Task 2 verdicts)
  </read_first>
  <action>
    Write `spikes/06-storage-injection-seam.mjs` with three sub-experiments, each PASS/FAIL/SKIP:
    (a) IF Task 3 was approved: install `@earendil-works/pi-storage-sqlite-node@0.83.0` (exact
    pin, recorded in package.json), create a session on the sqlite backend, prompt, kill the
    process, re-open the session in a new process, assert the transcript/leaf state resumes —
    run TWICE and diff the two traces (Nyquist sample per VALIDATION.md). If rejected: mark (a)
    SKIP with reason. (b) Custom `SessionStorage` injection: implement the pi-agent-core
    `SessionStorage` interface surface (getMetadata, getLeafId, setLeafId, createEntryId,
    appendEntry, getEntry, findEntries, getLabel, getSessionName, getSessionStats,
    getPathToRootOrCompaction, getEntries, cleanup) as a minimal in-memory class and attempt to
    wire it into `createAgentSession` through every documented seam (SessionManager factories,
    services object, subclass); record the exact seam that works, or FAIL with the type/runtime
    errors observed (this settles assumption A1 with evidence). (c) Fallback viability proof: run
    a session with the in-memory backend, capture the run's events, and demonstrate that
    NovelMind-owned run state (run id, status, input, final answer text, tool-call list) can be
    reconstructed from the event stream alone — i.e. D-11/D-12 hold even if Pi session memory is
    discarded. Then write FINDINGS.md: per-capability findings, A1 verdict (SEAM FOUND: <name> /
    NO SEAM — fallback adopted), A2 verdict, abort-propagation observation (A3), and the
    implications for 25.2-03 (whether `agent_session_entries` PG table and
    `pg-session-storage.ts` are needed, or in-memory + `skill_runs` suffices).
  </action>
  <acceptance_criteria>
    - `cd agent-service && node spikes/06-storage-injection-seam.mjs` exits 0 with all three sub-experiments recorded (a may be SKIP only if Task 3 was rejected)
    - FINDINGS.md contains an explicit A1 verdict line matching `A1: (SEAM FOUND|NO SEAM)` with evidence
    - If Task 3 approved: package.json contains `"@earendil-works/pi-storage-sqlite-node": "0.83.0"` and FINDINGS.md contains the two-run session-restore diff result
    - FINDINGS.md contains a fallback viability verdict for D-11/D-12
    - EXPERIMENTS.md has a recorded section for experiment 06
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && node spikes/06-storage-injection-seam.mjs && grep -E "A1: (SEAM FOUND|NO SEAM)" ../.planning/spikes/embedded-novel-agent/FINDINGS.md</automated>
  </verify>
  <done>A1 settled with evidence; session-restore validated (or fallback-only path confirmed); FINDINGS.md written</done>
</task>

<task type="auto">
  <name>Task 5: Test, Fix, and Confirm — replay scenarios and close spike evidence</name>
  <files>.planning/spikes/embedded-novel-agent/EXPERIMENTS.md, .planning/spikes/embedded-novel-agent/FINDINGS.md, agent-service/spikes/</files>
  <read_first>
    - .planning/spikes/embedded-novel-agent/EXPERIMENTS.md
    - .planning/spikes/embedded-novel-agent/FINDINGS.md
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-VALIDATION.md (per-plan sampling for 25.2-01)
  </read_first>
  <action>
    Re-run ALL spike scripts end-to-end from a clean state (`npm ci` in agent-service) and replay
    the session scenarios: create→abort→continue, cancel mid-tool-call, and (if approved) the
    sqlite session-restore twice with trace diff. Fix any propagation gaps found (e.g. abort not
    reaching tool `execute`, events missing from the curated SSE subset, ResourceLoader leaking an
    ambient surface) and re-record affected experiments. Finalize EXPERIMENTS.md and FINDINGS.md
    with the capability matrix, A1/A2 evidence, package pins, the explicit statement that no write
    path and no default coding tool was exercised, and constraints handed to 25.2-03/04 (e.g. skill
    instructions MUST be injected, never discovered). Do not create DECISION.md here; 25.2-06 owns
    the human decision. If any capability cannot be made to PASS, the plan does not complete —
    escalate instead of papering over.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npm ci && for f in spikes/*.mjs; do node "$f" || exit 1; done` exits 0
    - EXPERIMENTS.md verdicts are all PASS or SKIP-with-reason after the clean re-run
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npm ci && for f in spikes/*.mjs; do node "$f" || exit 1; done && grep -E "A1: (SEAM FOUND|NO SEAM)" ../.planning/spikes/embedded-novel-agent/FINDINGS.md</automated>
  </verify>
  <done>Clean-room replay green; experiment and finding evidence are complete for 25.2-06 decision input</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → agent-service | Third-party Pi packages installed into a new runtime (supply chain) |
| agent-service → LLM provider (spike) | Direct provider keys used inside spike only (D-15 exception); keys must never be committed or logged |
| spike scripts → host | Pi normally ships bash/file tools; D-05 must hold or the spike runtime can execute arbitrary commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.2-01-01 | Elevation of Privilege | agent session default tools | mitigate | Experiment 01 enumerates exposed tools and asserts bash/read/edit/write/grep/find/ls absent; `noTools: "all"` + explicit `tools` allowlist per RESEARCH Pattern 1 |
| T-25.2-01-02 | Elevation of Privilege | ResourceLoader ambient discovery | mitigate | Experiment 03 enumerates every discovery surface; overrides replaced; empty controlled `agentDir` fallback; A2 must end PROVEN or MITIGATED (D-18) |
| T-25.2-01-03 | Information Disclosure | spike provider keys | mitigate | Keys via env only; `src/config.ts` never logs token values; no key written into EXPERIMENTS/FINDINGS |
| T-25.2-01-04 | Tampering | session-restore trace | mitigate | Restore experiment run twice with trace diff (Nyquist) to detect nondeterministic/forged resume state |
| T-25.2-SC | Tampering | npm installs | mitigate | Exact pins + `npm view` re-verification before install; slopcheck [SUS] `@earendil-works/pi-storage-sqlite-node` gated by Task 3 blocking human checkpoint (never auto-approvable) |
</threat_model>

<verification>
- `cd agent-service && npm ci && npx vitest run` exits 0
- `cd agent-service && for f in spikes/*.mjs; do node "$f" || exit 1; done` exits 0
- `.planning/spikes/embedded-novel-agent/` contains CONTEXT.md, EXPERIMENTS.md and FINDINGS.md; DECISION.md is created by 25.2-06
- EXPERIMENTS.md: one recorded experiment per capability (npm re-verify, 01-07)
- FINDINGS.md: explicit A1/A2 verdicts and storage-seam evidence for 25.2-06
- No production write path exercised anywhere in the spike
</verification>

<success_criteria>
- REQ-AGENT-01 proven: sessions expose only registered NovelMind tools; coding tools absent
- A1 (storage seam) and A2 (loader closure) settled with recorded evidence
- Spike evidence is frozen and ready for 25.2-06 to record the human-selected go/no-go
</success_criteria>

<output>
Create `.planning/phases/25.2-embedded-novel-agent-runtime/25.2-01-SUMMARY.md` when done
</output>
