---
phase: 25.2-embedded-novel-agent-runtime
plan: 05
type: execute
wave: 5
depends_on: ["25.2-02", "25.2-03", "25.2-07"]
files_modified:
  - agent-service/package.json
  - agent-service/src/server.ts
  - agent-service/src/agent/session-factory.ts
  - agent-service/src/agent/resource-loader.ts
  - agent-service/src/agent/provider.ts
  - agent-service/src/tools/registry.ts
  - agent-service/src/tools/fastapi-client.ts
  - agent-service/src/transport/sse.ts
  - agent-service/src/storage/pg-session-storage.ts
  - agent-service/src/skills/loader.ts
  - agent-service/tests/registry.test.ts
  - agent-service/tests/session-factory.test.ts
  - agent-service/tests/skills-loader.test.ts
  - agent-service/tests/server.test.ts
autonomous: true
requirements: [REQ-AGENT-01]

must_haves:
  truths:
    - "Every production session is created with noTools 'all' + customTools + explicit tools allowlist and a fully-overridden ResourceLoader — zero coding tools, zero ambient resources, productionizing the 25.2-01 spike constraints (D-05/D-18)"
    - "All 7 read-only tools round-trip through the 25.2-02 FastAPI facade with auth forwarding, 30 s timeout, 64 KiB byte cap, and the frozen AGENT_TOOL_ERRORS codes (D-06/D-07)"
    - "Model calls route through a pi-ai custom openai-completions provider pointing at the FastAPI gateway — no provider keys, routing, or price tables in agent-service (D-15)"
    - "POST /agent/novels/{novel_id}/runs streams the curated SSE frame subset and aborts the Pi session when the client disconnects (req close → session.abort)"
    - "On agent_end with stopReason stop the service triggers the backend finalize path; on abort/cancel nothing is written (cancel-no-write holds across the service boundary)"
    - "skill.yaml is machine-validated fail-closed at load (unknown tool or missing D-09 field rejects), and skill instructions are injected deterministically — never via Pi skill discovery (D-09, Pitfall 3)"
    - "Session persistence follows the frozen spike DECISION: pg-session-storage adapter (go-pg-adapter) OR in-memory Pi session + skill_runs state (go-fallback)"
  artifacts:
    - path: "agent-service/src/server.ts"
      provides: "HTTP run API on :3100 with SSE response and disconnect-cancel"
      contains: "text/event-stream"
    - path: "agent-service/src/agent/session-factory.ts"
      provides: "createAgentSession wiring under spike-DECISION constraints"
      contains: "noTools"
    - path: "agent-service/src/tools/registry.ts"
      provides: "7 defineTool domain-tool registrations"
      contains: "get_narrative_memory"
    - path: "agent-service/src/tools/fastapi-client.ts"
      provides: "facade client with frozen error-code mirror"
      contains: "budget_exceeded"
    - path: "agent-service/src/agent/provider.ts"
      provides: "pi-ai custom provider -> NovelMind gateway"
      contains: "openai-completions"
    - path: "agent-service/src/transport/sse.ts"
      provides: "Pi event -> SSE frame serializer (Pattern 5)"
      contains: "tool_execution_start"
    - path: "agent-service/src/skills/loader.ts"
      provides: "fail-closed skill.yaml loader"
      contains: "allowed_tools"
    - path: "agent-service/tests/server.test.ts"
      provides: "run-API contract: stream, disconnect-cancel, finalize trigger"
      min_lines: 80
  key_links:
    - from: "agent-service/src/server.ts"
      to: "agent-service/src/agent/session-factory.ts"
      via: "run dispatch creates a session per accepted run"
      pattern: "createSession|session-factory"
    - from: "agent-service/src/tools/registry.ts"
      to: "POST /api/agent-tools/{tool_name} (25.2-02 facade)"
      via: "fastapi-client fetch with auth forwarding"
      pattern: "agent-tools"
    - from: "agent-service/src/agent/provider.ts"
      to: "POST /api/gateway/v1/chat/completions (25.2-02 gateway)"
      via: "pi-ai createProvider openai-completions with gateway token"
      pattern: "api/gateway/v1"
    - from: "agent-service/src/transport/sse.ts"
      to: "session.subscribe Pi events"
      via: "curated subset serialized as data: frames"
      pattern: "session\\.subscribe|subscribe\\("
    - from: "agent-service/src/server.ts"
      to: "POST /api/agent finalize/cancel (25.2-03 backend)"
      via: "agent_end stop -> finalize trigger; abort -> cancel, no artifact"
      pattern: "finalize|cancel"
---

<objective>
Build the production Agent Service runtime (D-02/D-03): the standalone Node HTTP service on
:3100 that 25.2-01 only spiked and 25.2-04's workspace consumes. It wires the Pi SDK under the
frozen spike-DECISION constraints (noTools "all" + customTools + allowlisted ResourceLoader),
registers the 7 read-only domain tools against the 25.2-02 FastAPI facade with the frozen
`AGENT_TOOL_ERRORS` mirror, routes all model calls through the pi-ai custom provider to the
25.2-02 gateway (D-15), serves the SSE run API (`POST /agent/novels/{novel_id}/runs`,
disconnect-cancel), loads and injects skills fail-closed (loader moved here from 25.2-03), and
persists sessions per the spike DECISION (pg-session-storage adapter OR in-memory + skill_runs
fallback) with `agent_end` triggering the backend finalize path.

Purpose: without this plan, 25.2-03's run dispatch, 25.2-04's SSE endpoint, and the ROADMAP
phase-verification scenario are undeliverable — D-02/D-03 would remain spike-only.

Output: runnable agent-service HTTP runtime + tool registry + provider + SSE transport + skill
loader + storage strategy + vitest coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-CONTEXT.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md
@.planning/spikes/embedded-novel-agent/DECISION.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-02-SUMMARY.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-SUMMARY.md

Execution gate (D-21): this plan MUST NOT start before Phase 22 records three consecutive
scheduled green observations per `.planning/STATE.md`.

<interfaces>
<!-- Contracts from prior plans — read the cited SUMMARYs at execution for final shapes. -->

From 25.2-02 (SUMMARY): frozen agent-tool error codes (forbidden, not_found, beyond_cutoff,
budget_exceeded, timeout, output_too_large, invalid_input, upstream_error) mirrored as
AGENT_TOOL_ERRORS; facade at POST /api/agent-tools/{tool_name} (user JWT or per-run internal
token); gateway at POST /api/gateway/v1/chat/completions behind require_gateway_token.

From 25.2-03 (SUMMARY): run lifecycle API — POST /api/agent/novels/{novel_id}/skill-runs (202,
commit-before-dispatch, mints the per-run internal token), run cancel/retry, backend finalize
trigger (artifact writes only in the FastAPI finalizer), skill_runs status set
(queued/running/cancelled/completed/failed); first skill assets at
agent-service/src/skills/answer-reading-question/ (skill.yaml + schemas + examples).

From .planning/spikes/embedded-novel-agent/DECISION.md: session constraints (noTools "all" +
customTools + tools allowlist; ResourceLoader overrides exhaustive or empty agentDir; skill
instructions injected, never discovered) and the storage strategy —
`pg-session-storage` (implement pi-agent-core SessionStorage: getMetadata, getLeafId, setLeafId,
createEntryId, appendEntry, getEntry, findEntries, getLabel, getSessionName, getSessionStats,
getPathToRootOrCompaction, getEntries, cleanup against the agent_session_entries table) OR
in-memory Pi session per run + skill_runs-only state (go-fallback).

From agent-service/src/config.ts (25.2-01): frozen config object — FASTAPI_BASE_URL,
NOVELMIND_GATEWAY_TOKEN (no default, fail fast, never logged), PORT (default 3100).

SSE frame shapes (Pattern 5 curation): {type:"delta",text}, {type:"tool_start",toolName,args},
{type:"tool_end",toolName,isError}, {type:"turn_end",usage}, plus run-terminal
({type:"run_end",runId,status}) and artifact-preview ({type:"artifact",...}) frames.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tool registry + FastAPI client mirroring the frozen AGENT_TOOL_ERRORS</name>
  <files>agent-service/src/tools/fastapi-client.ts, agent-service/src/tools/registry.ts, agent-service/tests/registry.test.ts</files>
  <behavior>
    - Each of the 7 tools (get_novel, get_chapter, search_novel_text, get_timeline, get_relationships, get_clues, get_narrative_memory) is a defineTool with TypeBox Type.Object params and round-trips through the facade
    - Facade error envelope {error:{code}} maps to the frozen AGENT_TOOL_ERRORS names exactly (forbidden, not_found, beyond_cutoff, budget_exceeded, timeout, output_too_large, invalid_input, upstream_error) — no agent-service-only codes invented
    - AbortSignal.any([runSignal, AbortSignal.timeout(30_000)]) bounds every call; aborting the run signal cancels in-flight tool fetches
    - Response bodies over 64 KiB reject as output_too_large before parsing
    - The Authorization header is forwarded unchanged (user JWT or per-run internal token); tokens are never logged
  </behavior>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Pattern 1; Code Examples — Custom domain tool + AGENT_TOOL_ERRORS; Pitfall 3)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md (sections: agent-service/src/tools/fastapi-client.ts, no-analog table rows for registry.ts)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-02-SUMMARY.md (frozen error-code table, facade route shapes)
    - agent-service/spikes/02-custom-tool-roundtrip.mjs (proven defineTool seam)
    - agent-service/src/config.ts (25.2-01)
  </read_first>
  <action>
    Create `agent-service/src/tools/fastapi-client.ts`: exported `fastapiToolCall(name, params,
    signal, auth)` per RESEARCH Code Examples — POST `${FASTAPI_BASE_URL}/api/agent-tools/${name}`
    with `content-type: application/json` and the caller's `authorization` forwarded unchanged;
    `AbortSignal.any([signal, AbortSignal.timeout(30_000)])`; read the body as text first, reject
    with `output_too_large` over 64 KiB; on non-2xx parse the `{error:{code}}` envelope and throw
    an `AgentToolError` whose `code` is validated against a local `AGENT_TOOL_ERRORS` const that
    MIRRORS the frozen 25.2-02 table exactly (unknown code → `upstream_error`); success returns
    `{ content: [{ type: "text", text }], details: {} }` (Pi tool result shape). Never log the
    auth header or token (V6). Create `agent-service/src/tools/registry.ts`: one `defineTool`
    per D-06 tool with TypeBox `Type.Object` parameters mirroring the 25.2-02 request schemas
    (get_novel {novel_id}; get_chapter {novel_id, chapter_id}; search_novel_text
    {novel_id, query, limit?}; get_timeline {novel_id, full_book?}; get_relationships
    {novel_id, full_book?}; get_clues {novel_id, full_book?}; get_narrative_memory
    {novel_id, query}), descriptions noting server-side owner/spoiler enforcement, and
    `execute` delegating to `fastapiToolCall` with the ambient run auth. Export
    `buildDomainTools(auth: string)` returning the 7-tool array plus `DOMAIN_TOOL_NAMES` (the
    allowlist `tools` array the session factory and skill loader both consume — single source).
    Write `agent-service/tests/registry.test.ts` (vitest, mocked global fetch): behavior block
    plus — each tool's params reject wrong types; a 409/422 envelope with code `beyond_cutoff`
    surfaces that exact code; a body of 64 KiB + 1 byte rejects `output_too_large`; aborting the
    run signal mid-fetch rejects; a test asserts `DOMAIN_TOOL_NAMES` deep-equals the 7 names so
    the registry, session allowlist, and skill validation cannot drift.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/registry.test.ts` exits 0
    - `grep -c "defineTool" agent-service/src/tools/registry.ts` returns >= 7
    - `grep -E "forbidden|not_found|beyond_cutoff|budget_exceeded|timeout|output_too_large|invalid_input|upstream_error" agent-service/src/tools/fastapi-client.ts` matches all eight codes
    - A test asserts the facade envelope code set equals AGENT_TOOL_ERRORS (single-source assertion, not a duplicated literal)
    - `cd agent-service && npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run tests/registry.test.ts</automated>
  </verify>
  <done>7 tools registered against the facade; error codes mirror the frozen table; timeout/byte-cap/abort proven by tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session factory + ResourceLoader allowlist + pi-ai gateway provider</name>
  <files>agent-service/src/agent/session-factory.ts, agent-service/src/agent/resource-loader.ts, agent-service/src/agent/provider.ts, agent-service/tests/session-factory.test.ts</files>
  <behavior>
    - createSession() always applies noTools "all" + customTools + the DOMAIN_TOOL_NAMES allowlist — there is no code path that creates a session with default coding tools
    - The ResourceLoader loads exactly the allowlisted skill set (from the skill loader) and nothing ambient; overrides are exhaustive per the spike A2 verdict (or agentDir points at an empty controlled directory)
    - The provider targets `${FASTAPI_BASE_URL}/api/gateway/v1` with the gateway token from config; Pi-side model cost is zeroed (cost authority stays in FastAPI); no provider key material exists anywhere in agent-service
    - Skill instructions for the active skill are injected via the spike-07-proven seam (system prompt / before_agent_start-style message), never via Pi skill discovery
  </behavior>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Patterns 1-3; Pitfalls 2-3)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md (no-analog table rows: session-factory.ts, resource-loader.ts, provider.ts)
    - .planning/spikes/embedded-novel-agent/DECISION.md + FINDINGS.md (A1/A2 verdicts, handoff constraints)
    - agent-service/spikes/01-no-tools-enumeration.mjs, agent-service/spikes/03-resource-loader-allowlist.mjs, agent-service/spikes/07-skill-instruction-injection.mjs (proven seams)
    - agent-service/src/tools/registry.ts (Task 1)
    - agent-service/src/config.ts (25.2-01)
  </read_first>
  <action>
    Create `agent-service/src/agent/provider.ts`: `buildGatewayModelRuntime()` per RESEARCH
    Pattern 2 — pi-ai `createModels` + `createProvider` (or the verified registerProvider
    equivalent) with `api: openAICompletionsApi()`, baseUrl
    `${config.FASTAPI_BASE_URL}/api/gateway/v1`, auth resolving `config.NOVELMIND_GATEWAY_TOKEN`,
    and one logical `Model` (id `reader-chat-default`, `cost` all zeros, reasoning false) — the
    ONLY model the service can call; D-15: no keys/routing/price tables here. Create
    `agent-service/src/agent/resource-loader.ts`: `buildResourceLoader(allowlistedSkills)` per
    Pattern 3 — `DefaultResourceLoader` with `systemPromptOverride` (Novel Agent system prompt),
    `skillsOverride` returning exactly the allowlisted skills from the skill loader,
    `agentsFilesOverride` empty, inline named `extensionFactories` only; apply the spike A2
    mitigation verbatim (empty controlled `agentDir` if the spike found any non-overridable
    surface — read FINDINGS.md and implement what it records, do not re-decide). Create
    `agent-service/src/agent/session-factory.ts`: `createSession({ auth, skill, storage? })`
    that composes the three — `createAgentSession({ noTools: "all", customTools:
    buildDomainTools(auth), tools: DOMAIN_TOOL_NAMES filtered by the skill's allowed_tools,
    resourceLoader, modelRuntime })` — injects the active skill's instructions via the spike-07
    seam, and wires the storage backend selected by the spike DECISION (Task 4 provides the
    pg adapter when `go-pg-adapter`; `go-fallback` passes nothing and runs in-memory). Enforce:
    `skill.allowed_tools` must be a subset of DOMAIN_TOOL_NAMES else throw before session
    creation (defense in depth with the loader/registry gates). Write
    `agent-service/tests/session-factory.test.ts` (vitest): assert the factory always passes
    `noTools: "all"` and never an empty/absent tools allowlist (mock createAgentSession and
    inspect the options object); provider baseUrl ends with `/api/gateway/v1` and cost fields
    are zero; loader override functions return exactly the allowlisted set; a skill whose
    allowed_tools escapes the registry is rejected before any session call.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/session-factory.test.ts` exits 0
    - `grep "noTools" agent-service/src/agent/session-factory.ts` matches; `grep -rn "apiKey" agent-service/src/agent/provider.ts` matches only the gateway token reference
    - `grep "api/gateway/v1" agent-service/src/agent/provider.ts` matches
    - `grep -rn "process.env" agent-service/src/agent/ agent-service/src/tools/` matches only via the frozen config object (no ad-hoc env reads)
    - `cd agent-service && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run tests/session-factory.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Sessions provably coding-tool-free and allowlist-only; model routing solely via the gateway provider; skill instructions injected deterministically</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: skill.yaml loader wiring (moved from 25.2-03) against the first-skill assets</name>
  <files>agent-service/src/skills/loader.ts, agent-service/tests/skills-loader.test.ts, agent-service/package.json</files>
  <behavior>
    - skill.yaml missing any D-09 required field → load fails closed naming the field
    - allowed_tools containing a name outside DOMAIN_TOOL_NAMES → load fails closed naming the tool
    - input.schema.json / output.schema.json that are not valid JSON Schema → load fails closed
    - validateRunInput(skill, input) rejects non-conforming run input before any session/tool call
    - Loading answer-reading-question (assets from 25.2-03) succeeds and exposes its SKILL.md instructions for deterministic injection
  </behavior>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (D-09 fields, Pitfall 3, assumption A5, Open Question 4)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md (No Analog Found — skills/loader.ts)
    - agent-service/src/skills/answer-reading-question/skill.yaml (25.2-03 Task 3 — the asset this loader must accept)
    - agent-service/src/tools/registry.ts (Task 1 — DOMAIN_TOOL_NAMES)
    - .planning/spikes/embedded-novel-agent/DECISION.md (skill-instruction injection handoff)
  </read_first>
  <action>
    Add exact-pinned deps `yaml`, `ajv`, `ajv-formats` to agent-service/package.json (RESEARCH
    A5 — pin, no `^`; re-run `npm view <pkg> version time.modified scripts` before install and
    record in the plan SUMMARY). Create `agent-service/src/skills/loader.ts`: `loadSkill(name)`
    reads `src/skills/<name>/skill.yaml`, ajv-validates against a meta-schema encoding the D-09
    required fields (`name`, `version`, `allowed_tools`, `read_permissions`,
    `write_permissions`, `forbidden_spaces`, `budget`, `approval_required_for`, `input_schema`,
    `output_schema`) — fail closed naming the offending field; validate every `allowed_tools`
    entry against `DOMAIN_TOOL_NAMES` from the registry (Task 1) — unknown tool → throw naming
    it, skill never registers; compile `input.schema.json`/`output.schema.json` as JSON Schema
    (invalid schema file → fail closed); expose `validateRunInput(skill, input)` (ajv check
    before any tool call) and `skillInstructions(skill)` returning the SKILL.md body for the
    session factory's deterministic injection (Pitfall 3 — Pi skill discovery stays
    metadata-only). `loadAllowlistedSkills()` loads exactly the allowlisted skill directory set
    (answer-reading-question only in 25.2) and is what the ResourceLoader override serves.
    Write `agent-service/tests/skills-loader.test.ts` (vitest, temp fixture skill dirs for the
    negative matrix): the behavior block plus unknown tool, bad yaml, missing field, invalid
    schema file, input mismatch, and the positive load of the real answer-reading-question
    assets asserting its allowed_tools exclude `get_narrative_memory` (Open Question 4).
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/skills-loader.test.ts` exits 0
    - A test asserts loading a skill whose allowed_tools contains a bogus name throws with the tool name in the error message
    - A test asserts the real answer-reading-question assets load and `allowed_tools` does not contain `get_narrative_memory`
    - `agent-service/package.json` contains exact pins for yaml, ajv, ajv-formats (no `^`)
    - `cd agent-service && npx vitest run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npm install && npx vitest run tests/skills-loader.test.ts</automated>
  </verify>
  <done>Loader fail-closed across the validation matrix; first-skill assets load; instructions available for injection; deps pinned</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: HTTP run API + SSE transport + spike-conditional session storage</name>
  <files>agent-service/src/server.ts, agent-service/src/transport/sse.ts, agent-service/src/storage/pg-session-storage.ts, agent-service/tests/server.test.ts</files>
  <behavior>
    - POST /agent/novels/{novel_id}/runs with a valid Bearer token creates the skill run via FastAPI (202 pattern, per-run internal token returned), starts the session, and responds text/event-stream
    - Frames arrive in curated order (delta/tool_start/tool_end/turn_end) followed by exactly one run-terminal frame; an artifact-preview frame precedes run_end on successful finalize
    - Client disconnect (req close) aborts the Pi session AND notifies the backend cancel path — the run ends cancelled with no artifact
    - agent_end with stopReason stop triggers the backend finalize trigger with the run id; stopReason aborted triggers cancel; no artifact write ever happens in agent-service
    - Storage follows the spike DECISION: go-pg-adapter → sessions resume across a service restart via the PG adapter; go-fallback → sessions are per-run in-memory and run state lives only in skill_runs
  </behavior>
  <read_first>
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-RESEARCH.md (Pattern 4, Pattern 5, Code Examples — SSE run endpoint + Cancel-without-write; Pitfall 4)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-PATTERNS.md (no-analog table rows: server.ts, transport/sse.ts, storage/pg-session-storage.ts)
    - .planning/spikes/embedded-novel-agent/DECISION.md (storage strategy — determines whether pg-session-storage.ts is built)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-SUMMARY.md (run-create response shape, per-run internal token, finalize/cancel endpoints)
    - agent-service/spikes/05-streaming-events.mjs (proven event capture + curated subset)
    - agent-service/src/agent/session-factory.ts (Task 2), agent-service/src/skills/loader.ts (Task 3)
  </read_first>
  <action>
    Create `agent-service/src/transport/sse.ts`: `createSseRunStream(res)` returning
    `{ send(frame), close() }` that writes `data: ${JSON.stringify(frame)}\n\n`, and
    `piEventToFrame(event)` implementing the Pattern 5 curation exactly (message_update →
    {type:"delta",text}; tool_execution_start → {type:"tool_start",toolName,args};
    tool_execution_end → {type:"tool_end",toolName,isError}; turn_end → {type:"turn_end",usage};
    all other Pi events dropped) — pure function, unit-testable. Create
    `agent-service/src/server.ts` with `node:http` (no new framework dep — RESEARCH Recommended
    Project Structure keeps deps minimal): `POST /agent/novels/{novel_id}/runs` accepts
    `{question, skill?, input?}`; rejects without an Authorization header (401 — no anonymous
    runs); forwards the Bearer to FastAPI run-create (`POST
    /api/agent/novels/{novel_id}/skill-runs`, 202 shape from 25.2-03) which performs the owner
    check and mints the per-run internal token (run dispatch forgery mitigation: agent-service
    NEVER trusts the client-supplied identity — the FastAPI 202 response is the only run
    authorization); validates run input via `validateRunInput`; creates the session via the
    Task 2 factory with the per-run token as tool auth; responds
    `text/event-stream` + `cache-control: no-cache`; subscribes once, pipes curated frames;
    `req.on("close")` → unsubscribe + `session.abort()` + POST the backend cancel endpoint
    (disconnect-cancel, D-19); on `agent_end`: stopReason `stop` → load/validate output against
    the skill output schema, POST the backend finalize trigger with run id + final answer +
    tool-call list (artifact written only by the FastAPI finalizer), emit `artifact` preview
    frame from the finalize response then `{type:"run_end",runId,status:"completed"}`; stopReason
    `aborted` → POST cancel, emit `{type:"run_end",runId,status:"cancelled"}`; errors →
    `{type:"run_end",runId,status:"failed",error_code}` from the frozen table. `GET /healthz`
    returns 200 JSON. Storage per spike DECISION: IF `go-pg-adapter` — create
    `agent-service/src/storage/pg-session-storage.ts` implementing the pi-agent-core
    `SessionStorage` interface surface (getMetadata, getLeafId, setLeafId, createEntryId,
    appendEntry, getEntry, findEntries, getLabel, getSessionName, getSessionStats,
    getPathToRootOrCompaction, getEntries, cleanup) persisting one row per entry to the
    `agent_session_entries` table via FastAPI (storage-adapter tampering mitigation: entries
    are scoped by run/session id from the server-issued run, never client-supplied; content is
    treated as replaceable session memory per D-11, never as fact); wire it into the session
    factory through the seam the spike recorded. IF `go-fallback` — do NOT create the PG
    adapter; sessions are in-memory per run and all durable state stays in skill_runs/artifacts
    (record the omission in the SUMMARY). Write `agent-service/tests/server.test.ts` (vitest,
    `node:http` server on an ephemeral port, mocked fetch for FastAPI calls, stubbed session
    factory): 401 without auth; happy-path frame ordering ending in exactly one run_end;
    disconnect triggers abort + backend cancel POST; agent_end stop triggers the finalize POST
    before run_end; input failing validateRunInput → 422 with no session created. If
    go-pg-adapter: a storage round-trip test (append entries, reopen, entries recovered). If
    go-fallback: a test asserting no pg-session-storage module is imported by the server.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/server.test.ts` exits 0
    - `grep "text/event-stream" agent-service/src/server.ts` matches; `grep 'req.on("close"\|req.on(.close.' agent-service/src/server.ts` matches disconnect handling
    - A test asserts the finalize POST fires on stop and the cancel POST fires on disconnect/abort
    - `grep -rn "artifacts" agent-service/src | grep -iv "test\|preview"` shows no artifact persistence (agent-service never writes artifacts)
    - Storage branch matches the spike DECISION verbatim (adapter file exists only under go-pg-adapter)
    - `cd agent-service && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run tests/server.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Run API streams curated frames with disconnect-cancel; finalize only via backend; storage matches the frozen DECISION</done>
</task>

<task type="auto">
  <name>Task 5: Test, Fix, and Confirm — live run against the real facade/gateway, cancel-no-artifact round trip</name>
  <files>agent-service/src/server.ts, agent-service/src/agent/session-factory.ts, agent-service/src/tools/fastapi-client.ts, agent-service/tests/server.test.ts</files>
  <read_first>
    - agent-service/tests/ (all suites from Tasks 1-4)
    - .planning/phases/25.2-embedded-novel-agent-runtime/25.2-VALIDATION.md (25.2-05 sampling row)
    - .planning/ROADMAP.md (Phase 25.2 Phase Verification scenario)
  </read_first>
  <action>
    Start the real dev stack (Docker Desktop + compose db/chroma per Pitfall 6, uvicorn :8010 —
    the `next.config.mjs` `BACKEND_URL` default; if you run uvicorn on :8000 instead, export
    `BACKEND_URL=http://127.0.0.1:8000` before starting Next — agent-service :3100) and drive
    one live run end-to-end with a fixture novel and the real
    `answer-reading-question` skill: POST /agent/novels/{novel_id}/runs with a test user JWT;
    assert frames stream in order, the tool summary shows facade calls only (no other HTTP
    egress from agent-service besides /api/agent-tools, /api/gateway, /api/agent), the backend
    finalize produced exactly one candidate artifact (verify via the 25.2-03 artifacts API),
    and the skill_runs row ends `completed` with model_lineage/source_versions recorded. Then a
    second live run cancelled mid-stream: assert run ends `cancelled` and zero new artifact
    rows (cancel-no-write across the service boundary). Fix all defects in the original code
    path (frame ordering, token forwarding, abort propagation, finalize payload), not the
    tests, then re-run affected suites. Full sweeps: `npx vitest run` (agent-service), backend
    full marker suite, `tests/ci` policy tests. Record the live-run evidence (run ids, artifact
    id, frame sequence) in the plan SUMMARY — 25.2-04's workspace and the phase verification
    scenario consume this endpoint.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run` exits 0
    - `cd backend && python -m pytest tests -m "unit or integration or contract or adversarial" -q` exits 0
    - `cd backend && python -m pytest tests/ci -q` exits 0
    - Live evidence recorded in SUMMARY: completed run → exactly one candidate artifact; cancelled run → zero artifacts; no egress besides the three FastAPI surfaces
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run && cd ../backend && python -m pytest tests -m "unit or integration or contract or adversarial" -q && python -m pytest tests/ci -q</automated>
  </verify>
  <done>Live run produces a finalized candidate artifact; cancel writes nothing; all suites green; endpoint evidence handed to 25.2-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → agent-service :3100 | Run dispatch requests with Bearer tokens; identity is untrusted until FastAPI confirms |
| agent-service → FastAPI (facade/gateway/agent API) | Per-run internal token + gateway token cross the wire; tool params are agent-generated (prompt-injection possible via novel text) |
| Pi session ↔ storage adapter | Session entries are replaceable memory (D-11); tampering must not reach domain state |
| skill.yaml (disk) → loader | Code-adjacent config; fail-closed validation before any session use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.2-05-01 | Information Disclosure | service auth token leakage (gateway token, per-run internal token, user JWT) | mitigate | Tokens live only in env/config (fail-fast, never logged — V6); per-run token minted by FastAPI and held in memory for the run's lifetime only; grep assertion in Task 2 that provider references only the config token; no token in SSE frames, URLs, or error messages |
| T-25.2-05-02 | Spoofing | run dispatch forgery (client fabricates a run/identity) | mitigate | Runs are authorized only by the FastAPI run-create 202 (owner check + per-run token mint, 25.2-03); agent-service trusts no client-supplied identity fields; no auth header → 401; test asserts no session is created on rejected dispatch |
| T-25.2-05-03 | Tampering | SSE session hijack / cross-run frame leakage | mitigate | One subscription per run on a per-connection stream; unsubscribe + session.abort() on req close; frames carry no auth material; run ids come from the server-issued run, never from the request path alone |
| T-25.2-05-04 | Tampering | storage-adapter tampering (forged session entries poisoning resumed context) | mitigate | Entries scoped by server-issued session/run id; storage is replaceable session memory per D-11 — the backend finalizer reads the frozen manifest, never session memory; go-fallback eliminates the surface entirely |
| T-25.2-05-05 | Elevation of Privilege | skill/session escaping the domain-tool allowlist | mitigate | Triple gate: loader validates allowed_tools vs DOMAIN_TOOL_NAMES, factory re-checks subset before session creation, noTools "all" kills coding tools; tests assert the mocked createAgentSession options |
| T-25.2-SC | Tampering | npm installs (yaml, ajv, ajv-formats) | mitigate | Exact pins only + `npm view` re-verification before install (recorded in SUMMARY); ubiquitous established packages (RESEARCH A5); full governance audit is Phase 25.3 scope |
</threat_model>

<verification>
- `cd agent-service && npx vitest run` exits 0 (registry, session-factory, skills-loader, server suites)
- `cd agent-service && npx tsc --noEmit` exits 0
- Live run: POST /agent/novels/{novel_id}/runs streams curated frames; agent_end stop → one candidate artifact via backend finalize; cancel → zero artifacts
- Storage branch matches `.planning/spikes/embedded-novel-agent/DECISION.md` verbatim
- No artifact writes originate in agent-service (grep assertion)
</verification>

<success_criteria>
- REQ-AGENT-01 productionized: the running service creates only allowlisted-tool sessions (the 25.2-01 spike result is now the shipped runtime)
- D-02/D-03 implemented: run dispatch, streaming, cancel, and finalize trigger work across the service boundary against the real FastAPI stack
- 25.2-04 unblocked: the SSE endpoint the workspace consumes is live and evidenced
</success_criteria>

<output>
Create `.planning/phases/25.2-embedded-novel-agent-runtime/25.2-05-SUMMARY.md` when done
</output>
