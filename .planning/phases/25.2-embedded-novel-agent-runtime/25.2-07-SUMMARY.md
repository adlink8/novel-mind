# 25.2-07 打包第一个 Skill（answer-reading-question）— 执行摘要

- **Phase**: 25.2-embedded-novel-agent-runtime
- **Plan**: 25.2-07（execute / wave 4）
- **依赖**: 25.2-03（skill 资产基础）
- **日期**: 2026-08-02
- **状态**: ✅ 完成（Task 1-2 验收通过；skill 包 registry-valid；无后端改动；无回归）

## 一、实现清单

### Task 1：完善第一个 Skill 包
| 文件 | 说明 | 状态 |
|---|---|---|
| `agent-service/src/skills/answer-reading-question/SKILL.md` | 新增「版本与契约」小节：version 1.0.0、6 个只读白名单、budget per-run 上限、零写入/零审批、不持久化任何行 | 修改 |
| `.../tests/basic.json` | skill-local fixture：独立于 examples/basic.json 的另一组合法 input（novel_id=7，chapter_range 1-5）+ expected_output | 新增 |
| `.../skill.yaml` | 不动（已含 D-09 全字段，allowed_tools 6 个只读工具、排除 get_narrative_memory） | 不变 |

### Task 2：Skill-local 测试
| 文件 | 说明 | 状态 |
|---|---|---|
| `agent-service/tests/skills/answer-reading-question.test.ts` | 29 个用例：schema 有效性、skill.yaml D-09 契约、registry-valid fail-closed 负面、双 fixture 校验、schema-mismatch 负面、取消语义。用 typebox `Value.Check` 校验外部 JSON Schema，内置 YAML 子集解析器（未引入 yaml/ajv 依赖，loader 属 25.2-05） | 新增 |

## 二、测试结果

| 命令 | 结果 |
|---|---|
| `cd agent-service && env -u HTTPS_PROXY -u HTTP_PROXY npx vitest run tests/skills/answer-reading-question.test.ts`（跑两次） | ✅ **29 passed** × 2 |
| `cd agent-service && env -u HTTPS_PROXY -u HTTP_PROXY npx vitest run`（全量） | ✅ **32 passed**（skill 29 + scaffold 3，无回归） |

## 三、偏离记录

无。按约束未创建 loader（25.2-05 的活）、未动 `backend/`、未改 skill.yaml。

## 四、Handoff

- **25.2-05**：loader 引入 yaml/ajv 依赖并消费本包；`tests/basic.json` 可作为其正加载测试 fixture。本测试中的 `validateSkillContract` 只做 skill-local 契约门禁，非 loader。
- 验收标准满足：valid + 负面用例（unknown-tool / get_narrative_memory / forbidden-write / approval / schema-mismatch）全绿；skill 包 write_permissions 与 approval_required_for 均为空（只读、零审批）。
