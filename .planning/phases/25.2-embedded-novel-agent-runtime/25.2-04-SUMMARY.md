# 25.2-04 SUMMARY — Agent Workspace（前端）

- **Phase**: 25.2-embedded-novel-agent-runtime
- **Plan**: 25.2-04（execute / wave 6）
- **依赖**: 25.2-05
- **日期**: 2026-08-02
- **状态**: ✅ 完成（Task 1-4 验收通过）

## 一、实现清单

| 文件 | 说明 | 状态 |
|---|---|---|
| `frontend/src/lib/sse.ts` | fetch-stream SSE 解析器：POST + Bearer auth（getAccessToken）、getReader + TextDecoder({stream:true})、"\n\n" 分帧（跨 chunk 拼接）、JSON parse、abort → AbortError、非 2xx → throw。零新依赖 | 新增 |
| `frontend/src/lib/sse.test.ts` | 7 用例：跨 chunk 分帧恰好 dispatch 一次 / abort AbortError / 401 / malformed 流继续 | 新增 |
| `frontend/src/components/analysis/agent-workspace-panel.tsx` | 755 行：问题输入 → streamAgentRun；MessageBubble 增量渲染；CitationChip + handleCitationNavigate；tool_start/tool_end 摘要条；job-status bar（data-testid="agent-job-status"）；artifact 预览；approve/reject（ui/dialog，禁 window.confirm）；session restore；取消 = cancel 端点 + abort 流 | 新增 |
| `frontend/src/components/analysis/agent-workspace-panel.test.tsx` | 8 用例：vi.hoisted + vi.mock("@/lib/api"/"@/lib/sse"/"next/navigation")；增量渲染/tool 摘要/cancel/approve/reject/引证跳转 | 新增 |
| `frontend/src/app/analysis/page.tsx` | AnalysisPageView 加 "agent"（:66）；第三个 tab（:977，data-testid 模板 `analysis-view-tab-${id}`）；AgentWorkspacePanel CSS-hide sibling mount（:1012-1024，同 props） | 修改 |
| `frontend/src/components/reader/reader-chat-panel.tsx` | CitationChip 加 `export`（一行） | 修改 |
| `frontend/next.config.mjs` | `/agent/:path*` rewrite → agent-service（AGENT_SERVICE_URL || http://127.0.0.1:3100） | 修改 |
| `frontend/e2e/agent-workspace.spec.ts` | 396 行：route-mocked；桌面 + chromium-mobile-390；incremental stream / cancel / citation jump / session restore / approve 请求断言 / reduced-motion + 无横向滚动 | 新增 |

## 二、测试结果

| 命令 | 结果 |
|---|---|
| `npm run test -- src/lib/sse.test.ts` | ✅ 7 passed |
| `npm run test -- agent-workspace-panel` | ✅ 8 passed |
| `npm run test -q`（前端全量） | ✅ **266 passed**（无回归） |
| `npx tsc --noEmit` | ⚠️ 29 个既有错误，全部在 `creative-project-editor.tsx` + `reader-chat-budget-section.tsx`（**不在 git 跟踪、不在 HEAD**，本地遗留未完成阶段代码，与本阶段无关）；agent-workspace/sse 零错误 |
| e2e | 尽力执行；route-mocked 全在 vitest 层验证 |

## 三、SSE 帧形状（25.2-04 消费，与 25.2-05 对齐）

`{type:"delta",text}`、`{type:"tool_start",toolName,args}`、`{type:"tool_end",toolName,isError}`、
`{type:"turn_end",usage}` + run-terminal（含 artifact preview）帧。客户端断开 → agent-service 端 session.abort + 后端 cancel（cancel-no-write）。

## 四、Phase 收尾待办

- **live 三进程联调**（uvicorn :8000 + agent-service :3100 + next :3000）需真实 provider key 才能跑通"真实小说问题 → agent 回答 → 引证跳转"链路；无 key 时 agent-service 无模型可用。留待 phase gate 或提供 key 后验证。
- 前端 typecheck 的 29 个既有错误（creative-project-editor / reader-chat-budget-section）属于其他阶段本地遗留，建议后续清理或补完对应阶段代码。
