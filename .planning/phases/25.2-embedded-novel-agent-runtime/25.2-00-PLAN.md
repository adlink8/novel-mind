---
phase: 25.2-embedded-novel-agent-runtime
plan: "00"
type: execute
wave: 0
depends_on: ["22-G3"]
files_modified:
  - scripts/check_agent_runtime_execution_gate.py
  - tests/ci/test_agent_runtime_execution_gate.py
autonomous: true
requirements: [REQ-NIGHTLY-03, REQ-AGENT-01]
must_haves:
  truths:
    - "No Phase 25.2 product or spike file is written until Phase 22 has three consecutive real scheduled green observations and Phase 25.1 is recognized implemented."
    - "Missing, malformed or unverifiable gate evidence exits non-zero; planning override never changes the execution verdict."
  artifacts:
    - path: "scripts/check_agent_runtime_execution_gate.py"
      provides: "Reusable fail-closed preflight for Phase 25.2 and 25.3 execution."
---

<objective>
Create and enforce the machine-verifiable execution preflight before any Pi Runtime work.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md
@.planning/phases/22-ci-nightly-gap-closure/22-G3-PLAN.md
</execution_context>

## Steps

<tasks>
<task type="auto">
  <name>Task 1: Fail closed before creating the gate implementation</name>
  <read_first>.planning/STATE.md; .planning/phases/22-ci-nightly-gap-closure/22-VALIDATION.md; IMPLEMENTATION-STATUS.md</read_first>
  <files>scripts/check_agent_runtime_execution_gate.py; tests/ci/test_agent_runtime_execution_gate.py</files>
  <action>Perform a read-only preflight first. Require three consecutive real scheduled green records with run/artifact/result lineage and authoritative recognition that Phase 25.1 is implemented. If either condition is absent, exit non-zero before creating or changing any file. Only after both pass, implement the reusable parser/CLI and fixtures. Planning-only overrides are explicitly ignored.</action>
  <acceptance_criteria>Current repository returns blocked without product writes; synthetic complete evidence returns 0; malformed/missing evidence is stable non-zero.</acceptance_criteria>
  <verify><automated>python -m pytest tests/ci/test_agent_runtime_execution_gate.py -q</automated></verify>
  <done>The Phase 25.2 entry gate is executable, tested and fail-closed.</done>
</task>
<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>scripts/check_agent_runtime_execution_gate.py; tests/ci/test_agent_runtime_execution_gate.py</read_first>
  <files>scripts/check_agent_runtime_execution_gate.py; tests/ci/test_agent_runtime_execution_gate.py</files>
  <action>Run current-blocked, synthetic-pass, missing-file, malformed-record, non-consecutive, wrong-event and planning-override fixtures; fix failures and rerun twice.</action>
  <acceptance_criteria>Only authoritative Phase 22 3/3 plus recognized Phase 25.1 returns 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/ci/test_agent_runtime_execution_gate.py -q</automated></verify>
  <done>Gate behavior is deterministic and no blocked run produces Phase 25.2 files.</done>
</task>
</tasks>

## Must-Haves

- Read-only entry check precedes all writes.
- Missing or contradictory evidence fails closed.
- Planning authorization cannot unlock implementation.

## Verification

Run the dedicated CI gate tests and the current repository blocked path.

## Success Criteria

Phase 25.2 plans 01-05 cannot start unless the machine gate returns zero.

