---
phase: 13-candidate-memory-contracts-and-provenance-authority
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - backend/app/services/narrative_memory/provenance.py
  - backend/app/services/narrative_memory/manifests.py
  - backend/tests/unit/narrative_memory/test_provenance.py
  - backend/tests/unit/narrative_memory/test_manifests.py
  - backend/tests/integration/narrative_memory/test_provenance_pg.py
  - backend/tests/integration/narrative_memory/test_no_pointer_side_effects.py
requirements: [V08-MEM-03, V08-MEM-04, V08-MEM-05]
autonomous: true
must_haves:
  truths:
    - "Every authoritative claim, including upper-level claims, has a direct exact link to at least one Phase 07 evidence leaf in the frozen source snapshot."
    - "Story Arc/Volume coverage is contiguous and legal, Global Story Model derives only from verified middle-level nodes, and the scoped graph is a DAG."
    - "A fresh observer proves candidate creation, manifest sealing, and structural validation do not change any existing production pointer or create a memory pointer."
  artifacts:
    - path: "backend/app/services/narrative_memory/provenance.py"
      provides: "Lossless scoped loading, graph/range validation, optional-source lineage, and authoritative Chapter re-slicing"
    - path: "backend/app/services/narrative_memory/manifests.py"
      provides: "Deterministic database-row manifest, immutable seal, and append-only structural validation report"
    - path: "backend/tests/integration/narrative_memory/test_no_pointer_side_effects.py"
      provides: "Independent fresh-session before/after authority and capability proof"
  key_links:
    - from: "narrative_memory_source_links"
      to: "Chapter.content[source_start:source_end]"
      via: "scoped Phase 07 evidence leaf lookup and server-side re-slice/hash verification"
      pattern: "source_start.*source_end"
    - from: "narrative_memory_manifests"
      to: "versions|nodes|claims|edges|source_links"
      via: "sorted lossless database rows, never caller/worker checksums"
      pattern: "manifest_checksum"
---

<objective>Close the candidate evidence graph end to end, seal a deterministic database-derived manifest, and prove with adversarial PostgreSQL observers that structural validation has no production side effects.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-CONTEXT.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-RESEARCH.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-02-SUMMARY.md
@backend/app/services/narrative_memory/audit_pg.py
@backend/app/services/narrative_memory/contracts.py
@backend/app/services/narrative_memory/authority.py
@backend/app/models/chunk_build.py
@backend/app/models/novel.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate graph, range, and direct claim-to-leaf closure</name>
  <files>backend/app/services/narrative_memory/provenance.py, backend/tests/unit/narrative_memory/test_provenance.py</files>
  <action>Implement deterministic pure validation for legal containment transitions, parent range containment, cycle rejection, full expected Chapter State coverage, continuous/non-overlapping Story Arc or Volume sibling ranges, and Global Story range/edges over already verified middle-level nodes only. Require every claim at every level to own at least one direct exact source link; graph ancestry alone, chapter-wide references, stored snippets, summaries, embeddings, similarity scores, or model citations never satisfy evidence closure. Return stable sorted reason codes and observed counts, but do not make a Phase 17 quality/release verdict. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_provenance.py -q -x</automated></verify>
  <done>Pure validation accepts only continuous legal DAGs whose every claim has direct leaf closure.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Re-slice authoritative text and seal a database-derived manifest</name>
  <files>backend/app/services/narrative_memory/provenance.py, backend/app/services/narrative_memory/manifests.py, backend/tests/unit/narrative_memory/test_manifests.py, backend/tests/integration/narrative_memory/test_provenance_pg.py</files>
  <action>Load all rows losslessly by owner/novel/version and fail closed on foreign scope, duplicates, unknown kinds/schema versions, or missing rows. For every link, verify version/build/snapshot lineage, Phase 07 evidence level and chapter identity, offsets, evidence stored content/hash, authoritative Chapter.content[start:end], and link hash. Optional timeline/relationship/clue IDs are enrichment only: validate their frozen lineage when present and still require the same Phase 07 leaf; Reader Chat is forbidden. Build the manifest from explicitly sorted PostgreSQL version lineage, nodes, claims, edges, and links; prove caller/worker checksums are ignored. Insert one immutable seal and an append-only structural report bound to the recomputed checksum, with success/failure changing no pointer. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_manifests.py tests/integration/narrative_memory/test_provenance_pg.py -q -x</automated></verify>
  <done>Only a complete same-snapshot re-sliced candidate can receive a matching immutable manifest seal and structural validation report.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Run adversarial PostgreSQL and fresh-observer no-pointer proof</name>
  <files>backend/tests/integration/narrative_memory/test_provenance_pg.py, backend/tests/integration/narrative_memory/test_no_pointer_side_effects.py</files>
  <action>Cover Unicode re-slicing plus out-of-range, stale Chapter content, wrong leaf hash/snapshot/build/chapter/owner/novel, missing claim link, package-external reference, illegal transition, arc gap/overlap, global bypass, and DAG cycle failures. Seed identical authority in different insertion orders and require identical manifests; vary every authoritative provenance component and require checksum change; attempt post-seal inserts and authority UPDATE/DELETE and require PostgreSQL rejection. In an independently opened fresh observer session, snapshot complete rows/revisions/checksums for chunk_active_pointers, timeline_active_pointers, clue_active_pointers, narrative_active_pointers, and active_baselines before and after real candidate creation, sealing, and validation; assert byte-equivalent state, no narrative_memory_active_pointers table, zero provider calls, and no pointer/promotion/Reader Chat capability in the Phase 13 package. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_provenance.py tests/unit/narrative_memory/test_manifests.py tests/integration/narrative_memory/test_provenance_pg.py tests/integration/narrative_memory/test_no_pointer_side_effects.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/provenance.py app/services/narrative_memory/manifests.py tests/unit/narrative_memory/test_provenance.py tests/unit/narrative_memory/test_manifests.py tests/integration/narrative_memory/test_provenance_pg.py tests/integration/narrative_memory/test_no_pointer_side_effects.py</automated></verify>
  <done>Adversarial evidence, graph, manifest, immutability, and fresh-observer no-pointer tests all pass without provider or production side effects.</done>
</task>
</tasks>

## Must-Haves

- Every claim has a direct exact Phase 07 evidence leaf from the same frozen snapshot.
- Server-side `Chapter.content[start:end]` re-slicing is final authority; stored prose and external citations are insufficient.
- Arc/Volume ranges are continuous and non-overlapping; Global Story uses only verified middle-level nodes; all edges form a legal DAG.
- Manifest identity comes only from sorted database rows and includes frozen lineage, nodes, claims, edges, and source links.
- Seals and validation reports are append-only; sealing prevents late content inserts.
- Validation emits structural candidate evidence only, not Phase 17 quality qualification or production release.
- Fresh observer and static capability scans prove zero pointer/provider/chat/promotion side effects.

## Verification

<verification>Run unit graph/manifest suites, PostgreSQL provenance/adversarial/fresh-observer suites, targeted Phase 07/08/09/11/12 regressions, Ruff, and final Alembic checks using `backend/.venv`.</verification>
<success_criteria>
- Same-snapshot leaf closure succeeds and every broken-link/re-slice/range/DAG case fails closed.
- Database-row manifests are insertion-order independent and sensitive to all authoritative changes.
- Existing production pointer snapshots are byte-equivalent before and after candidate lifecycle operations.
- `alembic heads` remains single and `alembic check` remains clean.
- Targeted sidecar regressions pass without changing Phase 07 hierarchy or Phase 08/09/11 selection semantics.
</success_criteria>

<regression_commands>
cd backend
.\.venv\Scripts\python.exe -m pytest tests/integration/chunking/test_pg_hierarchy_wiring.py tests/integration/timeline/test_version_lifecycle.py tests/integration/relationships/test_boundaries_pg.py tests/integration/clues/test_source_protocols.py tests/integration/narrative_memory/test_audit_pg.py -q -x
.\.venv\Scripts\python.exe -m alembic heads
.\.venv\Scripts\python.exe -m alembic check
</regression_commands>

<output>Create `13-03-SUMMARY.md`, then Phase 13 verification evidence covering V08-MEM-01..05 and the candidate-only/no-pointer boundary.</output>
