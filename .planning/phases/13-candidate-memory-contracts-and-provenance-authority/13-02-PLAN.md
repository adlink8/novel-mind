---
phase: 13-candidate-memory-contracts-and-provenance-authority
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/app/services/narrative_memory/contracts.py
  - backend/app/services/narrative_memory/authority.py
  - backend/tests/unit/narrative_memory/test_contracts.py
  - backend/tests/integration/narrative_memory/test_contract_authority_pg.py
requirements: [V08-MEM-01, V08-MEM-02, V08-MEM-04, V08-MEM-05]
autonomous: true
must_haves:
  truths:
    - "Chapter State, Story Arc/Volume, and Global Story Model candidates accept only a small strict discriminated claim/state-delta union."
    - "Extra fields, coercion, unknown enums, summary-only facts, arbitrary JSON authority, and references outside the frozen candidate package fail closed."
    - "Candidate writes require an explicit version_id and verified Phase 12 eligibility lineage, with no provider call or implicit current-version lookup."
  artifacts:
    - path: "backend/app/services/narrative_memory/contracts.py"
      provides: "Frozen strict DTOs, claim variants, enums, and canonical checksums"
    - path: "backend/app/services/narrative_memory/authority.py"
      provides: "Owner-scoped explicit-version candidate persistence with no pointer/provider capability"
    - path: "backend/tests/unit/narrative_memory/test_contracts.py"
      provides: "Round-trip and fail-closed contract matrix"
  key_links:
    - from: "backend/app/services/narrative_memory/authority.py"
      to: "backend/app/services/narrative_memory/audit_contracts.py"
      via: "Phase 12 EligibilityReport checksum and reusable_exact hierarchy precondition"
      pattern: "EligibilityReport"
    - from: "typed claim payload"
      to: "narrative_memory_claims.claim_checksum"
      via: "canonical strict serialization"
      pattern: "sort_keys=True"
---

<objective>Define the smallest closed typed memory language and an explicit-version persistence seam that cannot turn free text or caller-supplied lineage into authority.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-CONTEXT.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-RESEARCH.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-01-SUMMARY.md
@backend/app/services/narrative_memory/audit_contracts.py
@backend/app/services/narrative_memory/audit_pg.py
@backend/app/models/narrative_memory.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define strict memory and provenance contracts</name>
  <files>backend/app/services/narrative_memory/contracts.py, backend/tests/unit/narrative_memory/test_contracts.py</files>
  <action>Implement frozen Pydantic models with strict=True and extra=forbid for version lineage, chapter_state/story_arc/volume/global_story nodes, contains/derives_from edges, exact source links, uncertainty/confidence/visibility metadata, and the initial discriminated claim union: entity_state, event_fact, relationship_delta, clue_delta, world_state_delta, and open_loop_delta. Give each variant closed enums and typed prior/current/change fields; presentation statement/display text may exist only when explicitly non-authoritative and cannot satisfy a claim. Require explicit package-local node/claim/source keys and reject arbitrary dictionaries, unknown authoritative fields, strings coerced to numbers/booleans, and unknown enum values. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_contracts.py -q -x</automated></verify>
  <done>Every supported variant round-trips without coercion and the negative matrix fails closed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Canonicalize hashes and enforce package-local references</name>
  <files>backend/app/services/narrative_memory/contracts.py, backend/tests/unit/narrative_memory/test_contracts.py</files>
  <action>Add one narrative-memory canonical JSON encoder with explicit field ordering inputs, compact separators, Unicode preservation, and SHA-256 helpers for version lineage, nodes, claims, edges, and source links. Compute checksums from validated DTOs rather than accepting caller-provided content checksums. Validate positive/in-range visibility, confidence and uncertainty, inclusive chapter ranges, chapter_state singleton ranges, exact edge endpoint keys, direct claim-to-leaf references, and membership in the frozen candidate package. Prove insertion-order independence and sensitivity to every authoritative lineage/payload/reference field. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_contracts.py -q -x</automated></verify>
  <done>Equivalent validated objects hash identically, while any authoritative field change changes the relevant checksum.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Persist explicit-version candidates behind Phase 12 eligibility</name>
  <files>backend/app/services/narrative_memory/authority.py, backend/tests/integration/narrative_memory/test_contract_authority_pg.py</files>
  <action>Implement narrowly scoped repository/service functions to create an immutable version and insert validated nodes, claims, edges, and source links by explicit owner_id, novel_id, and version_id. Consume the Phase 12 EligibilityReport contract: require the hierarchy result to be reusable_exact, bind its policy/report checksum and exact build/snapshot/checksum, preserve optional-source lineage without making it required, and never derive provider authorization independently. Reject conflicting retries and package-external IDs; permit only byte-identical idempotent inserts before seal if the existing project pattern supports them. Add a forbidden-capability scan proving no provider gateway, worker dispatch, timeline/clue promotion setter, active pointer, Chroma, Reader Chat, or implicit current-version resolver is imported. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory/test_contracts.py tests/integration/narrative_memory/test_contract_authority_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/services/narrative_memory/contracts.py app/services/narrative_memory/authority.py tests/unit/narrative_memory/test_contracts.py tests/integration/narrative_memory/test_contract_authority_pg.py</automated></verify>
  <done>Only strict, eligibility-bound, explicit-version candidates reach PostgreSQL, and the package has no provider or production-selection capability.</done>
</task>
</tasks>

## Must-Haves

- All authoritative claims use a closed discriminated schema; free text is never sufficient authority.
- Canonical checksums are computed server-side from validated objects and frozen lineage.
- Chapter State is singleton-range; Story Arc/Volume and Global Story nodes use explicit inclusive ranges.
- Candidate package references are explicit and local; foreign or unresolved references fail before sealing.
- Phase 12 eligibility is consumed unchanged; no provider guard is recreated or weakened.
- All writes require an explicit version ID and cannot resolve or mutate a production/current pointer.

## Verification

<verification>Run strict unit round-trips and negative cases, PostgreSQL explicit-version persistence tests, forbidden-capability scans, and Ruff with `backend/.venv`.</verification>
<success_criteria>
- Six claim variants serialize deterministically and reject coercion/extra/unknown fields.
- Summary-only and arbitrary JSON facts cannot be persisted as claims.
- Blocked/rebuild-required hierarchy reports cannot create candidate authority.
- Candidate services contain no provider, worker, pointer, promotion, Chroma, or Reader Chat dependency.
</success_criteria>

<output>Create `13-02-SUMMARY.md` with the final claim union, canonicalization rules, fail-closed matrix, and persistence/capability test evidence.</output>
