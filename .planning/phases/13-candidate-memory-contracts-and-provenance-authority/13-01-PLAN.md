---
phase: 13-candidate-memory-contracts-and-provenance-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/narrative_memory.py
  - backend/app/models/__init__.py
  - backend/migrations/versions/13_narrative_memory_authority.py
  - backend/tests/integration/narrative_memory/test_candidate_authority_pg.py
requirements: [V08-MEM-01, V08-MEM-04, V08-MEM-05]
autonomous: true
must_haves:
  truths:
    - "Narrative memory has an owner/novel/version-scoped PostgreSQL authority that is independent from timeline, relationship, clue, and narrative-unit publication lifecycles."
    - "Every version, node, claim, edge, source link, manifest, and validation report is immutable or append-only and cannot cross owner, novel, or version scope."
    - "Phase 13 creates no run, stage, checkpoint, active pointer, promotion, rollback, provider, or consumer authority."
  artifacts:
    - path: "backend/app/models/narrative_memory.py"
      provides: "Candidate-only ORM authority for versions, nodes, claims, edges, source links, manifests, and validation reports"
    - path: "backend/migrations/versions/13_narrative_memory_authority.py"
      provides: "Additive migration from the exact current head 11cluetrack01 with database constraints and guards"
    - path: "backend/tests/integration/narrative_memory/test_candidate_authority_pg.py"
      provides: "PostgreSQL migration, scope, immutability, sealing, and no-pointer schema proof"
  key_links:
    - from: "narrative_memory_source_links.(hierarchy_build_id,evidence_node_id)"
      to: "chunk_hierarchy_nodes.(build_id,node_id)"
      via: "RESTRICT composite foreign key plus scope/snapshot validation trigger"
      pattern: "ForeignKeyConstraint"
    - from: "narrative_memory_nodes|claims|edges|source_links|manifests|validation_reports"
      to: "narrative_memory_versions.(owner_id,novel_id,id)"
      via: "repeated scope columns and composite foreign keys"
      pattern: "owner_id.*novel_id.*version_id"
---

<objective>Create the minimal additive PostgreSQL authority for immutable narrative-memory candidates, without introducing Phase 14 execution control or any production-selection path.</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-CONTEXT.md
@.planning/phases/13-candidate-memory-contracts-and-provenance-authority/13-RESEARCH.md
@.planning/phases/12-read-only-asset-audit-and-eligibility/12-VERIFICATION.md
@backend/app/models/chunk_build.py
@backend/app/models/knowledge_unit.py
@backend/migrations/versions/11_clue_tracking.py
</context>

## Steps

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the isolated candidate ORM authority</name>
  <files>backend/app/models/narrative_memory.py, backend/app/models/__init__.py, backend/tests/integration/narrative_memory/test_candidate_authority_pg.py</files>
  <action>Define exactly seven narrative_memory_* authorities: versions, nodes, claims, edges, source_links, manifests, and validation_reports. Repeat owner_id, novel_id, and version_id on subordinate rows; follow the Phase 05 composite scoped-key precedent; use RESTRICT for proof and candidate lineage; freeze source snapshot, Phase 07 hierarchy build/checksum, Phase 12 eligibility policy/report checksum, prompt/schema/model/decoding/config/policy lineage on the version. Keep node kinds closed to chapter_state, story_arc, volume, and global_story, while leaving Phase 07 ChunkHierarchyNode.level unchanged. Do not add a mutable version status, run/stage/checkpoint table, pointer, promotion journal, API, worker, Chroma projection, or chat consumer. Export the ORM models through the existing model registry. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_candidate_authority_pg.py -q -x</automated></verify>
  <done>Metadata contains the seven scoped candidate tables and contains no narrative_memory run/stage/checkpoint/pointer/promotion table.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add the migration, constraints, and physical guards</name>
  <files>backend/migrations/versions/13_narrative_memory_authority.py, backend/tests/integration/narrative_memory/test_candidate_authority_pg.py</files>
  <action>Create a single additive Alembic revision whose down_revision is exactly 11cluetrack01. Add composite unique keys/FKs, closed-kind and range/offset/confidence/checksum checks, required indexes, direct Phase 07 leaf identity FK, and triggers that verify build/novel/evidence-level/snapshot closure where Phase 07's key cannot encode owner. Add UPDATE/DELETE append-only guards to all seven authorities, a seal guard preventing late node/claim/edge/source-link inserts, and a deferred edge constraint trigger for allowed containment transitions, parent containment, and DAG cycles. Make downgrade explicitly drop every table trigger and then every migration-owned function so upgrade -> downgrade one revision -> upgrade is clean. Do not add run/stage/checkpoint authority; the roadmap shorthand is superseded by the approved Phase 13 context and those controls remain Phase 14. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m alembic upgrade head; .\.venv\Scripts\python.exe -m alembic downgrade 11cluetrack01; .\.venv\Scripts\python.exe -m alembic upgrade head; .\.venv\Scripts\python.exe -m alembic check</automated></verify>
  <done>Alembic has one head, round-trips to 11cluetrack01 and back, leaves no trigger/function residue on downgrade, and reports no metadata drift.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove database scope, immutability, and candidate-only boundaries</name>
  <files>backend/tests/integration/narrative_memory/test_candidate_authority_pg.py</files>
  <action>Exercise PostgreSQL with valid scoped rows and adversarial cross-owner, cross-novel, cross-version, wrong-build, non-evidence-node, invalid-range, direct-global-to-chapter, and cycle inserts. Assert all authority rows reject UPDATE/DELETE, content rows reject INSERT after seal, and downgrade removes migration-owned functions/triggers. Inspect information_schema/pg_catalog to assert no narrative_memory_active_pointers or equivalent current/production selector and no run/stage/checkpoint/promotion table exists. Test, Fix, and Confirm.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/narrative_memory/test_candidate_authority_pg.py -q -x; .\.venv\Scripts\ruff.exe check app/models/narrative_memory.py tests/integration/narrative_memory/test_candidate_authority_pg.py</automated></verify>
  <done>PostgreSQL itself rejects scope leaks and mutations, and schema inspection proves the Phase 13 authority is candidate-only.</done>
</task>
</tasks>

## Must-Haves

- The migration starts from exact head `11cluetrack01` and remains a single additive head.
- Seven candidate authority tables use composite owner/novel/version scope and immutable lineage.
- Direct leaf identity uses the existing Phase 07 `(build_id, node_id)` precedent without altering Phase 07.
- Version/content/seal/report authority is physically append-only, with seal and deferred graph guards.
- Run, stage, checkpoint, provider, API, worker, pointer, promotion, Chroma, and chat concerns remain absent.

## Verification

<verification>Run the full Alembic upgrade -> downgrade to `11cluetrack01` -> upgrade cycle, `alembic check`, PostgreSQL authority tests, and Ruff using `backend/.venv`.</verification>
<success_criteria>
- `alembic heads` reports one Phase 13 head whose parent is `11cluetrack01`.
- Cross-scope and invalid graph writes fail at the database boundary.
- Every migration-created trigger/function is gone after downgrade and recreated after upgrade.
- No production selector or Phase 14 control-plane table exists.
</success_criteria>

<output>Create `13-01-SUMMARY.md` with the exact revision ID, table/trigger inventory, migration round-trip evidence, and targeted test results.</output>
