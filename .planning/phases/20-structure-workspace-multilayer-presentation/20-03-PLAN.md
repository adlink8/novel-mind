---
phase: 20-structure-workspace-multilayer-presentation
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - frontend/src/components/structure/
  - frontend/src/components/timeline/
  - frontend/src/components/relationships/
  - frontend/src/components/clues/
  - backend/app/services/narrative_memory/structure_query.py
  - backend/tests/
  - frontend/src/**/*.test.tsx
requirements: [V10-STRUCT-05, V10-STRUCT-06]
autonomous: true
must_haves:
  truths:
    - "Selecting different structure scopes changes facet visible sets (chapter vs arc range)."
    - "NM claims panel can open source-links when present; no fake global without nodes."
    - "Phase 19 honesty badges remain visible on relationship/clue facets."
---

# 20-03 Scope-bound facets, claims drill, honesty polish

## Objective

Close the loop: structure selection drives **different** facet projections; NM claims drill to source links; progressive disclosure and honesty consistent with Phase 19.

## Steps

### Task 1: Facet density by scope
- When selected range spans **single chapter**: keep Phase 19 full chapter UX (multi-lane timeline, ego graph, clue cards)
- When range spans **multiple chapters** (arc/global selection):
  - Timeline: group/summarize by chapter or top-N major events (client filter + density note); do not render identical single-chapter scatter for 1000+ events without aggregation hint
  - Relationships: keep accepted-first; set through_chapter to range end
  - Clues: list threads intersecting range; show plant→payoff if both in range or partial
- UI copy: 「视图范围：第 A–B 章」 always visible near facet tabs

### Task 2: Claims + source-links drill
- On NM node select: fetch claims; click claim → fetch source-links
- Display chapter_number + short hash/offset; link to reader if existing pattern (`/novels/{id}?chapter=`)
- If no links: honest 「无叶子证据链接」

### Task 3: Regression guards
- Do not remove edge_kind / provisional controls from relationship workspace
- Do not remove clue plant→payoff card structure
- Structure collapse should not unmount facet state incorrectly (preserve novel selection)

### Task 4: Tests
- FE: scope label + filter reduces event count fixture
- BE: multi-chapter tree assembly still cutoff-safe
- **Test, Fix, and Confirm**

## Must-Haves

- Scope changes facet content
- Claims drill when data exists
- Phase 19 honesty preserved
- Still no promote

## Verification

```
cd backend
.\.venv\Scripts\python.exe -m pytest tests/unit/narrative_memory -q --tb=line
cd ../frontend
npm test -- --run
```

Run the narrowest failing subset first if full suite is slow; report what ran.
