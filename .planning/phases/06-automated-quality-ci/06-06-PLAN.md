---
phase: 06-automated-quality-ci
plan: 06
type: execute
wave: 6
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified: [.github/workflows/ci.yml, .github/workflows/backend-ci.yml, .github/workflows/frontend-ci.yml, .github/workflows/full-ci.yml, .github/codeql/codeql-config.yml, .github/ci/service-lock.json, .github/quality/baseline-policy.yml, scripts/ci/validate-workflow.py, scripts/ci/promote-baseline.py, tests/ci/test_workflow_security.py, tests/ci/test_artifact_policy.py, tests/ci/test_baseline_promotion.py]
autonomous: true
requirements: [REQ-AUTO-02, REQ-AUTO-09, REQ-AUTO-10]
must_haves:
  truths: ["统一DAG按PR/main/nightly安全分层", "fork不可达secret/self-hosted/write job", "artifacts/security/timeout策略精确", "nightly与baseline/alert失败关闭"]
  artifacts: [".github/workflows/ci.yml", ".github/quality/baseline-policy.yml", "scripts/ci/promote-baseline.py"]
  key_links: ["01..05 reports -> CI producer jobs", "nightly signed report -> promotion/alert policy"]
---

# Goal

建立统一CI生产者DAG、fork安全、coverage/artifact/security、nightly benchmark及baseline/alert policy；不负责最终ci-gate聚合或branch protection。（D-10、D-13、D-14、D-16..D-18）

# Dependencies

- 消费06-01..05全部命令和报告。actionlint锁v1.7.12并用Go checksum database验证。

# Steps

## Slice 1 — Unified DAG, fork safety, and security

<task type="auto">
  <name>Build event-safe CI producer DAG</name>
  <files>.github/workflows/ci.yml, .github/workflows/backend-ci.yml, .github/workflows/frontend-ci.yml, .github/workflows/full-ci.yml, .github/codeql/codeql-config.yml, .github/ci/service-lock.json, scripts/ci/validate-workflow.py, tests/ci/test_workflow_security.py</files>
  <action>收敛旧workflows：PR secretless deterministic+locked Postgres/Chroma+Chromium smoke；protected main full integration；schedule/self-hosted strict Ollama dual-model。fork不可达secrets/self-hosted/write jobs。workflow_dispatch验证protected ref、approved environment和fixed benchmark commit。锁job timeout static5m/unit10m/integration15m/browser15m/live45m/nightly60m；CodeQL Python+JS/TS、Ruff/Bandit/audits/actionlint只保留一套。</action>
  <verify><automated>go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12; actionlint -format JSON; pytest tests/ci/test_workflow_security.py</automated></verify>
  <done>各事件只运行允许jobs，fork权限不可升级，旧workflow不再重复触发。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>.github/workflows/ci.yml, scripts/ci/validate-workflow.py, tests/ci/test_workflow_security.py</files>
  <action>用PR/fork/protected-main/schedule/dispatch fixtures检查permissions、concurrency、needs、service locks、timeouts与CodeQL互斥并修复。</action>
  <verify><automated>actionlint -format JSON; pytest tests/ci/test_workflow_security.py -q</automated></verify>
  <done>所有事件和权限负例失败关闭。</done>
</task>

## Slice 2 — Artifacts, nightly, baseline, and isolated alerts

<task type="auto">
  <name>Implement retention, benchmark, promotion, and alert policy</name>
  <files>.github/workflows/ci.yml, .github/quality/baseline-policy.yml, scripts/ci/promote-baseline.py, tests/ci/test_artifact_policy.py, tests/ci/test_baseline_promotion.py, tests/ci/test_workflow_security.py</files>
  <action>retention锁PR JUnit/coverage/OpenAPI14d、Playwright failure7d、main integration/service logs30d、nightly signed RAG reports/baselines180d；敏感原文不上传。nightly执行3-repeat benchmark。仅passed/qualified签名报告可prepare/commit baseline。独立alert job仅schedule/protected main，经environment approval，权限仅contents:read+issues:write，不checkout PR代码，只消费hash/schema验证报告；fork不可达。</action>
  <verify><automated>pytest tests/ci/test_artifact_policy.py tests/ci/test_baseline_promotion.py tests/ci/test_workflow_security.py</automated></verify>
  <done>artifact、promotion和alert严格按来源、状态、签名及retention执行。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>.github/workflows/ci.yml, .github/quality/baseline-policy.yml, tests/ci/test_artifact_policy.py, tests/ci/test_baseline_promotion.py</files>
  <action>运行敏感数据、retention、签名、回归/blocked拒绝、告警去重、PR flake=0和30天required失败率<0.1%负例并修复。</action>
  <verify><automated>pytest tests/ci/test_artifact_policy.py tests/ci/test_baseline_promotion.py; actionlint -format JSON</automated></verify>
  <done>生产者DAG输出完整、签名、无敏感原文的reports供06-07聚合。</done>
</task>

# Must-Haves

- 自动tasks 4；files_modified 12；`.github/workflows/ci.yml`与06-07串行共享是最终聚合接线所必需。

# Verification

- 仅执行阶段运行task命令。

# Failure Modes

- fork越权、dispatch任意commit、artifact漂移、baseline误晋升或alert读取PR代码均阻断。

# Outputs

- CI producer DAG、nightly signed reports、baseline/alert与artifact/security策略。

