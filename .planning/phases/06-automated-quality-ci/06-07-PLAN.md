---
phase: 06-automated-quality-ci
plan: 07
type: execute
wave: 7
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified: [.github/workflows/ci.yml, scripts/ci/ci-gate.py, scripts/ci/configure-branch-protection.ps1, scripts/ci/verify-release-gate.py, tests/ci/test_ci_gate.py, tests/ci/test_branch_protection.py, tests/ci/test_release_gate.py]
autonomous: false
requirements: [REQ-AUTO-09, REQ-AUTO-10]
must_haves:
  truths: ["ci-gate拒绝failed/skipped/missing输入", "唯一required context远端回读为ci-gate", "权限不足明确阻断phase", "最终release gate验证七计划全部证据"]
  artifacts: ["scripts/ci/ci-gate.py", "scripts/ci/configure-branch-protection.ps1", "scripts/ci/verify-release-gate.py"]
  key_links: ["06-06 producer results -> ci-gate", "gh api write -> protection readback", "all phase manifests -> release gate"]
---

# Goal

实现最终ci-gate aggregate contract、branch protection幂等配置/回读、外部配置阻断和Phase 06 release gate。（D-19）

# Dependencies

- 依赖06-01..06全部报告与06-06统一workflow；仅为接入aggregate job串行修改共享`.github/workflows/ci.yml`。

# Steps

## Slice 1 — Aggregate and release contracts

<task type="auto">
  <name>Implement fail-closed ci-gate aggregate</name>
  <files>.github/workflows/ci.yml, scripts/ci/ci-gate.py, tests/ci/test_ci_gate.py</files>
  <action>接入唯一稳定job name `ci-gate`；使用always()读取事件适用矩阵、上游result、artifact hash/schema与policy。failed、cancelled、unexpected skipped、missing/stale artifact均失败；不重跑测试或重新解释质量分。</action>
  <verify><automated>pytest tests/ci/test_ci_gate.py</automated></verify>
  <done>只有事件要求的全部producer结果有效时ci-gate成功。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>scripts/ci/ci-gate.py, tests/ci/test_ci_gate.py</files>
  <action>运行success、failed need、cancelled、unexpected skipped、missing/hash/schema mismatch及timeout fixtures并修复。</action>
  <verify><automated>pytest tests/ci/test_ci_gate.py -q</automated></verify>
  <done>聚合门不存在skip/missing绕过。</done>
</task>

## Slice 2 — Branch protection and final release gate

<task type="auto">
  <name>Configure branch protection and verify phase release</name>
  <files>scripts/ci/configure-branch-protection.ps1, scripts/ci/verify-release-gate.py, tests/ci/test_branch_protection.py, tests/ci/test_release_gate.py</files>
  <action>以`gh api`幂等读取默认/protected branch，保留非required保护，将required contexts精确设为仅ci-gate并GET回读。权限不足输出blocked_external_configuration。release verifier检查七计划必需artifact、签名、状态、retention、安全报告、时限、flake/reliability及远端回读；任一缺失不完成phase。</action>
  <verify><automated>pytest tests/ci/test_branch_protection.py tests/ci/test_release_gate.py</automated></verify>
  <done>脚本幂等；release gate只能在唯一远端required context及全部证据有效时通过。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>scripts/ci/configure-branch-protection.ps1, scripts/ci/verify-release-gate.py, tests/ci/test_branch_protection.py, tests/ci/test_release_gate.py</files>
  <action>先运行mock正反例；有权限时运行配置两次并回读，再运行最终release verifier。无权限保留blocked_external_configuration。</action>
  <verify><automated>pytest tests/ci/test_branch_protection.py tests/ci/test_release_gate.py; powershell -File scripts/ci/configure-branch-protection.ps1 -Repository $env:GITHUB_REPOSITORY -Verify; python scripts/ci/verify-release-gate.py</automated></verify>
  <done>远端回读严格等于["ci-gate"]且release gate通过；否则phase未完成。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Resolve blocked external branch configuration</name>
  <files>scripts/ci/configure-branch-protection.ps1, tests/ci/test_branch_protection.py</files>
  <action>仅当gh api返回blocked_external_configuration时暂停；用户授予Administration写权限或由管理员运行同一幂等脚本。脚本从GITHUB_REPOSITORY解析owner/repo并通过GitHub API读取默认保护分支，不得跳过GET回读。</action>
  <verify><automated>powershell -File scripts/ci/configure-branch-protection.ps1 -Repository $env:GITHUB_REPOSITORY -Verify</automated></verify>
  <done>回读严格为["ci-gate"]；否则Phase 06保持未完成。</done>
</task>

# Must-Haves

- 自动tasks 4 + checkpoint 1；files_modified 7；无CI producer、安全扫描、artifact或alert职责。

# Verification

- 仅执行阶段运行task命令；checkpoint为不可绕过完成门。

# Failure Modes

- aggregate绕过、远端权限不足、required contexts不唯一或release证据缺失均阻断。

# Outputs

- 唯一ci-gate、已回读branch protection和Phase 06最终release verdict。
