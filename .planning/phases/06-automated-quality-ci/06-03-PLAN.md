---
phase: 06-automated-quality-ci
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified: [backend/app/models/eval.py, backend/app/schemas/eval.py, backend/migrations/versions/06_rag_fixture_jobs.py, backend/app/services/rag_fixture.py, backend/prompts/rag_fixture_generator.v1.txt, backend/prompts/rag_fixture_judge.v1.txt, backend/evals/calibration/rag-judge-calibration.v1.json, backend/evals/fixtures/rag-quality-benchmark.v1.json, backend/evals/fixtures/rag-quality-adversarial.v1.json, backend/tests/test_rag_quality_models.py, backend/tests/test_rag_quality_fixture.py, backend/tests/test_rag_quality_calibration.py, backend/tests/test_rag_quality_adversarial.py]
autonomous: true
requirements: [REQ-AUTO-05, REQ-AUTO-07]
must_haves:
  truths: ["冻结fixture以内容hash和evidence offsets为真值", "adversarial输入失败关闭", "G/J不同family及weights/revision", "Judge校准通过后才可供benchmark"]
  artifacts: ["backend/app/services/rag_fixture.py", "backend/evals/calibration/rag-judge-calibration.v1.json", "backend/evals/fixtures/rag-quality-adversarial.v1.json"]
  key_links: ["snapshot hash -> fixture signature", "calibration signature -> judge lineage"]
---

# Goal

交付自动 frozen fixture、对抗验证、Generator/Judge 隔离和独立签名 calibration，不执行 SUT 评分。（D-01..D-03、D-11、D-15）

# Dependencies

- 依赖 06-01 fail-closed 测试语义和 06-02 真实 PostgreSQL/Chroma 契约。

# Steps

## Slice 1 — Frozen fixture and adversarial contracts

<task type="auto">
  <name>Build frozen fixture contracts</name>
  <files>backend/app/models/eval.py, backend/app/schemas/eval.py, backend/migrations/versions/06_rag_fixture_jobs.py, backend/app/services/rag_fixture.py, backend/prompts/rag_fixture_generator.v1.txt, backend/prompts/rag_fixture_judge.v1.txt, backend/evals/fixtures/rag-quality-benchmark.v1.json, backend/evals/fixtures/rag-quality-adversarial.v1.json, backend/tests/test_rag_quality_models.py, backend/tests/test_rag_quality_fixture.py, backend/tests/test_rag_quality_adversarial.py</files>
  <action>实现source snapshot、chunk content hash、offset/quote hash、claims、equivalent evidence sets和签名fixture；G/J同时不同family及实际weights/revision。deterministic全过、Judge三项>=3/4、ambiguity=0；regenerate最多2后quarantine。对抗集覆盖instruction injection、超长、schema smuggling、恶意quote/offset和cross-owner，失败为invalid_fixture/failed_policy且metrics=null。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_models.py tests/test_rag_quality_fixture.py tests/test_rag_quality_adversarial.py -m "unit or contract" --junitxml=artifacts/fixture.xml</automated></verify>
  <done>无需人工逐题确认；DB自增ID不是真值；恶意输入不能冻结。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/test_rag_quality_fixture.py, backend/tests/test_rag_quality_adversarial.py</files>
  <action>运行hash/offset/equivalence/no-answer/hard-negative/regeneration/lineage/owner正反例并修复。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_fixture.py tests/test_rag_quality_adversarial.py --timeout=15</automated></verify>
  <done>fixture签名可重复，所有失败状态及空metrics精确。</done>
</task>

## Slice 2 — Independent Judge calibration

<task type="auto">
  <name>Freeze and evaluate Judge calibration suite</name>
  <files>backend/app/services/rag_fixture.py, backend/evals/calibration/rag-judge-calibration.v1.json, backend/tests/test_rag_quality_calibration.py</files>
  <action>冻结签名suite覆盖supported/partial/unsupported/contradictory/no-answer/hard-negative/equivalent evidence；每实际revision运行3次并输出混淆矩阵。critical false accept=0且consistency>=0.80，否则invalid_lineage。校验calibration与benchmark hash/domain隔离。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_calibration.py -m contract --junitxml=artifacts/calibration.xml</automated></verify>
  <done>仅绑定suite/prompt/schema/weights revision的合格报告可被06-04消费。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/test_rag_quality_calibration.py, backend/evals/calibration/rag-judge-calibration.v1.json</files>
  <action>运行签名、3-repeat、混淆矩阵、false accept、lineage及domain/hash隔离负例并修复。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_calibration.py --timeout=180</automated></verify>
  <done>不合格revision稳定返回invalid_lineage且metrics=null。</done>
</task>

# Must-Haves

- 自动 tasks 4；files_modified 13；不包含SUT scoring/worker/API职责。

# Verification

- 仅执行阶段运行 task 命令。

# Failure Modes

- snapshot、signature、owner、G/J隔离或calibration任一失败均不可进入benchmark。

# Outputs

- 签名 frozen fixture、adversarial suite与Judge calibration report契约。

