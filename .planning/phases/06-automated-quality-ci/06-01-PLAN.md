---
phase: 06-automated-quality-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/pytest.ini, backend/requirements-dev.txt, backend/tests/conftest.py, backend/tests/test_rag_e2e.py, backend/tests/test_vector_store.py, backend/tests/test_test_policy.py, backend/tests/fixtures/coverage-policy-invalid.yml, backend/tests/fixtures/coverage-policy-low.yml, frontend/vitest.config.ts, frontend/package.json, frontend/package-lock.json, frontend/src/__tests__/coverage-policy.test.ts, .quality/coverage-policy.yml, .quality/coverage-policy.schema.json]
autonomous: true
requirements: [REQ-AUTO-01, REQ-AUTO-02]
must_haves:
  truths: ["测试显式分类且默认不排除e2e", "随机向量只验证vector-store contract", "覆盖率/timeout/JUnit/flake门失败关闭"]
  artifacts: ["backend/pytest.ini", ".quality/coverage-policy.yml", ".quality/coverage-policy.schema.json"]
  key_links: ["markers -> CI selectors", "policy schema -> coverage and timeout gates"]
---

# Goal

建立测试分类、确定性语义边界及机器可执行的覆盖率、timeout、JUnit 与 flake 基础。（D-04、D-09、D-10、D-16）

# Dependencies

- 使用 RESEARCH 已核验锁：`pytest-cov==7.1.0`、`pytest-timeout==2.4.0`（pytest-dev），`vitest==4.1.10`、`@vitest/coverage-v8==4.1.10`；执行时更新 lockfile，本规划不安装。

# Steps

## Slice 1 — Backend classification and semantic fail-closed

<task type="auto">
  <name>Classify backend tests and remove semantic fallback</name>
  <files>backend/pytest.ini, backend/tests/conftest.py, backend/tests/test_rag_e2e.py, backend/tests/test_vector_store.py</files>
  <action>注册 unit/integration/contract/live/e2e 并启用 strict markers/config，删除默认 `not e2e`；collection gate 拒绝未分类测试。删除语义/live 路径随机 embedding fallback；随机固定向量只留在 vector-store contract。live 依赖不可用产生 blocked_dependency，metrics=null。</action>
  <verify><automated>cd backend; pytest --collect-only -q --strict-markers</automated></verify>
  <done>所有测试明确分类；语义测试无法以随机向量通过。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/conftest.py, backend/tests/test_rag_e2e.py, backend/tests/test_vector_store.py</files>
  <action>运行 collection、unit/contract 与依赖缺失 live 定向测试；修复分类与状态问题，确认 blocked_dependency 不产生可比较分。</action>
  <verify><automated>cd backend; pytest -m "unit or contract" tests/test_vector_store.py tests/test_rag_e2e.py --junitxml=artifacts/classification.xml</automated></verify>
  <done>未知/缺失 marker 失败；随机向量仅命中 vector-store contract。</done>
</task>

## Slice 2 — Coverage, timeout, JUnit, and flake policy

<task type="auto">
  <name>Lock tools and encode quality policy</name>
  <files>backend/requirements-dev.txt, frontend/package.json, frontend/package-lock.json, backend/pytest.ini, frontend/vitest.config.ts, .quality/coverage-policy.yml, .quality/coverage-policy.schema.json, backend/tests/test_test_policy.py, backend/tests/fixtures/coverage-policy-invalid.yml, backend/tests/fixtures/coverage-policy-low.yml, frontend/src/__tests__/coverage-policy.test.ts</files>
  <action>锁定指定工具版本。编码 backend 80/70、critical 90/85，frontend 75/65、hooks/store/API 85/75、diff 90；timeout unit5s/contract15s/integration30s/browser60s/live180s。policy 缺字段、critical glob零命中和低阈值均失败。PR flake=0；外部infra最多retry1且先保存首次失败。</action>
  <verify><automated>cd backend; pytest tests/test_test_policy.py -m contract --junitxml=artifacts/policy.xml</automated></verify>
  <done>版本、阈值、timeout和负例均由schema及测试锁定。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/test_test_policy.py, frontend/src/__tests__/coverage-policy.test.ts, .quality/coverage-policy.yml</files>
  <action>生成 backend Cobertura/JUnit 与 frontend LCOV/JUnit，执行policy正反例和diff coverage；修复至所有门按锁定值工作。</action>
  <verify><automated>cd backend; pytest -m "unit or contract" --cov=app --cov-branch --cov-report=xml:artifacts/backend-coverage.xml --junitxml=artifacts/backend-junit.xml; cd ../frontend; npm run test:coverage</automated></verify>
  <done>低覆盖/缺policy失败，合格报告通过且artifact存在。</done>
</task>

# Must-Haves

- D-04/D-09/D-10/D-16 全部精确编码；工具版本不可漂移。

# Verification

- 仅执行阶段运行上述 `<automated>` 命令；规划阶段不运行。

# Failure Modes

- 未分类、随机语义fallback、缺policy、coverage glob零命中、超timeout或PR flake均失败关闭。

# Outputs

- 确定性测试 taxonomy、锁定工具、coverage/timeout/flake policy 与标准报告。
