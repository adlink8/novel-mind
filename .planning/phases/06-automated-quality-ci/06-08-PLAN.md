---
phase: 06-automated-quality-ci
plan: 08
type: execute
gap_closure: true
wave: 1
depends_on: [06-03, 06-04]
files_modified:
  - backend/app/models/eval.py
  - backend/migrations/versions/07_quality_runs.py
  - backend/app/schemas/eval.py
  - backend/app/api/eval.py
  - backend/app/services/rag_quality_worker.py
  - backend/app/services/rag_quality.py
  - backend/tests/test_rag_quality_models.py
  - backend/tests/test_rag_quality_worker.py
  - backend/tests/test_rag_quality_scoring.py
  - backend/tests/test_eval_api.py
autonomous: true
requirements: [REQ-AUTO-11]
must_haves:
  truths:
    - "Quality run、租约、checkpoint、stage cache、metrics 与终态跨进程重启持久存在。"
    - "每个可比较 run 都持有 chunker_name、chunker_version、chunker_config_hash、chunk_manifest_hash、source_snapshot_hash 五元 canonical lineage。"
    - "五元 lineage 进入 input hash、每个 stage-cache/idempotency key、report/output hash 与 report signature；同 snapshot 不同 chunker 永不碰撞。"
    - "迁移前记录不伪造 lineage，固定为不可比较。"
  artifacts:
    - path: "backend/app/models/eval.py"
      provides: "QualityRun 持久 ORM 与 lineage/hash/lease/checkpoint 字段"
    - path: "backend/migrations/versions/07_quality_runs.py"
      provides: "quality_runs 表、约束、索引和 legacy fail-closed 迁移"
    - path: "backend/app/services/rag_quality_worker.py"
      provides: "数据库 repository/store 与 lineage-aware worker 幂等状态机"
    - path: "backend/app/api/eval.py"
      provides: "通过 AsyncSession 注入持久 repository 的质量运行 API"
  key_links:
    - from: "backend/app/services/rag_quality_worker.py"
      to: "backend/app/models/eval.py"
      via: "QualityJobStore repository methods use transactional QualityRun rows"
      pattern: "QualityRun|AsyncSession"
    - from: "backend/app/services/rag_quality_worker.py"
      to: "backend/app/services/rag_quality.py"
      via: "canonical lineage is supplied to scoring and signed report construction"
      pattern: "lineage|input_hash|output_hash|report_signature"
    - from: "backend/app/api/eval.py"
      to: "backend/app/services/rag_quality_worker.py"
      via: "route dependency creates owner-scoped AsyncSession repository instead of global in-memory store"
      pattern: "AsyncSession|get_db|repository"
---

<objective>
关闭 REQ-AUTO-11 的运行事实源缺口：把当前 `rag_quality_worker.py` 中内存 `QualityJob`/`QualityJobStore` 替换为真正持久的 QualityRun repository，并使五元 chunker/source lineage 成为所有运行、缓存和签名 hash 的组成部分。

Purpose: 进程重启后仍能安全恢复，且任何 chunker/source 差异都不能复用错误缓存或产生碰撞报告。
Output: QualityRun ORM/migration、持久 store、AsyncSession API wiring、canonical lineage schema、lineage-aware scoring/hash/signature 与回归测试。
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-quality-ci/06-03-SUMMARY.md
@.planning/phases/06-automated-quality-ci/06-04-SUMMARY.md
@backend/app/models/eval.py
@backend/app/schemas/eval.py
@backend/app/api/eval.py
@backend/app/services/rag_quality_worker.py
@backend/app/services/rag_quality.py

<interfaces>
Current `QualityJob` and `QualityJobStore` are both defined in `backend/app/services/rag_quality_worker.py`; the store is a process-global in-memory dictionary. Preserve the public worker operations `create_job`, `acquire_lease`, `heartbeat`, `release_lease`, `request_cancel`, `run`, `resume` and owner isolation while changing their backing implementation. `run_quality_evaluation` in `rag_quality.py` owns report assembly and `report_signature`; `stable_hash` is the existing canonical hash helper. `RagSourceSnapshot.manifest_hash` is the persisted source evidence anchor. Do not add a second lineage service file.
</interfaces>
</context>

## Steps

1. Define the database contract and migration.
2. Replace the in-memory dictionary with a transactional repository and route-level AsyncSession wiring while preserving worker behavior.
3. Make canonical lineage part of every identity/cache/output/signature boundary.
4. Prove restart recovery, collision isolation, and legacy fail-closed behavior.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist QualityRun and canonical five-tuple lineage</name>
  <files>backend/app/models/eval.py, backend/migrations/versions/07_quality_runs.py, backend/app/schemas/eval.py, backend/tests/test_rag_quality_models.py</files>
  <behavior>
    - PostgreSQL stores run payload, state, lease, checkpoint, stage cache, metrics, report, hashes and all five lineage fields.
    - Canonical lineage requires non-empty chunker_name/version and SHA-256 config/manifest/snapshot hashes; config hash is recomputed from canonical config rather than trusted from the caller.
    - New comparable rows require complete lineage; migrated/legacy rows remain readable with quality_comparable=false and an explicit legacy_incomparable reason.
  </behavior>
  <action>For REQ-AUTO-11, first add failing ORM/migration/schema tests. Add `QualityRun` to `backend/app/models/eval.py` with unique job_id, owner/work scope, status/attempt/cancel, lease timestamps/token, JSON payload/checkpoint/stage_cache/metrics/report, input_hash/output_hash/report_signature, five explicit lineage columns, quality_comparable and error_detail. Create the next Alembic revision after the actual current head; use constraints/indexes for owner lookup, status/recovery, unique job id, and fail-closed comparable lineage. Define the canonical lineage request/response contract in `schemas/eval.py`; derive `chunker_config_hash` using stable canonical JSON and verify snapshot/manifest hashes against persisted evidence. Existing rows or requests without provable lineage are legacy-incomparable, never backfilled with invented values. Test, Fix, and Confirm: run the model tests, fix schema/migration mismatches, then confirm upgrade/downgrade and legacy rows preserve the fail-closed invariant.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_models.py -x</automated></verify>
  <done>QualityRun is a real persisted database contract; complete canonical lineage is mandatory for comparability and old data is explicitly incomparable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the in-memory QualityJobStore with a transactional repository</name>
  <files>backend/app/services/rag_quality_worker.py, backend/app/api/eval.py, backend/tests/test_rag_quality_worker.py, backend/tests/test_eval_api.py</files>
  <behavior>
    - A fresh worker/repository instance can load and resume a previously created run with checkpoint and stage cache intact.
    - Lease acquisition is atomic; concurrent or stale lease holders cannot mutate a run, while expired leases can be reclaimed.
    - Create/get/save/list/cancel/resume preserve owner isolation and the existing API-facing public shape through an AsyncSession-injected repository.
  </behavior>
  <action>For REQ-AUTO-11, write restart, concurrent-lease and API dependency failures first. Refactor the existing `QualityJobStore` location into a database-backed repository over `QualityRun` and explicit transactions; remove the dictionary as the production fact source (a test double may implement the same repository protocol). Use compare-and-set status/lease predicates so two workers cannot acquire or checkpoint the same active lease. Serialize and restore payload, checkpoint and stage_cache on every committed stage, and make cancel/terminal transitions atomic. Update `api/eval.py` quality routes to inject the project AsyncSession dependency and construct/pass an owner-scoped repository or async worker facade; no route may use the old process-global store. Preserve existing paths, status codes and public response fields. Test, Fix, and Confirm: run worker/API tests, fix transaction/session issues, then use a second repository/worker and fresh API dependency scope to confirm resume without duplicate scorer/model calls.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_worker.py tests/test_eval_api.py -x</automated></verify>
  <done>Production quality jobs survive restart and use database concurrency controls through real quality API routes; no process-global dictionary is authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind lineage into input, stage-cache, output and signature identities</name>
  <files>backend/app/services/rag_quality_worker.py, backend/app/services/rag_quality.py, backend/tests/test_rag_quality_worker.py, backend/tests/test_rag_quality_scoring.py</files>
  <behavior>
    - input_hash includes the complete canonical lineage and differs for the same snapshot under any different chunker name/version/config/manifest.
    - Every stage-cache/idempotency key includes run input_hash or the canonical lineage digest, so cross-chunker calls cannot be reused.
    - Report payload contains canonical lineage before output_hash and report_signature are calculated; changing lineage changes both and invalidates signature reuse.
    - Missing/mismatched lineage terminates invalid_lineage with metrics=null and quality_comparable=false before scoring.
  </behavior>
  <action>For REQ-AUTO-11, add collision and tamper tests first. Build input_hash from snapshot/cases/policy/baseline plus all five canonical lineage values. Thread that identity through `run_case_once`/stage cache keys and persisted checkpoints. In `run_quality_evaluation`, include canonical lineage in the signed report payload; compute output_hash over the complete unsigned report, then sign the complete report contract consistently, storing output_hash and signature on QualityRun. Validate lineage before retrieval/scoring and atomically clear metrics on invalid_lineage. Test, Fix, and Confirm: run scoring and worker tests, fix every cache/signature mismatch, then confirm a same-snapshot two-chunker fixture has distinct input keys, stage keys, output hashes and valid independent signatures.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_worker.py tests/test_rag_quality_scoring.py -x</automated></verify>
  <done>Canonical lineage is cryptographically and idempotently inseparable from each run and report; same-snapshot chunkers never collide.</done>
</task>

</tasks>

## Must-Haves

- REQ-AUTO-11 five-tuple: `chunker_name`, `chunker_version`, `chunker_config_hash`, `chunk_manifest_hash`, `source_snapshot_hash`.
- QualityRun is the persistent fact source for repository, lease, checkpoint, cache, metrics and report.
- Every identity boundary includes canonical lineage; validation happens before scoring.
- Legacy records remain readable but cannot become comparable or baseline-eligible.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller → lineage schema | Caller-supplied names/config/hashes are untrusted |
| worker → PostgreSQL | Lease/checkpoint updates require atomic ownership checks |
| persisted run → cache/report | Stored payload can be stale or tampered and must be revalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-08-01 | Tampering | lineage/hash chain | mitigate | Recompute canonical config hash, verify evidence hashes, include lineage in all hashes/signature |
| T-06-08-02 | Elevation of Privilege | QualityRun repository | mitigate | Owner-scoped queries and lease-token CAS updates |
| T-06-08-03 | Repudiation | legacy/mismatched runs | mitigate | Persist explicit incomparable reason and immutable report identity |
| T-06-08-SC | Tampering | package installs | mitigate | No package installs are planned |
</threat_model>

## Verification

- `cd backend; pytest tests/test_rag_quality_models.py tests/test_rag_quality_worker.py tests/test_rag_quality_scoring.py -x`
- `cd backend; alembic upgrade head; alembic current; alembic check`
- `cd backend; pytest tests/test_rag_quality_models.py tests/test_rag_quality_worker.py tests/test_rag_quality_scoring.py tests/test_eval_api.py -x`

<success_criteria>
REQ-AUTO-11 run persistence is complete when a database-backed QualityRun can restart/resume safely, only complete proven five-tuple lineage can be comparable, same-snapshot chunkers cannot share any idempotency/report identity, and legacy data is explicitly incomparable.
</success_criteria>

<output>Create `.planning/phases/06-automated-quality-ci/06-08-SUMMARY.md` when done.</output>
