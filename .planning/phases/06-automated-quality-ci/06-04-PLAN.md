---
phase: 06-automated-quality-ci
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified: [backend/app/services/eval_service.py, backend/app/services/rag_quality.py, backend/app/services/rag_quality_worker.py, backend/app/api/eval.py, backend/scripts/run_rag_quality.py, backend/scripts/migrate_legacy_eval.py, backend/prompts/rag_answer_judge.v1.txt, backend/evals/rag-quality-policy.v1.yml, backend/tests/test_rag_quality_scoring.py, backend/tests/test_rag_quality_worker.py, backend/tests/test_eval_api.py, backend/tests/test_eval_service.py, backend/tests/live/test_rag_quality_dual_model.py]
autonomous: true
requirements: [REQ-AUTO-06, REQ-AUTO-10]
must_haves:
  truths: ["SUT真实检索并生成答案", "四项质量指标与阈值失败关闭", "后台run可持久resume", "旧Eval API兼容迁移"]
  artifacts: ["backend/app/services/rag_quality.py", "backend/app/services/rag_quality_worker.py", "backend/evals/rag-quality-policy.v1.yml"]
  key_links: ["06-03 signed fixture/calibration -> SUT run", "arbiter -> terminal status", "legacy API -> durable job adapter"]
---

# Goal

消费 06-03 合格fixture与lineage，实现SUT scoring、四项质量指标、policy arbiter、durable worker及旧Eval API兼容迁移。（D-06..D-08）

# Dependencies

- 依赖06-01测试policy、06-02真实服务、06-03签名fixture与calibration。

# Steps

## Slice 1 — SUT scoring and deterministic policy

<task type="auto">
  <name>Implement SUT answer and quality arbiter</name>
  <files>backend/app/services/rag_quality.py, backend/prompts/rag_answer_judge.v1.txt, backend/evals/rag-quality-policy.v1.yml, backend/tests/test_rag_quality_scoring.py, backend/tests/live/test_rag_quality_dual_model.py</files>
  <action>执行retrieval+answer，计算answer faithfulness/relevance、context precision/recall@5、critical unsupported claims、3-repeat consistency、cost和p95 latency。锁定D-08全部阈值；缺policy/p95/baseline/health/lineage失败关闭。回归为failed_policy/quality_regression；仅passed/qualified可用于baseline，其余metrics=null。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_scoring.py -m "unit or contract" --junitxml=artifacts/scoring.xml</automated></verify>
  <done>四项指标、统计边界、成本/延迟和状态均由确定性arbiter决定。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/test_rag_quality_scoring.py, backend/tests/live/test_rag_quality_dual_model.py</files>
  <action>运行policy边界、3-repeat及live G/J/SUT；依赖缺失必须只返回blocked_dependency且metrics=null。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_scoring.py --timeout=30; pytest tests/live/test_rag_quality_dual_model.py -m live --timeout=180</automated></verify>
  <done>live可比较结果仅来自完整依赖和已校准lineage。</done>
</task>

## Slice 2 — Durable worker and Eval API migration

<task type="auto">
  <name>Implement durable run/resume and legacy adapter</name>
  <files>backend/app/services/eval_service.py, backend/app/services/rag_quality_worker.py, backend/app/api/eval.py, backend/scripts/run_rag_quality.py, backend/scripts/migrate_legacy_eval.py, backend/tests/test_rag_quality_worker.py, backend/tests/test_eval_api.py, backend/tests/test_eval_service.py</files>
  <action>实现lease/heartbeat/checkpoint/resume/cancel与阶段幂等。保留旧POST/GET/list/PATCH字段并追加job/status/deprecation；legacy gold可证明时迁移hash/evidence，不可证明则quarantine。异常不得catch成0。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_worker.py tests/test_eval_api.py tests/test_eval_service.py -m "unit or contract" --junitxml=artifacts/worker-api.xml</automated></verify>
  <done>crash后从checkpoint恢复，无重复模型调用；旧客户端继续可用。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/test_rag_quality_worker.py, backend/tests/test_eval_api.py, backend/tests/test_eval_service.py</files>
  <action>运行crash/resume、idempotency、cross-owner、cancel、legacy/new response和迁移负例并修复。</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_worker.py tests/test_eval_api.py tests/test_eval_service.py --timeout=30</automated></verify>
  <done>后台与兼容API状态一致，blocked/invalid/fail均无聚合分。</done>
</task>

# Must-Haves

- 自动tasks 4；files_modified 13；不拥有fixture/calibration生成文件。

# Verification

- 仅执行阶段运行task命令。

# Failure Modes

- 未校准fixture、依赖outage、policy缺失、worker重复或兼容破坏均阻断。

# Outputs

- 可恢复SUT质量run、签名报告及兼容Eval API。

