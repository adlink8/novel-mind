---
phase: 06-automated-quality-ci
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [docker-compose.ci.yml, .github/ci/service-lock.json, backend/requirements.txt, backend/tests/integration/conftest.py, backend/tests/integration/test_postgres_migrations.py, backend/tests/integration/test_postgres_semantics.py, backend/tests/integration/test_chroma_contract.py, backend/tests/integration/test_chroma_recovery.py]
autonomous: true
requirements: [REQ-AUTO-03, REQ-AUTO-04]
must_haves:
  truths: ["PostgreSQL16真实迁移与语义通过", "Chroma版本/digest/client/health一致", "服务故障恢复不伪成功"]
  artifacts: ["docker-compose.ci.yml", ".github/ci/service-lock.json", "backend/tests/integration/test_postgres_migrations.py"]
  key_links: ["registry digest -> lock manifest -> compose", "service health -> integration fixtures"]
---

# Goal

以锁定 PostgreSQL 16 与 Chroma 验证迁移、专属数据库行为、故障与恢复。（D-05、D-07）

# Dependencies

- 依赖 06-01 marker/timeout/report。PostgreSQL tag `postgres:16.10`；Chroma image 与 Python client 使用 RESEARCH 精确锁。

# Steps

## Slice 1 — PostgreSQL migration and semantics

<task type="auto">
  <name>Pin services and verify PostgreSQL migration matrix</name>
  <files>docker-compose.ci.yml, .github/ci/service-lock.json, backend/tests/integration/conftest.py, backend/tests/integration/test_postgres_migrations.py, backend/tests/integration/test_postgres_semantics.py</files>
  <action>registry解析 `postgres:16.10` digest并写CI lock manifest，digest缺失/漂移fail closed。配置health与隔离DB。验证 heads、空库upgrade/current/check/history、历史revision升级、tsvector、约束、owner隔离及import/promotion/rollback并发。</action>
  <verify><automated>cd backend; alembic heads; alembic upgrade heads; alembic current; alembic check; alembic history; pytest -m integration tests/integration/test_postgres_migrations.py tests/integration/test_postgres_semantics.py --junitxml=artifacts/postgres.xml</automated></verify>
  <done>tag/digest锁定且所有PostgreSQL16迁移/语义/并发断言通过。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>.github/ci/service-lock.json, backend/tests/integration/test_postgres_migrations.py, backend/tests/integration/test_postgres_semantics.py</files>
  <action>从空库与选定历史revision分别执行完整矩阵；修复后确认digest、schema、数据和并发约束一致。</action>
  <verify><automated>cd backend; pytest -m integration tests/integration/test_postgres_migrations.py tests/integration/test_postgres_semantics.py --timeout=30</automated></verify>
  <done>历史升级无丢失，SQLite结果未被用于PostgreSQL结论。</done>
</task>

## Slice 2 — Chroma contract, outage, and recovery

<task type="auto">
  <name>Lock Chroma and implement recovery contracts</name>
  <files>docker-compose.ci.yml, .github/ci/service-lock.json, backend/requirements.txt, backend/tests/integration/test_chroma_contract.py, backend/tests/integration/test_chroma_recovery.py</files>
  <action>锁 `chromadb/chroma:1.5.9@sha256:abcce7c335e2dab9f11ef629296f7309b09cb19ae4b34da32ac7e34ff5773140`、health `/api/v2/heartbeat`、Python `chromadb==1.5.9`。验证CRUD/reconcile与DB/Chroma stop、timeout、坏响应、恢复幂等；固定向量只做store contract。</action>
  <verify><automated>cd backend; pytest -m integration tests/integration/test_chroma_contract.py tests/integration/test_chroma_recovery.py --junitxml=artifacts/chroma.xml --timeout=30</automated></verify>
  <done>版本/health/client一致；故障无部分成功和质量分。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/integration/test_chroma_contract.py, backend/tests/integration/test_chroma_recovery.py</files>
  <action>执行故障注入，首次失败证据先落盘，最多retry1；恢复后核对collection count/checksum与DB journal。</action>
  <verify><automated>docker compose -f docker-compose.ci.yml up -d --wait db chroma; cd backend; pytest -m integration tests/integration/test_chroma_contract.py tests/integration/test_chroma_recovery.py --timeout=30</automated></verify>
  <done>恢复幂等、无重复写、首次失败证据存在。</done>
</task>

# Must-Haves

- PostgreSQL digest manifest与Chroma精确锁缺失均fail closed。

# Verification

- 仅执行阶段运行 task 中命令。

# Failure Modes

- digest、health、client、revision或故障状态不匹配即失败；blocked依赖metrics=null。

# Outputs

- 固定真实服务拓扑、lock manifest及集成/恢复证据。
