---
phase: 06-automated-quality-ci
plan: 05
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified: [backend/openapi-baseline.json, backend/scripts/export_openapi.py, backend/tests/contract/test_openapi_contract.py, backend/tests/fixtures/openapi/nonbreaking.json, backend/tests/fixtures/openapi/breaking.json, frontend/src/lib/api.ts, frontend/src/lib/api.contract.test.ts, frontend/src/app/eval/page.test.tsx, frontend/src/hooks/use-eval.test.ts, frontend/src/stores/eval.test.ts, frontend/playwright.config.ts, frontend/e2e/core-flow.spec.ts, frontend/e2e/error-and-isolation.spec.ts, frontend/package.json, frontend/package-lock.json]
autonomous: true
requirements: [REQ-AUTO-08]
must_haves:
  truths: ["OpenAPI breaking diff阻断", "frontend兼容legacy/new Eval", "desktop与390px核心/错误/隔离旅程通过"]
  artifacts: ["backend/scripts/export_openapi.py", "backend/openapi-baseline.json", "frontend/playwright.config.ts"]
  key_links: ["OpenAPI -> consumer tests", "06-04 statuses -> eval UI", "real services -> Playwright"]
---

# Goal

自动验证OpenAPI、frontend consumer/component/hooks/store和desktop/390px Playwright。（D-07、D-12）

# Dependencies

- 依赖06-04最终兼容API。锁`@playwright/test==1.61.1`与oasdiff v1.17.0；Go checksum database验证。

# Steps

## Slice 1 — OpenAPI and frontend contracts

<task type="auto">
  <name>Implement OpenAPI export and frontend contracts</name>
  <files>backend/openapi-baseline.json, backend/scripts/export_openapi.py, backend/tests/contract/test_openapi_contract.py, backend/tests/fixtures/openapi/nonbreaking.json, backend/tests/fixtures/openapi/breaking.json, frontend/src/lib/api.ts, frontend/src/lib/api.contract.test.ts, frontend/src/app/eval/page.test.tsx, frontend/src/hooks/use-eval.test.ts, frontend/src/stores/eval.test.ts, frontend/package.json, frontend/package-lock.json</files>
  <action>确定性导出OpenAPI；用`go install github.com/oasdiff/oasdiff@v1.17.0`及checksum验证。锁定breaking命令；正反fixture覆盖删除/类型/required/auth/status。frontend消费legacy/new job和全部质量状态，API/hooks/store达到85/75。</action>
  <verify><automated>cd backend; python -c "from pathlib import Path; Path('artifacts').mkdir(exist_ok=True)"; python scripts/export_openapi.py --output artifacts/openapi.json; oasdiff breaking openapi-baseline.json artifacts/openapi.json --fail-on ERR; pytest tests/contract/test_openapi_contract.py -m contract; cd ../frontend; npm run test:coverage</automated></verify>
  <done>真实schema无breaking，反fixture失败，frontend契约与覆盖率达标。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>backend/tests/contract/test_openapi_contract.py, backend/tests/fixtures/openapi/nonbreaking.json, backend/tests/fixtures/openapi/breaking.json, frontend/src/lib/api.contract.test.ts, frontend/src/app/eval/page.test.tsx, frontend/src/hooks/use-eval.test.ts, frontend/src/stores/eval.test.ts</files>
  <action>运行export、真实baseline、nonbreaking正例、breaking反例及Vitest coverage并修复。</action>
  <verify><automated>cd backend; python -c "from pathlib import Path; Path('artifacts').mkdir(exist_ok=True)"; oasdiff breaking openapi-baseline.json tests/fixtures/openapi/nonbreaking.json --fail-on ERR; python -c "import subprocess; assert subprocess.run(['oasdiff','breaking','openapi-baseline.json','tests/fixtures/openapi/breaking.json','--fail-on','ERR']).returncode != 0"; cd ../frontend; npm run test:coverage</automated></verify>
  <done>正反契约和frontend状态渲染均稳定。</done>
</task>

## Slice 2 — Browser journeys

<task type="auto">
  <name>Implement desktop and 390px journeys</name>
  <files>frontend/playwright.config.ts, frontend/e2e/core-flow.spec.ts, frontend/e2e/error-and-isolation.spec.ts, frontend/package.json, frontend/package-lock.json</files>
  <action>配置Chromium desktop/390px、browser60s、trace/screenshots；覆盖注册登录上传导入搜索阅读eval resume报告、跨用户404、DB/Chroma blocked和错误状态。核心成功路径不用route mock。</action>
  <verify><automated>cd frontend; npx playwright test --project=chromium-desktop --project=chromium-mobile-390</automated></verify>
  <done>双viewport核心与错误旅程通过。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <files>frontend/playwright.config.ts, frontend/e2e/core-flow.spec.ts, frontend/e2e/error-and-isolation.spec.ts</files>
  <action>在真实服务上运行并修复console/page errors、owner隔离和artifact路径。</action>
  <verify><automated>cd frontend; npx playwright test --project=chromium-desktop --project=chromium-mobile-390 --reporter=junit,html</automated></verify>
  <done>无跨用户泄漏，失败trace/screenshots可供06-06上传。</done>
</task>

# Must-Haves

- 自动tasks 4；files_modified 15；从原06-04完整迁移且依赖06-04。

# Verification

- 仅执行阶段运行task命令。

# Failure Modes

- checksum、breaking diff、frontend覆盖或任一viewport失败均阻断。

# Outputs

- API/frontend/browser自动化证据。
