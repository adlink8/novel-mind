---
phase: 06-automated-quality-ci
plan: 09
type: execute
gap_closure: true
wave: 2
depends_on: [06-08]
files_modified:
  - backend/app/models/eval.py
  - backend/migrations/versions/08_baseline_candidates.py
  - backend/app/schemas/eval.py
  - backend/app/services/rag_quality.py
  - backend/app/api/eval.py
  - backend/tests/test_rag_quality_models.py
  - backend/tests/test_rag_quality_scoring.py
  - backend/tests/test_eval_api.py
autonomous: true
requirements: [REQ-AUTO-11]
must_haves:
  truths:
    - "BaselineCandidate、prepare evidence 与 commit journal 持久存在，prepare 和 commit 都从数据库重读 QualityRun 并复验完整可比较 lineage。"
    - "commit 只消费未变化的 qualified/passed run；lineage、hash、signature 或状态在 prepare 后变化则拒绝且不替换 active baseline。"
    - "报告按同一 source snapshot 聚合并并列不同 chunker；不完整、旧数据及不同 snapshot 被明确排除。"
    - "现有 quality run API 路径和既有响应字段保持兼容，只增量提供 baseline/report 能力与 lineage 字段。"
  artifacts:
    - path: "backend/app/models/eval.py"
      provides: "BaselineCandidate durable prepare/commit state and journal"
    - path: "backend/app/services/rag_quality.py"
      provides: "two-phase baseline revalidation and cross-chunker report aggregation"
    - path: "backend/app/api/eval.py"
      provides: "compatible prepare/commit/report endpoints"
  key_links:
    - from: "backend/app/services/rag_quality.py"
      to: "backend/app/models/eval.py"
      via: "prepare/commit reload QualityRun and persist BaselineCandidate journal"
      pattern: "BaselineCandidate|prepare|commit"
    - from: "backend/app/api/eval.py"
      to: "backend/app/services/rag_quality.py"
      via: "owner-scoped baseline and report services"
      pattern: "baseline|report"
---

<objective>
完成 REQ-AUTO-11 的消费端闭环：实现持久 BaselineCandidate prepare/commit 两阶段复验和同 snapshot 跨 chunker 报告聚合，同时保持旧 Eval/quality API 兼容。

Purpose: baseline 不能消费旧、缺失或在 prepare 后变化的 lineage；报告必须真实展示同源不同 chunker 的可比较结果。
Output: BaselineCandidate ORM/migration、两阶段服务、聚合报告、兼容 API 与测试。
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automated-quality-ci/06-08-SUMMARY.md
@backend/app/models/eval.py
@backend/app/services/rag_quality.py
@backend/app/api/eval.py
@backend/app/schemas/eval.py

<interfaces>
Consume the persisted `QualityRun` repository and canonical five-tuple lineage created by 06-08. Existing routes `POST /api/eval/quality/runs`, `GET /api/eval/quality/runs/{job_id}`, resume/cancel/list and legacy `/api/eval/runs` contracts must retain their paths, status codes and existing fields. Additive fields/endpoints are allowed. Baseline decisions must use database state, not a client-supplied baseline dictionary.
</interfaces>
</context>

## Steps

1. Persist the two-phase baseline candidate and immutable prepare evidence.
2. Implement prepare/commit revalidation against current QualityRun state.
3. Aggregate same-snapshot cross-chunker reports and expose additive compatible APIs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist BaselineCandidate prepare and commit journal</name>
  <files>backend/app/models/eval.py, backend/migrations/versions/08_baseline_candidates.py, backend/app/schemas/eval.py, backend/tests/test_rag_quality_models.py</files>
  <behavior>
    - Candidate stores owner, source QualityRun id, state, canonical lineage, prepared input/output/report signature, metrics snapshot and prepare/commit timestamps.
    - Only one commit transition succeeds; rejection/expiry/commit states are auditable and cannot silently overwrite history.
    - Legacy or incomplete QualityRun cannot satisfy the database/service eligibility contract.
  </behavior>
  <action>For REQ-AUTO-11, write persistence/state-transition tests first. Add `BaselineCandidate` with an FK to 06-08 `QualityRun`, owner scope, prepare token/version, copied five-tuple lineage, run hashes/signature, metrics snapshot, state/reason and append-only journal/audit fields. Create the next Alembic revision after 06-08 with indexes and uniqueness needed for idempotent prepare and single commit. Define additive prepare/commit/report schemas in `schemas/eval.py`; do not change legacy request requirements. Test, Fix, and Confirm: run model tests, fix migration/state constraints, then confirm candidate history persists and duplicate commit cannot create a second active result.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_models.py -x</automated></verify>
  <done>BaselineCandidate is durable and records enough immutable prepare evidence for commit-time revalidation and audit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce database-backed prepare and commit revalidation</name>
  <files>backend/app/services/rag_quality.py, backend/tests/test_rag_quality_scoring.py</files>
  <behavior>
    - Prepare accepts only owner-matching passed/qualified QualityRun with complete canonical lineage, quality_comparable=true, metrics present, and valid output hash/report signature.
    - Commit reloads both candidate and QualityRun and repeats all checks; any status, lineage, metrics, input/output hash or signature change rejects atomically.
    - Rejected commit leaves the current active baseline unchanged; successful repeated requests are idempotent.
  </behavior>
  <action>For REQ-AUTO-11, first add prepare/commit race and tamper tests. Implement service functions that query the 06-08 repository under transaction/row lock. Prepare validates the full run and persists a fingerprint over canonical lineage, metrics, input/output hashes and signature. Commit never trusts request payload or the prepared boolean: reload current rows, verify signature and fingerprint, require complete comparable lineage, then atomically commit the candidate and active baseline pointer/journal. Remove baseline eligibility based solely on `make_baseline_from_metrics`; keep that helper only where legacy tests/API need a non-promotable metrics shape. Test, Fix, and Confirm: run scoring tests, fix race/idempotency behavior, then mutate each protected field between phases and confirm every commit rejects without baseline replacement.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_scoring.py -x</automated></verify>
  <done>Both phases independently consume current complete comparable lineage; TOCTOU or legacy data cannot promote a baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add same-snapshot cross-chunker reports with legacy API compatibility</name>
  <files>backend/app/services/rag_quality.py, backend/app/api/eval.py, backend/app/schemas/eval.py, backend/tests/test_rag_quality_scoring.py, backend/tests/test_eval_api.py</files>
  <behavior>
    - Report query groups by source_snapshot_hash and returns separate deterministic series for each complete chunker name/version/config/manifest lineage.
    - Two runs sharing a snapshot but using different chunkers both appear; no series is folded or cache-collided.
    - Different snapshots, legacy rows, incomplete lineage, invalid signatures and quality_comparable=false rows are excluded with machine-readable reasons.
    - Existing create/get/list/resume/cancel and legacy Eval API tests still pass with unchanged existing fields and status codes.
  </behavior>
  <action>For REQ-AUTO-11, write API/service report fixtures first using two persisted 06-08 QualityRuns on one snapshot with different chunkers, plus legacy/invalid/different-snapshot controls. Implement owner-scoped aggregation: source snapshot is the outer comparison boundary and the remaining four lineage members form a visible series identity; include run metrics, cost/latency and full canonical lineage, with deterministic sorting and explicit exclusions. Add prepare/commit/report routes or additive operations in `api/eval.py` without replacing existing quality routes; move the inline `QualityRunCreate` DTO to/reuse `schemas/eval.py` only if compatibility tests prove identical validation. Test, Fix, and Confirm: run API and scoring suites, fix serialization/status compatibility, then confirm old payloads remain accepted but are marked incomparable and cannot enter baseline/report comparison sets.</action>
  <verify><automated>cd backend; pytest tests/test_rag_quality_scoring.py tests/test_eval_api.py -x</automated></verify>
  <done>Reports compare same-source chunkers without collision, invalid/legacy data is explicit but excluded, and all old API behavior remains compatible.</done>
</task>

</tasks>

## Must-Haves

- Baseline prepare and commit are both persisted operations and both revalidate current database state.
- Only complete comparable canonical lineage can be consumed; old data is never inferred or promoted.
- Report outer boundary is source snapshot; chunker name/version/config/manifest remain separate visible series dimensions.
- Existing Eval and quality run APIs retain their established paths and existing response fields.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| QualityRun → baseline candidate | A signed run may be stale, legacy or mutated after prepare |
| API caller → prepare/commit | Candidate ids and owner scope are untrusted |
| database → report | Historical rows may be incomplete or invalid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-09-01 | Tampering | baseline commit | mitigate | Row-lock and revalidate current lineage/hash/signature/fingerprint at commit |
| T-06-09-02 | Elevation of Privilege | baseline/report API | mitigate | Owner-scoped repository lookups and not-found semantics |
| T-06-09-03 | Repudiation | candidate journal/report exclusions | mitigate | Persist transition reasons and return machine-readable exclusion reasons |
| T-06-09-SC | Tampering | package installs | mitigate | No package installs are planned |
</threat_model>

## Verification

- `cd backend; pytest tests/test_rag_quality_models.py tests/test_rag_quality_scoring.py tests/test_eval_api.py -x`
- `cd backend; alembic upgrade head; alembic current; alembic check`

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 06 | Frozen evidence and deterministic automated RAG quality gate | 06-08, 06-09 | COVERED | Persistent signed run evidence feeds durable baseline/report gates |
| REQ | REQ-AUTO-11 | Comparable reports bind chunker name/version/config hash/manifest hash/source snapshot | 06-08, 06-09 | COVERED | 06-08 produces and binds lineage; 06-09 exclusively consumes it |
| RESEARCH | Recommendation/Compatibility | Quality scores bind frozen lineage; preserve legacy Eval API | 06-08, 06-09 | COVERED | Hash/signature chain plus additive API compatibility tests |
| CONTEXT | D-15, Acceptance Thresholds | Missing lineage is invalid and cannot degrade into comparable metrics | 06-08, 06-09 | COVERED | invalid_lineage/false/null before scoring and at both promotion phases |
| VERIFICATION | Gap: REQ-AUTO-11 | Current worker store is memory-only and reports lack chunker lineage | 06-08, 06-09 | COVERED | Database QualityRun then database BaselineCandidate/report aggregation |

Deferred ideas and Phase 07 chunker implementation remain excluded; these plans only establish and consume the Phase 06 quality contract.

<success_criteria>
REQ-AUTO-11 closes when durable prepare/commit independently reject every incomplete, legacy or changed lineage; successful promotion is auditable and idempotent; same-snapshot different chunkers appear as distinct report series; and legacy APIs remain compatible.
</success_criteria>

<output>Create `.planning/phases/06-automated-quality-ci/06-09-SUMMARY.md` when done.</output>
