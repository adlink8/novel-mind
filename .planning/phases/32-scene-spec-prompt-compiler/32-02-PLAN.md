---
phase: 32-scene-spec-prompt-compiler
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - backend/app/services/scene_spec/compiler.py
  - backend/app/api/scene_specs.py
  - backend/tests/unit/scene_spec/test_compiler.py
  - backend/tests/integration/scene_spec/test_scope.py
autonomous: true
requirements: [REQ-VIS-03]
must_haves:
  truths:
    - "SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/scene_spec/compiler.py"
      provides: "实现 evidence-to-spec 的确定性编译、连续性和 unsupported-detail gate。"
    - path: "backend/tests/integration/scene_spec/test_scope.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/scene_spec/compiler.py"
      to: "backend/app/api/scene_specs.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/scene_spec/compiler.py"
      to: "Scene Spec → PromptArtifact"
      via: "versioned candidate artifact reference"

---

<objective>
实现 evidence-to-spec 的确定性编译、连续性和 unsupported-detail gate。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-03 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/scene_spec/compiler.py, backend/app/api/scene_specs.py, backend/tests/unit/scene_spec/test_compiler.py, backend/tests/integration/scene_spec/test_scope.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md
@.planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md
@.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>backend/app/services/scene_spec/compiler.py, backend/app/api/scene_specs.py, backend/tests/unit/scene_spec/test_compiler.py, backend/tests/integration/scene_spec/test_scope.py</files>
  <action>实现 `SceneSpecCompiler.compile`：从 frozen SceneCandidateSet + VisualBible revision 组装人物、地点、动作、时间、构图、style、negative constraints；每一条都保留 evidence/interpretation provenance，canonical serialize/hash，冲突与缺失返回 reason-coded unresolved。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 evidence-to-spec compiler、canonical hashing 和 fail-closed unresolved 映射。按 D-32-02、D-32-03。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/scene_spec/test_compiler.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/scene_spec/test_compiler.py -q</automated><acceptance>同输入同 hash；Visual Bible ID/角色地点连续性保持；unsupported canon、spoiler 和冲突不被放行。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>backend/app/services/scene_spec/compiler.py, backend/app/api/scene_specs.py, backend/tests/unit/scene_spec/test_compiler.py, backend/tests/integration/scene_spec/test_scope.py</files>
  <action>实现 preview/create/diff API，使用 owner scope、SceneCandidate/VisualBible version revalidation，并把 compiled SceneSpec 持久化为 candidate；预览阶段严禁 provider/network call。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 typed API、preview boundary 和 candidate persistence 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/scene_spec/test_scope.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/scene_spec/test_scope.py -q</automated><acceptance>旧 Visual Bible 变更后 spec 标记 stale；每个正/负 clause 可回跳证据。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md; backend/app/services/scene_spec/compiler.py; backend/app/api/scene_specs.py; backend/tests/unit/scene_spec/test_compiler.py; backend/tests/integration/scene_spec/test_scope.py</read_first>
  <files>backend/app/services/scene_spec/compiler.py, backend/app/api/scene_specs.py, backend/tests/unit/scene_spec/test_compiler.py, backend/tests/integration/scene_spec/test_scope.py</files>
  <action>运行 compiler golden、adversarial unsupported/spoiler/provider-field fixtures，修复后确认 preview 无 provider 调用。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 golden compiler、negative constraints 和 provider-neutral boundary 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/scene_spec tests/integration/scene_spec -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/scene_spec tests/integration/scene_spec -q</automated><acceptance>SceneSpec 可确定性重建并满足 evidence、owner、spoiler、candidate-only 门。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/scene_spec tests/integration/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-03 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
