---
phase: 32-scene-spec-prompt-compiler
plan: 04
type: execute
wave: 3
depends_on: [32-02, 32-03]
files_modified:
  - frontend/src/components/scene-spec/preview.tsx
  - frontend/src/components/scene-spec/diff.tsx
  - frontend/src/lib/scene-spec-api.ts
  - backend/app/api/prompt_revisions.py
  - backend/app/services/prompt_compiler/revisions.py
  - backend/tests/integration/prompt_compiler/test_prompt_revision_review.py
  - frontend/src/components/scene-spec/scene-spec.test.tsx
  - frontend/e2e/scene-spec.spec.ts
autonomous: true
requirements: [REQ-VIS-03]
must_haves:
  truths:
    - "SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "frontend/src/components/scene-spec/preview.tsx"
      provides: "完成 Scene Spec/Prompt preview、diff、validation error 和显式编辑门。"
    - path: "backend/app/services/prompt_compiler/revisions.py"
      provides: "PromptRevision append-only review、approve/reject/history 与 stale/hash gate。"
    - path: "backend/app/api/prompt_revisions.py"
      provides: "owner/version/hash scoped PromptRevision approve/reject/history API。"
    - path: "backend/tests/integration/prompt_compiler/test_prompt_revision_review.py"
      provides: "PromptRevision review contract 与重复 action 回归证据。"
    - path: "frontend/e2e/scene-spec.spec.ts"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "frontend/src/components/scene-spec/preview.tsx"
      to: "frontend/src/components/scene-spec/diff.tsx"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "frontend/src/components/scene-spec/preview.tsx"
      to: "Scene Spec → PromptArtifact"
      via: "versioned candidate artifact reference"
    - from: "backend/app/api/prompt_revisions.py"
      to: "backend/app/services/prompt_compiler/revisions.py"
      via: "owner/version/hash checked append-only review API"

---

<objective>
完成 Scene Spec/Prompt preview、diff、validation error 和显式编辑门。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-03 交付可执行的、证据可重放的版本化切片。
Output: frontend/src/components/scene-spec/preview.tsx, frontend/src/components/scene-spec/diff.tsx, frontend/src/lib/scene-spec-api.ts, backend/app/api/prompt_revisions.py, backend/app/services/prompt_compiler/revisions.py, backend/tests/integration/prompt_compiler/test_prompt_revision_review.py, frontend/src/components/scene-spec/scene-spec.test.tsx, frontend/e2e/scene-spec.spec.ts
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md
@.planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md
@.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>frontend/src/components/scene-spec/preview.tsx, frontend/src/components/scene-spec/diff.tsx, frontend/src/lib/scene-spec-api.ts, backend/app/api/prompt_revisions.py, backend/app/services/prompt_compiler/revisions.py, backend/tests/integration/prompt_compiler/test_prompt_revision_review.py, frontend/src/components/scene-spec/scene-spec.test.tsx, frontend/e2e/scene-spec.spec.ts</files>
  <action>实现 typed API client 与 side-by-side `SceneSpecPreview`：展示 candidate status、每条 detail 的 evidence/Visual Bible/interpretation 来源及 negative constraints，不让前端拼接 provider prompt。同步为 PromptRevision 提供 approve/reject/history API 与 append-only review events；服务端只允许 owner/version/hash 校验通过的 candidate，approval 只能把 PromptRevision 标记为 approved 输入，不改写 SceneSpec 或原始来源。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 provider-neutral adapter、lineage hash 和 preview boundary 映射。按 D-32-02、D-32-04。</action>
  <acceptance_criteria>`cd frontend; npm test -- scene-spec`</acceptance_criteria>
<verify><automated>cd frontend; npm test -- scene-spec</automated><acceptance>每个 clause 可追溯，unsupported/future spoiler 显示拒绝或 unresolved。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd frontend; npm test -- scene_spec 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>frontend/src/components/scene-spec/preview.tsx, frontend/src/components/scene-spec/diff.tsx, frontend/src/lib/scene-spec-api.ts, backend/app/api/prompt_revisions.py, backend/app/services/prompt_compiler/revisions.py, backend/tests/integration/prompt_compiler/test_prompt_revision_review.py, frontend/src/components/scene-spec/scene-spec.test.tsx, frontend/e2e/scene-spec.spec.ts</files>
  <action>实现 `PromptDiff`、显式 edit action、stale revision 和 no-provider-call 状态；补齐 PromptRevision approve/reject/history API、事件读取和重复 action 幂等测试。只有 approved candidate prompt 才能成为 Phase 33 输入，stale、跨 owner 或 version/hash 不匹配的 revision 必须拒绝，预览不触发生成。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 prompt revision、append-only review 和 provider-call boundary 映射。按 D-32-04。</action>
  <acceptance_criteria>`cd frontend; npm run test:e2e -- scene-spec --project=chromium-desktop --project=chromium-mobile-390`</acceptance_criteria>
 <verify><automated>cd frontend; npm test -- scene-spec && npm run test:e2e -- scene-spec --project=chromium-desktop --project=chromium-mobile-390</automated><acceptance>Vitest/contract smoke 失败时 E2E 不启动；桌面/390px 能查看 diff、编辑 interpretation、看到 validation error 和 stale 提示。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd frontend; npm test -- scene_spec 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md; frontend/src/components/scene-spec/preview.tsx; frontend/src/components/scene-spec/diff.tsx; frontend/src/lib/scene-spec-api.ts; backend/app/api/prompt_revisions.py; backend/app/services/prompt_compiler/revisions.py; backend/tests/integration/prompt_compiler/test_prompt_revision_review.py; frontend/src/components/scene-spec/scene-spec.test.tsx; frontend/e2e/scene-spec.spec.ts</read_first>
  <files>frontend/src/components/scene-spec/preview.tsx, frontend/src/components/scene-spec/diff.tsx, frontend/src/lib/scene-spec-api.ts, backend/app/api/prompt_revisions.py, backend/app/services/prompt_compiler/revisions.py, backend/tests/integration/prompt_compiler/test_prompt_revision_review.py, frontend/src/components/scene-spec/scene-spec.test.tsx, frontend/e2e/scene-spec.spec.ts</files>
  <action>先运行 frontend Vitest/contract smoke，失败立即停止 E2E；再运行 Phase 32 backend/frontend/browser validation，执行 prompt hash/diff、PromptRevision approve/reject/history event 和 adversarial provider injection，修复并确认。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 compiler golden、adapter contract 和 preview diff 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/scene_spec tests/unit/prompt_compiler tests/adversarial/test_scene_spec_boundaries.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/scene_spec tests/unit/prompt_compiler tests/adversarial/test_scene_spec_boundaries.py -q</automated><acceptance>REQ-VIS-03 通过，且 `SceneSpec → PromptArtifact` 版本链可重放。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/scene_spec tests/integration/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-03 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
