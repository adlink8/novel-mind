---
phase: 32-scene-spec-prompt-compiler
plan: 03
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - backend/app/services/prompt_compiler/adapters.py
  - backend/app/services/prompt_compiler/serialization.py
  - backend/app/api/prompt_revisions.py
  - backend/tests/unit/prompt_compiler/test_adapters.py
  - backend/tests/unit/prompt_compiler/test_golden.py
autonomous: true
requirements: [REQ-VIS-03]
must_haves:
  truths:
    - "SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/prompt_compiler/adapters.py"
      provides: "实现 provider-neutral 到 provider-specific PromptArtifact 的 adapter 契约与可重放序列化。"
    - path: "backend/tests/unit/prompt_compiler/test_golden.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/prompt_compiler/adapters.py"
      to: "backend/app/services/prompt_compiler/serialization.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/prompt_compiler/adapters.py"
      to: "Scene Spec → PromptArtifact"
      via: "versioned candidate artifact reference"

---

<objective>
实现 provider-neutral 到 provider-specific PromptArtifact 的 adapter 契约与可重放序列化。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-03 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/prompt_compiler/adapters.py, backend/app/services/prompt_compiler/serialization.py, backend/app/api/prompt_revisions.py, backend/tests/unit/prompt_compiler/test_adapters.py, backend/tests/unit/prompt_compiler/test_golden.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md
@.planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md
@.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md
@.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>backend/app/services/prompt_compiler/adapters.py, backend/app/services/prompt_compiler/serialization.py, backend/app/api/prompt_revisions.py, backend/tests/unit/prompt_compiler/test_adapters.py, backend/tests/unit/prompt_compiler/test_golden.py</files>
  <action>定义 `PromptAdapter` protocol 与 mock/一个已配置 provider adapter；输入只能是 SceneSpec，输出包含 sections、negative constraints、adapter_version、input/output hashes，禁止把 adapter branching 反写 SceneSpec。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 provider-neutral adapter、adapter contract 和 hash lineage 映射。按 D-32-01、D-32-03。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/prompt_compiler/test_adapters.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/prompt_compiler/test_adapters.py -q</automated><acceptance>两种 adapter 均通过 contract；canonical meaning 不变，provider tokens 不进入 SceneSpec。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-CONTEXT.md; .planning/phases/32-scene-spec-prompt-compiler/32-RESEARCH.md; .planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md</read_first>
  <files>backend/app/services/prompt_compiler/adapters.py, backend/app/services/prompt_compiler/serialization.py, backend/app/api/prompt_revisions.py, backend/tests/unit/prompt_compiler/test_adapters.py, backend/tests/unit/prompt_compiler/test_golden.py</files>
  <action>实现 `PromptRevisionService.compile_revision` 与 preview/diff persistence，记录 schema/prompt/compiler/config/adapter lineage；人工 edit 产生显式 candidate revision，不调用 image provider。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 PromptRevision、append-only edit 和 no-provider-call preview 映射。按 D-32-04。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/prompt_compiler/test_golden.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/prompt_compiler/test_golden.py -q</automated><acceptance>相同输入 prompt hash 稳定，编辑 diff/父 revision 可审计，错误不是空成功。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md; backend/app/services/prompt_compiler/adapters.py; backend/app/services/prompt_compiler/serialization.py; backend/app/api/prompt_revisions.py; backend/tests/unit/prompt_compiler/test_adapters.py; backend/tests/unit/prompt_compiler/test_golden.py</read_first>
  <files>backend/app/services/prompt_compiler/adapters.py, backend/app/services/prompt_compiler/serialization.py, backend/app/api/prompt_revisions.py, backend/tests/unit/prompt_compiler/test_adapters.py, backend/tests/unit/prompt_compiler/test_golden.py</files>
  <action>运行 prompt-golden、prompt-edit 和 adapter contract tests，检查 prompt diff 能还原 SceneSpec 后修复确认。参考 analog `.planning/phases/32-scene-spec-prompt-compiler/32-PATTERNS.md` 的 prompt golden、diff round-trip 和 adapter contract 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/prompt_compiler -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/prompt_compiler -q</automated><acceptance>PromptArtifact 可供 Phase 33 job 使用且 lineage 完整。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/scene_spec tests/integration/scene_spec -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- SceneSpec 与 PromptArtifact 可从证据和 Visual Bible 版本确定性重建，unsupported detail fail closed。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/32-scene-spec-prompt-compiler/32-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-03 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
