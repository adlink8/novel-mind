# Plan 01-02: 前后端契约启动修复

## Steps

1. Fix novel list response handling between backend and frontend.
2. Fix AI model create/list field alignment between frontend and backend.
3. Make AI model test UI respect backend `success` and `error`.
4. Make unimplemented feature endpoints return honest status instead of fake completion.

## Must-Haves

- Book shelf must handle paginated backend responses.
- AI settings must submit backend-required fields: `name`, `provider`, `model_id`.
- Failed model tests must display failure even when the HTTP request itself succeeds.
- Placeholder features must not be mistaken for completed AI analysis, timeline extraction, character extraction, or fanfiction generation.

## Implementation Slices

### Slice 1: Novel API Contract

1. Update TypeScript API types for paginated novel list.
2. Update Zustand store to read `res.data.items`.
3. Check search/sort behavior still receives arrays.
4. Test, Fix, and Confirm.

### Slice 2: AI Model Contract

1. Align frontend model types with backend schema.
2. Update add-model form payload.
3. Update model card display fields.
4. Test, Fix, and Confirm.

### Slice 3: Honest Placeholder Semantics

1. Review analysis / timeline / characters / fanfiction placeholder routes.
2. Return explicit not-implemented or not-ready payloads.
3. Update frontend assumptions if needed.
4. Test, Fix, and Confirm.

## Verification

- Frontend typecheck/build passes.
- Backend imports app without schema errors.
- Model creation payload matches `AIModelConfigCreate`.
- Novel list store receives an array.
- Placeholder API behavior is documented and unambiguous.
