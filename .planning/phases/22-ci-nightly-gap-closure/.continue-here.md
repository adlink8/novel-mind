# Phase 22-G2 Continuation

## Current Position

- Active plan: `22-G2`
- Production commit: `495b29a feat(22-G2): add hosted nightly control plane`
- Merged baseline: `912ca6b` through PR #31.
- Local verification: `tests/ci` 126 passed; actionlint passed; Ruff passed.
- Remote control-plane observation: workflow-dispatch run `30623438107` produced signed
  `blocked_dependency` artifacts and alert #32 without scheduling the provider job.
- Plan summary is intentionally not written: real scheduled artifact inspection is pending.

## Remaining Gates

1. Complete `22-G2-USER-SETUP.md`: online labeled Runner plus read-only inventory token.
2. Inspect one real scheduled terminal artifact and record it in `22-VALIDATION.md`.
3. Write `22-G2-SUMMARY.md` only after the scheduled artifact proves the provider path.
4. Continue G3 until the ledger contains 3/3 consecutive scheduled green runs.

## Critical Anti-Patterns

| severity | anti-pattern | prevention |
|---|---|---|
| warning | Treating a PR run or workflow dispatch as a scheduled-green observation | Only `event=schedule` entries may fill the 3/3 ledger |
| warning | Treating `blocked_dependency` as quality success | Keep `quality_comparable=false`, `metrics=null`, and promotion skipped |
