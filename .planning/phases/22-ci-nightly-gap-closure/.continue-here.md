# Phase 22-G2 Continuation

## Current Position

- Active plan: `22-G2`
- Production commit: `495b29a feat(22-G2): add hosted nightly control plane`
- Local verification: `tests/ci` 126 passed; actionlint passed; Ruff passed.
- Plan summary is intentionally not written: scheduled artifact inspection is still pending.

## Remaining Gates

1. Push the branch and merge its PR after required CI passes.
2. Complete `22-G2-USER-SETUP.md`: online labeled Runner plus read-only inventory token.
3. Inspect one real scheduled terminal artifact and record it in `22-VALIDATION.md`.
4. Write `22-G2-SUMMARY.md` only after the scheduled artifact proves the control-plane path.
5. Continue G3 until the ledger contains 3/3 consecutive scheduled green runs.

## Critical Anti-Patterns

| severity | anti-pattern | prevention |
|---|---|---|
| warning | Treating a PR run or workflow dispatch as a scheduled-green observation | Only `event=schedule` entries may fill the 3/3 ledger |
| warning | Treating `blocked_dependency` as quality success | Keep `quality_comparable=false`, `metrics=null`, and promotion skipped |
