---
phase: 33-illustration-generation-consistency
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - backend/app/services/illustrations/gateway.py
  - backend/app/services/illustrations/worker.py
  - backend/app/services/illustrations/storage.py
  - backend/app/api/illustrations.py
  - backend/tests/integration/illustrations/test_generation.py
autonomous: true
requirements: [REQ-VIS-04]
must_haves:
  truths:
    - "Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/illustrations/gateway.py"
      provides: "实现 provider-neutral mock generation、durable worker、幂等、存储和成本结算。"
    - path: "backend/tests/integration/illustrations/test_generation.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/illustrations/gateway.py"
      to: "backend/app/services/illustrations/worker.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/illustrations/gateway.py"
      to: "PromptArtifact → IllustrationRevision"
      via: "versioned candidate artifact reference"

---

<objective>
实现 provider-neutral mock generation、durable worker、幂等、存储和成本结算。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-04 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/illustrations/gateway.py, backend/app/services/illustrations/worker.py, backend/app/services/illustrations/storage.py, backend/app/api/illustrations.py, backend/tests/integration/illustrations/test_generation.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md
@.planning/phases/33-illustration-generation-consistency/33-RESEARCH.md
@.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md
@.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/gateway.py, backend/app/services/illustrations/worker.py, backend/app/services/illustrations/storage.py, backend/app/api/illustrations.py, backend/tests/integration/illustrations/test_generation.py</files>
  <action>实现 `IllustrationGateway` protocol、mock provider 和 worker 的 claim/heartbeat/settle/retry/reconcile；超时/5xx/unknown 有 reason code，retry bounded 且不能绕过 budget 或重复成功 attempt。补充 approved-only、stale PromptRevision、跨 owner/novel/version 的 integration tests，证明 generation entrypoint 只接受 approved 且未过期的 prompt。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 durable worker、lease/attempt reconciliation 和 provider-neutral gateway 映射。按 D-33-01、D-33-02。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/illustrations/test_generation.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/illustrations/test_generation.py -q</automated><acceptance>并发 duplicate 只生成一次/计费一次；失败可恢复，永不以空文件标记 success。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/gateway.py, backend/app/services/illustrations/worker.py, backend/app/services/illustrations/storage.py, backend/app/api/illustrations.py, backend/tests/integration/illustrations/test_generation.py</files>
  <action>实现 `AssetStorage` content-hash bytes、MIME/size/owner containment/quarantine cleanup，API 只接受 approved 且 Visual Bible/SceneSpec lineage 未 stale 的 PromptRevision candidate 并输出 immutable AssetRevision；provider error 脱敏。用 approved-only、stale、owner/version mismatch tests 锁定 fail-closed 行为。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 content-hash storage、owner containment 和 immutable asset lineage 映射。按 D-33-03。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/illustrations -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/illustrations -q</automated><acceptance>asset 文件与 DB lineage 一致，跨 owner、错误 prompt/spec hash、预算超限均拒绝。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md; backend/app/services/illustrations/gateway.py; backend/app/services/illustrations/worker.py; backend/app/services/illustrations/storage.py; backend/app/api/illustrations.py; backend/tests/integration/illustrations/test_generation.py</read_first>
  <files>backend/app/services/illustrations/gateway.py, backend/app/services/illustrations/worker.py, backend/app/services/illustrations/storage.py, backend/app/api/illustrations.py, backend/tests/integration/illustrations/test_generation.py</files>
  <action>运行 mock success、duplicate、timeout、unknown、budget exhaustion、retry/reconcile、approved-only、stale、owner/version scope tests，修复后确认 durable 状态和成本账本。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 deterministic mock provider、budget ledger 和 explicit unknown outcome 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q</automated><acceptance>Phase 33 generation path 可重放，只有 candidate AssetRevision 流向 review。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-04 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
