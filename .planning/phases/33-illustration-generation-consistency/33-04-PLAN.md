---
phase: 33-illustration-generation-consistency
plan: 04
type: execute
wave: 4
depends_on: [33-02, 33-03]
files_modified:
  - backend/app/services/illustrations/review.py
  - frontend/src/components/illustrations/gallery.tsx
  - frontend/src/components/illustrations/compare.tsx
  - frontend/src/components/illustrations/approval.tsx
  - backend/tests/integration/illustrations/test_review.py
  - frontend/src/components/illustrations/illustrations.test.tsx
  - frontend/e2e/illustrations.spec.ts
autonomous: true
requirements: [REQ-VIS-04]
must_haves:
  truths:
    - "Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/illustrations/review.py"
      provides: "完成 generation candidate gallery、compare、显式 approval/reject/supersede/retry 工作流。"
    - path: "frontend/src/components/illustrations/illustrations.test.tsx"
      provides: "本计划的自动化验证与回归证据"
    - path: "frontend/e2e/illustrations.spec.ts"
      provides: "desktop/mobile approval、compare、lineage 与失败恢复浏览器证据"
  key_links:
    - from: "backend/app/services/illustrations/review.py"
      to: "frontend/src/components/illustrations/gallery.tsx"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/illustrations/review.py"
      to: "PromptArtifact → IllustrationRevision"
      via: "versioned candidate artifact reference"

---

<objective>
完成 generation candidate gallery、compare、显式 approval/reject/supersede/retry 工作流。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-04 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/illustrations/review.py, frontend/src/components/illustrations/gallery.tsx, frontend/src/components/illustrations/compare.tsx, frontend/src/components/illustrations/approval.tsx, backend/tests/integration/illustrations/test_review.py, frontend/src/components/illustrations/illustrations.test.tsx
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md
@.planning/phases/33-illustration-generation-consistency/33-RESEARCH.md
@.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md
@.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/review.py, frontend/src/components/illustrations/gallery.tsx, frontend/src/components/illustrations/compare.tsx, frontend/src/components/illustrations/approval.tsx, backend/tests/integration/illustrations/test_review.py, frontend/src/components/illustrations/illustrations.test.tsx</files>
  <action>实现 `IllustrationReviewService.append_event` 的 compare/mark_proposal_ready/reject/supersede/retry transitions；proposal_ready 要求成功 job、完整 source/prompt/model/asset lineage、rights 状态和可见 consistency report。Phase 33 只形成供 Phase 34 使用的 proposal，不创建 publish approval、不切 active pointer。参考 `33-PATTERNS.md` 的 append-only review、proposal gate 和 durable job lineage 映射。按 D-33-03、D-33-04。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/illustrations/test_review.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/illustrations/test_review.py -q</automated><acceptance>重复 proposal-ready 事件幂等；缺 lineage/rights/成功 asset/预算结算或 consistency report 不可进入 proposal_ready；不存在 published transition。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/review.py, frontend/src/components/illustrations/gallery.tsx, frontend/src/components/illustrations/compare.tsx, frontend/src/components/illustrations/approval.tsx, backend/tests/integration/illustrations/test_review.py, frontend/src/components/illustrations/illustrations.test.tsx</files>
  <action>实现 candidate gallery/compare/lineage drawer/status/error/retry UI，显示 cost/usage/failed/paused/unknown 和 human action；不将 score 自动转为批准。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 candidate gallery、compare/approval boundary 和 explicit failure state 映射。</action>
  <acceptance_criteria>`cd frontend; npm test -- illustrations`</acceptance_criteria>
<verify><automated>cd frontend; npm test -- illustrations</automated><acceptance>用户可比较 revision、查看 lineage、明确批准/拒绝，失败可恢复且无空成功。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md; backend/app/services/illustrations/review.py; frontend/src/components/illustrations/gallery.tsx; frontend/src/components/illustrations/compare.tsx; frontend/src/components/illustrations/approval.tsx; backend/tests/integration/illustrations/test_review.py; frontend/src/components/illustrations/illustrations.test.tsx</read_first>
  <files>backend/app/services/illustrations/review.py, frontend/src/components/illustrations/gallery.tsx, frontend/src/components/illustrations/compare.tsx, frontend/src/components/illustrations/approval.tsx, backend/tests/integration/illustrations/test_review.py, frontend/src/components/illustrations/illustrations.test.tsx</files>
  <action>先执行 frontend Vitest/contract smoke，失败立即停止 E2E；再运行 Phase 33 backend/frontend/browser validation；mock 全流程，live canary 默认禁用，只有显式 hard budget 才可记录。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 mock provider、budget ledger 和 optional canary 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q</automated><acceptance>REQ-VIS-04 通过，proposal_ready AssetRevision 可被 Phase 34 AnchorProposal 消费，但尚不可被 reader/export 消费。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>
<tasks>
<task type="auto"><name>Test, Fix, and Confirm: illustration review browser gate</name><read_first>frontend/src/components/illustrations/illustrations.test.tsx; frontend/e2e/illustrations.spec.ts; backend/tests/integration/illustrations/test_review.py; 33-VALIDATION.md</read_first><files>frontend/e2e/illustrations.spec.ts</files><action>新增双 viewport Playwright spec，覆盖候选比较、approval/reject/supersede、lineage、provider failure 与 retry/resume；先执行 integration 和 Vitest smoke。</action><acceptance_criteria>审批和失败恢复在 desktop/mobile 均可观察且保持 candidate-only。</acceptance_criteria><verify><automated>Push-Location backend; pytest tests/integration/illustrations/test_review.py -q; $code=$LASTEXITCODE; Pop-Location; if ($code -ne 0) { exit $code }; Push-Location frontend; npm test -- illustrations; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test e2e/illustrations.spec.ts; $code=$LASTEXITCODE; Pop-Location; exit $code</automated></verify><done>illustration review integration、component 与双 viewport E2E 全部通过。</done></task>
</tasks>

## Must-Haves

<must_haves>

- Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-04 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
