---
phase: 33-illustration-generation-consistency
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - backend/app/services/illustrations/consistency.py
  - backend/app/api/illustrations.py
  - backend/tests/unit/illustrations/test_consistency.py
  - backend/tests/integration/illustrations/test_consistency.py
autonomous: true
requirements: [REQ-VIS-04]
must_haves:
  truths:
    - "Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/illustrations/consistency.py"
      provides: "实现 identity/style consistency 的冻结 fixture 评估与证据化报告。"
    - path: "backend/tests/integration/illustrations/test_consistency.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/illustrations/consistency.py"
      to: "backend/app/api/illustrations.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/illustrations/consistency.py"
      to: "PromptArtifact → IllustrationRevision"
      via: "versioned candidate artifact reference"

---

<objective>
实现 identity/style consistency 的冻结 fixture 评估与证据化报告。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-04 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/illustrations/consistency.py, backend/app/api/illustrations.py, backend/tests/unit/illustrations/test_consistency.py, backend/tests/integration/illustrations/test_consistency.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md
@.planning/phases/33-illustration-generation-consistency/33-RESEARCH.md
@.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md
@.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/consistency.py, backend/app/api/illustrations.py, backend/tests/unit/illustrations/test_consistency.py, backend/tests/integration/illustrations/test_consistency.py</files>
  <action>实现 `ConsistencyEvaluator.evaluate`，对同一角色跨场景 identity、style、negative constraint drift 产出 versioned `ConsistencyReport`，保留 evaluator/model/fixture/policy lineage；无 evaluator 返回 unavailable，不能自动 approve。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 fixture-based qualification、consistency report lineage 和 unavailable state 映射。按 D-33-04。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/illustrations/test_consistency.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/illustrations/test_consistency.py -q</automated><acceptance>identity/style drift、unsupported detail、unavailable 状态可区分；score 是 review signal 而非 canon。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-CONTEXT.md; .planning/phases/33-illustration-generation-consistency/33-RESEARCH.md; .planning/phases/33-illustration-generation-consistency/33-PATTERNS.md</read_first>
  <files>backend/app/services/illustrations/consistency.py, backend/app/api/illustrations.py, backend/tests/unit/illustrations/test_consistency.py, backend/tests/integration/illustrations/test_consistency.py</files>
  <action>将报告挂接 AssetRevision/SceneSpec/VisualBible revision，提供只读 compare/report API；不修改 Visual Bible，不把 score 写成 authority/canon，不绕过 approval。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 AssetRevision lineage、read-only compare 和 evidence-not-canon 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/illustrations/test_consistency.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/illustrations/test_consistency.py -q</automated><acceptance>报告可重放且 owner/version/source/prompt/model lineage 完整。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md; backend/app/services/illustrations/consistency.py; backend/app/api/illustrations.py; backend/tests/unit/illustrations/test_consistency.py; backend/tests/integration/illustrations/test_consistency.py</read_first>
  <files>backend/app/services/illustrations/consistency.py, backend/app/api/illustrations.py, backend/tests/unit/illustrations/test_consistency.py, backend/tests/integration/illustrations/test_consistency.py</files>
  <action>运行三场景 consistency fixture 与 cross-owner/adversarial tests，修复评估和挂接问题。参考 analog `.planning/phases/33-illustration-generation-consistency/33-PATTERNS.md` 的 identity/style fixture set、cross-owner scope 和 review signal 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q</automated><acceptance>一致性证据可供人工 review，未批准资产仍不能进入 Phase 34。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/illustrations tests/integration/illustrations -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- Illustration job/asset/review 具备 durable、budget、cost、consistency 和 candidate-only 证据。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/33-illustration-generation-consistency/33-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-04 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
