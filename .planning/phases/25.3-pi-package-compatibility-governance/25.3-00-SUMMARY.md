# 25.3-00-SUMMARY.md

**Phase**: 25.3-pi-package-compatibility-governance · **Plan**: 00 · **类型**: execute · **wave**: 0
**依赖**: 25.2-04, 25.2-05 · **日期**: 2026-08-02

## 目标

在包治理工作开始前，用机器门禁强制 Phase 25.2 已通过验证：只有权威、匹配、已 passed
的 25.2-VERIFICATION.md 才能放行 25.3-01..05 的 package/lockfile/runtime 写入。

## 实现

- `scripts/check_agent_runtime_execution_gate.py`（新建，25.2-00 未落盘故新建）：
  Phase 25.3 模式校验——
  1. `25.2-VERIFICATION.md` 必须存在；
  2. front-matter phase=`25.2`、slug=`embedded-novel-agent-runtime`、status=`passed`；
  3. `source_commit` 必须为合法 hex，且与 `--expected-commit` 或 git HEAD 一致（无 HEAD 时
     仅校验格式）；
  4. 必填段落齐全：`## Verification Results`（至少一行 passed/PASS 结果）+ `## Conclusion`
     （passed 结论）。
  任何缺失/过期/畸形 → 逐条列错误到 stderr 并 exit 1（fail-closed）。
  只读取 VERIFICATION 工件：planning override（STATE.md）与任何 SUMMARY 都不构成证据。
- `backend/tests/ci/test_agent_package_governance_execution_gate.py`（pytest.mark.unit，
  无 DB/网络）：9 项覆盖——当前仓库 PASS、合成完整证据 0、缺失/畸形/非 passed/
  wrong-phase/过期 commit/缺段落稳定非零、override+SUMMARY 不能替代 VERIFICATION。

## 验收

- `cd backend && venv/Scripts/python.exe -m pytest tests/ci/test_agent_package_governance_execution_gate.py -q`
  → **9 passed**。
- 当前仓库模式：**PASS**（25.2-VERIFICATION 已存在且 passed）。计划撰写时 VERIFICATION
  尚未完成故写 "blocks"；现按任务要求调整：存在→0、缺失→非零。

## 备注

- Phase 22 门禁由用户授权执行跳过（STATE.md `Execution Override (2026-08-02)`），本 gate
  不代偿也不撤销该跳过，仅验证 25.2 工件本身。
