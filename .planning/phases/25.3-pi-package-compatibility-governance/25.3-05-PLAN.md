---
phase: 25.3-pi-package-compatibility-governance
plan: 05
type: execute
wave: 5
depends_on: ["25.3-03", "25.3-06"]
files_modified:
  - .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md
  - frontend/src/components/analysis/cited-answer-artifact.tsx
  - frontend/src/components/analysis/cited-answer-artifact.test.tsx
  - frontend/src/components/analysis/analysis-chat-panel.tsx
  - frontend/e2e/agent-artifact.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The feasibility verdict is recorded: pi-web-ui contributes renderer PATTERNS only (registerToolRenderer/registerMessageRenderer registry shape, compact tool-result cards, artifact panel layout) and is never installed or imported (D-03 selective verdict)"
    - "The rejection record is explicit: ChatPanel adoption, IndexedDB session authority and browser-held provider keys are rejected because Next.js + PostgreSQL remain the session and rendering authority (25.2 D-15); the mini-lit/Tailwind v4 component stack and the 0.75.3 dependency train (pdfjs-dist, CDN xlsx tarball, jszip, ollama, @lmstudio/sdk) are rejected with the version-drift evidence (0.75.3 vs SDK 0.83.0)"
    - "One CitedAnswerArtifact renders inside the existing Analysis Chat — blocks plus citation chips with working citation jump — on desktop and 390px"
    - "An external_evidence artifact (25.3-03 schema) renders with an explicit external label and NO citation-jump affordance into novel text — the UI cannot launder external claims into canon-styled citations (D-08/D-09 display discipline)"
    - "No @earendil-works/pi-web-ui string exists in any package.json, lockfile or import — enforced by the 25.3-01 tests/ci gate"
  artifacts:
    - path: ".planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md"
      provides: "feasibility note: borrowed patterns, rejected patterns with reasons, verdict"
      contains: "registerToolRenderer"
    - path: "frontend/src/components/analysis/cited-answer-artifact.tsx"
      provides: "React renderer registry + CitedAnswerArtifact renderer (blocks, citation chips, jump)"
      contains: "CitedAnswerArtifact"
    - path: "frontend/src/components/analysis/cited-answer-artifact.test.tsx"
      provides: "colocated vitest with vi.hoisted API mocks"
      min_lines: 60
    - path: "frontend/e2e/agent-artifact.spec.ts"
      provides: "route-mocked Playwright spec, desktop + chromium-mobile-390"
      contains: "cited-answer"
  key_links:
    - from: "frontend/src/components/analysis/analysis-chat-panel.tsx"
      to: "frontend/src/components/analysis/cited-answer-artifact.tsx"
      via: "artifact preview renders through the type-keyed renderer registry"
      pattern: "cited-answer-artifact|CitedAnswerArtifact"
    - from: "frontend/src/components/analysis/cited-answer-artifact.tsx"
      to: "frontend/src/components/reader/reader-chat-panel.tsx"
      via: "composes the exported MessageBubble/CitationChip (shared since 25.1) — design-only pi-web-ui influence, zero imports"
      pattern: "MessageBubble|CitationChip"
    - from: "frontend/src/components/analysis/cited-answer-artifact.tsx"
      to: "citation jump route"
      via: "router.push(/novels/{id}?chapter=..&start=..) per analysis-chat-panel convention"
      pattern: "router\\.push"
---

<objective>
Close Phase 25.3 with the pi-web-ui renderer feasibility study (D-03 selective verdict):
record the pattern-only evaluation — which renderer designs are borrowed
(`registerToolRenderer`/`registerMessageRenderer` registry shape, compact tool-result
rendering) and which are explicitly rejected (ChatPanel, IndexedDB session authority,
browser-held provider keys, the 0.75.3 dependency train) — then prove the borrowed pattern
by shipping one React `CitedAnswerArtifact` renderer inside the existing Analysis Chat,
including the external_evidence display discipline.

Purpose: the ROADMAP requires a feasibility note plus a prototype render of one artifact
type with Next.js and PostgreSQL remaining session/rendering authority; this is also the
phase's visible proof that pattern borrowing never becomes package adoption by drift.

Output: `25.3-05-FEASIBILITY.md` + `cited-answer-artifact.tsx` renderer wired into the
Analysis Chat artifact preview + colocated vitest + Playwright spec (desktop + 390px).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-CONTEXT.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-VALIDATION.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-03-SUMMARY.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-04-SUMMARY.md

Execution gate (D-13): this plan MUST NOT start before Phase 22 records three consecutive
scheduled green observations per `.planning/STATE.md`, Phase 25.2 has landed (the 25.2-04
Analysis workspace with artifact preview exists), and 25.3-03 (external_evidence schema)
and 25.3-04 (approval dialog wiring in the same panel) are on record.

<interfaces>
<!-- Contracts the executor codes against — no codebase exploration needed. -->

Borrowed patterns (RESEARCH — pi-web-ui npm README, pattern source only):
registerToolRenderer / registerMessageRenderer — a type-keyed component registry resolving
an artifact/tool-result type to its renderer; compact tool-result cards; artifact panel
layout. Rejected (with reasons, to record verbatim in the feasibility note): ChatPanel
(its own session state machine conflicts with the 25.2-04 workspace), IndexedDB stores
(session authority must be PostgreSQL), browser-held provider keys (25.2 D-15 forbids key
material outside FastAPI), mini-lit + Tailwind v4 stack (repo is React/Next), deps
pdfjs-dist / CDN xlsx tarball / jszip / ollama / @lmstudio/sdk, version drift 0.75.3 vs
SDK 0.83.0.

Renderer composition analogs (PATTERNS): reader-chat-panel.tsx exports MessageBubble and
CitationChip "Shared with the analysis chat panel (Phase 25.1) — same bubble + citation
UI"; CitationChip renders <button data-testid="reader-chat-citation"
data-source-start={...}>; citation jump convention from analysis-chat-panel.tsx:
router.push(`/novels/${novelId}?chapter=..&start=..&from=timeline`).

Artifact envelopes: CitedAnswerArtifact (25.2-03 — type, schema_version, owner/novel/branch,
producing skill+version, model lineage, source versions, input_hash, evidence_refs,
status, parent_revision); ExternalEvidenceArtifact (25.3-03 — sources, retrieval_time,
claims, confidence, prohibited_from_canon Literal[True], release_status "external").

Test idioms (PATTERNS): colocated vitest with vi.hoisted API mocks
(analysis-chat-panel.test.tsx:16-58); Playwright route-mocked spec
(e2e/reader-chat.spec.ts:10-37) on desktop + chromium-mobile-390 projects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feasibility note — borrowed vs rejected patterns with verdict</name>
  <files>.planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md</files>
  <read_first>
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md (Summary pi-web-ui paragraph; Standard Stack pi-web-ui row; Alternatives Considered — React renderer reimplementation row; Anti-Patterns — installing pi-web-ui; Pitfall 3)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-CONTEXT.md (D-03 pi-web-ui verdict; the agent's Discretion — prototype scope)
    - agent-service/qualification/earendil-pi-web-ui.md (25.3-01 — the PackageQualificationReport this note cross-references)
  </read_first>
  <action>
    Write `.planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md`
    with four sections. VERDICT: pattern-only feasible — renderer designs transfer to a
    React implementation inside the existing Analysis Chat; the package itself is never
    installed (D-03 selective). BORROWED PATTERNS: the registerToolRenderer /
    registerMessageRenderer registry shape (type-keyed renderer resolution), compact
    tool-result cards, and the artifact panel layout — each with one line on how the React
    port maps it. REJECTED PATTERNS (the explicit rejection record): ChatPanel adoption —
    own session state machine conflicting with the 25.2-04 workspace; IndexedDB session
    authority — session authority is PostgreSQL (25.2 architecture); browser-held provider
    keys — forbidden by 25.2 D-15, no key material outside FastAPI; mini-lit + Tailwind v4
    component stack — repo is React/Next with ui/dialog.tsx conventions; the 0.75.3
    dependency train (pdfjs-dist, CDN-tarball xlsx, jszip, ollama, @lmstudio/sdk) and the
    0.75.3-vs-0.83.0 SDK drift (Pitfall 3). BOUNDARY: what the prototype renders (one
    CitedAnswerArtifact + the external_evidence display discipline), what stays authority
    (Next.js rendering, PostgreSQL sessions, FastAPI approvals), and the standing rule that
    any future pi-web-ui reconsideration re-enters through the Package Legitimacy Gate,
    never a direct dependency. Cross-reference the 25.3-01 qualification report and keep
    the note under two pages.
  </action>
  <acceptance_criteria>
    - File exists and `grep -n "registerToolRenderer" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md` matches
    - `grep -n "ChatPanel" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md` matches inside the REJECTED section
    - `grep -n "IndexedDB" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md` matches inside the REJECTED section
    - `grep -ni "provider key\|provider-key" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md` matches inside the REJECTED section
    - `grep -n "0.75.3" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md` matches (drift evidence recorded)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "REJECTED\|BORROWED\|VERDICT" .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md</automated>
  </verify>
  <done>Feasibility verdict on record: borrowed patterns enumerated, ChatPanel/IndexedDB/provider-key rejection explicit, prototype boundary fixed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CitedAnswerArtifact React renderer + Analysis Chat integration</name>
  <files>frontend/src/components/analysis/cited-answer-artifact.tsx, frontend/src/components/analysis/cited-answer-artifact.test.tsx, frontend/src/components/analysis/analysis-chat-panel.tsx</files>
  <behavior>
    - A type-keyed renderer registry (registerToolRenderer-shape, design-only) maps artifact type "cited_answer" to CitedAnswerArtifactView and "external_evidence" to ExternalEvidenceView; unknown types render an explicit fallback, never crash
    - CitedAnswerArtifactView renders the answer blocks and one CitationChip per evidence ref; clicking a chip routes the citation jump via router.push with the analysis-chat-panel convention; composing the reader-chat-panel MessageBubble/CitationChip exports — no duplicated bubble markup
    - ExternalEvidenceView renders sources/claims/confidence with a visible "External evidence — prohibited from canon" label and NO citation-jump affordance into novel text (display-level enforcement of D-08/D-09)
    - The analysis-chat-panel artifact preview resolves artifacts through the registry instead of ad-hoc markup; Next.js stays the rendering authority (server data, client render — no IndexedDB, no provider keys)
  </behavior>
  <read_first>
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md (section: cited-answer-artifact.tsx — analogs and design-only rule; test idioms)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-05-FEASIBILITY.md (Task 1 — the boundary the renderer implements)
    - frontend/src/components/reader/reader-chat-panel.tsx (exported MessageBubble/CitationChip at :813-887 — the composition source)
    - frontend/src/components/analysis/analysis-chat-panel.tsx (the file being modified — artifact preview area, citation jump at :426-432, status bar conventions at :524-569)
    - frontend/src/components/analysis/analysis-chat-panel.test.tsx (vi.hoisted mock idiom at :16-58)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-03-SUMMARY.md (ExternalEvidenceArtifact final field list)
  </read_first>
  <action>
    Create `frontend/src/components/analysis/cited-answer-artifact.tsx`: an exported
    `ARTIFACT_RENDERERS` registry object keyed by artifact type ("cited_answer",
    "external_evidence") plus `resolveArtifactRenderer(type)` returning the component or an
    explicit UnknownArtifactFallback; `CitedAnswerArtifactView({artifact, novelId})`
    rendering the answer blocks and mapping evidence_refs to the shared CitationChip with
    onCitationNavigate performing router.push per the analysis-chat-panel jump convention;
    `ExternalEvidenceView({artifact})` rendering the D-09 fields (sources with server/tool/
    uri, retrieval_time, claims, confidence) under a persistent visible label "External
    evidence — prohibited from canon" and rendering NO reader-citation buttons (no
    data-source-start navigation into novel text); a data-testid on every artifact root
    ("analysis-artifact-cited-answer", "analysis-artifact-external-evidence") for the e2e
    spec. Zero imports from @earendil-works/pi-web-ui — the registry is a plain React
    record. Modify `frontend/src/components/analysis/analysis-chat-panel.tsx`: the artifact
    preview area resolves the artifact's type through ARTIFACT_RENDERERS and renders the
    result (replacing any ad-hoc preview markup), passing novelId and the citation
    navigate handler. Write
    `frontend/src/components/analysis/cited-answer-artifact.test.tsx` (vitest with
    vi.hoisted API mocks): cited_answer fixture renders blocks + one chip per evidence ref;
    chip click calls router.push with the expected chapter/start query; external_evidence
    fixture renders the label and asserts no element with data-testid
    "reader-chat-citation" exists inside it; unknown type renders the fallback; the
    registry contains exactly the two known types (drift guard).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- cited-answer-artifact` exits 0
    - `grep -rn "@earendil-works/pi-web-ui" frontend/src` returns nothing
    - `grep -n "ARTIFACT_RENDERERS\|CitedAnswerArtifactView\|ExternalEvidenceView" frontend/src/components/analysis/cited-answer-artifact.tsx` matches all three
    - `grep -n "prohibited from canon" frontend/src/components/analysis/cited-answer-artifact.tsx` matches the visible label
    - `cd frontend && npm run test -q` exits 0 (no regression in analysis-chat-panel tests)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- cited-answer-artifact analysis-chat-panel</automated>
  </verify>
  <done>Registry-based renderer live in the Analysis Chat; cited-answer chips jump to source; external evidence is visibly labeled and cannot masquerade as a novel citation</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm — browser proof desktop + 390px and no-dependency gate</name>
  <files>frontend/e2e/agent-artifact.spec.ts, frontend/src/components/analysis/cited-answer-artifact.tsx, frontend/src/components/analysis/analysis-chat-panel.tsx</files>
  <read_first>
    - frontend/e2e/reader-chat.spec.ts (route-mocked spec analog at :10-37 — the file shape being copied)
    - frontend/src/components/analysis/cited-answer-artifact.tsx (Task 2 — the component under test)
    - frontend/src/components/analysis/analysis-chat-panel.tsx (Task 2 — integration surface)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-VALIDATION.md (25.3-05 sampling row; phase-gate sweeps)
    - .planning/ROADMAP.md (Phase 25.3 Phase Verification)
  </read_first>
  <action>
    Create `frontend/e2e/agent-artifact.spec.ts` copying the reader-chat route-mocked
    shape: intercept the analysis workspace's run/artifact API routes and serve a fixture
    run whose completed artifact is a CitedAnswerArtifact with two evidence refs, then a
    second fixture with an ExternalEvidenceArtifact; assert — on the desktop project and
    the chromium-mobile-390 project — the cited-answer artifact renders with both citation
    chips, clicking the first chip navigates to the expected /novels/{id}?chapter=..&start=..
    URL, the external-evidence artifact renders its label and zero citation chips, and the
    390px layout keeps the artifact readable without horizontal overflow. Fix all defects
    in the original component (chip mapping, navigation params, responsive layout, label
    rendering), not the tests, and re-run. Then the phase-level sweeps for this plan's
    surface: `cd frontend && npm run test -q`, `npm run test:e2e -- e2e/agent-artifact.spec.ts`,
    `cd backend && python -m pytest tests/ci -q` (the 25.3-01 gate asserting no
    @earendil-works/pi-web-ui anywhere must be green against the final tree), and a repo-wide
    grep proving the package never entered any manifest or import. Record the desktop/390px
    evidence (spec outcomes, any layout fixes) in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run test:e2e -- e2e/agent-artifact.spec.ts` exits 0 on desktop and chromium-mobile-390 projects
    - `cd frontend && npm run test -q` exits 0
    - `cd backend && python -m pytest tests/ci -q` exits 0
    - Repo-wide: `grep -rn "@earendil-works/pi-web-ui" --include="package.json" --include="package-lock.json" .` returns nothing and `grep -rn "from \"@earendil-works/pi-web-ui\"\|require(\"@earendil-works/pi-web-ui" frontend agent-service 2>/dev/null` returns nothing
    - E2E evidence recorded in SUMMARY: chips navigate correctly; external artifact shows label + zero citation chips on both viewports
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -q && npm run test:e2e -- e2e/agent-artifact.spec.ts && cd ../backend && python -m pytest tests/ci -q</automated>
  </verify>
  <done>Prototype render proven in real browsers on both viewports; no-dependency gate green; feasibility study complete with the verdict, renderer and rejection record on file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pi-web-ui (npm, pattern source) -> NovelMind frontend | Design ideas cross; code must NOT — adoption by drift would import the 0.75.3 dependency train and its storage model |
| Artifact payloads (API) -> React renderer | Artifact content is model/agent-produced data rendered into the DOM; external claims must never present as canon citations |
| Browser session state -> session authority | Session/rendering authority must stay with Next.js + PostgreSQL; browser-local stores (IndexedDB) are a shadow-authority risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.3-05-01 | Tampering | adoption-by-drift — the prototype quietly pulls in @earendil-works/pi-web-ui or wraps its web components | mitigate | Pattern-only rule (D-03) enforced by the 25.3-01 tests/ci gate (forbidden in any package.json) plus this plan's repo-wide grep acceptance criteria; feasibility note records the rejection so the decision is auditable |
| T-25.3-05-02 | Tampering / Info Disclosure | external_evidence rendered as a canon-styled citation, laundering external claims into trusted reading context (UI-level Pitfall 6) | mitigate | ExternalEvidenceView carries a persistent "prohibited from canon" label and renders NO citation-jump affordance; unit + e2e tests assert zero reader-citation elements inside external artifacts |
| T-25.3-05-03 | Elevation of Privilege | shadow session authority via browser-local storage or provider keys in the frontend (25.2 D-15 violation) | mitigate | Feasibility note's explicit rejection record (ChatPanel/IndexedDB/provider-key); renderer consumes server data only; no storage or key code introduced — verified by review and the no-import gates |
| T-25.3-05-04 | Tampering | XSS via artifact content rendered into the DOM | mitigate | React text rendering only — no dangerouslySetInnerHTML anywhere in the new component (review checklist item; artifact blocks render as text nodes) |
| T-25.3-05-05 | Denial of Service | unknown/malformed artifact type crashing the Analysis Chat | mitigate | resolveArtifactRenderer returns an explicit UnknownArtifactFallback; unit test covers the unknown-type path |
| T-25.3-SC | Tampering | npm installs | mitigate | None this plan — pattern-source only; @earendil-works/pi-web-ui was legitimacy-audited [OK] as a pattern donor in 25.3-01 and is never installed |
</threat_model>

<verification>
- `cd frontend && npm run test -- cited-answer-artifact` exits 0
- `cd frontend && npm run test:e2e -- e2e/agent-artifact.spec.ts` exits 0 (desktop + chromium-mobile-390)
- `cd backend && python -m pytest tests/ci -q` exits 0 (no-pi-web-ui gate)
- Feasibility note contains VERDICT / BORROWED / REJECTED sections with the ChatPanel, IndexedDB and provider-key rejections explicit
- ROADMAP phase-verification contribution: one artifact type renders; Next.js/PostgreSQL remain session and rendering authority
</verification>

<success_criteria>
- 25.3-05 delivered: feasibility note + prototype render of one artifact type inside the existing Analysis Chat
- D-03 selective verdict honored: renderer patterns borrowed, package never adopted — rejection record explicit and grep-provable
- D-08/D-09 display discipline: external evidence visibly labeled, never citation-styled
- Phase 25.3 fully planned deliverables complete: governance lock (01), collision gate (02), MCP isolation verdict (03), policy + approval (04), renderer feasibility (05)
</success_criteria>

<output>
Create `.planning/phases/25.3-pi-package-compatibility-governance/25.3-05-SUMMARY.md` when done
</output>
