---
phase: 25.3-pi-package-compatibility-governance
plan: 04
type: execute
wave: 3
depends_on: ["25.3-02"]
files_modified:
  - agent-service/src/policy/engine.ts
  - agent-service/src/policy/session-approvals.ts
  - agent-service/src/policy/tool-visibility.ts
  - agent-service/tests/policy-engine.test.ts
  - backend/app/schemas/agent_approvals.py
  - backend/app/models/agent_runtime.py
  - backend/app/models/__init__.py
  - backend/app/services/agent_runtime/approvals.py
  - backend/app/api/agent.py
  - backend/migrations/versions/27_approval_requests.py
  - backend/tests/integration/agent_runtime/test_approvals.py
  - backend/tests/integration/test_postgres_migrations.py
  - backend/tests/integration/reader_chat/test_migration.py
autonomous: true
requirements: [REQ-AGENT-07]

must_haves:
  truths:
    - "The policy engine evaluates allow/ask/deny over domain actions (never file paths or bash) with the D-10/D-15 vocabulary frozen in code: candidate creation/read actions allow; publication, fork, derivative apply/divergence/visual and export approval actions ask as declared; modify_original_canon and move_active_pointer deny; unknown actions deny"
    - "deny is deterministic and terminal: the two deny-seeded actions have NO code path to approval — not via session approvals, not via skill rules, not via the API (D-10, ASVS V4)"
    - "Unknown actions resolve deny (fail-closed); invalid higher-precedence config clamps allow to ask; rule algebra is last-match-wins within a list and most-restrictive-wins (deny > ask > allow) across global/skill/session layers (pi-permission-system documented semantics, RESEARCH Pattern 4)"
    - "ask creates a Web ApprovalRequest persisted by FastAPI (owner-scoped); confirm/reject through the FastAPI endpoints is the ONLY decision path — the SSE frame notifies, the server decides (D-11, approval-forgery defense)"
    - "Session-scoped approvals live per run in agent-service memory and never persist as standing permission (D-11, ASVS V3); tool-visibility filtering is restrict-only — the active tool set only ever shrinks"
    - "The approval UX is a ui/dialog.tsx-based dialog in the Analysis workspace (Approve once / Approve for this session / Reject) — never window.confirm, never a TUI (D-11)"
  artifacts:
    - path: "agent-service/src/policy/engine.ts"
      provides: "DOMAIN_ACTION_POLICY frozen table + evaluate() precedence engine (~250 lines clean-room)"
      contains: "DOMAIN_ACTION_POLICY"
    - path: "agent-service/src/policy/session-approvals.ts"
      provides: "per-run session approval cache"
      contains: "sessionApprovals"
    - path: "agent-service/src/policy/tool-visibility.ts"
      provides: "restrict-only tool-set filter"
      contains: "filterToolSet"
    - path: "backend/app/services/agent_runtime/approvals.py"
      provides: "confirm/reject sole-mutator service with owner check and expiry"
      contains: "def confirm"
    - path: "backend/migrations/versions/27_approval_requests.py"
      provides: "approval_requests table DDL chaining down_revision 26agentrun01"
      contains: "approval_requests"
    - path: "backend/app/api/agent.py"
      provides: "POST /api/agent/approval-requests + confirm/reject + owner-scoped list"
      contains: "approval-requests"
    - path: "frontend/src/components/analysis/approval-request-dialog.tsx"
      provides: "ui/dialog.tsx approval dialog with three decision buttons"
      contains: "Approve for this session"
    - path: "agent-service/tests/policy-engine.test.ts"
      provides: "full decision matrix (every D-10 action x layers) + fail-closed cases"
      min_lines: 100
  key_links:
    - from: "agent-service/src/server.ts"
      to: "agent-service/src/policy/engine.ts"
      via: "gated action evaluation pre-dispatch inside the run; deny throws PolicyDenied, ask parks the action"
      pattern: "evaluate\\(|PolicyDenied"
    - from: "agent-service/src/server.ts"
      to: "POST /api/agent/approval-requests (FastAPI)"
      via: "ask -> createApprovalRequest -> SSE approval_request frame -> short-poll with run abort signal"
      pattern: "approval_request|approval-requests"
    - from: "frontend/src/components/analysis/analysis-chat-panel.tsx"
      to: "POST /api/agent/approval-requests/{id}/confirm|reject"
      via: "approval-request-dialog buttons with getAccessToken() auth"
      pattern: "approval-requests/.*/(confirm|reject)"
    - from: "backend/app/api/agent.py"
      to: "backend/app/services/agent_runtime/approvals.py"
      via: "service-delegated transitions with owner check — router-level require_user, 404-hide on cross-owner"
      pattern: "approvals\\.(confirm|reject)"
---

<objective>
Build the domain-action permission policy core and its Web Approval adapter (D-10/D-11):
a clean-room ~250-line engine reusing pi-permission-system's *documented semantics*
(last-match-wins, most-restrictive-wins, fail-closed clamps, session approvals,
restrict-only visibility — never its Pi-hook/TUI/tree-sitter implementation), the frozen
D-10 seed table, the FastAPI `ApprovalRequest` authority (model + migration
`27_approval_requests` + service + owner-scoped API reusing the 25.2-03 approve/reject
shape), the agent-service ask-round-trip (park action → POST → SSE frame → short-poll), and
the web Approval dialog.

Purpose: REQ-AGENT-07 — high-impact actions route through allow/ask/deny with Web Approval,
and Canon mutation / active-pointer moves are deny with no approval path. This is the
phase's proof that an agent cannot escalate itself past human decision points.

Output: `src/policy/*` + decision-matrix suite, `approval_requests` persistence + API +
integration suite, ask-round-trip wiring in agent-service, and the Analysis-workspace
Approval dialog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-CONTEXT.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-VALIDATION.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-02-SUMMARY.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-PLAN.md

Execution gate (D-13): this plan MUST NOT start before Phase 22 records three consecutive
scheduled green observations per `.planning/STATE.md`, Phase 25.2 has landed (agent-service
runtime + 25.2-03 artifact approve/reject API + 25.2-04 Analysis workspace exist), and the
25.3-02 ToolRegistryManifest is on record.

Scope exception (orchestrator decision, revision 2026-08-01 — recorded, do NOT split):
this plan carries 18 files_modified, above the 15-file threshold. The exception is
accepted because the per-task distribution is sane (4 / 9 / 6 files), the
policy-engine → persistence → web-round-trip chain is one coherent vertical slice, and
most files are mechanical new files following 25.2/PATTERNS templates (strict Pydantic
schemas, ORM model, migration, colocated tests). Executors MUST NOT expand scope
mid-flight — any additional file requires returning to the orchestrator. The same
exception is recorded in VALIDATION.md.

Decision traceability (revision 2026-08-01, checker D-12 disposition): the D-12 extension
patterns land here as follows — permission-gate → the policy engine (Task 1, D-10/D-11);
confirm-destructive and slow-mode/review-gate → the approval flow (ask → Web
ApprovalRequest → confirm/reject, Tasks 2-3); event bus → the SSE transport
(`approval_request` frame added to the curated serializer, Task 3). The remaining D-12
patterns (question/questionnaire, todo, notify, custom-compaction) are intentionally
deferred to the workspace/derivative phases that need them — see CONTEXT.md `<deferred>`.

<interfaces>
<!-- Contracts the executor codes against — no codebase exploration needed. -->

Policy engine (RESEARCH Pattern 4 — clean-room, semantics cited from pi-permission-system
configuration.md): types PolicyAction = "allow"|"ask"|"deny", DomainActionRule {action,
policy, reason?}; exported frozen const DOMAIN_ACTION_POLICY with the D-10 seed;
evaluate(action, ctx {skillRules?, sessionApprovals}) returns PolicyAction. Deliberate
hardening over the RESEARCH sketch: a global-table deny is TERMINAL — evaluate returns deny
before consulting session approvals or skill rules, so modify_original_canon and
move_active_pointer have no approval path at all (D-10 + ASVS V4 trump the upstream
session-override semantics). Then: session approval -> allow; per-skill rule (from
skill.yaml approval_required_for) and global rule combine most-restrictive-wins
(deny > ask > allow); last-match-wins within an ordered list; unknown action -> deny;
invalid higher-precedence config clamps allow -> ask on load.

ask round trip (RESEARCH Code Examples + open question 1 resolution — SSE push to the
browser + agent-service short-polls FastAPI with the run abort signal, no new channel):
on ask, the run parks the gated action; agent-service POSTs /api/agent/approval-requests
{run_id, action, payload_summary}; emits SSE frame {type: "approval_request", request};
short-polls GET /api/agent/approval-requests/{id} with AbortSignal; approved -> proceed,
approved_for_session -> sessionApprovals.add(action) for this run only (A5), rejected /
expired / aborted -> PolicyDenied.

Backend shape (PATTERNS — 25.2-03 approve/reject analog): router = APIRouter(dependencies
=[Depends(require_user)]); owner-scoped 404-hide; service functions are the sole mutators;
APPROVAL_REQUEST_STATUSES = ("pending", "approved", "approved_for_session", "rejected",
"expired", "cancelled"); model conventions — JSONB variant, TimestampMixin, denormalized
owner_id FK users.id ondelete CASCADE, ck_ CheckConstraint, no relationship(); migration
raw-SQL idempotent style chaining down_revision "26agentrun01"; EXPECTED_HEAD bump in
tests/integration/test_postgres_migrations.py:22 and stale head assertions in
tests/integration/reader_chat/test_migration.py:50,:97 in the SAME commit (Pitfall 5).

Schema-module note: ApprovalRequest Pydantic payloads go in a NEW module
backend/app/schemas/agent_approvals.py (imported by api/agent.py) — NOT
schemas/agent_runtime.py, which same-wave plan 25.3-03 owns for ExternalEvidenceArtifact;
parallel-wave file ownership requires the split (orchestrator wave constraint).

Frontend shape (PATTERNS): dialog on frontend/src/components/ui/dialog.tsx
(@base-ui/react/dialog — Dialog, DialogContent); token via api.ts getAccessToken(); never
window.confirm; SSE approval_request frames consumed in analysis-chat-panel.tsx (25.2-04
workspace) rendering the dialog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean-room policy engine — DOMAIN_ACTION_POLICY, evaluate(), session approvals, tool visibility</name>
  <files>agent-service/src/policy/engine.ts, agent-service/src/policy/session-approvals.ts, agent-service/src/policy/tool-visibility.ts, agent-service/tests/policy-engine.test.ts</files>
  <behavior>
    - DOMAIN_ACTION_POLICY is one exported frozen const covering the full D-15 vocabulary: search_original_text, create_scene_spec_candidate, generate_image_candidate, publish_illustration, attach_illustration_to_text, create_canon_fork, apply_derivative_edit, publish_derivative_revision, allow_divergence, publish_derivative_visual, prepare_export, approve_export, materialize_export, download_export, modify_original_canon and move_active_pointer. Each action has an explicit allow/ask/deny rule; the final two are permanent deny. Tests consume this table and reject missing/unknown actions.
    - evaluate(): global deny is terminal (returned before session approvals — no approval path for the two deny actions); session approval -> allow; skill + global layers combine most-restrictive-wins; last-match-wins within a list; unknown action -> deny; invalid higher-precedence scope clamps allow -> ask
    - Session approvals are a per-run in-memory Set keyed by action; nothing persists them; a new run starts empty
    - filterToolSet(activeTools, deniedOrDisabled) only ever removes entries — output is always a subset of input; unregistered/unknown tools are blocked before permission checks
    - The engine is pure/domain-only: no file paths, no bash parsing, no Pi hooks, no imports from @gotgenes/pi-permission-system (pattern-source only, D-03)
  </behavior>
  <read_first>
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md (Pattern 4 — semantics list, seed table, fork-vs-clean-room rationale; Security Domain — policy bypass row)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md (section: agent-service/src/policy/ — NO ANALOG conventions: frozen-table mirror test, restrict-only visibility, deterministic deny)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-CONTEXT.md (D-10, D-11)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-VALIDATION.md (25.3-04 sampling row — full decision matrix)
  </read_first>
  <action>
    Create `agent-service/src/policy/engine.ts`: exported types `PolicyAction` and
    `DomainActionRule`; exported frozen `DOMAIN_ACTION_POLICY` with the exact D-10 seed and
    reasons verbatim from CONTEXT; exported `PolicyDenied` error carrying the action and
    reason; exported `evaluate(action: string, ctx: { skillRules?: DomainActionRule[];
    sessionApprovals: ReadonlySet<string> }): PolicyAction` implementing the hardened
    precedence from the interfaces block — global deny first (terminal), then session
    approvals, then most-restrictive across skill/global layers, unknown -> deny — plus an
    exported `loadSkillRules(raw)` that validates a skill's approval_required_for entries
    and clamps allow -> ask when the entry is malformed (fail-closed clamp). Create
    `agent-service/src/policy/session-approvals.ts`: a tiny `SessionApprovals` class (add /
    has / size) constructed per run with no persistence path. Create
    `agent-service/src/policy/tool-visibility.ts`: exported
    `filterToolSet(activeTools: string[], deniedOrDisabled: ReadonlySet<string>): string[]`
    returning only the subset not in the denied set, and
    `assertKnownTools(tools, registryNames)` throwing on any tool absent from the
    ToolRegistryManifest names (25.3-02) before permission checks run. Write
    `agent-service/tests/policy-engine.test.ts` (vitest): the FULL decision matrix — every
    D-10 action x {no layers, skill ask, skill allow (clamped where invalid), session
    approval} asserting the exact outcome; deny-terminal cases (session approval present,
    skill rule allow present — modify_original_canon and move_active_pointer still deny);
    unknown action -> deny; malformed skill rule -> ask clamp; filterToolSet subset
    property (fuzz: output always subset of input); assertKnownTools blocks an unregistered
    tool name; a mirror test asserting DOMAIN_ACTION_POLICY covers exactly the full D-15
    actions so the table and tests cannot drift.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/policy-engine.test.ts` exits 0
    - `grep -c "policy:" agent-service/src/policy/engine.ts` >= 7 (full D-10 seed present)
    - `grep -rn "@gotgenes/pi-permission-system" agent-service/src` returns nothing (clean-room, pattern-source only)
    - `grep -n "modify_original_canon\|move_active_pointer" agent-service/src/policy/engine.ts` matches both with deny
    - `cd agent-service && npx tsc --noEmit` exits 0 and `npx vitest run` exits 0 (no 25.2/25.3-02 regression)
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run tests/policy-engine.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Decision matrix green across all layers; deny actions have no approval path; visibility restrict-only; zero upstream imports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend ApprovalRequest authority — model, migration 27, service, API, integration tests</name>
  <files>backend/app/schemas/agent_approvals.py, backend/app/models/agent_runtime.py, backend/app/models/__init__.py, backend/app/services/agent_runtime/approvals.py, backend/app/api/agent.py, backend/migrations/versions/27_approval_requests.py, backend/tests/integration/agent_runtime/test_approvals.py, backend/tests/integration/test_postgres_migrations.py, backend/tests/integration/reader_chat/test_migration.py</files>
  <behavior>
    - approval_requests table: id, denormalized owner_id (FK users.id ondelete CASCADE), run linkage, action, payload_summary JSONB, status with ck_ CheckConstraint over APPROVAL_REQUEST_STATUSES, decided_at, expires_at, TimestampMixin; no relationship()
    - Migration 27_approval_requests.py raw-SQL idempotent style, down_revision "26agentrun01"; EXPECTED_HEAD and the two stale reader_chat migration assertions bumped in the SAME commit (Pitfall 5)
    - approvals.confirm/reject are the ONLY status mutators; owner check inside the service; pending -> approved | approved_for_session | rejected; expiry on run end/timeout; a direct service-level forgery attempt (mutating status outside confirm/reject) is rejected by construction and proven by test
    - API: POST /api/agent/approval-requests (agent-service mints with the per-run internal token), POST /{id}/confirm, POST /{id}/reject, GET list {"items","total","skip","limit"} + GET /{id} for the short-poll — router-level require_user, owner-scoped 404-hide; a non-owner can never confirm (approval forgery defense)
    - Schemas live in the new backend/app/schemas/agent_approvals.py (strict Pydantic) — agent_runtime.py is owned by 25.3-03 this wave
  </behavior>
  <read_first>
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md (sections: api/agent.py router-level auth + approve/reject contract + pagination shape; models/agent_runtime.py + migration — ORM/migration conventions + Pitfall 5; services/agent_runtime/approvals.py analog; schemas/agent_runtime.py strict-base conventions)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md (Pattern 4 ask round trip; Code Examples — FastAPI endpoint; A3; Security Domain V2/V4 rows)
    - backend/app/api/reader_chat.py (the router-level require_user / 404-hide / pagination analog)
    - backend/app/models/reader_chat.py + backend/app/models/base.py (the file conventions being copied)
    - backend/app/models/agent_runtime.py (the file being modified — 25.2-03 models)
    - backend/migrations/versions/26_agent_runtime.py (25.2-03 — the down_revision parent and DDL style)
    - backend/tests/integration/test_postgres_migrations.py (EXPECTED_HEAD at :22 — being bumped)
    - backend/tests/integration/reader_chat/test_migration.py (:50, :97 — stale assertions being bumped)
  </read_first>
  <action>
    Create `backend/app/schemas/agent_approvals.py` on the strict Pydantic base:
    `ApprovalRequestCreate {run_id, skill_version_id, artifact_id, artifact_revision_id,
    owner_id, novel_id, branch_id, fork_id, action, payload_hash, expires_at}`,
    `ApprovalRequestView {id,
    run_id, action, payload_summary, status, created_at, decided_at, expires_at}`,
    `ApprovalDecision {mode: Literal["once","session"]}` for confirm. Modify
    `backend/app/models/agent_runtime.py`: add `ApprovalRequest` following the house
    conventions (TimestampMixin + Base, JSONB variant for payload_summary, denormalized
    owner_id, status String with CheckConstraint ck_approval_requests_status over
    APPROVAL_REQUEST_STATUSES defined as a module constant tuple, expires_at nullable, no
    relationship()); register the import and __all__ entry in models/__init__.py. Create
    `backend/migrations/versions/27_approval_requests.py` in the raw-SQL idempotent house
    style (inspector guard, symmetric downgrade) with revision "27approval01" and
    down_revision "26agentrun01"; IN THE SAME COMMIT bump EXPECTED_HEAD in
    tests/integration/test_postgres_migrations.py:22 to "27approval01" and update the two
    stale head assertions in tests/integration/reader_chat/test_migration.py:50,:97
    (Pitfall 5 — never split across commits). Create
    `backend/app/services/agent_runtime/approvals.py`: `create(db, payload, owner_id)`,
    `confirm(db, request_id, owner_id, mode)` and `reject(db, request_id, owner_id)` as the
    sole mutators — owner mismatch returns None (404-hide), non-pending requests are
    terminal (re-decision raises the stable conflict), confirm mode "session" records
    approved_for_session, expiry marks expired past expires_at or at run end; no other
    function in the codebase writes the status column. Modify `backend/app/api/agent.py`:
    add the four endpoints per the interfaces block — create (internal per-run token auth
    like the 25.2-03 finalize path), confirm/reject (user JWT, owner-scoped, 404-hide),
    list with the {"items","total","skip","limit"} shape, get-by-id for the agent-service
    short-poll. Write `backend/tests/integration/agent_runtime/test_approvals.py`: round
    trip ask -> pending persisted -> confirm(once) -> approved; confirm(session) ->
    approved_for_session; reject -> rejected and terminal; cross-owner confirm -> 404 (not
    403); re-confirm of a decided request -> conflict; expired request -> decision refused;
    forgery attempt — direct status mutation outside the service functions has no API path
    and the service refuses unknown transitions; list pagination shape asserted.
  </behavior>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/integration/agent_runtime/test_approvals.py -q` exits 0 (docker compose db up)
    - `cd backend && python -m pytest tests/integration/test_postgres_migrations.py tests/integration/reader_chat/test_migration.py -q` exits 0 with EXPECTED_HEAD "27approval01"
    - `grep -n "27approval01" backend/migrations/versions/27_approval_requests.py` matches revision; `grep -n 'down_revision = "26agentrun01"' backend/migrations/versions/27_approval_requests.py` matches
    - `grep -n "approval-requests" backend/app/api/agent.py` matches the new routes
    - `grep -rn "status =" backend/app/services/agent_runtime/approvals.py | grep -v "confirm\|reject\|expire"` returns no stray mutators
    - `cd backend && python -m pytest tests/ci tests/contract -q` exits 0 (no regression)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/agent_runtime/test_approvals.py tests/integration/test_postgres_migrations.py tests/integration/reader_chat/test_migration.py -q && python -m pytest tests/ci tests/contract -q</automated>
  </verify>
  <done>ApprovalRequest persists owner-scoped with sole-mutator transitions; migration head chain green; forgery and cross-owner paths provably closed</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm — policy and Approval authority</name>
  <files>agent-service/src/policy/engine.ts, agent-service/src/policy/session-approvals.ts, agent-service/src/policy/tool-visibility.ts, agent-service/tests/policy-engine.test.ts, backend/app/services/agent_runtime/approvals.py, backend/tests/integration/agent_runtime/test_approvals.py</files>
  <read_first>
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-PATTERNS.md (section: approval-request-dialog.tsx — dialog primitives, hard rule never window.confirm, token source, three buttons)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-RESEARCH.md (Code Examples — ask round trip; open question 1; Anti-Patterns — prompt/client-side decisions)
    - frontend/src/components/ui/dialog.tsx (the house dialog primitives)
    - frontend/src/components/analysis/analysis-chat-panel.tsx (the file being modified — 25.2-04 SSE frame consumption)
    - frontend/src/lib/api.ts (getAccessToken at :42-45)
    - agent-service/src/transport/sse.ts (the file being modified — 25.2-05 curated frame serializer)
    - agent-service/src/server.ts (the file being modified — run dispatch + 25.3-02 startup chain)
    - agent-service/src/policy/engine.ts (Task 1)
    - .planning/phases/25.3-pi-package-compatibility-governance/25.3-VALIDATION.md (25.3-04 sampling row + Manual-Only Verifications: browser confirm/reject)
  </read_first>
  <action>
    Run the policy and backend Approval suites, fixing failures in the policy/authority code.
    Confirm all D-15 actions are covered, unknown actions deny, permanent denies cannot be
    session-approved, and owner/hash/revision/expiry/replay checks fail closed. The browser,
    SSE and round-trip wiring are exclusively owned by 25.3-06. Do not modify frontend or
    transport files in this plan. The 25.3-06 plan will wire the ask round trip in agent-service: modify `agent-service/src/server.ts` so gated
    actions evaluate through the policy engine pre-dispatch — deny throws PolicyDenied into
    the run's stable error path; ask POSTs /api/agent/approval-requests with the per-run
    internal token, emits the SSE frame, and awaits `waitForApproval(requestId, runSignal)`
    implemented as a short-poll of GET /api/agent/approval-requests/{id} (2 s interval,
    AbortSignal-bound, run cancel aborts the wait and resolves denied); approved proceeds,
    approved_for_session also adds the action to the run's SessionApprovals, anything else
    throws PolicyDenied. Modify `agent-service/src/transport/sse.ts`: add the
    {type:"approval_request", request} frame to the curated serializer. Create
    `frontend/src/components/analysis/approval-request-dialog.tsx` on ui/dialog.tsx: props
    {request, onDecide}; shows action name and payload_summary; three buttons — "Approve
    once" POSTs confirm {mode:"once"}, "Approve for this session" POSTs confirm
    {mode:"session"}, "Reject" POSTs reject — all with getAccessToken() auth; pending state
    disables buttons; NEVER window.confirm. Modify
    `frontend/src/components/analysis/analysis-chat-panel.tsx`: on an approval_request SSE
    frame render the dialog and POST the decision back. Write
    `frontend/src/components/analysis/approval-request-dialog.test.tsx` (vitest, vi.hoisted
    API mocks): each button POSTs the exact payload to the right endpoint; reject resolves
    denied; no window.confirm usage (static assertion). Then Test, Fix, and Confirm: run a
    live round trip against the real stack (compose db + uvicorn + agent-service + Next) —
    trigger an ask action (publish_illustration fixture), confirm once in the browser and
    verify the action proceeds; reject once and verify the deterministic block; verify
    session-approval scoping (second run asks again); attempt a client-side self-approval
    (forged confirm without ownership) and verify 404-hide. Fix all defects in the original
    code path (precedence bugs, poll abort, frame ordering), not the tests. Full sweeps:
    agent-service vitest + tsc, backend full marker suite + tests/ci, frontend vitest.
    Record the round-trip evidence in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `cd agent-service && npx vitest run tests/policy-engine.test.ts && npx vitest run` exits 0
    - `cd frontend && npm run test -- approval-request-dialog` exits 0
    - `grep -rn "window.confirm" frontend/src/components/analysis/` returns nothing
    - `grep -n "approval_request" agent-service/src/transport/sse.ts` matches the new frame type
    - `cd backend && python -m pytest tests -m "unit or integration or contract or adversarial" -q` exits 0
    - Live evidence recorded in SUMMARY: confirm-once proceeds, reject blocks, second run re-asks (session scope), forged confirm 404-hidden
  </acceptance_criteria>
  <verify>
    <automated>cd agent-service && npx vitest run tests/policy-engine.test.ts && cd ../backend && python -m pytest tests/integration/agent_runtime/test_approvals.py -q</automated>
  </verify>
  <done>The full policy vocabulary and authoritative ApprovalRequest transitions pass; Web transport remains delegated to 25.3-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (via tools/skills) -> policy engine | Agent-initiated actions are untrusted; the engine is the pre-dispatch choke point |
| agent-service -> FastAPI approval API | Approval creation/decision cross the service boundary; forged approvals are the primary abuse |
| browser -> FastAPI confirm/reject | The human decision enters here; non-owner or replayed decisions must fail |
| SSE stream -> browser | Notification only — the frame must never carry decision authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.3-04-01 | Elevation of Privilege | policy bypass — agent performs an ask/deny action without approval (unregistered tool, unknown action, skill-rule override, session-approval laundering) | mitigate | Unknown tools blocked before permission checks (assertKnownTools vs 25.3-02 manifest); unknown actions resolve deny; global deny is terminal — evaluated before session approvals so the two deny actions have no approval path; most-restrictive-wins across layers; malformed skill rules clamp allow -> ask; full decision-matrix suite |
| T-25.3-04-02 | Tampering | approval forgery — client or non-owner self-approves (forged confirm, cross-owner confirm, re-decision, status mutation outside the service) | mitigate | Confirm/reject only via FastAPI endpoints behind router-level require_user with owner check and 404-hide; service functions are the sole status mutators (grep acceptance criterion); terminal-state re-decision refused; SSE frame is notification, never authority; integration tests cover forgery, cross-owner, replay |
| T-25.3-04-03 | Elevation of Privilege | session approval persisting as standing permission | mitigate | SessionApprovals is per-run in-memory only (A5, ASVS V3); no persistence path exists; live test: second run re-asks |
| T-25.3-04-04 | Tampering | prompt/client-side permission decisions (model talks itself into allow, browser decides policy) | mitigate | deny is deterministic in the engine (code, not prompts); the browser only renders the dialog and POSTs the human's choice — FastAPI decides; anti-pattern asserted by the no-window.confirm gate and the round-trip tests |
| T-25.3-04-05 | Tampering | migration head-chain split or stale head assertions masking a wrong down_revision (Pitfall 5) | mitigate | EXPECTED_HEAD + both stale reader_chat assertions bumped in the SAME commit as migration 27; integration tests enforce the chain |
| T-25.3-04-06 | Information Disclosure | approval payload_summary leaking sensitive run data to the browser | mitigate | payload_summary is a curated summary written by agent-service, not raw tool I/O; list/get endpoints are owner-scoped with 404-hide |
| T-25.3-SC | Tampering | npm/pip installs | mitigate | None — clean-room engine (zero upstream import, grep-gated); backend/frontend reuse existing pinned deps; @gotgenes/pi-permission-system stays pattern-only per D-03 and the 25.3-01 tests/ci gate |
</threat_model>

<verification>
- `cd agent-service && npx vitest run tests/policy-engine.test.ts` exits 0 (full decision matrix)
- `cd backend && python -m pytest tests/integration/agent_runtime/test_approvals.py -q` exits 0 (round trip + forgery + cross-owner)
- Migration head chain green with EXPECTED_HEAD "27approval01"
- `cd frontend && npm run test -- approval-request-dialog` exits 0; no window.confirm in the analysis tree
- Live round trip: confirm-once proceeds, reject blocks, session scope per run, forged confirm 404-hidden
</verification>

<success_criteria>
- REQ-AGENT-07 satisfied: high-impact actions route through allow/ask/deny with Web Approval; modify_original_canon and move_active_pointer are deny with no approval path
- D-10/D-11 implemented: frozen seed, precedence semantics, session-scoped approvals, restrict-only visibility, web dialog UX
- Approval state round-trips server-side; the SSE frame notifies, FastAPI decides
</success_criteria>

<output>
Create `.planning/phases/25.3-pi-package-compatibility-governance/25.3-04-SUMMARY.md` when done
</output>
