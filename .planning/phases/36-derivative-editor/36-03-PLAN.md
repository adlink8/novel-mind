---
phase: 36-derivative-editor
plan: 03
type: execute
wave: 2
depends_on: [36-02]
files_modified:
  - backend/app/models/derivative_revision.py
  - backend/app/services/derivative_editor/revisions.py
  - backend/app/api/derivative_revisions.py
  - backend/tests/integration/test_derivative_revision_history.py
autonomous: true
requirements: [REQ-FORK-02, REQ-CRE-04]
must_haves:
  truths: [autosave 不丢草稿, optimistic concurrency 冲突可恢复, history/diff/rollback 可审计]
  artifacts: ["backend/app/models/derivative_revision.py", "backend/app/services/derivative_editor/revisions.py"]
  key_links: ["base revision CAS -> immutable revision parent/checksum -> rollback creates new revision"]
---
<objective>实现自动保存、不可变版本历史、确定性 diff 与可恢复 rollback。</objective>
<execution_context>@.planning/phases/36-derivative-editor/36-CONTEXT.md
@.planning/phases/36-derivative-editor/36-RESEARCH.md
@backend/app/models/reader_chat.py</execution_context>
## Steps
<tasks>
<task type="auto">
<name>Task 1: Implement revision CAS and recovery</name>
<read_first>backend/app/models/derivative_chapter.py; backend/app/models/reader_chat.py; backend/app/services/reader_chat/budget.py; .planning/phases/36-derivative-editor/36-CONTEXT.md</read_first>
<files>backend/app/models/derivative_revision.py, backend/app/services/derivative_editor/revisions.py, backend/app/api/derivative_revisions.py</files>
<action>按 D-36-02 创建 parent_revision/checksum/content/actor/timestamp immutable rows；autosave 使用 base_revision 条件更新，冲突返回 409 和最新 revision，diff 基于 canonical Markdown，rollback 只创建新 child revision 并记录原因/approval 状态。</action>
<acceptance_criteria>并发写不 last-write-wins；崩溃重试幂等；rollback 可追溯且不覆盖历史。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/test_derivative_revision_history.py -q</automated></verify>
<done>revision 行不可变，autosave 使用 base_revision CAS，冲突返回 409 最新 revision，rollback 生成新 child。</done>
</task>
<task type="auto">
<name>Task 2: Test, Fix, and Confirm</name>
<read_first>backend/app/services/derivative_editor/revisions.py; backend/tests/integration/test_derivative_revision_history.py</read_first>
<files>backend/tests/integration/test_derivative_revision_history.py, backend/tests/adversarial/test_derivative_revision_concurrency.py</files>
<action>注入重复 autosave、并发 base revision、网络重试、崩溃恢复、diff 边界和 rollback 后继续编辑；修复任何历史覆盖或 owner 泄露。</action>
<acceptance_criteria>history/diff/rollback/CAS 与 owner isolation 全绿，任何冲突都给出可恢复结果。</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/test_derivative_revision_history.py tests/adversarial/test_derivative_revision_concurrency.py -q</automated></verify>
<done>重复 autosave、并发、重试、崩溃恢复、diff 边界和 rollback 后续编辑均无历史覆盖或 owner 泄露。</done>
</task>
</tasks>
## Must-Haves

## Verification

<verification>执行 revision、并发、恢复和 rollback 测试。</verification>
<success_criteria>编辑系统具备 durable draft 和完整版本 lineage，可被生成/export 消费。</success_criteria>
<must_haves>schema migration 已在 36-01；此计划强化 owner、evidence、approval、candidate-only 版本门。</must_haves>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| concurrent clients -> revision store | 客户端可能提交旧 base_revision |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-36-03-01 | Tampering | revision write | mitigate | conditional CAS and immutable parent |
| T-36-03-02 | Repudiation | rollback | mitigate | actor/reason/approval journal |
| T-36-03-SC | Tampering | dependencies | mitigate | no new package |
</threat_model>
<output>Execution may create 36-03-SUMMARY.md; planner does not.</output>
