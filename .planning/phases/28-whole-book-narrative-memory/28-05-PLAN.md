---
phase: 28-whole-book-narrative-memory
plan: "05"
type: execute
wave: 5
depends_on: ["28-04"]
files_modified:
  - agent-service/skills/narrative-memory/SKILL.md
  - agent-service/skills/narrative-memory/skill.yaml
  - agent-service/skills/narrative-memory/input.schema.json
  - agent-service/skills/narrative-memory/output.schema.json
  - agent-service/tests/skills/narrative-memory.test.ts
  - backend/tests/integration/agent_runtime/test_phase_28_skill.py
autonomous: true
requirements: [REQ-NM-01, REQ-NM-02, REQ-NM-03, REQ-NM-04, REQ-NM-05, REQ-NM-06, REQ-AGENT-02, REQ-AGENT-03]
must_haves:
  truths:
    - "Phase 28 is consumed through the versioned analyze-chapter and build-story-arc Skill or declared Agent mode, not an untracked one-off model call."
    - "Every run binds owner/novel/branch where applicable, SkillVersion, SkillRun, ToolRuns, input hash, source snapshot, model/runtime lineage and output Artifact revision."
    - "Pi may orchestrate only the declared Domain Tool allowlist; FastAPI and deterministic validators retain permission, evidence, state-transition and publication authority."
    - "Cancellation, unknown tools, schema drift, wrong owner/version/cutoff and approval bypass fail closed without an official Artifact or domain write."
    - "ChapterAnalysisArtifact context fields obey bounded source/input hash, cutoff, max-length and spoiler-policy lineage; digests are compressed non-indexed payloads and next hints only disambiguate without future facts."
    - "OutlineCandidateArtifact and MainlineCandidateArtifact are uncertainty-bearing, source-lineage-bound candidates and never become Canon by generation alone."
  artifacts:
    - path: "agent-service/skills/narrative-memory/skill.yaml"
      provides: "Versioned Skill manifest with schemas, allowed tools, budget, permissions and approval actions."
    - path: "backend/tests/integration/agent_runtime/test_phase_28_skill.py"
      provides: "End-to-end Runtime to Tool to Artifact to Validator/Approval boundary proof."
  key_links:
    - from: "agent-service/skills/narrative-memory/skill.yaml"
      to: ".planning/AGENT-RUNTIME-CONTRACT.md"
      via: "Phase 28 consumption contract"
    - from: "SkillRun"
      to: "ChapterAnalysisArtifact and StoryArcArtifact"
    via: "immutable producing-skill, tool-call, source and evidence lineage"
---

<objective>
Bind the Phase 28 deterministic domain capabilities to the Issue #29 Agent Runtime through
the versioned analyze-chapter and build-story-arc Skill and persist ChapterAnalysisArtifact and StoryArcArtifact as the official output.

Purpose: make the Phase 28 Agent-consumption boundary executable without moving factual,
permission or publication authority into Pi.
Output: Skill package, schemas, registry wiring and integration tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AGENT-RUNTIME-CONTRACT.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-PLAN.md
@.planning/phases/25.2-embedded-novel-agent-runtime/25.2-05-PLAN.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-02-PLAN.md
@.planning/phases/25.3-pi-package-compatibility-governance/25.3-04-PLAN.md
@.planning/phases/28-whole-book-narrative-memory/28-CONTEXT.md
@.planning/phases/28-whole-book-narrative-memory/28-RESEARCH.md
@.planning/phases/28-whole-book-narrative-memory/28-PATTERNS.md
@.planning/phases/28-whole-book-narrative-memory/28-VALIDATION.md
</execution_context>

## Steps

<tasks>
<task type="auto">
  <name>Task 1: Define and register the Phase 28 Skill and Artifact contract</name>
  <read_first>.planning/AGENT-RUNTIME-CONTRACT.md; .planning/phases/25.2-embedded-novel-agent-runtime/25.2-03-PLAN.md; .planning/phases/25.3-pi-package-compatibility-governance/25.3-02-PLAN.md</read_first>
  <files>agent-service/skills/narrative-memory/SKILL.md; agent-service/skills/narrative-memory/skill.yaml; agent-service/skills/narrative-memory/input.schema.json; agent-service/skills/narrative-memory/output.schema.json; agent-service/tests/skills/narrative-memory.test.ts</files>
  <action>Define analyze-chapter and build-story-arc with exact version, input/output schemas, budget, read/write permissions, forbidden spaces, cancellation policy and allowed tools: get_chapter, get_evidence_span, get_events, get_character_state, get_relationships, get_clue_chain, get_world_rules, get_narrative_memory. Register it only through the Phase 25.2 ResourceLoader allowlist and Phase 25.3 ToolRegistryManifest. The output schema materializes ChapterAnalysisArtifact and StoryArcArtifact plus OutlineCandidateArtifact and MainlineCandidateArtifact with artifact/revision IDs, SkillRun and ToolRun IDs, owner/novel/branch scope where applicable, input/content hashes, source snapshot, evidence refs, runtime/model lineage, uncertainty and legal statuses. ChapterAnalysisArtifact must expose bounded `chapter_digest`, chunk digests, `previous_context_summary`, `next_context_hint` and `continuity_notes`, each bound to source/input hash, cutoff, max length and spoiler policy; digests are compressed payloads only and never retrieval-index inputs or EvidenceRefs, while next hints only disambiguate and never leak future facts. Outline/Mainline are candidate-only and cannot enter Canon. Unknown fields, tools or actions fail closed.</action>
  <acceptance_criteria>The Skill loader accepts the pinned manifest and rejects ambient discovery, unknown tools, schema mismatch, undeclared permissions and missing lineage.</acceptance_criteria>
  <verify><automated>cd agent-service && npx vitest run tests/skills/narrative-memory.test.ts</automated></verify>
  <done>The versioned Skill and Artifact schemas are registry-valid and replayable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire deterministic validation, approval and publication boundaries</name>
  <read_first>.planning/AGENT-RUNTIME-CONTRACT.md; agent-service/skills/narrative-memory/skill.yaml; .planning/phases/25.3-pi-package-compatibility-governance/25.3-04-PLAN.md</read_first>
  <files>backend/tests/integration/agent_runtime/test_phase_28_skill.py; agent-service/tests/skills/narrative-memory.test.ts</files>
  <action>Exercise SkillRun to ToolRun to candidate Artifact to deterministic Validator/Gate. Narrative Memory is candidate-only: this phase creates no ApprovalRequest, invokes no Publisher and performs no promotion or active-pointer change. FastAPI persists candidate/blocked/cancelled state only. Assert wrong scope, stale hashes, missing evidence, timeout, cancellation, context length/spoiler violations, future-fact next hints, digest index/EvidenceRef misuse and Outline/Mainline Canon attempts produce stable blocked/cancelled outcomes without official writes.</action>
  <acceptance_criteria>Positive and adversarial integration cases prove the Agent cannot bypass domain, evidence, approval or publication authority.</acceptance_criteria>
  <verify><automated>cd backend && python -m pytest tests/integration/agent_runtime/test_phase_28_skill.py -q</automated></verify>
  <done>The Phase 28 Agent integration is deterministic at every authority transition.</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>agent-service/tests/skills/narrative-memory.test.ts; backend/tests/integration/agent_runtime/test_phase_28_skill.py; .planning/phases/28-whole-book-narrative-memory/28-VALIDATION.md</read_first>
  <files>agent-service/skills/narrative-memory/SKILL.md; agent-service/skills/narrative-memory/skill.yaml; agent-service/skills/narrative-memory/input.schema.json; agent-service/skills/narrative-memory/output.schema.json; agent-service/tests/skills/narrative-memory.test.ts; backend/tests/integration/agent_runtime/test_phase_28_skill.py</files>
  <action>Run both suites, fix failures, then replay success, cancellation, retry, unknown-tool, wrong-scope, stale-lineage, validator-failure, approval-bypass, digest-index/EvidenceRef, spoiler/future-hint and Outline/Mainline Canon fixtures. Confirm all official outputs are versioned ChapterAnalysisArtifact and StoryArcArtifact records plus candidate-only OutlineCandidateArtifact/MainlineCandidateArtifact, and no Agent session state or emotional memory is treated as domain truth.</action>
  <acceptance_criteria>All targeted tests pass twice with stable hashes/statuses and no forbidden domain writes.</acceptance_criteria>
  <verify><automated>cd agent-service && npx vitest run tests/skills/narrative-memory.test.ts && cd ../backend && python -m pytest tests/integration/agent_runtime/test_phase_28_skill.py -q</automated></verify>
  <done>Phase 28 Agent consumption is verified and ready for phase-level verification.</done>
</task>
</tasks>

## Must-Haves

- Versioned Skill/SkillRun/ToolRun/Artifact lineage is complete and replayable.
- ToolRegistryManifest, server-side domain guards and policy decisions fail closed.
- Validator authority is not delegated to prompts; this candidate-only phase has no ApprovalRequest or Publisher role.
- ChapterAnalysisArtifact context/continuity fields are hash-, cutoff-, length- and spoiler-bound; digests remain compressed, non-indexed payloads and next hints are disambiguation-only.
- OutlineCandidateArtifact and MainlineCandidateArtifact retain uncertainty/source lineage and remain outside Canon.
- Emotional memory is explicitly out of scope for the Phase 28 Skill schemas and artifacts.

## Verification

- Run the targeted Node Skill tests and FastAPI integration tests.
- Confirm cancellation/retry and all adversarial authority cases.
- Confirm ChapterAnalysisArtifact and StoryArcArtifact plus the explicitly candidate-only OutlineCandidateArtifact/MainlineCandidateArtifact are the only official Agent outputs for this slice; no emotional-memory output is admitted.

## Success Criteria

- analyze-chapter and build-story-arc consumes the Phase 28 deterministic services through controlled tools.
- ChapterAnalysisArtifact and StoryArcArtifact carries complete lineage and valid state transitions.
- Chapter context/continuity fields satisfy bounded hash/cutoff/length/spoiler policy, non-indexed digest, and non-leaking next-hint rules; outline/mainline candidates preserve uncertainty and lineage without Canon writes.
- No default coding tool, ambient package, direct database write or authority bypass is reachable.
