---
phase: 34-illustration-anchor-export
plan: 04
type: execute
wave: 4
depends_on: [34-02, 34-03]
files_modified:
  - backend/app/services/export/manifest.py
  - backend/app/services/export/markdown.py
  - backend/app/services/export/html.py
  - backend/app/services/export/epub.py
  - backend/app/api/export.py
  - backend/tests/integration/export/test_parity.py
  - frontend/src/components/export/export.test.tsx
  - frontend/e2e/export.spec.ts
autonomous: true
requirements: [REQ-VIS-05]
must_haves:
  truths:
    - "批准插图通过 hash-verified anchor 在 reader 与导出中保持一致，失效和缺失显式呈现。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/export/manifest.py"
      provides: "冻结文本/asset/anchor/citation manifest，完成 Markdown/HTML/EPUB 导出与 UAT。"
    - path: "backend/app/services/export/html.py"
      provides: "从同一 frozen manifest 生成 HTML，并与 EPUB3 保持 parity。"
    - path: "frontend/e2e/export.spec.ts"
      provides: "本计划的自动化验证与回归证据"
    - path: "frontend/src/components/export/export.test.tsx"
      provides: "可被 Vitest 发现的 export smoke 与状态契约"
  key_links:
    - from: "backend/app/services/export/manifest.py"
      to: "backend/app/services/export/markdown.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/export/manifest.py"
      to: "IllustrationRevision → IllustrationAnchor"
      via: "versioned candidate artifact reference"
    - from: "backend/app/services/export/manifest.py"
      to: "backend/app/services/export/html.py + backend/app/services/export/epub.py"
      via: "same frozen manifest for HTML/EPUB3 parity"

---

<objective>
冻结文本/asset/anchor/citation manifest，完成 Markdown/HTML/EPUB 导出与 UAT。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-05 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/export/manifest.py, backend/app/services/export/markdown.py, backend/app/services/export/html.py, backend/app/services/export/epub.py, backend/app/api/export.py, backend/tests/integration/export/test_parity.py, frontend/e2e/export.spec.ts
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-illustration-anchor-export/34-CONTEXT.md
@.planning/phases/34-illustration-anchor-export/34-RESEARCH.md
@.planning/phases/34-illustration-anchor-export/34-PATTERNS.md
@.planning/phases/34-illustration-anchor-export/34-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/34-illustration-anchor-export/34-CONTEXT.md; .planning/phases/34-illustration-anchor-export/34-RESEARCH.md; .planning/phases/34-illustration-anchor-export/34-PATTERNS.md</read_first>
  <files>backend/app/services/export/manifest.py, backend/app/services/export/markdown.py, backend/app/services/export/html.py, backend/app/services/export/epub.py, backend/app/api/export.py, backend/tests/integration/export/test_parity.py, frontend/e2e/export.spec.ts</files>
  <action>实现 `ExportManifestService.freeze`，一次性绑定 text/source snapshot、approved assets、valid anchors、captions/citations、content/version hashes；missing asset 写 explicit missing-asset record，不编造 URL、不静默丢 provenance。参考 analog `.planning/phases/34-illustration-anchor-export/34-PATTERNS.md` 的 source hash、approved asset FK 和 frozen manifest 映射。按 D-34-04。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/export/test_parity.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/export/test_parity.py -q</automated><acceptance>manifest 可重放、排序确定、approved-only、hash/owner/version scope 完整。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/integration/export/test_parity.py -q 通过；frozen manifest 的 export parity、approved-only、hash/owner/version scope 可重放；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/34-illustration-anchor-export/34-CONTEXT.md; .planning/phases/34-illustration-anchor-export/34-RESEARCH.md; .planning/phases/34-illustration-anchor-export/34-PATTERNS.md</read_first>
  <files>backend/app/services/export/manifest.py, backend/app/services/export/markdown.py, backend/app/services/export/html.py, backend/app/services/export/epub.py, backend/app/api/export.py, backend/tests/integration/export/test_parity.py, frontend/e2e/export.spec.ts</files>
  <action>实现 `html.py`、Markdown 与 `epub.py` adapters，三者只消费同一份 frozen manifest；FastAPI exporter 使用固定 EPUB3 packaging，按标准库/现有能力生成 zip/XML 并检查 mimetype、META-INF/container.xml、OPF manifest/spine、HTML image references、manifest parity 和缺失资产报告；不新增生产依赖、不从 DB 独立读取。参考 analog `.planning/phases/34-illustration-anchor-export/34-PATTERNS.md` 的 frozen manifest、deterministic adapter 和 no-independent-DB-read 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/export -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/export -q</automated><acceptance>文本、插图 revision、caption、citation、hash 与 manifest parity；缺 asset 有报告/placeholder。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/export -q 通过；HTML/EPUB3 同一 frozen manifest 的 export parity、EPUB3 ZIP/XML/resources 与缺失资源报告全部满足；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/34-illustration-anchor-export/34-VALIDATION.md; backend/app/services/export/manifest.py; backend/app/services/export/markdown.py; backend/app/services/export/html.py; backend/app/services/export/epub.py; backend/app/api/export.py; backend/tests/integration/export/test_parity.py; frontend/e2e/export.spec.ts</read_first>
  <files>backend/app/services/export/manifest.py, backend/app/services/export/markdown.py, backend/app/services/export/html.py, backend/app/services/export/epub.py, backend/app/api/export.py, backend/tests/integration/export/test_parity.py, frontend/e2e/export.spec.ts</files>
  <action>先执行 frontend Vitest/contract smoke，失败立即停止 E2E；再执行 Phase 34 unit/integration/frontend/browser validation 与 export-golden inspection；自动解包 EPUB 并检查固定 EPUB3 packaging、mimetype/container.xml/OPF manifest-spine、HTML/EPUB manifest parity、source/version/hash/caption/citation parity，修复 hash mismatch、missing asset、responsive overlap 后确认。参考 analog `.planning/phases/34-illustration-anchor-export/34-PATTERNS.md` 的 frozen manifest、reader block 和 export parity 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/illustration_anchors tests/unit/export tests/integration/illustration_anchors tests/integration/export -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/illustration_anchors tests/unit/export tests/integration/illustration_anchors tests/integration/export -q && cd ../frontend && npm test -- export && npm run test:e2e -- export --project=chromium-desktop --project=chromium-mobile-390</automated><acceptance>backend export parity 通过后，frontend Vitest/contract smoke 失败时 E2E 不启动；approved illustration 经稳定 anchor 进入 reader/export，Phase 22 状态保持不变。</acceptance></verify>
  <done>完成 Task 3：先执行并通过 frontend Vitest/contract smoke，再执行 backend export tests 与 E2E；export parity、HTML/EPUB3 ZIP/XML/resources、source/version/hash/caption/citation parity 和 responsive overlap 验证通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>
<tasks>
<task type="auto"><name>Test, Fix, and Confirm: export parity and package gate</name><read_first>backend/tests/integration/export/test_parity.py; frontend/src/components/export/export.test.tsx; frontend/e2e/export.spec.ts; backend/app/services/export/html.py; backend/app/services/export/epub.py</read_first><files>frontend/src/components/export/export.test.tsx; frontend/e2e/export.spec.ts</files><action>新增 Vitest export smoke，并以 integration test 检查同一 frozen manifest 驱动 Markdown/HTML/EPUB3；检查 ZIP mimetype、container.xml、package.opf、资源 hash 与 citation/anchor parity，最后运行 Playwright。</action><acceptance_criteria>Vitest 能发现 export.test.tsx，EPUB3 包结构和三种导出 parity 可自动检查，Playwright 覆盖下载与错误状态。</acceptance_criteria><verify><automated>Push-Location backend; pytest tests/integration/export/test_parity.py -q; $code=$LASTEXITCODE; Pop-Location; if ($code -ne 0) { exit $code }; Push-Location frontend; npm test -- export; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx playwright test e2e/export.spec.ts; $code=$LASTEXITCODE; Pop-Location; exit $code</automated></verify><done>Markdown/HTML/EPUB3 parity、ZIP/XML/resources、Vitest smoke 与浏览器下载路径全部通过。</done></task>
</tasks>

## Must-Haves

<must_haves>

- 批准插图通过 hash-verified anchor 在 reader 与导出中保持一致，失效和缺失显式呈现。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/34-illustration-anchor-export/34-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-05 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
