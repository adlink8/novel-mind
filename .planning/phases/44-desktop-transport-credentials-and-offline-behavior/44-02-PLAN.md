---
phase: 44-desktop-transport-credentials-and-offline-behavior
plan: "02"
type: execute
wave: 1
depends_on: ["44-01"]
files_modified:
  - desktop/src/security/credential-store.ts
  - desktop/src/security/local-auth.ts
  - desktop/src/shared/credential-status.ts
  - desktop/tests/security/credential-store.test.ts
  - backend/app/middleware/desktop_local_auth.py
  - backend/app/config.py
  - backend/tests/security/test_desktop_local_auth.py
  - agent-service/src/middleware/desktop-local-auth.ts
  - agent-service/tests/desktop-local-auth.test.ts
autonomous: true
requirements: [REQ-DESK-06]
must_haves:
  truths:
    - "D-44-03/D-44-04: credentials are main/OS protected and local auth never falls back open."
  artifacts:
    - path: desktop/src/security/credential-store.ts
      provides: async safeStorage-backed store with unavailable/rotation states
  key_links:
    - from: desktop/src/security/local-auth.ts
      to: backend/app/middleware/desktop_local_auth.py
      via: audience/expiry-bound loopback token
---

<objective>把凭据移出 renderer，并为本地 FastAPI/Agent Service 建立 fail-closed 会话认证。</objective>

<execution_context>@desktop/src/runtime/bootstrap.ts
@desktop/src/main/ipc/register.ts
@backend/app/config.py
@agent-service/src
@.planning/phases/44-desktop-transport-credentials-and-offline-behavior/44-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Implement OS-protected credential storage and redacted status</name>
<read_first>desktop/src/shared/bridge-contract.ts; desktop/src/data/app-data-layout.ts; .planning/phases/44-desktop-transport-credentials-and-offline-behavior/44-RESEARCH.md</read_first>
<files>desktop/src/security/credential-store.ts; desktop/src/shared/credential-status.ts; desktop/tests/security/credential-store.test.ts</files>
<action>Use Electron asynchronous `safeStorage` in main after app ready, store encrypted blobs only under the app-data secrets root, distinguish unavailable/decrypt-failed/rotation-needed, re-encrypt on rotation, and expose only redacted provider/local-auth status. Never use plaintext fallback, renderer localStorage or logs.</action>
<acceptance_criteria>Available/encrypt/decrypt/rotation/unavailable/corrupt cases are tested; disk/log scan finds no plaintext fixture secret; renderer receives status only.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/security/credential-store.test.ts</automated></verify>
<done>Credentials remain main-owned and OS-protected with honest failure states.</done>
</task>
<task type="auto">
<name>Task 2: Require audience- and expiry-bound local service authentication</name>
<read_first>desktop/src/security/credential-store.ts; desktop/src/runtime/bootstrap.ts; backend/app/config.py; backend/app/main.py; agent-service/src</read_first>
<files>desktop/src/security/local-auth.ts; backend/app/middleware/desktop_local_auth.py; backend/app/config.py; backend/tests/security/test_desktop_local_auth.py; agent-service/src/middleware/desktop-local-auth.ts; agent-service/tests/desktop-local-auth.test.ts</files>
<action>First preserve dirty `backend/app/config.py` changes. Mint separate short-lived audience tokens for backend and Agent Service, inject them only into owned process environments and authenticated client headers, validate audience/expiry/session/loopback source, rotate on restart and fail closed on missing/invalid material. Keep browser dev auth behavior explicitly configured, never implicit bypass.</action>
<acceptance_criteria>Valid session succeeds; missing, wrong audience, expired, old-restart and external-source tokens reject; no environment/token appears in renderer payload/logs.</acceptance_criteria>
<verify><automated>cd backend; python -m pytest tests/security/test_desktop_local_auth.py -q; cd ../agent-service; npx vitest run tests/desktop-local-auth.test.ts</automated></verify>
<done>Local services authenticate the desktop session without renderer-held secrets.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/security/credential-store.test.ts; backend/tests/security/test_desktop_local_auth.py; agent-service/tests/desktop-local-auth.test.ts</read_first>
<files>desktop/src/security/credential-store.ts; desktop/src/security/local-auth.ts; backend/app/middleware/desktop_local_auth.py; backend/app/config.py; agent-service/src/middleware/desktop-local-auth.ts</files>
<action>Run all three suites twice, rotate/restart during active requests, corrupt encrypted storage and scan logs/build artifacts for fixture secrets. Fix any fallback-open or stale-token acceptance.</action>
<acceptance_criteria>Suites pass twice, all stale/corrupt/unavailable paths fail closed, and secret scan is empty.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/security/credential-store.test.ts; cd ../backend; python -m pytest tests/security/test_desktop_local_auth.py -q; cd ../agent-service; npx vitest run tests/desktop-local-auth.test.ts</automated></verify>
<done>Credential and local-auth boundaries are fail-closed and redacted.</done>
</task>
</tasks>

## Must-Haves

- safeStorage guarantee is described accurately as Windows user-bound DPAPI protection.
- Missing/unavailable credentials never disable local authentication.

## Verification

Run credential, backend auth and Agent Service auth positive/negative suites plus secret scan.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| OS credential store -> local services | Stored/provider tokens and local session tokens have different audiences |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-44-02-01 | Information Disclosure | credential store/logs | mitigate | safeStorage, redaction and no renderer value |
| T-44-02-02 | Spoofing | local auth | mitigate | audience/session/expiry/loopback validation |
| T-44-02-03 | Replay | restarted session | mitigate | rotate and reject prior session token |
</threat_model>

