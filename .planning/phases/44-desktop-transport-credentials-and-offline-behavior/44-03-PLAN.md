---
phase: 44-desktop-transport-credentials-and-offline-behavior
plan: "03"
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - frontend/src/lib/sse.ts
  - frontend/src/lib/runtime/capability-status.ts
  - frontend/src/components/desktop/ProviderCapabilityGate.tsx
  - frontend/src/lib/runtime/__tests__/capability-status.test.ts
  - desktop/tests/integration/sse-recovery.spec.ts
  - desktop/tests/integration/offline-workflows.spec.ts
autonomous: true
requirements: [REQ-DESK-06, REQ-DESK-08]
must_haves:
  truths:
    - "D-44-05/D-44-06/D-44-07/D-44-08: SSE terminals and per-capability offline authority remain honest."
  artifacts:
    - path: desktop/tests/integration/offline-workflows.spec.ts
      provides: offline local-pass/provider-block proof
  key_links:
    - from: frontend/src/lib/runtime/capability-status.ts
      to: frontend/src/components/desktop/ProviderCapabilityGate.tsx
      via: typed per-capability availability
---

<objective>保持 SSE 失败/取消语义，并证明断网时本地工作流可用、provider 功能诚实阻塞。</objective>

<execution_context>@frontend/src/lib/sse.ts
@frontend/src/lib/runtime/endpoint-resolver.ts
@backend/app
@agent-service/src
@.planning/phases/44-desktop-transport-credentials-and-offline-behavior/44-CONTEXT.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Preserve SSE terminal state across reconnect and cancellation</name>
<read_first>frontend/src/lib/sse.ts; frontend/src/lib/api/client.ts; frontend/src/lib/runtime/endpoint-resolver.ts; agent-service/src</read_first>
<files>frontend/src/lib/sse.ts; desktop/tests/integration/sse-recovery.spec.ts</files>
<action>Carry session/auth bootstrap into existing SSE connection, preserve event IDs and authoritative `completed|failed|cancelled` terminals, bound reconnect attempts, prevent duplicate materialization on replay, and invalidate connections on runtime restart/token rotation. Do not translate timeout/disconnect into success.</action>
<acceptance_criteria>Disconnect/reconnect resumes once, cancellation stays cancelled, failed stays failed, duplicate event IDs do not duplicate side effects and rotated token forces clean re-bootstrap.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/integration/sse-recovery.spec.ts</automated></verify>
<done>SSE behavior remains authoritative and replay-safe in desktop runtime.</done>
</task>
<task type="auto">
<name>Task 2: Implement per-capability offline/provider states</name>
<read_first>frontend/src/lib/runtime/endpoint-resolver.ts; frontend/src/app; backend/app/config.py; desktop/src/shared/credential-status.ts</read_first>
<files>frontend/src/lib/runtime/capability-status.ts; frontend/src/components/desktop/ProviderCapabilityGate.tsx; frontend/src/lib/runtime/__tests__/capability-status.test.ts; desktop/tests/integration/offline-workflows.spec.ts</files>
<action>Compute typed availability from local runtime readiness, provider configuration and last request result, not one global online flag. Permit library/reader/editor/local data while network is disabled; render provider generation/embedding/image operations as unavailable/blocked/misconfigured with no empty result or fabricated artifact.</action>
<acceptance_criteria>Offline E2E completes representative reading/editing/local-data flows and blocks each provider-dependent action with explicit reason and zero authoritative write.</acceptance_criteria>
<verify><automated>cd frontend; npm test -- --run src/lib/runtime/__tests__/capability-status.test.ts; cd ../desktop; npx playwright test tests/integration/offline-workflows.spec.ts</automated></verify>
<done>Offline behavior is capability-specific and truthful.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/integration/sse-recovery.spec.ts; desktop/tests/integration/offline-workflows.spec.ts; frontend/src/lib/runtime/__tests__/capability-status.test.ts</read_first>
<files>frontend/src/lib/sse.ts; frontend/src/lib/runtime/capability-status.ts; frontend/src/components/desktop/ProviderCapabilityGate.tsx; desktop/tests/integration/sse-recovery.spec.ts; desktop/tests/integration/offline-workflows.spec.ts</files>
<action>Run SSE/offline suites twice with network disabled, provider missing, mid-stream disconnect, cancellation and runtime restart. Fix stale capability/terminal behavior and verify no provider failure becomes success.</action>
<acceptance_criteria>Suites pass twice with stable terminals, local workflows remain usable and all provider failures remain explicit blocked/unavailable/failed.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/integration/sse-recovery.spec.ts tests/integration/offline-workflows.spec.ts; cd ../frontend; npm test -- --run src/lib/runtime/__tests__/capability-status.test.ts</automated></verify>
<done>Phase 44 transport, credentials, streaming and offline behavior are verified.</done>
</task>
</tasks>

## Must-Haves

- No fabricated success after SSE/provider failure.
- Local reading/editing/data remains available offline.

## Verification

Run SSE replay/cancellation and offline capability E2E twice.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| provider/network outcome -> UI/domain status | Disconnects may be replayed or misreported |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-44-03-01 | Tampering | SSE replay | mitigate | event IDs and idempotent materialization |
| T-44-03-02 | Repudiation | terminal status | mitigate | preserve backend terminal evidence |
| T-44-03-03 | Spoofing | capability state | mitigate | derive from main/backend authority, not renderer guess |
</threat_model>

