---
phase: 44-desktop-transport-credentials-and-offline-behavior
plan: "01"
type: execute
wave: 0
depends_on: ["43-04"]
files_modified:
  - desktop/src/runtime/bootstrap.ts
  - desktop/src/shared/bootstrap-contract.ts
  - frontend/src/lib/runtime/endpoint-resolver.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/sse.ts
  - frontend/src/lib/runtime/__tests__/endpoint-resolver.test.ts
autonomous: true
requirements: [REQ-DESK-06]
must_haves:
  truths:
    - "D-44-01/D-44-02: runtime endpoints are session bootstrap data, never fixed build-time public env."
  artifacts:
    - path: frontend/src/lib/runtime/endpoint-resolver.ts
      provides: single logical-service endpoint seam
  key_links:
    - from: desktop/src/runtime/bootstrap.ts
      to: frontend/src/lib/runtime/endpoint-resolver.ts
      via: typed DesktopBridge bootstrap capability
---

<objective>建立动态 endpoint/bootstrap 合同并把前端 API/SSE 统一接到单一 resolver。</objective>

<execution_context>@desktop/src/runtime/process-graph.ts
@desktop/src/shared/bridge-contract.ts
@frontend/next.config.mjs
@frontend/src
@.planning/phases/44-desktop-transport-credentials-and-offline-behavior/44-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Produce a typed one-session bootstrap capability</name>
<read_first>desktop/src/runtime/desktop-runtime.ts; desktop/src/shared/bridge-contract.ts; desktop/src/main/ipc/register.ts</read_first>
<files>desktop/src/runtime/bootstrap.ts; desktop/src/shared/bootstrap-contract.ts</files>
<action>Create a main-owned bootstrap after runtime ready with logical `api`, `agent` and renderer origin handles, session ID, expiry and capability states. Return no provider key/raw env/process path. Validate loopback schemes/hosts and invalidate bootstrap on runtime restart/shutdown.</action>
<acceptance_criteria>Bootstrap cannot be created before ready, contains only approved loopback services, expires/invalidates deterministically and serializes no secret/provider key.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/bootstrap.test.ts</automated></verify>
<done>Renderer receives bounded runtime configuration only after readiness.</done>
</task>
<task type="auto">
<name>Task 2: Route frontend HTTP and SSE through one endpoint resolver</name>
<read_first>frontend/next.config.mjs; frontend/src/lib/api/client.ts; frontend/src/lib/sse.ts; frontend/src/lib/desktop/capabilities.ts; desktop/src/shared/bootstrap-contract.ts</read_first>
<files>frontend/src/lib/runtime/endpoint-resolver.ts; frontend/src/lib/api/client.ts; frontend/src/lib/sse.ts; frontend/src/lib/runtime/__tests__/endpoint-resolver.test.ts</files>
<action>First reconcile dirty `frontend/next.config.mjs` without overwriting it. Resolve logical service URLs from desktop bootstrap in Electron and existing relative rewrites in browser development. Remove production fixed-port assumptions and forbid secrets/endpoints in new `NEXT_PUBLIC_*` variables. Preserve existing request/SSE behavior.</action>
<acceptance_criteria>Desktop tests use allocated ports, browser tests use relative routes, stale/missing bootstrap fails with typed unavailable state, and static scan finds no production `127.0.0.1:8010/3100` outside dev config.</acceptance_criteria>
<verify><automated>cd frontend; npm test -- --run src/lib/runtime/__tests__/endpoint-resolver.test.ts; npm run typecheck</automated></verify>
<done>HTTP/SSE transport no longer embeds packaged ports or renderer secrets.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>frontend/src/lib/runtime/endpoint-resolver.ts; frontend/src/lib/api/client.ts; frontend/src/lib/sse.ts; desktop/src/runtime/bootstrap.ts</read_first>
<files>desktop/src/runtime/bootstrap.ts; desktop/src/shared/bootstrap-contract.ts; frontend/src/lib/runtime/endpoint-resolver.ts; frontend/src/lib/api/client.ts; frontend/src/lib/sse.ts; frontend/src/lib/runtime/__tests__/endpoint-resolver.test.ts</files>
<action>Run resolver/bootstrap tests twice with port changes, restart invalidation, malformed endpoint and browser fallback. Fix cached stale endpoint behavior and inspect built renderer for fixed ports/secrets.</action>
<acceptance_criteria>All cases pass twice and built client bundle contains neither fixed packaged endpoints nor auth/provider material.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/runtime/bootstrap.test.ts; cd ../frontend; npm test -- --run src/lib/runtime/__tests__/endpoint-resolver.test.ts; npm run build</automated></verify>
<done>Dynamic transport bootstrap is deterministic in desktop and compatible in browser dev.</done>
</task>
</tasks>

## Must-Haves

- No runtime endpoint/credential is frozen into a public build variable.
- Existing API/SSE semantics remain backend-owned.

## Verification

Run bootstrap/resolver tests, frontend build and bundle scan.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| main bootstrap -> renderer network client | Malicious endpoint data could redirect privileged local traffic |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-44-01-01 | Spoofing | endpoint resolver | mitigate | main-owned allowlisted loopback endpoints |
| T-44-01-02 | Information Disclosure | bootstrap payload | mitigate | no secrets/env and bounded schema |
</threat_model>

