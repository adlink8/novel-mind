---
phase: 30-visual-bible
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - backend/app/services/visual_bible/evidence.py
  - backend/app/services/visual_bible/authority.py
  - backend/app/api/visual_bible.py
  - backend/tests/integration/visual_bible/test_scope.py
autonomous: true
requirements: [REQ-VIS-01]
must_haves:
  truths:
    - "用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/visual_bible/evidence.py"
      provides: "实现实体/角色/地点/派系/物品/风格候选的证据物化、owner scope 和 candidate-only API。"
    - path: "backend/tests/integration/visual_bible/test_scope.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/visual_bible/evidence.py"
      to: "backend/app/services/visual_bible/authority.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/visual_bible/evidence.py"
      to: "Visual Bible → Scene Candidate"
      via: "versioned candidate artifact reference"

---

<objective>
实现实体/角色/地点/派系/物品/风格候选的证据物化、owner scope 和 candidate-only API。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-01 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/visual_bible/evidence.py, backend/app/services/visual_bible/authority.py, backend/app/api/visual_bible.py, backend/tests/integration/visual_bible/test_scope.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-visual-bible/30-CONTEXT.md
@.planning/phases/30-visual-bible/30-RESEARCH.md
@.planning/phases/30-visual-bible/30-PATTERNS.md
@.planning/phases/30-visual-bible/30-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/services/visual_bible/evidence.py, backend/app/services/visual_bible/authority.py, backend/app/api/visual_bible.py, backend/tests/integration/visual_bible/test_scope.py</files>
  <action>实现 `VisualBibleEvidenceService.materialize_claim` 与 `VisualBibleAuthorityService.create_revision`：重新读取 source snapshot leaf evidence，校验 hash/cutoff/owner/novel，生成 canonical content_hash 和不可变 candidate；无证据或冲突只返回 reason-coded unresolved。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 evidence materialization、authority boundary 和 candidate envelope 映射。按 D-30-01、D-30-02、D-30-03。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/visual_bible/test_scope.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/visual_bible/test_scope.py -q</automated><acceptance>跨 owner/version、错 hash、spoiler cutoff 和无证据 canon claim 均 fail closed；source 不被改写。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/services/visual_bible/evidence.py, backend/app/services/visual_bible/authority.py, backend/app/api/visual_bible.py, backend/tests/integration/visual_bible/test_scope.py</files>
  <action>实现 `/api/novels/{novel_id}/visual-bible` 的只读列表/详情和显式 candidate revision 创建路由，统一使用 `require_owned_novel` 与 typed envelope；响应暴露 authority、evidence、review_status、rights/provenance，不暴露未授权版本。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 owned API、typed envelope 和 review action 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/visual_bible -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/visual_bible -q</automated><acceptance>owner 隔离、版本过滤、candidate-only 与 evidence refs 在 API 层可重放。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/30-visual-bible/30-VALIDATION.md; backend/app/services/visual_bible/evidence.py; backend/app/services/visual_bible/authority.py; backend/app/api/visual_bible.py; backend/tests/integration/visual_bible/test_scope.py</read_first>
  <files>backend/app/services/visual_bible/evidence.py, backend/app/services/visual_bible/authority.py, backend/app/api/visual_bible.py, backend/tests/integration/visual_bible/test_scope.py</files>
  <action>运行 scope、evidence 重验、spoiler 和幂等 fixture；对失败路径修复后执行相关 unit/integration 测试。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 source snapshot/hash recheck、candidate-only 和 append-only review 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q</automated><acceptance>所有 gate 通过，重复创建不产生隐性 active pointer，Phase 30 仍不调用 image provider。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- 用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/30-visual-bible/30-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-01 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
