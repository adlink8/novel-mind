---
phase: 30-visual-bible
plan: 04
type: execute
wave: 3
depends_on: [30-02, 30-03]
files_modified:
  - backend/app/services/visual_bible/review.py
  - backend/app/api/visual_bible.py
  - frontend/src/components/visual-bible/review-actions.tsx
  - backend/tests/unit/visual_bible/test_review_gates.py
  - backend/tests/integration/visual_bible/test_review.py
autonomous: true
requirements: [REQ-VIS-01]
must_haves:
  truths:
    - "用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/services/visual_bible/review.py"
      provides: "完成 Visual Bible review/versioning envelope，并冻结后续 Scene Candidate 可消费的 approved candidate revision。"
    - path: "backend/tests/integration/visual_bible/test_review.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/services/visual_bible/review.py"
      to: "backend/app/api/visual_bible.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/services/visual_bible/review.py"
      to: "Visual Bible → Scene Candidate"
      via: "versioned candidate artifact reference"

---

<objective>
完成 Visual Bible review/versioning envelope，并冻结后续 Scene Candidate 可消费的 approved candidate revision。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-01 交付可执行的、证据可重放的版本化切片。
Output: backend/app/services/visual_bible/review.py, backend/app/api/visual_bible.py, frontend/src/components/visual-bible/review-actions.tsx, backend/tests/unit/visual_bible/test_review_gates.py, backend/tests/integration/visual_bible/test_review.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-visual-bible/30-CONTEXT.md
@.planning/phases/30-visual-bible/30-RESEARCH.md
@.planning/phases/30-visual-bible/30-PATTERNS.md
@.planning/phases/30-visual-bible/30-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/services/visual_bible/review.py, backend/app/api/visual_bible.py, frontend/src/components/visual-bible/review-actions.tsx, backend/tests/unit/visual_bible/test_review_gates.py, backend/tests/integration/visual_bible/test_review.py</files>
  <action>实现 `VisualBibleReviewService.append_event` 与合法 transition `approve|reject|edit|supersede|needs_relink`；事件 append-only，服务端检查 evidence/rights/owner/version，approval 只改变 candidate review projection，不改变 Original Canon/active pointer。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 append-only review、candidate revision 和 explicit review boundary 映射。按 D-30-01、D-30-04。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/visual_bible/test_review_gates.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/visual_bible/test_review_gates.py -q</automated><acceptance>重复 action 幂等；无证据或 rights unresolved 不可 approve；旧 revision 永久可读。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/services/visual_bible/review.py, backend/app/api/visual_bible.py, frontend/src/components/visual-bible/review-actions.tsx, backend/tests/unit/visual_bible/test_review_gates.py, backend/tests/integration/visual_bible/test_review.py</files>
  <action>实现 review API/UI 与 `VisualBibleReviewEnvelope`，提供历史事件、reason code、父 revision 和供 Phase 31/32 使用的 immutable revision ref；不得引入 provider 调用或 Phase 35 authority namespace。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 review envelope、owner-scoped API 和 immutable lineage 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/integration/visual_bible/test_review.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/integration/visual_bible/test_review.py -q</automated><acceptance>review action 可审计、candidate-only，后续链路得到稳定 version/hash/evidence 契约。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; pytest tests/integration/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/30-visual-bible/30-VALIDATION.md; backend/app/services/visual_bible/review.py; backend/app/api/visual_bible.py; frontend/src/components/visual-bible/review-actions.tsx; backend/tests/unit/visual_bible/test_review_gates.py; backend/tests/integration/visual_bible/test_review.py</read_first>
  <files>backend/app/services/visual_bible/review.py, backend/app/api/visual_bible.py, frontend/src/components/visual-bible/review-actions.tsx, backend/tests/unit/visual_bible/test_review_gates.py, backend/tests/integration/visual_bible/test_review.py</files>
  <action>执行 Phase 30 全套 unit/integration/frontend/browser validation，检查 fixture 中角色/地点/物品/派系/style 与 interpretation 分离，修复并确认。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 artifact flow、authority labels 和 review fixture 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q</automated><acceptance>REQ-VIS-01 全部可验证，Visual Bible revision 可被后续 Scene Candidate 引用。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- 用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/30-visual-bible/30-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-01 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
