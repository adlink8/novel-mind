---
phase: 30-visual-bible
plan: 01
type: execute
wave: 1
depends_on: [29-04]
files_modified:
  - backend/app/models/visual_bible.py
  - backend/app/schemas/visual_bible.py
  - backend/migrations/versions/20260801_visual_bible.py
  - backend/tests/unit/visual_bible/test_contracts.py
autonomous: true
requirements: [REQ-VIS-01]
must_haves:
  truths:
    - "用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/models/visual_bible.py"
      provides: "定义 Visual Bible candidate Artifact 的严格契约、版本和证据模型。"
    - path: "backend/tests/unit/visual_bible/test_contracts.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/models/visual_bible.py"
      to: "backend/app/schemas/visual_bible.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/models/visual_bible.py"
      to: "Visual Bible → Scene Candidate"
      via: "versioned candidate artifact reference"

---

<objective>
定义 Visual Bible candidate Artifact 的严格契约、版本和证据模型。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-01 交付可执行的、证据可重放的版本化切片。
Output: backend/app/models/visual_bible.py, backend/app/schemas/visual_bible.py, backend/migrations/versions/20260801_visual_bible.py, backend/tests/unit/visual_bible/test_contracts.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-visual-bible/30-CONTEXT.md
@.planning/phases/30-visual-bible/30-RESEARCH.md
@.planning/phases/30-visual-bible/30-PATTERNS.md
@.planning/phases/30-visual-bible/30-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/models/visual_bible.py, backend/app/schemas/visual_bible.py, backend/migrations/versions/20260801_visual_bible.py, backend/tests/unit/visual_bible/test_contracts.py</files>
  <action>执行 Phase 22 执行资格只读 preflight：读取 `.planning/STATE.md` 与 `.planning/phases/22-ci-nightly-gap-closure/22-G2-PLAN.md`，确认 Phase 22 仍为 blocked/deferred、不得声称 3/3 Nightly 通过，且不修改 Phase 22 文件。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 evidence/review/owner-scope 映射。建立 `VisualBibleVersion`、`VisualEntity`、`VisualClaim`、`VisualEvidenceRef`、`VisualReferenceAsset` 的 SQLAlchemy/Pydantic 契约；authority 仅允许四类标签，所有 canon_fact 必须带 source_snapshot、chapter/range、offset/hash/cutoff；stable ID 按 owner_id+novel_id+version_id 作用域，禁止复用 Novel.cover_url。按 D-30-02、D-30-03 实现。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/visual_bible/test_contracts.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/visual_bible/test_contracts.py -q</automated><acceptance>契约拒绝 extra 字段、越界 offset、缺证据 canon_fact、重复 stable ID；candidate 版本含 parent/hash/schema/policy lineage。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/30-visual-bible/30-CONTEXT.md; .planning/phases/30-visual-bible/30-RESEARCH.md; .planning/phases/30-visual-bible/30-PATTERNS.md</read_first>
  <files>backend/app/models/visual_bible.py, backend/app/schemas/visual_bible.py, backend/migrations/versions/20260801_visual_bible.py, backend/tests/unit/visual_bible/test_contracts.py</files>
  <action>创建 Alembic migration，新增版本、实体、claim、证据、style/constraint、reference asset 表及复合 owner/novel/version 约束；确保向下迁移不删除既有 cover/upload 数据。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 versioned schema、scoped lineage 和 migration 映射。</action>
  <acceptance_criteria>`cd backend; alembic check`</acceptance_criteria>
<verify><automated>cd backend; alembic check</automated><acceptance>migration 可升级/降级，metadata 与约束覆盖 owner、novel、source snapshot、content hash。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; alembic upgrade head; alembic downgrade -1 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/30-visual-bible/30-VALIDATION.md; backend/app/models/visual_bible.py; backend/app/schemas/visual_bible.py; backend/migrations/versions/20260801_visual_bible.py; backend/tests/unit/visual_bible/test_contracts.py</read_first>
  <files>backend/app/models/visual_bible.py, backend/app/schemas/visual_bible.py, backend/migrations/versions/20260801_visual_bible.py, backend/tests/unit/visual_bible/test_contracts.py</files>
  <action>按 validation fixture 补齐 `vb-basic-v1`、`vb-interpretation-v1`、`vb-invalid`，运行契约与 migration 测试，修复失败并确认 Phase 22 状态未被触碰。参考 analog `.planning/phases/30-visual-bible/30-PATTERNS.md` 的 strict contract、candidate envelope 和 evidence flow 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/visual_bible/test_contracts.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/visual_bible/test_contracts.py -q</automated><acceptance>严格 schema、hash、authority、版本 lineage 和 migration 测试全部通过。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/visual_bible tests/integration/visual_bible -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- 用户可查看带证据和 authority 标签的 Visual Bible candidate，并显式 review 版本。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/30-visual-bible/30-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-01 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
