---
phase: 31-key-scene-detection
plan: 01
type: execute
wave: 1
depends_on: [30-05]
files_modified:
  - backend/app/models/key_scene.py
  - backend/app/schemas/key_scene.py
  - backend/migrations/versions/20260801_key_scene.py
  - backend/app/services/key_scenes/boundaries.py
  - backend/tests/unit/key_scenes/test_contracts.py
autonomous: true
requirements: [REQ-VIS-02]
must_haves:
  truths:
    - "用户可查看有证据范围、salience/diversity 理由且防剧透的 key-scene candidate set。"
    - "owner_id、novel_id、版本、source snapshot、evidence/hash 与 spoiler cutoff 在所有适用边界生效。"
    - "candidate-only、approval、budget、rights/provenance 和 schema migration 门按本计划范围可验证。"
  artifacts:
    - path: "backend/app/models/key_scene.py"
      provides: "定义证据优先的 SceneCandidate/SceneCandidateSet 契约与持久化边界。"
    - path: "backend/tests/unit/key_scenes/test_contracts.py"
      provides: "本计划的自动化验证与回归证据"
  key_links:
    - from: "backend/app/models/key_scene.py"
      to: "backend/app/schemas/key_scene.py"
      via: "strict typed contract, immutable lineage and server-side gate"
    - from: "backend/app/models/key_scene.py"
      to: "Scene Candidate → Scene Spec"
      via: "versioned candidate artifact reference"

---

<objective>
定义证据优先的 SceneCandidate/SceneCandidateSet 契约与持久化边界。

Purpose: 以当前 phase 的锁定决策和 REQ REQ-VIS-02 交付可执行的、证据可重放的版本化切片。
Output: backend/app/models/key_scene.py, backend/app/schemas/key_scene.py, backend/migrations/versions/20260801_key_scene.py, backend/app/services/key_scenes/boundaries.py, backend/tests/unit/key_scenes/test_contracts.py
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-key-scene-detection/31-CONTEXT.md
@.planning/phases/31-key-scene-detection/31-RESEARCH.md
@.planning/phases/31-key-scene-detection/31-PATTERNS.md
@.planning/phases/31-key-scene-detection/31-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</execution_context>

## Steps

按任务依赖执行；不得修改本计划目录之外的规划状态文件。实现必须保持 Visual Bible→Scene Candidate→Scene Spec→PromptArtifact→IllustrationRevision→IllustrationAnchor 版本化链路；每个 task 的 read_first 是执行前置上下文。

<tasks>
<task type="auto">
  <name>Task 1: 定义/建立契约与迁移</name>
  <read_first>.planning/phases/31-key-scene-detection/31-CONTEXT.md; .planning/phases/31-key-scene-detection/31-RESEARCH.md; .planning/phases/31-key-scene-detection/31-PATTERNS.md</read_first>
  <files>backend/app/models/key_scene.py, backend/app/schemas/key_scene.py, backend/migrations/versions/20260801_key_scene.py, backend/app/services/key_scenes/boundaries.py, backend/tests/unit/key_scenes/test_contracts.py</files>
  <action>执行 Phase 22 执行资格只读 preflight：读取 `.planning/STATE.md` 与 `.planning/phases/22-ci-nightly-gap-closure/22-G2-PLAN.md`，确认 Phase 22 仍为 blocked/deferred、不得声称 3/3 Nightly 通过，且不修改 Phase 22 文件。参考 analog `.planning/phases/31-key-scene-detection/31-PATTERNS.md` 的 evidence-range、candidate review 和 deterministic ranking 映射。创建 `SceneCandidate`、`SceneCandidateSet`、`SceneEvidenceRange`、`SceneReviewDecision` 契约和 ORM；字段必须包括 chapter/range/source_hash、cast/place/time/POV、spoiler_cutoff、salience_reasons、diversity_key、detector/policy lineage，以及 `approved_visual_bible_revision_id`、`approved_visual_bible_revision_hash`、Visual Bible owner/version/approval verification fields；创建和冻结前必须验证这些 revision 的 owner、version、approved 状态与 hash 一致。按 D-31-01、D-31-02。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/key_scenes/test_contracts.py -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/key_scenes/test_contracts.py -q</automated><acceptance>strict schema 拒绝 unsupported coordinates、spoiler range 和缺 source hash；候选不是 canon。</acceptance></verify>
  <done>完成 Task 1：执行命令 cd backend; pytest tests/unit/key_scenes -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Task 2: 实现核心服务与 API/工作流</name>
  <read_first>.planning/phases/31-key-scene-detection/31-CONTEXT.md; .planning/phases/31-key-scene-detection/31-RESEARCH.md; .planning/phases/31-key-scene-detection/31-PATTERNS.md</read_first>
  <files>backend/app/models/key_scene.py, backend/app/schemas/key_scene.py, backend/migrations/versions/20260801_key_scene.py, backend/app/services/key_scenes/boundaries.py, backend/tests/unit/key_scenes/test_contracts.py</files>
  <action>实现 `SceneBoundaryService`，优先复用 chapter/scene/evidence hierarchy，保留 malformed/ambiguous reason code；新增 Alembic migration 与复合 owner/novel/source-snapshot/version 约束。参考 analog `.planning/phases/31-key-scene-detection/31-PATTERNS.md` 的 persisted boundaries、source snapshot 和 malformed-state 映射。</action>
  <acceptance_criteria>`cd backend; alembic check`</acceptance_criteria>
<verify><automated>cd backend; alembic check</automated><acceptance>边界可重放、迁移可升级/降级，source text 和 active reader state 不被改变。</acceptance></verify>
  <done>完成 Task 2：执行命令 cd backend; alembic upgrade head; alembic downgrade -1 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>

<task type="auto">
  <name>Test, Fix, and Confirm</name>
  <read_first>.planning/phases/31-key-scene-detection/31-VALIDATION.md; backend/app/models/key_scene.py; backend/app/schemas/key_scene.py; backend/migrations/versions/20260801_key_scene.py; backend/app/services/key_scenes/boundaries.py; backend/tests/unit/key_scenes/test_contracts.py</read_first>
  <files>backend/app/models/key_scene.py, backend/app/schemas/key_scene.py, backend/migrations/versions/20260801_key_scene.py, backend/app/services/key_scenes/boundaries.py, backend/tests/unit/key_scenes/test_contracts.py</files>
  <action>运行 action/quiet/ambiguous/duplicate/spoiler fixtures，修复契约与边界问题。参考 analog `.planning/phases/31-key-scene-detection/31-PATTERNS.md` 的 evidence range、candidate artifact 和 reason-code 映射。</action>
  <acceptance_criteria>`cd backend; pytest tests/unit/key_scenes -q`</acceptance_criteria>
<verify><automated>cd backend; pytest tests/unit/key_scenes -q</automated><acceptance>所有候选均可定位到证据范围并保留 detector lineage。</acceptance></verify>
  <done>完成 Task 3：执行命令 cd backend; pytest tests/unit/key_scenes tests/integration/key_scenes -q 通过；原行为验收断言全部满足；相关契约、实现和测试文件存在并可重放。</done>
</task>
</tasks>

## Must-Haves

<must_haves>

- 用户可查看有证据范围、salience/diversity 理由且防剧透的 key-scene candidate set。
- 所有跨用户/小说/版本访问使用服务端 owner scope；所有 source-linked claim/detail/asset/anchor 重新验证 evidence、offset/range、hash、cutoff。
- schema 变化必须通过 Alembic migration；禁止用 cover_url、DOM index、embedding-only ranking、空成功 asset 或独立 DB export 读取替代本计划契约。
- approval/review 是 append-only、显式且幂等；任何未批准或 unresolved candidate 不得进入下游 active reader/export。
- budget/cost/usage、rights/provenance、failure/unknown/partial 状态适用时必须显式持久化，禁止静默降级。
</must_haves>

## Verification

- 定向自动化命令必须按每个 task 的 <verify> 执行；失败时先判断是否由本切片引起，修复后重跑原命令。
- phase validation 入口：.planning/phases/31-key-scene-detection/31-VALIDATION.md；不得生成 VERIFICATION.md 或 SUMMARY.md。
- 浏览器/人工检查仅作为自动化之后的补充，不得替代 owner/spoiler/evidence/hash/approval/budget 门。
- Phase 22 仍保持 0/3 blocked/accepted risk；本计划不得声称其 qualification 已完成。

<success_criteria>
REQ REQ-VIS-02 的 roadmap goal、CONTEXT 锁定决策以及 RESEARCH/PATTERNS/VALIDATION 中适用于本 plan 的约束均有任务与自动化验收；输出可被下一 phase 直接消费，且无 active-pointer cutover。
</success_criteria>
