---
phase: 45-windows-packaging-migration-and-desktop-qualification
plan: "02"
type: execute
wave: 1
depends_on: ["45-01"]
files_modified:
  - desktop/src/update/upgrade-coordinator.ts
  - desktop/src/update/uninstall-policy.ts
  - desktop/scripts/create-upgrade-fixture.ps1
  - desktop/tests/update/upgrade-preservation.spec.ts
  - desktop/tests/update/upgrade-recovery.spec.ts
  - desktop/tests/update/uninstall-preservation.spec.ts
  - docs/desktop-upgrade-recovery.md
autonomous: true
requirements: [REQ-DESK-09, REQ-DESK-10]
must_haves:
  truths:
    - "D-45-04/D-45-05: upgrade is backup-first/reversible and uninstall preserves user data by default."
  artifacts:
    - path: desktop/tests/update/upgrade-recovery.spec.ts
      provides: intentional failure and rollback evidence
  key_links:
    - from: desktop/src/update/upgrade-coordinator.ts
      to: desktop/src/data/migration-runner.ts
      via: versioned backup-first migration gate
---

<objective>实现并验证版本升级、失败恢复和默认保留数据的卸载策略。</objective>

<execution_context>@desktop/src/data/migration-runner.ts
@desktop/src/data/backup.ts
@desktop/electron-builder.yml
@docs/desktop-data-lifecycle.md</execution_context>

## Steps

<tasks>
<task type="auto">
<name>Task 1: Coordinate versioned backup-first upgrade</name>
<read_first>desktop/src/data/migration-runner.ts; desktop/src/data/backup.ts; desktop/src/data/version-state.ts; desktop/electron-builder.yml</read_first>
<files>desktop/src/update/upgrade-coordinator.ts; desktop/scripts/create-upgrade-fixture.ps1; desktop/tests/update/upgrade-preservation.spec.ts; docs/desktop-upgrade-recovery.md</files>
<action>Create a checksum-pinned prior desktop fixture with library/chapter/analysis/visual/derivative sample data, stop the owned runtime, verify backup capacity, run declared migrations, validate post-upgrade hashes/domain probes, and advance version only after success.</action>
<acceptance_criteria>Upgrade preserves fixture content/metadata/assets and advances one version; repeat invocation is idempotent and mismatched artifact/fixture hash rejects.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/update/upgrade-preservation.spec.ts</automated></verify>
<done>Compatible upgrade preserves real NovelMind data across versions.</done>
</task>
<task type="auto">
<name>Task 2: Prove failed-upgrade recovery and uninstall preservation</name>
<read_first>desktop/src/update/upgrade-coordinator.ts; desktop/src/data/migration-runner.ts; desktop/electron-builder.yml; docs/desktop-data-lifecycle.md</read_first>
<files>desktop/src/update/uninstall-policy.ts; desktop/tests/update/upgrade-recovery.spec.ts; desktop/tests/update/uninstall-preservation.spec.ts; docs/desktop-upgrade-recovery.md</files>
<action>Inject migration interruption/corruption/insufficient-space and verify the old version plus backup remains recoverable without ready state. Configure uninstall to remove binaries only and preserve `%APPDATA%/NovelMind` by default; expose data deletion only as a separate explicit labelled path with exact target validation.</action>
<acceptance_criteria>Failed upgrade restores/retains previous readable data; default uninstall/reinstall preserves hashes; explicit delete cannot target outside resolved appData and requires confirmation.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/update/upgrade-recovery.spec.ts tests/update/uninstall-preservation.spec.ts</automated></verify>
<done>Upgrade failure is reversible and uninstall does not silently delete user work.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/update/upgrade-preservation.spec.ts; desktop/tests/update/upgrade-recovery.spec.ts; desktop/tests/update/uninstall-preservation.spec.ts</read_first>
<files>desktop/src/update/upgrade-coordinator.ts; desktop/src/update/uninstall-policy.ts; desktop/tests/update/upgrade-preservation.spec.ts; desktop/tests/update/upgrade-recovery.spec.ts; desktop/tests/update/uninstall-preservation.spec.ts; docs/desktop-upgrade-recovery.md</files>
<action>Run successful/failed upgrade and uninstall/reinstall suites twice; compare pre/post hashes and docs; fix non-idempotent, out-of-root or false-ready behavior.</action>
<acceptance_criteria>Suites pass twice, all fixture hashes are accounted for, failure never advances ready/version and docs match behavior.</acceptance_criteria>
<verify><automated>cd desktop; npx playwright test tests/update</automated></verify>
<done>Upgrade, rollback and uninstall policies are qualified against preserved data.</done>
</task>
</tasks>

## Must-Haves

- Upgrade evidence uses a prior version fixture, not an empty database.
- Explicit data deletion is separate, confirmed and path-contained.

## Verification

Run complete update fixture suite twice and compare data hashes.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| old appData -> new migration | Crafted/corrupt data and deletion paths can cause loss or traversal |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-45-02-01 | Tampering | upgrade fixture/data | mitigate | checksums, backup and post-migration probes |
| T-45-02-02 | Elevation of Privilege | deletion path | mitigate | resolved appData containment and confirmation |
| T-45-02-03 | Repudiation | migration result | mitigate | manifest, version state and recovery log |
</threat_model>

