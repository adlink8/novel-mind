---
phase: 45-windows-packaging-migration-and-desktop-qualification
plan: "01"
type: execute
wave: 0
depends_on: ["41-03", "42-03", "43-04", "44-03"]
files_modified:
  - desktop/electron-builder.yml
  - desktop/scripts/stage-runtime.ps1
  - desktop/scripts/build-windows.ps1
  - desktop/src/main/single-instance.ts
  - desktop/tests/package/package-layout.test.ts
  - desktop/tests/package/process-behavior.windows.test.ts
  - docs/desktop-installation.md
autonomous: false
requirements: [REQ-DESK-09, REQ-DESK-10]
must_haves:
  truths:
    - "D-45-01/D-45-02/D-45-03: installer contains approved runtimes, is single-instance, console-free and keeps mutable data outside installation resources."
    - "Installer dependency/config selection must match the Phase 41 GO and remains subject to explicit dependency approval."
  artifacts:
    - path: desktop/electron-builder.yml
      provides: reproducible Windows packaging contract
    - path: desktop/tests/package/package-layout.test.ts
      provides: bundled resource and mutable-path audit
  key_links:
    - from: desktop/scripts/stage-runtime.ps1
      to: desktop/electron-builder.yml
      via: hashed approved runtime resource tree
---

<objective>基于 Phase 41 GO 生成 Windows 安装包构建链，保证依赖完整、单实例、无控制台与资源/数据隔离。</objective>

<execution_context>@.planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md
@desktop/proof/runtime-manifest.json
@desktop/src/runtime/packaged-process-adapter.ts
@desktop/src/data/app-data-layout.ts
@.planning/phases/45-windows-packaging-migration-and-desktop-qualification/45-CONTEXT.md
@.planning/phases/45-windows-packaging-migration-and-desktop-qualification/45-RESEARCH.md</execution_context>

## Steps

<tasks>
<task type="checkpoint:decision" gate="blocking">
<name>Task 1: Confirm final installer configuration and dependency approval</name>
<read_first>.planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md; desktop/proof/runtime-manifest.json; desktop/package.json</read_first>
<files>desktop/electron-builder.yml; docs/desktop-installation.md</files>
<action>Verify Phase 41 GO evidence still matches pinned runtime versions, then present the exact installer target, packaging dependencies, artifact format, supported Windows matrix and unsigned-test limitation for user approval. Do not add/purchase signing or publication services.</action>
<acceptance_criteria>The selected installer matches Phase 41 evidence and explicit approval is recorded before adding/changing production packaging dependencies.</acceptance_criteria>
<verify><automated>if (-not (Select-String -Path .planning/phases/41-electron-architecture-and-packaging-proof/41-DECISION.md -Pattern '^Verdict: GO$' -Quiet)) { exit 1 }</automated></verify>
<done>Final package configuration is approved and bounded to unsigned local qualification.</done>
</task>
<task type="auto">
<name>Task 2: Stage immutable runtimes and build a deterministic Windows artifact</name>
<read_first>desktop/proof/runtime-manifest.json; desktop/src/runtime/packaged-process-adapter.ts; desktop/src/data/app-data-layout.ts; desktop/package.json</read_first>
<files>desktop/electron-builder.yml; desktop/scripts/stage-runtime.ps1; desktop/scripts/build-windows.ps1; desktop/tests/package/package-layout.test.ts; docs/desktop-installation.md</files>
<action>Stage exactly the hash/version-pinned Next, FastAPI/Python, Agent Service, PostgreSQL/pgvector and vector runtime resources from the GO manifest. Configure Windows executable/installer, no child console windows, immutable resource paths and `%APPDATA%/NovelMind` mutable paths. Emit artifact checksum plus bundled-component/version/license inventory; reject downloads or PATH fallbacks during first run.</action>
<acceptance_criteria>Two builds from the same lock/source produce equivalent staged inventories and declared checksums; package audit finds all runtimes, no writable install target and no first-run download prerequisite.</acceptance_criteria>
<verify><automated>powershell -File desktop/scripts/build-windows.ps1 -Verify; cd desktop; npm test -- --run tests/package/package-layout.test.ts</automated></verify>
<done>The Windows artifact is self-contained and auditable.</done>
</task>
<task type="auto">
<name>Task 3: Enforce single-instance and clean process behavior</name>
<read_first>desktop/src/main/index.ts; desktop/src/runtime/process-owner.ts; desktop/electron-builder.yml</read_first>
<files>desktop/src/main/single-instance.ts; desktop/tests/package/process-behavior.windows.test.ts; docs/desktop-installation.md</files>
<action>Use the Electron single-instance lock, route second-launch intent to the existing window without starting another runtime graph, configure hidden child windows, and verify normal/forced app exit shuts down the owned tree while preserving unrelated processes.</action>
<acceptance_criteria>Second launch yields one UI/runtime instance, no child console window appears, and normal/forced exit leaves zero owned descendants.</acceptance_criteria>
<verify><automated>cd desktop; npm test -- --run tests/package/process-behavior.windows.test.ts</automated></verify>
<done>Packaged process behavior is user-safe and deterministic.</done>
</task>
<task type="auto">
<name>Test, Fix, and Confirm</name>
<read_first>desktop/tests/package/package-layout.test.ts; desktop/tests/package/process-behavior.windows.test.ts; desktop/scripts/build-windows.ps1</read_first>
<files>desktop/electron-builder.yml; desktop/scripts/stage-runtime.ps1; desktop/scripts/build-windows.ps1; desktop/src/main/single-instance.ts; desktop/tests/package/package-layout.test.ts; desktop/tests/package/process-behavior.windows.test.ts</files>
<action>Build twice, inspect unpacked resources and run process tests twice with PATH-cleared/user-runtime-disabled fixture. Fix nondeterminism, missing resources, console windows or orphan behavior.</action>
<acceptance_criteria>Both package builds and process suites pass; no Docker/system runtime is resolved and artifacts include checksums/inventory.</acceptance_criteria>
<verify><automated>powershell -File desktop/scripts/build-windows.ps1 -Verify; cd desktop; npm test -- --run tests/package/package-layout.test.ts tests/package/process-behavior.windows.test.ts</automated></verify>
<done>Phase 45 packaging baseline is reproducible, self-contained and process-clean.</done>
</task>
</tasks>

## Must-Haves

- No Docker or user runtime prerequisite.
- Signing/publication remains external and unclaimed.

## Verification

Build twice and run package-layout/single-instance/process-tree tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staged runtime inputs -> installer artifact | Substituted binaries or paths can compromise installed app |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-45-01-01 | Tampering | staged binaries | mitigate | pinned hashes and inventory verification |
| T-45-01-02 | Denial of Service | multi-instance runtime | mitigate | single-instance lock before runtime start |
| T-45-01-03 | Information Disclosure | child consoles/logs | mitigate | hidden windows and redacted logs |
</threat_model>

