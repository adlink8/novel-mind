---
phase: 10-reader-selection-ai-and-multi-session-conversations
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - backend/app/services/reader_chat/__init__.py
  - backend/app/services/reader_chat/conversations.py
  - backend/app/api/reader_chat.py
  - backend/app/main.py
  - backend/tests/integration/reader_chat/test_conversations_api.py
  - backend/tests/integration/reader_chat/test_owner_isolation.py
autonomous: true
requirements:
  - REQ-CHAT-02
  - REQ-CHAT-07
must_haves:
  truths:
    - "Users can create, rename, list, switch by reading, archive, restore and delete multiple conversations per owned novel."
    - "Messages are replayed in stable order and duplicate client sends do not create duplicate user messages or jobs."
    - "Cross-owner child IDs return 404 without leaking metadata."
  artifacts:
    - path: backend/app/api/reader_chat.py
      provides: owner-scoped conversation/message/job HTTP contracts
    - path: backend/app/services/reader_chat/conversations.py
      provides: transactional lifecycle, sequence and deletion operations
  key_links:
    - from: backend/app/api/reader_chat.py
      to: require_owned_novel
      via: every route starts from owned novel scope
      pattern: "Depends\(require_owned_novel\)"
    - from: backend/app/services/reader_chat/conversations.py
      to: reader_conversations.next_sequence
      via: row-locked message append
      pattern: "with_for_update"
---

<objective>
Expose a complete owner-scoped multi-conversation lifecycle and durable message submission/replay API.

Purpose: Deliver the non-AI persistence surface independently so Wave 3 can consume queued jobs without changing lifecycle semantics.
Output: Registered FastAPI router, transactional service and PostgreSQL API/IDOR tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-CONTEXT.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-SPEC.md
@backend/app/api/dependencies.py
@backend/app/api/novels.py
@backend/app/main.py
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-01-SUMMARY.md

<interfaces>
Consume `ReaderConversation`, `ReaderMessage`, `ReaderGenerationJob` and strict request/response schemas created by Plan 01. All child reads must include owner_id + novel_id + conversation_id; inaccessible IDs return 404.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify conversation lifecycle, concurrency and IDOR contracts</name>
  <files>backend/tests/integration/reader_chat/test_conversations_api.py, backend/tests/integration/reader_chat/test_owner_isolation.py</files>
  <behavior>
    - Multiple conversations per novel support create/list/rename/archive/restore/delete and metadata-only list responses.
    - Archived conversations are readable but reject new messages until restored.
    - Duplicate client message IDs return the original message/job; distinct concurrent sends receive unique monotonic sequences.
    - Deleting a conversation requests cancellation of active jobs and removes all private descendants atomically.
    - Cross-owner novel/conversation/message/job matrices return 404 with no body/count differences.
  </behavior>
  <action>Write PostgreSQL integration/API tests for D-02, D-08 and D-09. Include same-user wrong-novel IDs, second-user IDs, archived transitions, rename validation, list pagination, replay `after_sequence`, hard deletion, concurrent append and idempotent retry. Message creation should validate the request envelope and queue a durable job; context assembly is injected and may use a deterministic test builder until Plan 03 supplies production assembly, without weakening the committed manifest requirement.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/reader_chat/test_conversations_api.py tests/integration/reader_chat/test_owner_isolation.py -q</automated>
  </verify>
  <done>Tests fail only for absent service/router behavior and fully encode lifecycle and owner boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional conversation and message APIs</name>
  <files>backend/app/services/reader_chat/__init__.py, backend/app/services/reader_chat/conversations.py, backend/app/api/reader_chat.py, backend/app/main.py</files>
  <behavior>
    - API operations preserve stable sequence, immutable reply linkage and child scope.
    - POST message commits user message, supplied validated manifest graph and queued job in one transaction, returning 202.
  </behavior>
  <action>Implement the API contract from 10-RESEARCH.md under `/api/novels/{novel_id}/conversations`. Start every route with `require_owned_novel`; scope child resources through owner+novel+conversation and return 404 for inaccessible IDs. Use row locking for `next_sequence`, unique client idempotency handling and stable `after_sequence` replay. PATCH supports title and active/archive transitions only. DELETE first marks nonterminal jobs canceled then hard-deletes the conversation graph in one transaction per the agent discretion. Register the router in `main.py`. Per D-03/D-08, message submission accepts a production context-builder dependency and cannot commit a user message without its selection/manifest/evidence graph.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/reader_chat/test_conversations_api.py tests/integration/reader_chat/test_owner_isolation.py -q</automated>
  </verify>
  <done>All lifecycle, concurrency, replay, delete and owner-isolation tests pass through FastAPI against PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm API boundaries</name>
  <files>backend/app/services/reader_chat/__init__.py, backend/app/services/reader_chat/conversations.py, backend/app/api/reader_chat.py, backend/app/main.py, backend/tests/integration/reader_chat/test_conversations_api.py, backend/tests/integration/reader_chat/test_owner_isolation.py</files>
  <action>Run contract tests, OpenAPI generation and Ruff. Fix Phase 10 issues only. Inspect generated operations to confirm no suggestion accept/apply route, no unscoped conversation route and no message body/evidence excerpt in list responses.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/reader_chat/test_conversations_api.py tests/integration/reader_chat/test_owner_isolation.py -q; .\.venv\Scripts\python.exe -c "from app.main import app; s=app.openapi(); assert all('apply' not in p and 'accept-suggestion' not in p for p in s['paths']); print('openapi-ok')"; .\.venv\Scripts\python.exe -m ruff check app/services/reader_chat app/api/reader_chat.py tests/integration/reader_chat</automated>
  </verify>
  <done>API tests and OpenAPI boundary checks pass with no data leak or domain-write surface.</done>
</task>

</tasks>

## Must-Haves

- Full D-02 lifecycle is available through one owned-novel hierarchy.
- API message submission is durable and idempotent; it does not synchronously wait for the model.
- Privacy and 404 semantics are regression-tested.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated user → child resource ID | user-controlled IDs may target another owner/novel/conversation |
| concurrent clients → sequence/idempotency | retries and parallel sends race on durable ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-04 | Spoofing/Disclosure | conversation child lookup | mitigate | owned novel root + composite scope + 404 matrix |
| T-10-05 | Tampering | concurrent append | mitigate | row lock, unique sequence and idempotency key |
| T-10-06 | Information Disclosure | list/delete APIs | mitigate | metadata-only list and transactional cascade |
| T-10-SC | Tampering | package supply chain | accept | no package install occurs |
</threat_model>

## Verification

<verification>
PostgreSQL API tests prove positive lifecycle, concurrent behavior and the complete cross-owner matrix; OpenAPI contains no mutation backdoor.
</verification>

<success_criteria>
- REQ-CHAT-02 is API-complete.
- REQ-CHAT-07 owner/concurrency coverage exists.
- No Phase 09 file or domain mutation service is touched.
</success_criteria>

<output>
Create `.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-02-SUMMARY.md` when done.
</output>

