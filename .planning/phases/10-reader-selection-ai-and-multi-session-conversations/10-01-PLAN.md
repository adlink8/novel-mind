---
phase: 10-reader-selection-ai-and-multi-session-conversations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/reader_chat.py
  - backend/app/models/__init__.py
  - backend/app/schemas/reader_chat.py
  - backend/migrations/versions/12_reader_chat_conversations.py
  - backend/tests/unit/reader_chat/test_contracts.py
  - backend/tests/integration/reader_chat/test_migration.py
autonomous: true
requirements:
  - REQ-CHAT-01
  - REQ-CHAT-02
  - REQ-CHAT-04
  - REQ-CHAT-05
must_haves:
  truths:
    - "A user message cannot exist without immutable selection and visible-context lineage."
    - "Conversation messages have stable owner-scoped ordering and replayable citation relationships."
    - "Generation jobs, model attempts and conversation/novel budget reservations are PostgreSQL facts."
    - "Suggestion candidates have no domain-write authority."
  artifacts:
    - path: backend/app/models/reader_chat.py
      provides: conversation, message, selection, manifest, evidence, citation, job, call and budget ORM contracts
    - path: backend/app/schemas/reader_chat.py
      provides: strict API and reader-answer schemas
    - path: backend/migrations/versions/12_reader_chat_conversations.py
      provides: PostgreSQL tables, constraints, indexes and cascades
  key_links:
    - from: reader_messages
      to: reader_context_manifests
      via: one immutable manifest per user message
      pattern: "user_message_id.*unique"
    - from: reader_message_citations
      to: reader_context_evidence_refs
      via: citation FK restricted to persisted allowlist rows
      pattern: "context_evidence_ref_id"
    - from: reader_generation_jobs
      to: reader_model_call_attempts
      via: durable call lineage
      pattern: "generation_job_id"
---

<objective>
Define the authoritative Phase 10 persistence and strict contracts before any API, worker or UI consumes them.

Purpose: Make selection, context, citations, ordering, cancellation and cost auditable by construction while preventing chat from becoming a domain-fact source.
Output: One Alembic migration, SQLAlchemy models, Pydantic contracts and contract/migration tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-SPEC.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-CONTEXT.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-AI-SPEC.md
@backend/app/models/base.py
@backend/app/models/analysis.py
@backend/app/models/timeline.py
@backend/migrations/versions/10_analysis_timeline_versions.py

<interfaces>
Existing ORM convention: Base + TimestampMixin, Mapped/mapped_column, async PostgreSQL authority.
Existing Phase 08 pattern: AnalysisRun/ModelCallAttempt/AnalysisBudgetLedger use durable status, reservations and call lineage; reader chat requires separate chat-scoped tables rather than timeline FKs.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock persistence and strict-schema invariants in failing tests</name>
  <files>backend/tests/unit/reader_chat/test_contracts.py, backend/tests/integration/reader_chat/test_migration.py</files>
  <behavior>
    - User messages require exactly one selection and one immutable context manifest; assistant messages cannot own selections.
    - Source offsets are non-negative code-point half-open ranges with exact text/chapter/selection SHA-256 fields.
    - Conversation sequence and `(conversation_id, client_message_id)` are unique under concurrency.
    - Citations can target only context evidence rows owned by the replied-to user message manifest.
    - One nonterminal generation job may exist per user message; status/lease/cancel/retry fields are constrained.
    - Conversation and novel ledger scopes can coexist and reservations retain worst-case and settled usage.
    - ReaderAnswerEnvelope rejects uncited blocks, unknown refs at business validation, extra fields and suggestions without literal confirmation.
  </behavior>
  <action>Create Wave 0 unit tests for strict Pydantic contracts and PostgreSQL migration tests for tables, FKs, CHECK constraints, partial unique indexes and delete cascades. Include fiction-only source types and assert no table/FK projects chat data into timeline, relationship or clue facts. Tests must fail before production contracts exist. Cover D-03, D-06, D-08, D-09 and D-12.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_contracts.py tests/integration/reader_chat/test_migration.py -q</automated>
  </verify>
  <done>Tests encode all persistence and schema invariants and initially fail only because Phase 10 contracts/migration are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reader-chat ORM, migration and Pydantic contracts</name>
  <files>backend/app/models/reader_chat.py, backend/app/models/__init__.py, backend/app/schemas/reader_chat.py, backend/migrations/versions/12_reader_chat_conversations.py</files>
  <behavior>
    - PostgreSQL can atomically persist/replay a user message, selection, manifest, evidence refs and queued job.
    - Assistant blocks and citations remain tied to the originating manifest allowlist.
    - Dual-scope budgets and model attempts retain full lineage without storing secrets in public schemas.
  </behavior>
  <action>Implement the normalized tables specified in 10-RESEARCH.md with modern SQLAlchemy mappings. Use stable enum/CHECK values, owner/novel/conversation composite indexes, monotonic `next_sequence`, client idempotency, immutable manifest checksum, content hashes, call lineage, lease/cancel/retry fields, and conversation/novel budget ledger plus reservations. Set the migration's `down_revision` to the actual Phase 09 Alembic head present at execution; fail if Phase 09 creates multiple heads instead of guessing. Add strict Pydantic request/response and `ReaderAnswerEnvelope`/`SuggestionCandidate` contracts. Per D-06, suggestions are chat data with `requires_explicit_confirmation=true`; add no confirmation/apply/write model. Per D-08, PostgreSQL is authority. Preserve unrelated dirty worktree edits.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_contracts.py tests/integration/reader_chat/test_migration.py -q</automated>
  </verify>
  <done>Migration upgrades/downgrades on PostgreSQL, all contract tests pass, and no chat schema can directly write or parent domain facts.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm persistence authority</name>
  <files>backend/app/models/reader_chat.py, backend/app/models/__init__.py, backend/app/schemas/reader_chat.py, backend/migrations/versions/12_reader_chat_conversations.py, backend/tests/unit/reader_chat/test_contracts.py, backend/tests/integration/reader_chat/test_migration.py</files>
  <action>Run the targeted suite, Alembic single-head/current/check against PostgreSQL, and Ruff. Fix only Phase 10 contract defects. Confirm hard-delete cascades private chat content and that no table, enum or schema adds history support, relationship UI, clue tracking or domain mutation per D-12.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_contracts.py tests/integration/reader_chat/test_migration.py -q; .\.venv\Scripts\python.exe -m alembic heads; .\.venv\Scripts\python.exe -m alembic check; .\.venv\Scripts\python.exe -m ruff check app/models/reader_chat.py app/schemas/reader_chat.py tests/unit/reader_chat tests/integration/reader_chat/test_migration.py</automated>
  </verify>
  <done>All automated checks pass with one Alembic head and exact Phase 10 persistence invariants.</done>
</task>

</tasks>

## Must-Haves

- Selection, progress snapshot, context manifest and evidence refs are immutable user-message facts.
- Chat deletion removes private conversation content after active jobs are canceled.
- No chat-to-domain write contract exists.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API payload → PostgreSQL | untrusted selection, question and idempotency inputs become durable private data |
| chat citation → evidence ref | model-produced IDs must not escape the persisted allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01 | Tampering | selection/manifest rows | mitigate | CHECK/FK/hash/immutability tests and server-owned refs |
| T-10-02 | Information Disclosure | cascades/list schemas | mitigate | hard-delete cascade and metadata-only list responses |
| T-10-03 | Elevation of Privilege | suggestion contracts | mitigate | no apply endpoint/FK/domain mutation contract |
| T-10-SC | Tampering | package supply chain | accept | no package install occurs in this plan |
</threat_model>

## Verification

<verification>
Targeted unit + real PostgreSQL migration checks must pass in under 60 seconds per command; Alembic must have one head after the Phase 09 head is present.
</verification>

<success_criteria>
- All six artifacts exist and validate.
- REQ-CHAT-01/02/04/05 invariants are enforced by schema or database, not comments.
- No Phase 09 file is modified.
</success_criteria>

<output>
Create `.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-01-SUMMARY.md` when done.
</output>

