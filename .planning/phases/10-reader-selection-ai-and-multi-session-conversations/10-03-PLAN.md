---
phase: 10-reader-selection-ai-and-multi-session-conversations
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - backend/app/services/timeline/query.py
  - backend/app/services/reader_chat/context.py
  - backend/app/services/reader_chat/retrieval.py
  - backend/tests/unit/reader_chat/test_context.py
  - backend/tests/integration/reader_chat/test_context_manifest.py
autonomous: true
requirements:
  - REQ-CHAT-01
  - REQ-CHAT-03
  - REQ-CHAT-07
must_haves:
  truths:
    - "The server proves selected text against owned Chapter.content and active hierarchy evidence before persistence."
    - "Every context manifest is frozen, checksum-addressed and contains only evidence visible at the message's reading snapshot."
    - "The existing persisted full-book switch is the only authority that expands context."
    - "Phase 09 relationship observations are consumed through a read-only version/evidence/spoiler contract."
  artifacts:
    - path: backend/app/services/reader_chat/context.py
      provides: selection validation, progress snapshot and immutable manifest assembly
    - path: backend/app/services/reader_chat/retrieval.py
      provides: bounded visible evidence retrieval and RelationshipObservationReader protocol
  key_links:
    - from: backend/app/services/reader_chat/context.py
      to: Chapter.content
      via: exact code-point slice and SHA-256 validation
      pattern: "source_start.*source_end"
    - from: backend/app/services/reader_chat/context.py
      to: backend/app/services/timeline/query.py
      via: shared server-side cutoff resolver
      pattern: "resolve_chapter_cutoff"
    - from: backend/app/services/reader_chat/retrieval.py
      to: RelationshipObservationReader
      via: owner/version/cutoff-scoped read-only protocol
      pattern: "RelationshipObservationReader"
---

<objective>
Build the immutable selection and spoiler-safe context manifest boundary independently of model generation.

Purpose: Ensure the model can never receive evidence that the user could not see at send time.
Output: Exact selection validator, shared spoiler cutoff, bounded retrieval/Phase 09 consumer contract and unit/PostgreSQL tests.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-RESEARCH.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-AI-SPEC.md
@backend/app/services/timeline/query.py
@backend/app/services/timeline/evidence.py
@backend/app/services/knowledge/evidence.py
@backend/app/models/chunk_build.py
@backend/app/services/hybrid_search.py
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-01-SUMMARY.md

<interfaces>
`Novel.reading_progress.chapter_id` and `.timeline_full_book` are the persisted spoiler authority. `ChunkHierarchyNode` exposes source_start/source_end/content_hash. Phase 09 consumer output is versioned/evidence-bound/spoiler-filtered; this plan creates only the reader protocol and never edits Phase 09.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify exact selection and visible-manifest behavior</name>
  <files>backend/tests/unit/reader_chat/test_context.py, backend/tests/integration/reader_chat/test_context_manifest.py</files>
  <behavior>
    - CJK, emoji, surrogate pairs, combining marks, CRLF and repeated substrings validate by exact code-point range, not fuzzy search.
    - Stale chapter hash/text, invalid bounds, empty/oversized selection and client-forged evidence refs fail before model/job call.
    - No progress exposes first chapter only; persisted progress exposes through that chapter; only persisted `timeline_full_book` permits all chapters.
    - Future evidence, events, relationship observations, counts and errors never enter the canonical manifest.
    - Retry reads the original manifest checksum instead of rebuilding with current progress.
  </behavior>
  <action>Write pure unit and real PostgreSQL integration tests for D-03, D-04, D-05, D-09 and D-10. Seed active hierarchy evidence, accepted knowledge/timeline rows and a fake Phase 09 reader with both visible and future observations. Assert canonical ordering/checksum, bounded excerpts, source/version lineage, explicit source-unavailable status, and that prior dialogue is flagged non-evidence.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_context.py tests/integration/reader_chat/test_context_manifest.py -q</automated>
  </verify>
  <done>Tests fail only for absent selection/context implementation and prove all spoiler side channels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement selection validation and visible-set-first retrieval</name>
  <files>backend/app/services/timeline/query.py, backend/app/services/reader_chat/context.py, backend/app/services/reader_chat/retrieval.py</files>
  <behavior>
    - Valid input returns one immutable, deterministic context graph ready for atomic persistence.
    - Optional evidence sources can be unavailable without widening scope or inventing content.
  </behavior>
  <action>Extract a public reusable `resolve_chapter_cutoff` from timeline query without changing Phase 08 behavior. Implement exact Chapter.content slicing/hash checks and active hierarchy overlap resolution; never trust client evidence IDs. Assemble visible evidence only after owner/novel/cutoff/version filters, then deterministic rank/cap and canonical checksum. Define `RelationshipObservationReader` and strict observation DTO using D-10's version/evidence/spoiler fields, then bind the completed Phase 09 public reader at Phase 10 execution without direct Phase 09 ORM imports or file changes. If the required Phase 09 public contract is absent, stop on the declared phase dependency; do not add a null adapter. Revalidate every returned observation and represent runtime source outages explicitly. Persist `timeline_full_book` snapshot only from the existing novel preference per D-04. Conversation history remains non-evidence framing per D-05.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_context.py tests/integration/reader_chat/test_context_manifest.py tests/integration/timeline/test_spoilers.py -q</automated>
  </verify>
  <done>Selection and context tests pass, Phase 08 spoiler regression remains green, and no hidden evidence enters the manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm spoiler/evidence closure</name>
  <files>backend/app/services/timeline/query.py, backend/app/services/reader_chat/context.py, backend/app/services/reader_chat/retrieval.py, backend/tests/unit/reader_chat/test_context.py, backend/tests/integration/reader_chat/test_context_manifest.py</files>
  <action>Run targeted context/timeline suites and Ruff. Fix only Phase 10/shared cutoff regressions. Inspect canonical manifests to confirm they contain no raw future metadata and that Phase 09 absence is explicit rather than replaced with guessed relationships.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat/test_context.py tests/integration/reader_chat/test_context_manifest.py tests/integration/timeline/test_spoilers.py -q; .\.venv\Scripts\python.exe -m ruff check app/services/reader_chat/context.py app/services/reader_chat/retrieval.py app/services/timeline/query.py tests/unit/reader_chat/test_context.py tests/integration/reader_chat/test_context_manifest.py</automated>
  </verify>
  <done>All evidence, Unicode and spoiler tests pass; Phase 09 files remain untouched.</done>
</task>

</tasks>

## Must-Haves

- Client text/offsets are claims that the server verifies, not authority.
- Visible context is immutable at send time and retry-safe.
- Relationship observations are optional read-only evidence, never silently synthesized.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser DOM → Chapter.content | UTF-16 client offsets cross into code-point server source coordinates |
| retrieval stores → model manifest | visible and hidden evidence share underlying stores |
| Phase 09 reader → Phase 10 context | external phase contract may return malformed scope/version data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-07 | Tampering | selection coordinates | mitigate | exact slice/hash and Unicode adversarial tests |
| T-10-08 | Information Disclosure | context retrieval | mitigate | visible-set-first query and side-channel assertions |
| T-10-09 | Tampering/Disclosure | relationship observations | mitigate | strict read-only DTO plus owner/version/cutoff revalidation |
| T-10-SC | Tampering | package supply chain | accept | no package install occurs |
</threat_model>

## Verification

<verification>
Unit and PostgreSQL tests must prove exact source mapping and zero future evidence across all source types; existing timeline spoiler tests remain green.
</verification>

<success_criteria>
- REQ-CHAT-01 and REQ-CHAT-03 are enforced server-side.
- D-10 is represented by a consumer protocol without Phase 09 changes.
- Manifest checksums are deterministic and retry-stable.
</success_criteria>

<output>
Create `.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-03-SUMMARY.md` when done.
</output>
