---
phase: 10-reader-selection-ai-and-multi-session-conversations
plan: "05"
type: execute
wave: 4
depends_on:
  - "10-02"
  - "10-04"
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/reader-selection.ts
  - frontend/src/lib/reader-selection.test.ts
  - frontend/src/components/reader/reader-content.tsx
  - frontend/src/components/reader/reader-chat-panel.tsx
  - frontend/src/components/reader/reader-chat-panel.test.tsx
  - frontend/src/app/novels/[id]/page.tsx
  - frontend/e2e/reader-chat.spec.ts
  - frontend/e2e/reader-chat-real.spec.ts
  - tests/ci/test_reader_chat_release_gate.py
autonomous: true
requirements:
  - REQ-CHAT-02
  - REQ-CHAT-03
  - REQ-CHAT-06
  - REQ-CHAT-07
must_haves:
  truths:
    - "Selecting visible text exposes a lightweight action bound to exact chapter source offsets."
    - "The reader can continue reading while a collapsible conversation window is open on desktop and mobile."
    - "Users can manage multiple conversations, send/cancel/retry, replay citations and jump to exact source evidence."
    - "A real desktop and 390px stack proves owner, spoiler, persistence and no-domain-write behavior."
  artifacts:
    - path: frontend/src/lib/reader-selection.ts
      provides: DOM Range to chapter code-point coordinate conversion
    - path: frontend/src/components/reader/reader-chat-panel.tsx
      provides: collapsible multi-session conversation UI and job states
    - path: frontend/e2e/reader-chat-real.spec.ts
      provides: real Next.js/FastAPI/PostgreSQL journey with controlled provider
    - path: tests/ci/test_reader_chat_release_gate.py
      provides: independent requirement/DB/command/browser release authority
  key_links:
    - from: frontend/src/components/reader/reader-content.tsx
      to: frontend/src/lib/reader-selection.ts
      via: page base + DOM Range conversion
      pattern: "sourceStartUtf16"
    - from: frontend/src/components/reader/reader-chat-panel.tsx
      to: frontend/src/lib/api.ts
      via: conversation/message/job polling client
      pattern: "readerChatApi"
    - from: citation click
      to: frontend/src/app/novels/[id]/page.tsx
      via: chapter switch then source-offset focus/highlight
      pattern: "source_start"
---

<objective>
Deliver the reader-facing selection and multi-session conversation experience, then qualify the complete Phase 10 stack.

Purpose: Make the backend guarantees usable without obscuring reading, including mobile selection and verifiable citation navigation.
Output: Typed client, offset utility, reader panel integration, component tests, real browser tests and release gate.
</objective>

<execution_context>
@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/li/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-SPEC.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-VALIDATION.md
@frontend/src/app/novels/[id]/page.tsx
@frontend/src/components/reader/reader-content.tsx
@frontend/src/lib/api.ts
@frontend/e2e/timeline-real.spec.ts
@tests/ci/test_timeline_release_gate.py
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-02-SUMMARY.md
@.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-04-SUMMARY.md

<interfaces>
Use Plan 02 conversation/message API and Plan 04 job status/cancel/retry response schemas. The selection payload uses exact code-point `[source_start, source_end)`, selected text and chapter hash; server remains authority.
</interfaces>
</context>

## Steps

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify selection mapping and responsive conversation behavior</name>
  <files>frontend/src/lib/reader-selection.test.ts, frontend/src/components/reader/reader-chat-panel.test.tsx, frontend/e2e/reader-chat.spec.ts, frontend/e2e/reader-chat-real.spec.ts</files>
  <behavior>
    - CJK/emoji/combining/CRLF/paginated selections map to exact chapter code-point offsets and survive DOM selection collapse.
    - Desktop panel does not cover the reading column; mobile panel is bounded, collapsible and supports native touch selection.
    - Conversation create/rename/switch/archive/restore/delete, message replay, loading, cancel, retry, budget/dependency/error and empty states are operable.
    - Citation click loads the cited chapter and focuses/highlights the source range.
    - Real browser refresh replays PostgreSQL conversations; default cutoff never displays future citations; full book works only after existing preference confirmation.
  </behavior>
  <action>Write Vitest/Testing Library and Playwright tests for D-01/D-02/D-04/D-05/D-09/D-11/D-12. The real spec must start actual Next.js, FastAPI and PostgreSQL and control only provider transport. Include desktop and `chromium-mobile-390`, keyboard operation, mobile touch selection, panel collapse, concurrent sessions, cancel/late response, cross-owner navigation and zero console errors. Mocked component/browser tests may cover visual states but cannot satisfy release authority alone.</action>
  <verify>
    <automated>cd frontend; npm test -- --run src/lib/reader-selection.test.ts src/components/reader/reader-chat-panel.test.tsx; npm run test:e2e -- reader-chat.spec.ts reader-chat-real.spec.ts</automated>
  </verify>
  <done>Tests describe the complete responsive reader journey and fail only because UI/client code is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reader selection, collapsible panel and citation navigation</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/reader-selection.ts, frontend/src/components/reader/reader-content.tsx, frontend/src/components/reader/reader-chat-panel.tsx, frontend/src/app/novels/[id]/page.tsx</files>
  <behavior>
    - A selected span becomes an immutable payload before the native selection disappears.
    - UI polls durable jobs and never fabricates optimistic assistant content.
  </behavior>
  <action>Add typed reader-chat API contracts and polling helpers while preserving unrelated existing edits in `api.ts`. Refactor page splitting to retain each page's UTF-16 base; convert DOM Range offsets to chapter code points with the tested utility. Show a small selection action near the selection, then open a collapsible panel. Desktop reserves/non-overlaps the reading column; 390px uses a bounded bottom panel that collapses to a chip and leaves the reader scrollable. Implement full D-02 lifecycle, send/cancel/retry states, strict answer blocks, uncertainty, display-only suggestions, citation chips and chapter/source-offset navigation. Do not persist conversation truth in localStorage; only presentation state may be local. Do not add a new full-book control—read/use the existing confirmed preference per D-04. Do not add relationship graph or clue UI per D-12.</action>
  <verify>
    <automated>cd frontend; npm test -- --run src/lib/reader-selection.test.ts src/components/reader/reader-chat-panel.test.tsx; npm run lint; npm run build</automated>
  </verify>
  <done>Component tests, lint and production build pass; desktop/mobile panel and citation behavior match D-01/D-02/D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Test, Fix, and Confirm full-stack release evidence</name>
  <files>frontend/e2e/reader-chat.spec.ts, frontend/e2e/reader-chat-real.spec.ts, tests/ci/test_reader_chat_release_gate.py</files>
  <action>Run the complete backend reader-chat suite, frontend tests/build, both real browser projects and the CI release gate. Implement the release verifier to independently check command exit/digests, PostgreSQL conversation/message/manifest/citation/job/attempt/budget invariants, spoiler observations, browser artifact metadata and all REQ-CHAT IDs. It must reject self-reported success, missing real browser evidence, Phase 09 file changes, chat-to-domain writes, suggestion apply routes and any Phase 11/chat fact coupling. Fix Phase 10 defects only.</action>
  <verify>
    <automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/unit/reader_chat tests/integration/reader_chat tests/adversarial/test_reader_chat_boundaries.py -q; cd ..\frontend; npm test -- --run src/lib/reader-selection.test.ts src/components/reader/reader-chat-panel.test.tsx; npm run lint; npm run build; npm run test:e2e -- reader-chat.spec.ts reader-chat-real.spec.ts; cd ..; .\backend\.venv\Scripts\python.exe -m pytest tests/ci/test_reader_chat_release_gate.py -q</automated>
  </verify>
  <done>All unit/integration/adversarial/component/build/desktop/mobile/release gates pass with independent DB and command evidence.</done>
</task>

</tasks>

## Must-Haves

- Reader selection is exact and survives pagination/Unicode/mobile behavior.
- The panel is lightweight, collapsible and never blocks continued reading.
- Real-stack evidence closes every REQ-CHAT requirement and protects Phase 11's fact boundary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DOM selection → API payload | browser geometry and UTF-16 coordinates are untrusted |
| API response → rendered citations | private evidence and model text enter the DOM |
| browser release harness → qualification | mocked/self-reported success could hide backend failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-14 | Tampering | selection utility | mitigate | exact Unicode tests and server revalidation |
| T-10-15 | Information Disclosure | citation/panel UI | mitigate | render only server-visible refs; no hidden preload/counts |
| T-10-16 | Denial of Service | polling/UI | mitigate | bounded polling with terminal stop/backoff and cancel controls |
| T-10-17 | Repudiation | release evidence | mitigate | independent DB observations and command/browser digests |
| T-10-SC | Tampering | package supply chain | accept | no package install occurs |
</threat_model>

## Verification

<verification>
Frontend unit/build and real desktop/mobile journeys pass, then independent release gate binds those outputs to PostgreSQL-observed backend facts.
</verification>

<success_criteria>
- REQ-CHAT-06/07 pass on desktop and mobile.
- All seven requirements are independently evidenced.
- No Phase 09 files, history support, clue tracking or relationship graph UI are introduced.
</success_criteria>

<output>
Create `.planning/phases/10-reader-selection-ai-and-multi-session-conversations/10-05-SUMMARY.md` when done.
</output>

