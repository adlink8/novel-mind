---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified: [backend/app/api/timeline.py, backend/app/schemas/timeline.py, backend/app/services/timeline/query.py, frontend/src/lib/api.ts, backend/tests/unit/timeline/test_api.py, backend/tests/integration/timeline/test_spoilers.py, frontend/src/lib/api.contract.test.ts]
autonomous: true
requirements: [REQ-TIME-01, REQ-TIME-03, REQ-TIME-06, REQ-TIME-08]
must_haves:
  truths:
    - "D-04/D-05: one API supports person filter and story/narrative ordering."
    - "D-13/D-16/D-17: partial progress is visible and default responses cannot leak unread events, edges or counts."
    - "D-20: missing reading progress exposes first chapter only, never the full book."
    - "D-21: running candidate and prior active results/aggregates remain separate in API responses."
---
# Goal
替换 timeline 501 占位，交付 owner-scoped progressive API 和服务端防剧透边界。
## Steps
<tasks>
<task type="auto" tdd="true"><name>Task 1: Expose durable run and separate-version APIs</name><files>backend/app/api/timeline.py, backend/app/schemas/timeline.py, backend/tests/unit/timeline/test_api.py</files><action>实现首次进入 start-or-resume、status/cancel/resume/version/rollback/edit endpoints，全部 AsyncSession + owner isolation，兼容既有 prefix。按 D-21 明确返回 `active` 与 `running_candidate` 两个独立 envelope/source selector；事件、进度、counts 和 aggregates 不得合并。Test, Fix, and Confirm concurrent start returns one run and version sources remain separate。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_api.py -x</automated></verify><done>前端可驱动真实持久任务，并独立查看旧 active 与运行 candidate。</done></task>
<task type="auto" tdd="true"><name>Task 2: Derive every response from the visible event set first</name><files>backend/app/services/timeline/query.py, backend/app/api/timeline.py, backend/tests/integration/timeline/test_spoilers.py</files><action>先按 owner、选定 version source、Novel.reading_progress 与 full-book preference 计算 visible event ID CTE/set，再且仅从该集合叠加 field overrides，并派生 participants、causal edges（两端都 visible）、person filter、story/narrative ordering、counts、aggregates、previews 和 error/detail payload。无 reading progress 时按 D-20 cutoff=第一章（无章节则空），绝不默认为全书；full-book 必须显式且按小说持久偏好。测试隐藏事件不能通过 override 文本、dangling edge、人物计数、空筛选差异或错误信息泄露。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_spoilers.py -x</automated></verify><done>所有响应及派生数据均遵循 visible-set-first，chapter 5 不返回 chapter 6+，无进度只见第一章。</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><files>frontend/src/lib/api.ts, frontend/src/lib/api.contract.test.ts, backend/tests/unit/timeline/test_api.py</files><action>更新 typed consumer，覆盖 active/candidate、ordering/person/causal/full-book/no-progress/progress/error contracts，导出 OpenAPI 并检查兼容。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_api.py tests/integration/timeline/test_spoilers.py -x; cd ../frontend; npm test -- --run src/lib/api.contract.test.ts</automated></verify><done>API/consumer 契约闭合且 owner、版本隔离和 spoiler tests 通过。</done></task>
</tasks>
## Must-Haves
- 默认响应不含未来信息的任何派生泄漏。
- partial/paused/failed 不伪装 completed。
## Verification
`cd backend; pytest tests/unit/timeline/test_api.py tests/integration/timeline/test_spoilers.py -x`
