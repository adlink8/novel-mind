---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [backend/app/schemas/timeline.py, backend/app/services/timeline/model_gateway.py, backend/app/services/timeline/extraction.py, backend/app/services/timeline/evidence.py, backend/prompts/timeline_chapter_extract.v1.txt, backend/tests/unit/timeline/test_schema.py, backend/tests/unit/timeline/test_model_gateway.py, backend/tests/unit/timeline/test_extraction.py, backend/tests/adversarial/test_timeline_evidence.py]
autonomous: true
requirements: [REQ-TIME-04, REQ-TIME-05, REQ-TIME-09]
must_haves:
  truths:
    - "D-05/D-06: narrative position and story-time constraints are distinct; exact dates are never invented."
    - "D-09/D-14: strict low-cost chapter extraction cannot publish without valid offsets/evidence and lineage."
    - "D-08: evidence-valid LLM events publish automatically; Phase 08 has no mandatory review queue."
---

# Goal
交付严格 schema 的章节事件抽取和证据验证，覆盖四种时间精度与双顺序原始信号。

## Steps
<tasks>
<task type="auto" tdd="true"><name>Task 1: Lock timeline extraction schemas</name><files>backend/app/schemas/timeline.py, backend/tests/unit/timeline/test_schema.py</files><action>定义 strict EventCandidate、Participant、StoryTimeConstraint、EvidenceRef；extra forbid，exact/relative/fuzzy/unknown 枚举，叙述 offsets 必填。按 D-05/D-06 验证 narrative tuple 与 story constraint 分离。Test, Fix, and Confirm malformed/time-smuggling rejection。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_schema.py -x</automated></verify><done>LLM 输出不能制造日期、跨章证据或自由写库字段。</done></task>
<task type="auto" tdd="true"><name>Task 2: Add a strict timeline model gateway</name><files>backend/app/services/timeline/model_gateway.py, backend/tests/unit/timeline/test_model_gateway.py</files><action>在统一 timeline gateway（若选择扩展现有 AIService，则保持同等专用契约）透传 Pydantic `response_format`、timeout、`num_retries=0`、stream=false；调用前检查 resolved deployment 的 structured-response capability，无支持则 `paused_dependency`。每次响应仍本地 `model_validate_json` 和业务门验证；schema 失败只允许一个有独立 ModelCallAttempt 与 worst-case budget reservation 的 repair，禁止 SDK hidden retry 和 tier/model fallback，显式模型变化必须新 lineage/version。按 D-14/D-15 实施。Test, Fix, and Confirm capability、local-validation、one-repair、no-fallback。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_model_gateway.py -x</automated></verify><done>所有 timeline 模型调用具有一致的结构化输出、能力、审计、预算和失败语义。</done></task>
<task type="auto" tdd="true"><name>Task 3: Extract and cache evidence-bound chapter candidates</name><files>backend/app/services/timeline/extraction.py, backend/app/services/timeline/evidence.py, backend/prompts/timeline_chapter_extract.v1.txt, backend/tests/unit/timeline/test_extraction.py, backend/tests/adversarial/test_timeline_evidence.py</files><action>通过 Task 2 gateway 使用 balanced tier，输入 Phase 07 scene/evidence 包并验证 source hash/offset/owner/novel。调用前使用 08-01 预算门预留 worst-case。exact cache tuple 必须逐项包含 `stage + source_snapshot_hash + hierarchy_build_id/checksum + unit_id + evidence_package_hash + prompt_hash + schema_hash + resolved_model_provider/id/revision + decoding_hash + config_hash`；只缓存通过 schema 与业务门的完整输出，失败/refusal/outcome_unknown 不缓存；cache hit 仍写 `call-skipped` audit、原产物 checksum 和来源 attempt，cache miss 写实际 attempt/usage/cost。验证后按 D-08/D-09 自动发布章节 candidate partial。Test, Fix, and Confirm no valid evidence means no event。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_extraction.py tests/adversarial/test_timeline_evidence.py -x</automated></verify><done>章节候选低成本、幂等、可追溯、渐进持久化，且 exact cache 与调用审计完全符合 AI-SPEC。</done></task>
</tasks>
## Must-Haves
- LLM 只提出候选；脚本验证证据、精度和写入。
- 中间摘要不成为前端模式（D-02）。
## Verification
`cd backend; pytest tests/unit/timeline/test_schema.py tests/unit/timeline/test_model_gateway.py tests/unit/timeline/test_extraction.py tests/adversarial/test_timeline_evidence.py -x`
