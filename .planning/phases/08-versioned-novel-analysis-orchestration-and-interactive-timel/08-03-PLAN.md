---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [backend/app/services/timeline/reconcile.py, backend/app/services/timeline/overrides.py, backend/app/services/timeline/promotion.py, backend/tests/unit/timeline/test_reconcile.py, backend/tests/unit/timeline/test_overrides.py, backend/tests/integration/timeline/test_version_lifecycle.py]
autonomous: true
requirements: [REQ-TIME-02, REQ-TIME-04, REQ-TIME-05, REQ-TIME-06, REQ-TIME-09]
must_haves:
  truths:
    - "D-07/D-14: quality-tier reconciliation creates story order and evidence-backed causal edges."
    - "D-10/D-15: manual overrides survive reanalysis; exhausted budget pauses before another model call."
    - "D-22: unknown provider pricing pauses as paused_budget before any provider call."
---
# Goal
完成跨章归并、因果约束、人工修正保护、预算暂停和候选 promotion。
## Steps
<tasks>
<task type="auto" tdd="true"><name>Task 1: Reconcile cross-chapter chronology and causality</name><files>backend/app/services/timeline/reconcile.py, backend/tests/unit/timeline/test_reconcile.py</files><action>通过 08-02 strict gateway 使用 quality tier；每个 reconcile/repair call 前复用 08-01 budget reserve，价格未知按 D-22 `paused_budget`，禁止 tier fallback。归一重复事件/人物，脚本执行拓扑约束并保留冲突；生成 causes/triggers/responds_to/blocks 边且每边有证据。Test, Fix, and Confirm cycles/contradictions do not fabricate order。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_reconcile.py -x</automated></verify><done>双顺序、参与者和可选因果图确定、可解释且每次调用先完成 worst-case reservation。</done></task>
<task type="auto" tdd="true"><name>Task 2: Protect overrides and relink superseded events</name><files>backend/app/services/timeline/overrides.py, backend/tests/unit/timeline/test_overrides.py</files><action>按 D-10 实现 field-level append-only manual overlay/audit；重分析后用 stable lineage/evidence identity 将 override relink 到新 machine event，歧义或目标消失则保留 override 并标记 `needs_relink`，绝不覆盖机器行或静默丢弃。参与者、边与查询派生都读取 overlay 后的可见字段。Test, Fix, and Confirm reanalysis、supersession 与 ambiguous relink。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_overrides.py -x</automated></verify><done>人工修正跨版本保留，可自动重连或显式待重连。</done></task>
<task type="auto" tdd="true"><name>Task 3: Prove PostgreSQL promotion and rollback lifecycle</name><files>backend/app/services/timeline/promotion.py, backend/tests/integration/timeline/test_version_lifecycle.py</files><action>实现 manifest validation、ActivePointer stale-CAS rejection、pointer+journal 单事务 promotion/rollback。真实 PostgreSQL 测试必须覆盖两个并发候选的 stale expected-version CAS 被拒、rollback 后 active manifest 及其 event/participant/evidence/edge checksums 与历史版本 byte-identical、override 在 supersession 后 relink 或 `needs_relink`，失败候选不改变旧 active。按 D-10/D-11 实施。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_version_lifecycle.py -x</automated></verify><done>合法候选才可 active；stale promotion 无效，rollback 可逐字节复现，override 生命周期可审计。</done></task>
</tasks>
## Must-Haves
- 冲突显式保留，不强造时间。
- override、budget、promotion 使用数据库当前状态复验。
## Verification
`cd backend; pytest tests/unit/timeline/test_reconcile.py tests/unit/timeline/test_overrides.py tests/integration/timeline/test_version_lifecycle.py -x`
