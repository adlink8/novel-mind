---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/models/analysis.py, backend/app/models/timeline.py, backend/app/schemas/analysis.py, backend/migrations/versions/10_analysis_timeline_versions.py, backend/app/services/timeline/jobs.py, backend/app/services/timeline/budget.py, backend/tests/unit/timeline/test_jobs.py, backend/tests/unit/timeline/test_budget.py, backend/tests/integration/timeline/test_persistence.py]
autonomous: true
requirements: [REQ-TIME-01, REQ-TIME-02, REQ-TIME-03]
must_haves:
  truths:
    - "D-11/D-12: deep analysis starts idempotently on first analysis entry; immutable candidates never move active before validation."
    - "D-13: leases, checkpoints, partial chapter completion, cancel and restart survive process loss."
  artifacts:
    - path: backend/app/services/timeline/jobs.py
      provides: durable owner-scoped analysis job repository and worker state machine
---

# Goal
建立 Phase 08 的 PostgreSQL 事实源：持久 job、版本 manifest、active pointer、lease/checkpoint 和增量章节状态。

## Steps

<tasks>
<task type="auto" tdd="true"><name>Task 1: Define the complete PostgreSQL timeline authority</name><files>backend/app/models/analysis.py, backend/app/models/timeline.py, backend/app/schemas/analysis.py, backend/migrations/versions/10_analysis_timeline_versions.py, backend/tests/integration/timeline/test_persistence.py</files><action>先写 PostgreSQL 迁移、约束和 round-trip 失败测试，再用 `10_` 迁移定义 owner/novel scoped AnalysisRun、AnalysisVersion、MachineTimelineEvent、Participant、EvidenceRef、CausalEdge、field-level Override、supersession 与 `needs_relink` 状态、ModelCallAttempt、exact-cache audit、chapter stage/checkpoint、budget ledger/reservation、ActivePointer 和 append-only promotion/rollback journal。事件必须持久化 exact/relative/fuzzy/unknown mixed-time union、独立 narrative tuple 与 story constraint/rank 双顺序、confidence、source offsets/hash 及 prompt/schema/model lineage；机器版本不可变，旧 AnalysisResult 仅只读兼容。按 D-05/D-06/D-09/D-10/D-11 实施。Test, Fix, and Confirm migration upgrade/current/check 及真实 PG 约束。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_persistence.py -x; alembic upgrade head; alembic current; alembic check</automated></verify><done>真实 PostgreSQL 完整保存机器事件图、人工覆盖、调用/缓存审计、预算、候选、单一 active pointer 与可逆 journal，且双顺序和混合时间无损 round-trip。</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement durable jobs and pre-call budget foundation</name><files>backend/app/services/timeline/jobs.py, backend/app/services/timeline/budget.py, backend/tests/unit/timeline/test_jobs.py, backend/tests/unit/timeline/test_budget.py</files><action>实现 CAS lease、heartbeat、cancel/resume、chapter checkpoint、stable stage key 与原子 reserve/settle/release。任何 extraction、repair 或 reconcile 网络调用都必须先按该 attempt 的最大输入、最大输出、call count 和价格快照预留 worst-case；价格未知立即按 D-22 转 `paused_budget`，不得仅靠 token/call 上限继续。导入不得调用 timeline LLM，首次进入并发请求只能创建一个 active run。Test, Fix, and Confirm restart、no-duplicate、unknown-pricing 和 zero-call-after-pause。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_jobs.py tests/unit/timeline/test_budget.py -x</automated></verify><done>在 Phase 08 第一次模型调用前已有可复用预算门；任务可恢复、可取消、并发安全且暂停后零调用。</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><files>backend/tests/unit/timeline/test_jobs.py, backend/tests/unit/timeline/test_budget.py, backend/tests/integration/timeline/test_persistence.py</files><action>注入进程重启、过期 lease、重复首次进入、并发 reservation、未知价格、候选失败和 rollback journal；验证 ModelCallAttempt/outcome_unknown/cache-hit call-skipped 审计可持久恢复，并运行迁移及目标套件。</action><verify><automated>cd backend; pytest tests/unit/timeline/test_jobs.py tests/unit/timeline/test_budget.py tests/integration/timeline/test_persistence.py -x; alembic upgrade head; alembic current; alembic check</automated></verify><done>REQ-TIME-01/02/03/09 的 PostgreSQL 与预算基础有可重复证据。</done></task>
</tasks>

## Must-Haves
- PostgreSQL 是 job/version/pointer 真值；旧结果不伪造 lineage。
- active 只在完整候选验证后 CAS 切换。

## Verification
`cd backend; pytest tests/unit/timeline/test_jobs.py tests/unit/timeline/test_budget.py tests/integration/timeline/test_persistence.py -x`
