---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 09
type: execute
gap_closure: true
wave: 9
depends_on: [08-08]
files_modified: [backend/app/services/timeline/worker.py, backend/app/services/timeline/reconcile.py, backend/app/services/timeline/query.py, backend/app/schemas/timeline.py, frontend/src/lib/api.ts, frontend/src/app/analysis/page.tsx, frontend/src/components/timeline/timeline-chart.tsx, backend/scripts/run_timeline_qualification.py, tests/ci/test_timeline_release_gate.py, backend/tests/integration/timeline/test_final_gaps.py, frontend/src/app/analysis/page.test.tsx]
autonomous: true
requirements: [REQ-TIME-01, REQ-TIME-04, REQ-TIME-07, REQ-TIME-08, REQ-TIME-09]
must_haves:
  truths:
    - "Worker re-reads cancel_requested between every production stage and terminates without later provider calls or promotion."
    - "Reconciliation cache identity contains prompt and schema hashes in addition to all existing exact-key members."
    - "source_start is persisted, returned by real API/types, and used for frontend ordering without mock-only fields."
    - "Spoiler metrics are computed from visible/full production queries and release gate rejects self-hashed synthetic artifacts."
---
# Goal
关闭第二轮 verifier 的四个窄阻断项，使取消、缓存、排序与 qualification 证据均可由生产链证明。
## Steps
<tasks>
<task type="auto" tdd="true"><name>Task 1: Close worker cancellation and reconciliation cache gaps</name><files>backend/app/services/timeline/worker.py, backend/app/services/timeline/reconcile.py, backend/tests/integration/timeline/test_final_gaps.py</files><action>先写失败测试：在 evidence/extraction/persist/reconcile/promotion 各阶段设置 cancel 后，worker 必须重新读取DB并停止，后续provider calls=0且active不变。reconcile exact key加入实际 prompt_hash/schema_hash，变化任一项必须miss且写audit。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_final_gaps.py -x</automated></verify><done>运行中取消有效，reconcile 不复用跨prompt/schema缓存。</done></task>
<task type="auto" tdd="true"><name>Task 2: Propagate real source_start ordering contract</name><files>backend/app/services/timeline/query.py, backend/app/schemas/timeline.py, frontend/src/lib/api.ts, frontend/src/app/analysis/page.tsx, frontend/src/components/timeline/timeline-chart.tsx, frontend/src/app/analysis/page.test.tsx, backend/tests/integration/timeline/test_final_gaps.py</files><action>API从MachineTimelineEvent返回source_start；Pydantic/TypeScript设为必需；前端排序chapter_number→source_start→id。测试使用真实API序列化而非mock-only field，并保持active/candidate participant隔离。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_final_gaps.py -x; cd ../frontend; npm test -- --run src/app/analysis/page.test.tsx</automated></verify><done>非连续章节和同章事件真实顺序正确。</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><files>backend/scripts/run_timeline_qualification.py, tests/ci/test_timeline_release_gate.py, backend/tests/integration/timeline/test_final_gaps.py</files><action>spoiler metric通过同一DB分别调用默认visible与full query并检测未来event/edge/count泄漏；artifact包含run/version/manifest/call-audit IDs和原始证据hash。Release gate重查这些DB-backed refs与命令输出，拒绝仅自哈希合成JSON。运行全套并修复。</action><verify><automated>cd backend; pytest tests/unit/timeline tests/integration/timeline tests/adversarial/test_timeline_evidence.py -x; cd ..; pytest tests/ci/test_timeline_release_gate.py -x; cd frontend; npm test -- --run; npm run build</automated></verify><done>第二轮四个blocker全部有生产证据。</done></task>
</tasks>
## Must-Haves
- cancel、cache、source order 和 spoiler score 都不能靠mock或fixture声明。
- artifact hash只证明完整性，不替代来源真实性校验。
## Verification
Run final gap, full timeline, release gate and frontend suites above.
