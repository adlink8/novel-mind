---
phase: 08-versioned-novel-analysis-orchestration-and-interactive-timel
plan: 07
type: execute
gap_closure: true
wave: 7
depends_on: [08-06]
files_modified: [backend/app/services/timeline/worker.py, backend/app/services/timeline/model_gateway.py, backend/app/services/timeline/extraction.py, backend/app/services/timeline/reconcile.py, backend/app/services/timeline/jobs.py, backend/app/api/timeline.py, backend/tests/integration/timeline/test_production_worker.py, backend/tests/integration/timeline/test_persistent_calls.py]
autonomous: true
requirements: [REQ-TIME-01, REQ-TIME-02, REQ-TIME-03, REQ-TIME-05, REQ-TIME-09]
must_haves:
  truths:
    - "First-entry start reaches a production worker that loads Phase07 evidence, calls strict gateways, persists events, reconciles, validates, and promotes."
    - "Budget reservation, exact cache and ModelCallAttempt audit are PostgreSQL-backed and used before every extraction/reconcile call."
    - "Reconcile cannot bypass the qualified strict gateway or silently change model tier/lineage."
---
# Goal
关闭 verifier gaps 1-2：把已实现组件串成真实可恢复生产 worker，并把预算、缓存、调用审计接入数据库事实源。
## Steps
<tasks>
<task type="auto" tdd="true"><name>Task 1: Wire the production timeline worker</name><files>backend/app/services/timeline/worker.py, backend/app/services/timeline/jobs.py, backend/app/api/timeline.py, backend/tests/integration/timeline/test_production_worker.py</files><action>先写真实 AsyncSession E2E 失败测试，再实现 pending→evidence load→chapter extract→persist partial→reconcile→manifest validate→promote。API start/resume 必须调度该 worker；restart从checkpoint继续且已完成阶段零模型调用。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_production_worker.py -x</automated></verify><done>首次进入不再只留下 pending 行，active timeline 可由生产链生成。</done></task>
<task type="auto" tdd="true"><name>Task 2: Persist and enforce model-call boundaries</name><files>backend/app/services/timeline/model_gateway.py, backend/app/services/timeline/extraction.py, backend/app/services/timeline/reconcile.py, backend/tests/integration/timeline/test_persistent_calls.py</files><action>将每次 extraction/reconcile 统一通过 strict gateway；事务性 worst-case budget reserve 后才调用，结果 settle/release；exact cache/attempt/checksum写DB，cache hit写skipped audit。未知价格/无schema capability/跨tier fallback均零provider calls。Test, Fix, and Confirm。</action><verify><automated>cd backend; pytest tests/integration/timeline/test_persistent_calls.py -x</automated></verify><done>预算、cache、audit 和 lineage 在重启后仍可证明。</done></task>
<task type="auto"><name>Test, Fix, and Confirm</name><files>backend/tests/integration/timeline/test_production_worker.py, backend/tests/integration/timeline/test_persistent_calls.py</files><action>注入中断、重复resume、provider拒绝、预算并发、reconcile schema失败和stale promotion，修复后运行全部timeline tests。</action><verify><automated>cd backend; pytest tests/unit/timeline tests/integration/timeline tests/adversarial/test_timeline_evidence.py -x</automated></verify><done>Verifier gaps 1-2 有真实生产路径证据。</done></task>
</tasks>
## Must-Haves
- 不允许测试注入函数成为生产唯一实现。
- 未完整执行生产 pipeline 的 run 不可 promotion。
## Verification
`cd backend; pytest tests/unit/timeline tests/integration/timeline -x`
